# 下一窗口交接上下文（2026-04-09，详细版）

## 1) 项目当前目标与边界
- 目标：稳定“对话 + 任务 + 奖励 + NPC交互”整条链路，避免多人局不同步和提交流程错误。
- 关键业务规则：任务状态为**全局共享**（不是 per-player 独立）。
- 当前阶段：基础链路可跑，复杂奖励条件与边界场景需继续压测。
- 约束：每轮改动后执行 `npm run build` 并确认通过。

## 2) 架构现状（本轮收敛后的目录职责）
- `TS/系统/09．表现系统/01．UI工具/`：通用 UI 构建、布局、内容设置、帧控制。
- `TS/系统/09．表现系统/02．对话框系统入口/`：游戏级入口、任务状态桥接、配置查询、对话构建、提交与奖励逻辑。
- `TS/系统/09．表现系统/03．对话框系统/`：对话框渲染、打字机、立绘、任务对话 UI。
- `TS/系统/09．表现系统/04．NPC对话状态池.ts`：NPC 占用/对话状态管理（单流程约束关键点）。
- `TS/系统/08．任务系统/00．配置表/`：对话/NPC/任务配置（01~03 为 Excel 导表）。
- `TS/系统/08．任务系统/01．任务数据.ts`：全局任务数据库（`globalData` 为单份共享状态）。
- `TS/lib/扩展函数/物品相关函数/`：物品判断、叠加、次数扣除、返还策略等。

## 3) 已完成改造（可视为当前稳定基线）
- 对话框系统由单文件拆分为模块目录，并保留旧入口兼容导出。
- UI 工具按职责拆分，已统一虚拟分区注释。
- 任务奖励逻辑拆分为：
  - `06．任务奖励解析.ts`（条件判定）
  - `07．任务提交流程.ts`（提交主流程）
  - `08．任务奖励执行.ts`（奖励执行）
  - `09．任务展示文案.ts`（展示文案）
- 新增 `rewardDisplay`，实现“外部显示文案”与“内部执行 reward”解耦。
- `type=给予` 任务流程已改为检查 NPC 背包并处理返还逻辑。
- 物品次数逻辑改为 charges 语义（`GetItemCharges/SetItemCharges`），成功消耗使用 `RemoveItem`。
- `main.ts` 已改为集中加载新入口和 `物品函数入口`。
- 删除 `11．便捷函数（偶尔用）`，并迁移其引用。

## 4) 本轮针对“玩家2提交可能有 bug”的修复
- 文件：`TS/系统/09．表现系统/02．对话框系统入口/03．配置查询.ts`
  - `findAcceptedQuestBySubmitNpc(npcName)` -> `findAcceptedQuestBySubmitNpc(npcName, playerId)`。
- 文件：`TS/系统/09．表现系统/02．对话框系统入口/05．选择触发入口.ts`
  - 调用改为 `findAcceptedQuestBySubmitNpc(npcName, playerId)`，显式传入当前触发玩家。
- 文件：`TS/系统/09．表现系统/02．对话框系统入口/07．任务提交流程.ts`
  - `isKillQuestObjectiveCompleted` 改为接收 `playerId`。
  - 提交时传 `dialogOwnerId`，避免默认值串线。

## 5) 已确认的硬规则（下次窗口必须继承）
- 任务状态全局共享：谁接/进行/完成，全员任务 UI 与 NPC 状态一致。
- `requireCount` 缺省、`0`、`1` 统一按 `1`。
- `wood/木材` 在本图语义为“能量碎片”。
- `type=给予` 且 reward 为“外部+内部条件”的任务，失败提示统一，不暴露失败细节。
- 不允许回退到旧的硬编码村长兜底对话逻辑。
- 奖励展示优先级：`rewardDisplay` >（条件场景通用文案）> `reward`。

## 6) 关键文件速查（下次窗口优先读）
- `TS/系统/09．表现系统/02．对话框系统入口/00．对话框系统入口.ts`
  - 集中初始化：`NPC生成器`、`NPC对话状态池`、`对话框UI`。
- `TS/系统/09．表现系统/02．对话框系统入口/02．任务状态.ts`
  - `setQuestState/getQuestState` 全局共享桥接逻辑。
- `TS/系统/09．表现系统/02．对话框系统入口/03．配置查询.ts`
  - 起始 NPC、提交 NPC、已接任务查找。
- `TS/系统/09．表现系统/02．对话框系统入口/04．对话构建.ts`
  - 对话文本解析与构建、提交按钮入口。
- `TS/系统/09．表现系统/02．对话框系统入口/07．任务提交流程.ts`
  - 提交主流程（资源/物品/给予分支/奖励执行/完成广播）。
- `TS/系统/09．表现系统/02．对话框系统入口/08．任务奖励执行.ts`
  - 金币/能量碎片/经验/等级/属性发放与 `IMaxBJ` 表达式支持。
- `TS/lib/扩展函数/物品相关函数/物品判断函数.ts`
  - 物品查找、次数统计、扣除、返还或掉落。

## 7) 明天优先测试矩阵（按顺序）
- A. 多人同步主线
  - 玩家1接任务，玩家2立即查看任务 UI 与 NPC 状态是否同步。
  - 玩家2提交任务，玩家1是否同步看到任务完成与 NPC 状态变化。
- B. `type=给予` 主线
  - 正确物品、数量足够 -> 提交成功、扣除正确、奖励正确、完成文案正确。
  - 错误物品 -> 返还（背包不满进包，满则掉脚下）+ 统一失败提示。
  - 条件分支不命中 -> 不完成、物品返还、统一失败提示。
- C. 奖励条件分支
  - `装备等级<D+` / `装备等级>D+&<C` / `装备等级=C+`
  - `I067|I08G`
  - `英雄等级＞N` / `英雄等级≤N`
- D. 任务类型
  - `击杀`：`targetUnit` 多个 ID 任意击杀均计数。
  - `目标击杀`：仅指定目标计数。

## 8) 已知风险与建议优先级
- 高优先：奖励条件解析未知条件的兜底策略需复核，避免误命中条件分支。
- 中优先：`装备等级:` 提交时可补测“背包多个候选物品”场景，确认不会因首个候选导致误判。
- 中优先：返还流程继续压测极端时序（NPC背包变化、英雄满包、同时交互）。
- 低优先：整理 `rewardDisplay` 的 Excel 导表流程，减少后续手工补字段。

## 9) 与 Excel 宏导表相关注意
- `TS/系统/08．任务系统/00．配置表/01~03` 为导表产物，手改后可能被覆盖。
- 建议把 `rewardDisplay` 正式加入 Excel 模板列，避免每次导表后再手工恢复展示文案。
- 导表后第一步：只做字段映射差异检查，不先做逻辑改动。

## 10) 下次窗口的建议执行顺序（可直接照做）
- 第一步：先读本文件 + `07．任务提交流程.ts` + `06/08/09` 奖励三件套。
- 第二步：跑一轮多人用例 A/B（至少 2 人）。
- 第三步：若复现问题，优先修 `07．任务提交流程.ts` 与 `03．配置查询.ts`。
- 第四步：每次修复后执行 `npm run build`，并记录“用例-结果-是否回归”。
- 第五步：用例全绿后，再做 reward 条件边界清理。

## 11) 禁改项（防止回归）
- 不改回 per-player 任务状态模型。
- 不移除 `type=给予` 的统一失败提示策略。
- 不恢复硬编码村长兜底对话。
- 不再把物品消耗改回 `UnitRemoveItemSwapped` 这类会掉地的路径。

## 12) 下次窗口开场指令（可复制）
“先读取 `下一窗口交接上下文.md`。本项目任务状态是全局共享，先按‘测试矩阵 A/B/C/D’从多人同步和 type=给予开始验证。若复现问题，优先改 `03．配置查询.ts` 与 `07．任务提交流程.ts`，每次改完都执行 `npm run build` 并汇报用例结果。”

