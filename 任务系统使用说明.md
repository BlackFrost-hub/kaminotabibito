# 任务系统使用说明

## 概述
任务系统为War3地图提供了完整的任务管理功能，包括主线、支线和小任务分类，具有完整的UI界面和任务追踪功能。

## 功能特性
- 主线任务（主要剧情）
- 支线任务（可选任务）
- 小任务（日常/小型任务）
- 任务列表按分类显示
- 任务详情查看
- 任务进度追踪
- 与War3原生任务日志集成（预留）
- 快捷键支持（F9打开/关闭界面）

## 文件结构
```
TS/系统/08．任务系统/
  任务数据.ts      # 数据结构和数据库
  任务管理器.ts    # 任务生命周期管理
  任务UI配置.ts    # UI样式和文本配置
  任务接受.ts      # 任务接受事件桥接（已更新）
  任务完成.ts      # 任务完成事件桥接（已更新）
  index.ts        # 统一导出入口

TS/系统/09．表现系统/
  任务UI.ts        # 主任务界面
  任务UI组件.ts    # 可复用UI组件
  UI工具.ts        # 通用UI工具函数

TS/系统/12．测试系统/
  任务测试.ts      # 系统测试
```

## 快速开始

### 1. 添加新任务
在 `任务数据.ts` 的 `createTestQuests()` 函数中添加任务定义示例：

```typescript
db.registerQuest({
  id: "quest_unique_id",
  type: QuestType.MAIN, // 或 QuestType.SIDE, QuestType.DAILY
  title: "任务标题",
  description: "任务描述",
  objectives: [
    { id: "obj1", description: "目标描述", current: 0, required: 5, completed: false }
  ],
  rewards: [
    { type: "experience", value: 100, description: "100经验" },
    { type: "gold", value: 50, description: "50金币" }
  ],
  status: QuestStatus.UNDISCOVERED,
  requiredLevel: 1,
  zone: "区域名称",
  icon: "ReplaceableTextures\\CommandButtons\\BTNIcon.blp",
  createdAt: Date.now(),
  updatedAt: Date.now()
});
```

### 2. JASS端触发任务事件
#### 任务接受事件
1. 设置全局变量：
```jass
set udg_QuestPlayerId = 玩家ID
set udg_QuestId = "任务ID字符串"
```
2. 触发事件：
```jass
call TriggerExecute(gg_trg_QuestAccepted) // 根据你的触发器名称
```

#### 任务完成事件
1. 设置全局变量：
```jass
set udg_QuestPlayerId = 玩家ID
set udg_QuestId = "任务ID字符串"
```
2. 触发事件：
```jass
call TriggerExecute(gg_trg_QuestCompleted) // 根据你的触发器名称
```

#### 任务目标更新事件
1. 设置全局变量：
```jass
set udg_QuestPlayerId = 玩家ID
set udg_QuestId = "任务ID字符串"
set udg_ObjectiveId = "目标ID字符串"
set udg_Progress = 进度值
```
2. 触发事件（需要自定义触发器）

### 3. 玩家操作
- **F9**: 打开/关闭任务界面
- **F10**: 运行任务系统测试（仅玩家1）

## UI界面说明
任务界面包含以下部分：
1. **标题栏**: 显示"任务日志"
2. **分类标签**: 主线/支线/小任务切换
3. **任务列表**: 显示当前分类下的任务
4. **详情面板**: 显示选中任务的详细信息

每个任务项显示：
- 任务图标和标题
- 任务类型标签
- 任务状态
- 进度条（如果有多项目标）

## 测试系统
系统包含完整的测试功能：
1. 按F10运行所有测试
2. 测试内容包括：
   - 任务数据加载
   - 任务接受/完成流程
   - UI显示/隐藏
3. 测试结果通过游戏内消息显示

## 自定义配置
### UI样式
修改 `任务UI配置.ts` 中的配置：
- `Colors`: 颜色配置
- `Sizes`: 尺寸配置
- `Texts`: 文本配置
- `Icons`: 图标路径

### 任务类型
系统支持三种任务类型：
- `QuestType.MAIN`: 主线任务
- `QuestType.SIDE`: 支线任务
- `QuestType.DAILY`: 小任务

### 任务状态
- `UNDISCOVERED`: 未发现
- `DISCOVERED`: 已发现
- `IN_PROGRESS`: 进行中
- `COMPLETED`: 已完成
- `FAILED`: 已失败

## 扩展开发
### 添加新任务类型
1. 在 `QuestType` 枚举中添加新类型
2. 在 `任务UI配置.ts` 中添加对应的配置
3. 在UI中添加对应的分类标签

### 集成War3原生任务
系统预留了War3原生任务集成接口，在 `任务管理器.ts` 的 `syncToWar3Quest()` 方法中实现。

### 多语言支持
修改 `任务UI配置.ts` 中的 `Texts` 配置即可实现多语言。

## 常见问题
1. **任务界面不显示**
   - 检查DzAPI是否可用
   - 检查控制台错误信息

2. **任务事件不触发**
   - 检查全局变量是否正确设置
   - 检查JASS触发器是否正确配置

3. **编译错误**
   - 检查TypeScript版本（需要5.0+）
   - 检查TSTL配置

## 开发说明
系统使用TypeScript开发，通过TSTL编译到Lua。所有UI使用DzAPI创建，兼容War3平台。

### 编译命令
```bash
npm run build:lua    # 仅编译TypeScript到Lua
npm run build        # 完整编译（包括后续处理）
```

### 依赖
- TypeScript 5.0+
- typescript-to-lua

## 版本历史
- v1.0.0: 初始版本，包含完整的任务系统和UI界面