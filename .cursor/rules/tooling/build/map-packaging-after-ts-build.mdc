---
description: 修改 TS/Lua 后刷新测试地图的打包规则，避免只 build 但 .build/war3map.lua 或测试 w3x 仍是旧代码
globs: TS/**/*.ts, src/**/*.lua, .build/war3map.lua
alwaysApply: false
---

# TS 修改后的测试地图打包规则

## 核心结论

`npm run build:files -- "<本次修改的TS文件>"` 用于日常局部构建；`npm run build` 用于公共接口、文件结构变化、局部构建失败或最终交付验证。两者都不保证 `.build/war3map.lua` 和 `.build/_warcraft_vscode_test.w3x` 已经刷新。

这里的“最终交付验证”仅指用户明确要求的最终整体验收、发布前验收或打包前验收。普通任务完成、准备回复用户、修改了高风险回调或需要检查相关生成 Lua，都不自动等于最终交付验证；这些情况仍优先局部构建，避免无必要的全量构建成本。

默认不要在代码修改后手动打包地图。用户平时固定通过 Warcraft VSCode 的 `Run Map` 启动地图，该命令会自行完成 compile + pack；插件正常工作时，AI 无需替它重复打包。

只有以下情况才允许 AI 手动刷新 `.build` 或打包 W3X：

- 用户明确说“打包地图”“生成测试图”“刷新 `.build`”或指定要一个 W3X 文件。
- 用户明确要求 AI 代为执行 `Run Map` / `Pack Map`。
- 当前任务本身是在诊断插件编译、打包、地图未更新或产物错误。

“接入代码”“生成资源”“更新配置”“构建验证”“可以进游戏测试”都不等于要求打包。遇到这些普通任务，只运行与改动相称的局部或全量构建；不要因为觉得用户可能马上测试就自行打包。

## TS 构建验证分流

1. 修改 TS 后，优先执行：

```powershell
npm run build:files -- "<本次修改的TS文件1>" "<本次修改的TS文件2>"
```

2. 参数只传本次修改或新建的 `TS` 目录下 `.ts` 文件；每个路径必须用双引号包裹。不要把未修改文件、`src/**/*.lua`、文档、规则、资源或其他非 TS 文件传给 `build:files`。
3. 遇到以下任一情况，执行 `npm run build`：
   - 修改 `TS/global.d.ts` 或 `tsconfig.json`。
   - 修改公共类型或导出签名。
   - 删除或重命名 TS 文件。
   - 局部构建失败。
   - 进行用户明确要求的最终整体验收、发布前验收或打包前验收。
4. 只修改文档、规则、资源或其他不参与 TSTL 的文件时，不运行 TS 构建。

| 用户意图 | 默认动作 |
|----------|----------|
| 只改文档、规则、资源或其他不参与 TSTL 的文件 | 不构建、不打包 |
| 新建或修改普通 TS 业务实现 | 对本次 TS 文件运行 `npm run build:files -- "..."`；不手动打包 |
| 修改公共类型/导出签名、全局配置，删除/重命名 TS，局部构建失败或用户明确要求最终整体验收 | 运行 `npm run build`；不手动打包 |
| 用户正常进图测试 | 由用户执行 Warcraft VSCode `Run Map` |
| 用户明确要求 W3X、Pack Map 或排查地图未更新 | 才进入下方手动流程 |

## 明确要求打包时的流程

1. 运行 `npm run build`。
2. 检查敏感生成物，例如 JASS/JAPI/DzAPI 调用、回调参数、`nil/self` 形态，优先看 `src/**/*.lua`。
3. 仅在本轮确实需要产出测试图时，确认 `.build/war3map.lua` 是否已经包含本次修改。
4. 如果 `.build/war3map.lua` 仍是旧代码，先刷新或同步它，再打包测试图。
5. 优先使用当前已安装的 Warcraft VSCode `wc3.exe` 完整编译并打包：

```powershell
$extension = Get-ChildItem 'C:\Users\Administrator\.vscode\extensions' -Directory |
  Where-Object Name -Like 'dencer.warcraft-vscode-*-win32-x64' |
  Sort-Object Name -Descending |
  Select-Object -First 1
$wc3 = Join-Path $extension.FullName 'bin\wc3.exe'
& $wc3 build (Get-Location).Path -c
```

不要把插件版本写死为 `0.3.6`；当前环境可能已经升级。旧版 `MopaqPack-rs.exe` 只作为确实仍安装旧插件时的备用工具。

## 验证点

- `.build/war3map.lua` 里能搜到本次修改后的关键代码。
- `.build/_warcraft_vscode_test.w3x` 的 `LastWriteTime` 晚于 `.build/war3map.lua`。
- 如果打包失败并提示占用，优先关闭正在运行的 Warcraft/WorldEdit 测试图，再重试。

## 常见坑

- 用户未要求手动打包时，不要为了检查 `.build` 而额外生成地图；让用户正常使用 `Run Map` 即可。
- 用户明确要求 AI 产出测试图时，不要只看 `src/**/*.lua` 就让用户进图测；测试图实际可能仍在用旧 `.build/war3map.lua`。
- 不要把打包失败误判成业务代码没生效；先确认 `.build` 产物和测试图时间戳。
- 改 DzAPI UI、物品主动技能、事件中心、伤害流程这类进图表现时，尤其要做 `.build` 和 `.w3x` 双确认。
- Warcraft VSCode `0.3.7` 的 Classic CLI 会按 UTF-8 读取地图内 `war3map.j`。如果原地图脚本是合法 GBK，会报 `stream did not contain valid UTF-8`。不要重写 TS 源文件，也不要只转换 `.build/war3map.j`，因为 `wc3 compile` 会重新从地图提取并覆盖它。
- 遇到上述 GBK 错误时，在 `.build` 创建临时 `map.w3x` 副本；从原图提取 `war3map.j`，按 GBK 解码并显式写成无 BOM UTF-8，再用 `mpqcli add -p war3map.j -w -g warcraft3-map` 替换临时副本中的脚本。随后对临时副本运行：

```powershell
& $wc3 compile (Get-Location).Path -c -m '<临时 UTF-8 地图副本.w3x>'
& $wc3 pack (Get-Location).Path -c -m '<临时 UTF-8 地图副本.w3x>' -o '.build\_warcraft_vscode_test.w3x'
```

- 若 `_warcraft_vscode_test.w3x` 被 Warcraft 占用，不要结束用户进程；改用带任务名或时间戳的新输出文件。打包后从成品 W3X 重新提取 `war3map.lua`，检查本次关键代码确实存在，再清理临时副本。

## Warcraft VSCode 命令 ID

如果需要让 Warcraft VSCode 插件刷新 `.build/war3map.lua`，优先使用 VSCode 命令面板中的插件命令，而不是只运行 `npm run build`。

命令 ID：

```text
extension.warcraft.compile.debug  -> Warcraft: Compile Script，刷新 `.build/war3map.lua`
extension.warcraft.pack.debug     -> Warcraft: Pack Map，按 packlist 打包测试图
extension.warcraft.run.debug      -> Warcraft: Run Map，compile + pack + 启动测试图
```

当前 Codex shell 不能直接通过 `code` CLI 调用 VSCode command ID；需要用户在 VSCode 命令面板执行、使用 Computer Use 操作 VSCode，或直接调用当前插件目录下的 `bin\wc3.exe`。直接调用 CLI 时仍需遵守上面的 Classic GBK 兼容流程与成品验证要求。
