---
description: 中文文件编码与补丁安全规则，避免整文件重写、编码漂移、回滚失败与 TSTL 产物误判
globs: TS/**/*.ts, src/**/*.lua, imports/**/*.fdf, **/*.md, **/*.txt
alwaysApply: false
---

# 编码与补丁安全规则

适用场景：

- 中文文件名、中文注释、中文字符串较多的 TS / Lua / FDF / 文档文件
- TSTL 回调改造、`@noSelfInFile` 调整、DzAPI UI 文件修复
- 任何“看起来像编码坏了 / 注释坏了 / 回滚失败”的场景

## 1. 小补丁优先，整文件重写最后

默认只允许：

- `apply_patch` 做局部增删改
- 按行替换、按块替换
- 修单个函数、单个接口、单个导入、单个注释块

默认不要：

- 因为一个小问题就整文件重写
- 用 shell 把整个文件重新输出一遍
- 先复制全文件再覆盖保存

## 2. 中文文件禁止“读全文件再整段写回”修小问题

对下列文件尤其严格：

- `TS/系统/**`
- `TS/lib/**`
- `imports/**/*.fdf`
- `src/**/*.lua`

不要用这类方式处理：

- PowerShell 整文件 `Get-Content` + `Set-Content`
- 脚本把整份文本读入内存后重写回去
- 为了修一行注释/一个回调，整文件重新生成文本

## 3. 优先保护可回滚性

修改前优先保证：

- diff 尽量小
- 同一轮只改与当前问题直接相关的块
- 不把无关格式化、无关重排、无关注释改动混进来

## 4. TSTL / `@noSelfInFile` 修复规则

当问题涉及：

- `@noSelfInFile`
- `pcall(nil, ...)`
- `DzFrameSetScript*`
- 命名回调 / 箭头函数改造

必须遵循：

1. 先改最小范围的回调签名，不整文件重写
2. 优先改“传入回调的位置”和“被传函数的签名”
3. build 通过前，不要拿旧 `src/**/*.lua` 产物当最终结论
4. 若发现 TSTL 对某个全局函数（如 `pcall`）生成异常，优先用局部别名绕开，例如：

```ts
const luaPcall: any = pcall;
```

5. 不要为了绕过 TSTL 生成问题，重新把命名函数改回匿名/箭头闭包

## 5. 命名函数优先，不要为修编码引入匿名闭包

即使在修 `this`、修 TSTL、修 no-self 问题时，也不要图省事改回：

- 箭头函数
- 内联匿名回调

正确做法是：

- 保持模块级命名函数
- 通过 `this: any` / `this: void` 或局部别名解决 TSTL 兼容

## 6. 对“乱码但功能没坏”的文件，不要顺手清洗

如果一个文件：

- 当前只是显示编码异常
- 但本轮任务并不依赖修它

则默认不要顺手整份“清乱码”。

## 7. 构建与产物核对

涉及 TSTL / Lua 产物的修复时：

1. 修改 TS
2. 运行 `npm run build`
3. 再检查 `src/**/*.lua`

不要在 build 失败时就根据旧产物下最终结论。

## 8. 任务 UI 特别补充

- 不要在同一轮里同时做“功能修复 + 结构重排 + 注释清洗 + 命名统一”
- 顺序必须是：先修功能，build 通过，验证产物，再做减法清理

## 9. 一句话口令

**中文文件小补丁优先；命名函数不回退；build 通过前不信旧 Lua；除非去检查tstl有没有 self/nil 错位，否则不用去看lua是否正确，能不整文件重写就绝不整文件重写。**
