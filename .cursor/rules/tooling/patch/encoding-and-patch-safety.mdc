---
description: 中文文件编码与补丁安全规则，避免整文件重写、编码漂移、回滚失败与 TSTL 产物误判
globs: TS/**/*.ts, src/**/*.lua, imports/**/*.fdf, **/*.md, **/*.txt
alwaysApply: false
---

# 编码与补丁安全规则

适用场景：

- 中文文件名、中文注释、中文字符串较多的 TS / Lua / FDF / 文档文件
- TSTL 回调改造、`@noSelfInFile` 调整、DzAPI UI 文件修复
- 任何“看起来像编码坏了 / 注释坏了 / 回滚失败”的场景

## 编辑纪律

- 用 `apply_patch` 做局部增删改；单次只改直接相关的函数、接口、导入或说明块。
- 不要用 shell 或脚本读完整文件再写回，尤其是 `TS/系统/**`、`TS/lib/**`、`imports/**/*.fdf` 与 `src/**/*.lua`。
- 保持 diff 小且可回滚；不要把功能修改、结构重排、格式化和注释清洗混在同一轮。

## TSTL 敏感补丁

- self、nil、pcall、Dz 回调与 `@noSelfInFile` 的语义以 [TSTL 硬规则](../../core/tstl-hard-rules.mdc) 和 [DzAPI 回调安全](../../engine/dzapi/lua-gc-desync-heuristics.mdc) 为准。
- 先改最小范围的调用点或函数签名；保留模块级具名函数，不回退为箭头或匿名回调。
- `pcall` 形态异常时优先使用局部别名，例如 `const luaPcall: any = pcall;`。

## 乱码与验收

- 终端乱码先视为显示或代码页问题；任务不依赖时，不要顺手整份“清乱码”。
- 涉及 TSTL 或 Lua 产物时：修改 TS → 运行 `npm run build` → 再检查本次相关的 `src/**/*.lua`。build 失败前，旧产物不能作为结论。
