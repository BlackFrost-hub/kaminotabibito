description: 最近周期内已复现的 TSTL 生成坑（仅增量，不重复总规则）
globs: TS/**/*.ts
alwaysApply: false
---

# 最近 TSTL 坑（增量版）

> 本文件只记录近期在本仓库真实复现过的问题。  
> 通用规则仍以 `war3-tstl/jass-pitfalls.mdc` 为主。

## 1) no-self/导出函数调用错位（`nil` 首参）

### 症状

- TS 调用看起来正确，生成 Lua 变成 `fn(nil, a, b, ...)`
- 被调函数参数整体右移，出现：
  - `timer=nil`（timer 不启动）
  - `playerId=nil`（状态读写失败）
  - `config=nil`（UI 创建失败）

### 最近实际踩点

- `safeTimerStart/safeDestroyTimer` 被 `nil` 首参调用，导致清理 timer 不执行
- `getSoleSelectedUnitForPlayer` 被 `nil` 首参调用，导致 `playerId=nil`
- UI 工具导出函数在调用侧 no-self 约定不一致，导致 frame 参数错位

### 硬约束

1. 跨模块导出函数必须统一 `this: void` + `@noSelfInFile` 语义。  
2. 生成 Lua 必须抽查调用点，确认没有参数右移。  
3. 若历史调用已大量存在 `fn(nil, ...)`，优先在中心层做兼容归一化，随后再逐步清调用点。

---

## 2) 中心层被下层反向依赖导致 `critical dependency`

### 症状

- 地图加载时报 `critical dependency`
- 常见于核心系统文件早期 require 了 lib 聚合入口，lib 又反向 require 回核心系统

### 最近实际踩点

- `中心计时器` -> `通用工具.index` -> `计时器封装` -> `中心计时器` 循环

### 硬约束

1. 核心系统底座（事件中心、中心计时器、核心桥接）禁止依赖 `lib` 聚合 `index`。  
2. 底座文件只允许最小内联 helper 或直接依赖稳定原生层。  
3. 新增 helper 时先画依赖方向：`系统底座 <- 业务/封装`，禁止反向箭头。

---

## 3) 不存在 API 的“安全调用”也会中断初始化链

### 症状

- 代码意图是“可选调用”，但实际直接调用不存在函数导致初始化中断
- UI 表现为“整块不显示”

### 最近实际踩点

- 运行环境无 `DzFrameSetLevel`，直接调用导致 BuffUI 初始化链中断

### 硬约束

1. API 是否存在以项目根目录 `jass表.txt` / `japi表.txt` 为准。  
2. 对未确认存在的 API，默认策略是“安全跳过”，不是盲目替代成另一 API。  
3. 不做全仓机械替换（例如直接把 `SetLevel` 全改 `SetPriority`），先确认语义需求。

---

## 4) 同一功能禁止混用两套机制（引擎托管 vs 手动托管）

### 症状

- 日志显示“已 show”，但界面仍不符合预期
- 根因是同一对象被两套系统同时管理

### 最近实际踩点

- 自定义 tooltip 同时使用：
  - 手动 hover `show/hide`
  - `DzFrameSetTooltip` 引擎托管

### 硬约束

1. 同一 UI 功能只能选一种托管模式。  
2. 选手动托管时，删除引擎托管绑定；选引擎托管时，删除手动 show/hide 链。

---

## 5) 规则优先级（本文件适用范围）

本文件仅记录“最近复现过”的增量坑，优先级如下：

1. 本文件（近期复现增量）
2. `war3-tstl/jass-pitfalls.mdc`（通用 TSTL 规则）
3. 相关子系统规则（`dzapi/`、`stes-ydlocal/` 等）

若冲突，先遵循更具体、离改动文件更近的规则。

