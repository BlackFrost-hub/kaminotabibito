---
description: 魔兽模型批量预览器 v4.1.5 的 GUI 操作、实时刷新、自主视觉迭代与验收流程
globs: imports/Common/Effect/**/*.mdx, imports/Common/Effect/**/*.mdl, scripts/generate-*.js, scripts/export-*.js, TS/系统/03．技能系统/**/*.md
alwaysApply: false
---

# 魔兽模型批量预览器 v4.1.5 使用规则

本规则专用于本机的 `魔兽模型批量预览器 v4.1.5`。制作或修改 Warcraft 3 模型时，AI 应主动使用该软件查看实时模型画面并自主迭代，不要等待用户反复截图指出同一类问题。

该软件本质是由独立 EXE 承载的浏览器/WebView 界面。Computer Use 能读取其中的按钮、组合框、选项、滑块和 Web 内容可访问树，因此应优先按控件语义操作，而不是盲点固定坐标。它不是 Codex 内置浏览器页面，除非实际发现可附着的浏览器目标，否则不要擅自改用 Browser 插件。

## 一、软件位置与窗口识别

```text
程序：C:\Users\Administrator\Desktop\魔兽模型批量预览器_v4.1.5.exe
进程窗口：process:C:\Users\Administrator\Desktop\魔兽模型批量预览器_v4.1.5.exe
窗口标题：魔兽模型批量预览器 v4.1.5 QQ群：1080617689
```

1. 控制 GUI 前必须先读取并遵守 Codex 的 `computer-use` 技能规则。
2. 先使用 Computer Use 列出窗口，按完整进程路径筛选；只有唯一匹配窗口时才可操作。
3. 软件已经打开时复用现有窗口，不重复启动。未打开时才使用明确的 EXE 路径启动。
4. 不通过 PowerShell、Win32 UI Automation 或坐标脚本绕过 Computer Use。
5. 每次操作前重新捕获窗口状态。优先使用可访问树中的按钮名、组合框、选项和元素索引；模型画布、相机拖动等没有语义控件的区域才使用当前截图 ID 与坐标。
6. 截图列表可能同时包含 15×15 的窗口图标和完整窗口画面。坐标操作必须选择面积最大的实时截图，禁止误用图标截图。

## 二、从批量模型进入单模型预览

1. 软件当前资源目录必须是本次模型所在目录；先核对左侧的 `目录:` 文本和目标模型文件名。
2. 主界面中央显示当前页的模型预览卡片。直接点击目标模型卡片的预览画面，即可进入该模型的单模型预览。
3. 模型卡片下方的复选框用于批量选择，不是进入单模型预览的必要步骤；只查看模型时不要误勾选后操作导出或删除。
4. 当前目录模型很多时，优先搜索、翻页或在当前页找到目标卡片后单独点开。单个模型的制作与优化默认使用单模型预览，不需要先打开模型地图。
5. 批量页面的卡片缩略图只用于快速定位和初步观察；动作、近景、视角和结束帧仍在单模型预览中检查。

## 三、按需打开模型地图

模型地图是左侧的独立功能，不是进入单模型预览的前置步骤，也不能把“模型地图 → 单模型预览”写成固定流程。

1. 只有需要同时比较少量模型、查看多个模型的相对尺寸和朝向，或在统一地面场景中检查地面效果时，才点击左侧 `模型地图`。
2. 出现“模型地图加载方式”对话框后，优先点击 `加载当页（N）`，只加载当前页模型。
3. 当前页模型过多、目标模型难以观察时，模型地图不如单模型预览；应返回批量模型主界面，直接点击目标模型卡片单独查看。
4. 只有目标比较组不在当前页且确实需要批量对比时，才选择 `加载200个`、`加载500个`、`加载1000个` 或 `加载全部`。不要无意义加载大量模型。
5. 模型地图适合横向比较，不替代单模型的动画、时间轴和近距离质量验收。

## 四、模型地图顶栏与批量对比

模型地图是浏览器式全屏画布。顶栏提供分页、全局动作、地面与布局控制，应按任务使用：

1. `上一页`、`下一页` 和 `当前页/总页数` 用于定位已加载模型。目标不在当前页时先翻页，不要重新加载全部模型。
2. `全局动画` 下拉框可选择 `--`、`Stand`、`Walk`、`Attack`、`Death`、`Birth`。比较同类模型时选择相同动作，使当前地图内所有支持该动作的模型同步播放。
3. `--` 表示不强制统一动作。检查某个模型的真实序列时，仍需进入单模型预览逐项选择，不能仅依赖全局动作。
4. 切换全局动作后必须等待动画开始，再分别观察开头、中段和末段；不能只看切换瞬间。
5. `地面纹理` 应选择接近实际地图的地表，地面法阵、拖尾、裂纹和范围层必须在有纹理地面上检查边界、亮度和辨识度。黑底只能辅助检查透明度，不能作为唯一视觉依据。
6. `自定义` 地面只在确有对应测试地表时使用，不随意导入或替换文件。
7. `全局高度` 用于批量观察模型与地面的相对位置。默认保持 `0`；只为诊断沉地或悬空临时调整，不能把滑块结果当成模型坐标已修复。
8. `列间距`、`行间距` 用于避免多个模型互相遮挡。调整只影响模型地图布局，不代表模型实际尺寸或技能间距。
9. `旋转方向` 用于统一观察朝向。需要比较正面、背面或弹道方向时可切换，但最终仍需进入单模型预览确认模型自身坐标。
10. 顶栏 `重命名导出` 默认保持关闭。它属于导出功能，与预览验收无关，不得为查看模型而开启。
11. 模型地图画布操作遵循界面提示：左键选择或框选，右键平移视角，`Ctrl+左键` 旋转视角，滚轮缩放，双击按软件当前提示执行重置。每次切换相机后重新捕获画面。
12. 批量比较时至少检查一次俯视和一次斜视。地面模型在俯视下完整，不代表 Warcraft 常用斜视下仍清楚。

## 五、单模型实时预览操作

1. 从批量模型主界面点击目标模型卡片进入单模型预览；不要假设必须先进入模型地图。
2. 每次观察前重新读取窗口状态，不复用旧截图 ID、旧元素索引或旧坐标。
3. 使用动作下拉框逐项检查模型实际存在的所有序列。常见快捷键只作辅助：`S` 为 Stand、`D` 为 Death；Birth 等没有稳定快捷键的动作必须从动作列表选择。
4. 使用左键旋转、右键平移、滚轮缩放、双击重置；至少检查俯视、游戏常用斜视、低角度斜侧和近距离视图。
5. 需要检查动画中段和结束帧时，可使用时间轴、暂停、逐帧按钮；不要只截动作开头一帧。
6. 预览器 RGB、背景亮度、俯仰、翻滚和偏航控件只用于观察。除非任务明确要求运行时调色，否则不得把预览器滑块效果当成模型已永久修改。

## 六、实时更新规则

此版本预览器会监测已打开目录中的模型文件变化。同路径 MDX 被重新生成或覆盖后，批量主界面卡片与单模型预览会实时重新加载；已经打开的模型地图场景不会热重载该模型。

1. 生成器写回目标 MDX 后，先等待预览器完成自动刷新，再重新捕获窗口画面。
2. 若当前处于模型地图，修改模型后必须先关闭模型地图，返回批量主界面确认卡片已更新，再重新点击 `模型地图` 并选择 `加载当页（N）`。禁止复用修改前已经加载的模型地图场景判断新版本。
3. 默认相信按上述流程重新加载后的实时画面对应最新文件，不得把“效果没区别”惯性归因于缓存。
4. 若修改未体现，先检查生成命令是否成功、目标绝对路径、修改时间、文件大小、模型名和结构数据；优先认定生成逻辑或模型结构仍有问题。
5. 只有文件元数据已变化、结构差异明确存在、模型地图也已按本节重新加载、预览器仍显示旧结构等证据同时成立时，才允许排查加载异常。
6. 本规则只描述该预览器 v4.1.5。Warcraft 3 游戏本体仍可能缓存导入资源，不能把两者混为一谈。

## 七、自主迭代闭环

制作中的模型必须执行以下闭环，直到达到可交给用户验收的质量：

1. 修改 Blender 源、MDL 生成器或导出脚本。
2. 重新生成 MDX，验证 FormatVersion、序列、Geoset、骨骼、材质、贴图路径和 sanity 结果。
3. 在批量模型主界面直接点击目标模型卡片，进入单模型预览。
4. 在单模型预览中检查全部动作、动画中段、结束帧和多个视角。
5. 只有确实需要横向比较或地面场景时，才打开模型地图并选择 `加载当页（N）`；模型过多影响观察时立即回到单模型预览。
6. 明确记录画面中的具体问题，例如：线条过粗、结构像范围圈、颜色过曝、平面感、骨骼错绑、Birth/Death 幅度不足、某层不随动画、黑边、穿模或贴图丢失。
7. 针对实际根因修改，不得只改文件名、颜色或无关参数制造“已经优化”的假象。
8. 再次生成并回到第 2 步。新截图必须来自修改后的实时窗口状态。

若实时预览已经明显暴露结构问题，AI 应继续优化，不应把明显不合格版本交给用户询问“这样可以吗”。

## 八、动作与结构验收

- `Birth`：主体应有明确展开、出现或建立过程；不能只从 90% 缩放到 100% 冒充动画。
- `Stand`：持续层必须稳定可见，旋转层应保持结构关系，不能长期播放后错位成线团。
- `Death`：需要退场的模型必须整体消失、破碎或收束；不能只缩小少量，也不能留下未绑定的外圈。
- 骨骼：逐项核对 Geoset 的矩阵索引与实际 Bone ObjectId，避免整体错一位绑定。
- 地面模型：俯视轮廓必须清楚，但不能仅像白色选择圈或范围提示；还要检查游戏常用斜视下的可读性。
- 立体模型：至少检查正面与侧面厚度，不能用单张纸片冒充镜面、屏障、晶体或实体标记。

## 九、操作边界

1. `删除选中`、`导入模型`、`导出选中`、`导出全部`、批量重命名等会改变文件的按钮，除非用户明确要求，否则不要操作。
2. 预览阶段只查看桌面制作目录中的候选，不自动迁入 `imports`，不接入 TS 技能逻辑。
3. 用户确认通过后，才按项目资源规则迁移、改内部私有贴图路径并更新资源文档。
4. 技术预览器可以用于高频自我迭代，但最终正式验收仍以 Warcraft 3 游戏内实际镜头和技能时序为准。

## 十、交付说明

完成一轮优化后应向用户说明：

- 实际查看的是哪个绝对路径模型；
- 在预览器中检查了哪些动作和视角；
- 发现并修复了什么具体问题；
- 当前模型是否仍在桌面制作目录，是否尚未迁入项目；
- 若尚有只能由游戏实机确认的风险，应明确列出，不能用技术预览器结果代替。
