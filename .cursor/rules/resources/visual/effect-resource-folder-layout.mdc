---
description: Warcraft 3 特效模型资源目录布局规则：分类目录直接放模型，共用 Texture 贴图目录
globs: imports/Common/Effect/**/*.mdx, imports/Common/Effect/**/*.mdl, imports/Common/Effect/**/*.blp, TS/系统/03．技能系统/**/*.ts, TS/系统/03．技能系统/**/*.md
alwaysApply: false
---

# 特效资源目录布局规则

## 基本结构

- 不要为每个特效模型单独创建一个同名子文件夹。
- 同一个分类目录下，所有 `.mdx` / `.mdl` 模型直接放在分类目录中。
- 同一个分类目录只保留一个共享 `Texture` 文件夹，用来放该分类下所有模型贴图。

正确示例：

```text
imports\Common\Effect\Form\Aura\HolyAura.mdx
imports\Common\Effect\Form\Aura\BloodAura.mdx
imports\Common\Effect\Form\Aura\Texture\Shockwave10.blp
```

错误示例：

```text
imports\Common\Effect\Form\Aura\HolyAura\HolyAura.mdx
imports\Common\Effect\Form\Aura\BloodAura\BloodAura.mdx
```

## 路径规则

- 游戏内模型路径必须去掉 `imports\` 前缀，例如 `Common\Effect\Form\Aura\HolyAura.mdx`。
- MDX / MDL 内部的项目私有贴图路径必须去掉 `imports\` 前缀，并指向分类目录共享 `Texture`，例如 `Common\Effect\Form\Aura\Texture\Shockwave10.blp`。
- Warcraft III 原生贴图必须保留源模型中的原生游戏路径，例如 `Textures\CloudSingle.blp`、`Textures\GenericGlow64.blp`；禁止改写到分类 `Texture`，也不要复制进 `imports`。
- 导出工具可能把原生贴图一并导出到模型目录；源目录里存在同名 `.blp` 不能证明它是私有贴图。迁移前必须用 Warcraft MPQ/CASC 文件索引按完整游戏路径判定。
- 只有模型名是 `file_000123`、`unnamed`、纯数字等无语义名字时，才按用途改成语义化名字；原本有明确语义的模型名不要无脑改名。

## 整理已有资源时

1. 先解析模型贴图表并区分原生贴图与私有贴图，只把私有贴图复制到父分类目录的共享 `Texture`。
2. 把模型移动到父分类目录。
3. 只修改模型内部的私有贴图路径，从 `...\模型子目录\Texture\...` 改为 `...\Texture\...`；原生贴图路径保持不变。
4. 修改 TS / 文档里的模型引用路径。
5. 确认旧子目录没有唯一资源后再删除。

## 文字资产目录

- `.cursor\rules\resources\visual\effect-asset-catalog.md` 只保留总入口、检索方法和数量概览。
- 详细视觉条目必须写入 `.cursor\rules\resources\visual\effect-asset-catalog\`，并与 `imports\Common\Effect` 的 `Form / Element / Projectile` 分类对齐。
- `Form` 和 `Element` 继续按实际子目录拆分 Markdown；不得把所有模型追加回一个大表。
- 每个模型只在一个主分类文件中建立正式条目，跨用途通过文字标签检索，不复制相同条目。
- 新建资源分类目录时，必须同时更新对应文字目录的 `README.md`；新建文字分类时，也必须确认存在对应的导入目录或明确标注为待创建。
