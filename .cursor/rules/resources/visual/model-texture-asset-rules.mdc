---
description: 技能/表现模型贴图资源经验：贴图挂模型、BLP 透明、MDX 材质与游戏内黑底排查
globs: imports/**/*.mdx, imports/**/*.mdl, imports/**/*.blp, imports/**/*.png, TS/系统/03．技能系统/**/*.ts, TS/系统/09．表现系统/**/*.ts
alwaysApply: false
---

# 模型与贴图资源经验

## 制作流程

1. 先找同类现成模型做模板，例如伤害数字、技能提示圈、Buff 图标模型；确认它在游戏内已经正常显示透明。
2. 准备源 PNG：尺寸先按显示质量做足，图标类优先 `100x100`；背景必须真透明，不要只靠查看器显示成透明。
3. 复制模板 MDX，并只改必要内容：
  - `TEXS` 贴图路径
  - 必要的材质过滤模式
  - 必要的模型缩放或面片尺寸
4. PNG 转 BLP：
  - 正式透明 BLP 必须使用 `scripts/war3-transparent-blp.js` 调用内置原生转换器
  - 禁止用 `war3-texture-preview` 的 `encodeTextureBuffer(..., 'blp')` 生成正式透明 BLP
  - 转完使用 `war3-texture-preview` 解码回临时 PNG 检查；检查完立即删除临时 PNG
  - 对透明贴图同时核对 BLP 头、alpha 和画面，不只看肉眼预览
5. 检查 MDX + BLP：
  - MDX 内部贴图路径和实际导入路径一致，并且不能带 `imports\` 前缀
  - BLP 尺寸正确
  - BLP 有 alpha，透明区 alpha 为 0
  - 正式透明 BLP 为 `BLP1 compression=0 / alphaBits=8 / pictureType=3 或 pictureType=4`
  - 材质 `FilterMode` 对透明图标可用
6. 接入 TS 时只引用模型路径，不直接引用 PNG；路径必须去掉 `imports\` 前缀。
7. 打包/导入后必须重开地图进游戏看实物效果；模型查看器、插件预览、PNG 预览都不能替代游戏内验证。

## 验收流程

每个新贴图模型至少确认这些点：

- 进游戏能显示模型，不是绿方块、黑图、丢贴图。
- 背景透明，没有完整黑色方块。
- 边缘没有明显黑边、脏边、白边。
- 大小和同类表现一致；宁可运行时缩小，不要源图过小再放大。
- 上浮、缩放、淡出等动画由已有表现系统驱动时，要确认模型不会抖动、错位或被数字遮住。
- 如果同一模板批量生成多个模型，必须至少抽查每一个；不要只测其中一个就默认全对。

## 贴图挂模型

- 简单图标模型优先复用现有同类数字/提示模型结构，只替换贴图路径和贴图文件，不要从零手搓复杂模型。
- 新模型路径和贴图路径要同时核对：MDX 内部 `TEXS` 路径、TS/物编引用路径都必须使用游戏内导入路径，不能带 `imports\` 前缀。
- 图标类贴图建议先做足够大（如 `100x100`），游戏内通过模型缩放控制大小；不要为了显示小就先把源贴图做得太小。

## 静态贴图与动画分层

- 地面残留、墨汁、沼泽、火焰路径、冰霜地面等持续区域，默认拆成两层思考：静态底图负责形状、颜色、材质真实感；动画层负责气泡、流动、涟漪、火星、烟雾等运动感。
- 不要为了“看起来干净”把原贴图的真实纹理全部抹平；如果原图已经更像目标材质，应优先保留真实质感，只修明显不符合语义的颜色、路径、透明边、黑底和脏边。
- 静态贴图里不要放明显应该运动的重复圆圈、气泡环、波纹环、爆点残影。没有动画时，这些元素会被玩家理解成漏了动画；需要这些表现时，应单独做循环动画模型或粒子叠层。
- 如果临时没有动画资源，优先交付“可信的静态底图”，并在文档或配置里标注后续补动画层；不要用静态假动画替代真正动画。
- 修改现有特效贴图时，先保留一份上一版可用预览，改完和原图并排比较。只有新图在语义、清晰度、游戏内可读性上明显更好，才覆盖正式 BLP。
- 墨汁/水渍/腐败沼泽这类软质地面效果，底图可以有细碎噪点、边缘厚薄变化和不规则透明度；气泡、冒泡、波纹、上涌烟雾等动态内容应作为独立模型或粒子层处理。

## BLP 透明

- PNG 看起来透明不代表进游戏透明；插件能回读透明也不代表 Warcraft 1.27 实机兼容。正式透明 BLP 必须同时通过头部、Alpha、MDX 材质和实机四项验收。
- 正式透明 BLP 统一使用 `scripts/war3-transparent-blp.js` 调用内置原生转换器，输出 `BLP1 compression=0 / alphaBits=8 / pictureType=3 或 pictureType=4`。不得直接使用 `war3-texture-preview` 的 `encodeTextureBuffer(..., 'blp')` 作为正式透明 BLP。
- `war3-texture-preview` 只用于把正式 BLP 回读为 PNG/TGA，检查尺寸、方向、颜色和 Alpha。回读文件属于临时预览，必须放在明确的临时目录，检查完立即删除；可编辑源 PNG 不属于临时预览，按资源源文件规则保留。
- 贴图挂在 MDX 面片上时，如果进游戏出现完整黑色方块，优先检查 BLP 透明兼容，而不是继续修 PNG 黑边。
- 本项目伤害数字贴图可作为透明 BLP 参考：`imports/UI/DamageNumbers/DmgNumTex_*.blp`。
- 已确认的失败格式：`war3-texture-preview` 的 `encodeTextureBuffer(..., 'blp')` 当前输出 `BLP1 compression=1 / pictureType=5` 直通调色板格式。插件自身能正确回读其中的 Alpha，但 Warcraft 1.27 实机在透明面片上会显示黑底。
- 透明图标 BLP 应重点核对：
  - `compression = 0`
  - `alphaBits = 8`
  - `pictureType = 3 或 pictureType = 4`
  - 尺寸正确
  - 解码后透明区 alpha 确实为 0
  - 半透明区和可见区都不为空

## MDX 材质

- 透明图标模型必须检查材质过滤模式；如果 `Blend` 在游戏里表现为黑底，优先改成 `Transparent`。但 `Transparent` 不能弥补不兼容的 BLP 编码，材质和 BLP 头必须同时合格。
- 不要只看模型查看器或 PNG 预览下结论；最终以游戏内特效显示为准。
- 对同一批由同模板生成的模型，一个有黑底，通常三个都会有同类问题，先查模板材质和 BLP 编码，不要逐张贴图乱修。

## 常见排查顺序

1. 看游戏截图：黑边是局部边缘，黑底是整张面片。
2. 黑边：处理 PNG 半透明边缘和透明像素 RGB，并重新转 BLP。
3. 黑底：先确认正式 BLP 不是 `compression=1 / pictureType=5`，再检查 Alpha 与 MDX `FilterMode`；不能因为插件回读透明就跳过 BLP 头检查。
4. 修完重新导入/打包并彻底重开游戏，避免 War3 使用旧资源缓存；最终以实机截图为准。
