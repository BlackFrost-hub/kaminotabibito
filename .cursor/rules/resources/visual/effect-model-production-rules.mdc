---
description: Warcraft 3 专属特效模型制作与验收规则：选型、2D/3D、MDL/MDX、动画、贴图、缓存、迁移、性能和常见失败
globs: imports/Common/Effect/**/*.mdx, imports/Common/Effect/**/*.mdl, imports/Common/Effect/**/*.blp, imports/Common/Effect/**/*.tga, imports/Common/Effect/**/*.png, scripts/generate-*.js, scripts/migrate-*.js, TS/系统/03．技能系统/**/*.md
alwaysApply: false
---

# Warcraft 3 特效模型制作与验收规则

本规则用于制作、改造和迁移 Warcraft 3 的 Boss 专属特效模型。目录与路径仍以同目录的 `effect-resource-folder-layout.mdc` 为准，贴图通用规则仍以 `model-texture-asset-rules.mdc` 为准。

## 已准备好的 Blender + MCP 环境

本机已经完成 Blender 与官方 Blender MCP 配置，后续 AI 不要重复下载或安装。

```text
Blender 版本：5.1.2
Blender 主程序：F:\Blender\5.1.2\blender-5.1.2-windows-x64\blender.exe
推荐启动脚本：F:\Blender\启动-Blender-5.1-MCP.cmd
Blender 用户配置：F:\Blender\UserData\5.1\
官方 MCP 源码：F:\Blender\MCP\blender_mcp\
MCP Python 环境：F:\Blender\MCP\venv\
MCP 服务端：F:\Blender\MCP\venv\Scripts\blender-mcp.exe
Codex MCP 名称：blender
Blender 本地桥：localhost:9876
```

- 官方 Blender MCP 版本为 `1.0.0`，扩展已安装并启用。
- Codex 全局 MCP 配置已经登记；可用 `codex mcp get blender` 检查，不要重复添加同名服务。
- 使用交互 MCP 前，先通过 `启动-Blender-5.1-MCP.cmd` 打开 Blender，等待本地桥启动。
- 当前 Codex 任务如果早于 MCP 安装时间创建，可能不会动态显示 Blender 工具；重新打开 Codex 或新建任务即可。
- Headless 自动建模可直接调用 Blender 主程序的 `--background --python`，不要求打开 GUI。
- Blender 4.5.9 已移除。旧 Warcraft MDL 导出桥尚未安装；只有确实需要兼容旧导出插件时，才另装隔离的旧版 Blender。
- 官方 MCP 会执行 AI 生成的 Blender Python。只在专用建模目录中操作，保存 `.blend` 版本和备份，不把敏感文件目录作为建模工作目录。

## 一、先决定是否真的要制作

1. 先搜索同目录的 `effect-asset-catalog.md`，再查 `imports\Common\Effect` 的对应分类、正式 Boss 模型动作和通用预警资源，不硬造重复内容。
2. 通用直线、扇形、四扇区、圆形预警已经能承担的内容，不再制作 Boss 专属预警模型。
3. 正式人物模型已经有合适攻击动作时，优先复用人物动作，只新增覆盖层、投射物、屏障、镜缘、阶段动画等主体特效。
4. 半透明亡灵、镜像、投影优先复用现有人物模型，并在运行时调透明度和颜色；只有多开正式模型性能过高时，才制作低成本替身。
5. 不合适的候选不迁移、不写入正式记录；找到明显更好的资源时可以替换旧候选。
6. AI 生图只负责钟面、蔷薇、血月、血晶、箭矢等 2D 纹理，不用 AI 图片代替完整人物模型和动作。
7. 外部模型迁入前计算完整 SHA-256；与项目模型完全相同时直接复用已有路径，不允许换名后再次导入。
8. 用户确认的新模型完成后，同步把游戏内路径、视觉描述、用途、限制、字节数和 SHA-256 前 16 位写入 `effect-asset-catalog.md`。

## 二、2D、3D 与混合 3D 的判断

### 保持平面的情况

- 光环、地面法阵、预警轮廓、镜面、月晕、刻度、数字、图标等，本质上就是平面信息。
- 需要始终朝向镜头的状态图标、倒计时刻度和小型装饰片，可以使用广告牌面。
- 不要为了“看起来高级”把所有平面元素强行挤出厚度。

### 应该有体积的情况

- 枪、箭、晶体、屏障、实体镜框、铠甲拘束、可绕视角观察的主体，应使用真实几何或浅体积结构。
- 写实月球、心脏等主体可以使用“浅浮雕弧面 / 单层凸面 + 平面辅助层”的混合结构，不必做完整封闭球体。

### 混合结构的原则

- 实体部分负责轮廓、厚度和侧视可读性。
- 透明贴图部分负责复杂细节、辉光、刻度、花纹和软边。
- 不要用多层几乎重合的透明面假装体积，容易产生深度排序、条带、闪烁和穿模。
- 不要让实体花托、凸框、轨道或装饰块盖住透明贴图主体。必要时在花朵、徽记等位置断开框线，而不是依赖 `PriorityPlane` 强行压过真实几何遮挡。

## 三、坐标、朝向与广告牌

1. Warcraft 3 常见正面广告牌应使用 YZ 竖直平面，即 X 基本恒定；XY 平面是水平地面片。
2. 不要只凭技术预览器判断朝向。必须至少检查正面、斜侧面、近侧面和游戏内实际镜头。
3. 需要始终面向镜头的独立图案，应让每个实例拥有自己的广告牌骨骼和正确枢轴；一个共享广告牌骨骼不适合控制分散在多个位置的图案。
4. 交叉面片只能在明确验证后使用。它会增加过亮、重叠和切片感，不能作为“侧面看不到贴图”的默认修复。
5. 平面图案侧视变窄是正常透视结果。若设计上只要求正面叠加到镜面，不要为了极端侧视而破坏正面质量。
6. 骨骼旋转、缩放必须围绕对应元素的枢轴。多个分散元素若共用错误枢轴，会整体漂移或绕模型中心转动。

## 四、几何制作

1. 简单特效优先用脚本生成 MDL，再由可靠解析器保存为 MDX；生成器就是可编辑源。
2. 正式交付默认只保留 `.mdx`。如果已经有生成脚本，不要在资源目录旁再留同名 `.mdl`；只有明确需要人工编辑时才保留 MDL 源。
3. 箭、枪、晶体等主体必须有清楚的尖端、杆身或骨质结构，不能只靠一条光尾表达。
4. 透明写实球体不要直接做完整前后球壳：前后面会叠加，常出现横向条带、接缝、色块和排序错误。优先做无背面的单层凸面或浅浮雕面。
5. 透明材质的 `TwoSided` 只在确有需要时开启。凸面主体同时使用背面和双面材质，容易造成重复采样和伪影。
6. 镜框、屏障等实体结构的前后面和侧壁要有合理厚度，但厚度不能大到遮挡贴图装饰。
7. 几何相交不是装饰。线条穿过月球、花朵、枪身等主体时，应调整半径、深度、断开区间或层级，而不是只改颜色。
8. 任何“看起来像占位块、八边形底座、粗支架”的几何都应删除或弱化，除非它本身就是设计主体。

## 五、动画与序列

1. 可持续模型至少包含 `Birth / Stand / Death`。
2. 一次性冲击也要有安全的清理序列，避免特效停留、回到首帧或销毁前闪现。
3. `Birth` 负责展开、进入、坠落或聚合；`Stand` 负责稳定循环；`Death` 负责破碎、消散或淡出。
4. 屏障必须能表达展开、维持、破碎，不能只有常驻球壳。
5. 心跳不能用整个模型的单轴缩放冒充。心脏应绑定独立骨骼，做轻微宽高收缩与回弹；外圈、刻度和 UI 结构不能跟着抖。
6. 十二刻度倒计时应有 12 个清楚的离散状态。每秒切换时使用很短的过渡帧移动或熄灭到下一个点，避免连续匀速旋转造成读秒含糊。
7. 若后续配 12 个滴答 SFX，声音由技能时序驱动；不要因为模型有 12 个刻度就默认把声音嵌进模型。
8. 死亡、破碎和粒子收束要控制亮度。战败消散应安静收束，不要自动叠加大范围冲击环和过曝爆炸。

## 六、贴图生成、转换与压缩

1. 透明源图必须保留真实 alpha；黑底预览不等于透明。
2. TGA 必须保留 alpha。PNG、TGA、BLP 的大小写和相对路径都要实测。
3. 优先使用 `war3-texture-preview` 插件完成 BLP 与 PNG/TGA 的转换。
4. BLP 转换后必须再用插件回读成 PNG 或 TGA，核对尺寸、方向、颜色和 alpha；不能只看源 PNG。
5. 图案类贴图先按实际屏幕占比选择尺寸。小型花朵、心脏、箭头等通常先测试 128 或 256；没有可见收益时不要保留 512×512、数百 KB 的贴图。
6. 压缩前保留可编辑 PNG 源；正式模型引用分类 `Texture` 目录中的 BLP/TGA，不引用 `image_temp`。
7. 透明像素的 RGB、半透明边缘和抠图残色都要检查，避免黑边、白边、绿边和脏边。
8. 预览器中的 RGB 滑块、粒子颜色或材质编辑不一定写回原模型。要永久改色，应实际修改材质/GeosetAnim；只需要技能实例变色时，使用运行时 `SetSpecialEffectColor`。
9. 出现默认蓝色、队伍色或与设计无关的冷色时，检查 `ReplaceableId`、队伍色材质和实际 GeosetAnim 颜色，不要只在预览器里拖 RGB。
10. MDL/解析器里的颜色通道顺序必须用生成后的实物验证，不要只按字段数组位置猜测 RGB；同一颜色至少检查 MDL、MDX 回读和预览结果。
11. 箭头、枪尖等贴图主体若显示成白色菱形或纯色块，优先检查贴图路径、材质 TextureID、UV 和 alpha，不要继续用光尾遮住错误主体。

## 七、模型与贴图路径迁移

1. 迁移 MDX 时必须完整解析模型引用的全部贴图路径，不凭文件名猜测。
2. 私有贴图复制到目标分类的共享 `Texture` 目录，并同步改写模型内部路径。
3. 游戏内路径、TS 配置和 MDX 内部贴图路径都不能带 `imports\` 前缀。
4. 路径大小写、反斜杠、扩展名必须与真实资源一致。
5. 复制完成后重新解析目标 MDX，确认它不再引用桌面、旧子目录或缺失贴图。
6. 工作树很脏时只处理本任务资源，保留用户无关改动；删除文件前确认它确实是本任务的重复源或临时文件。

## 八、缓存与“看起来没变化”处理

这是高优先级规则。任何同名模型出现“修改后完全没区别”时，先停止继续改模型。

1. 核对目标 MDX 的绝对路径、文件大小和修改时间。
2. 关闭模型预览窗口并重新打开，不只点击刷新或切换动画。
3. 确认预览器打开的是 `imports` 中的正式文件，而不是桌面导出、旧副本或临时目录。
4. 必要时复制为一个从未使用过的临时模型名进行对照，验证后删除临时副本。
5. 对比明确可识别的结构变化，例如顶点数、面数、几何厚度和贴图路径；不要仅凭颜色或透明度判断是否刷新。
6. 只有排除缓存后，才允许继续改几何、骨骼、材质或贴图。
7. 不要因为缓存未刷新而连续叠加交叉面、花托、PriorityPlane 或额外材质；这会把原本正确的模型改坏。
8. Warcraft 3 本体也可能缓存资源。覆盖同名资源后需要重新进入地图，必要时彻底关闭并重开游戏。

## 九、验证与验收

### 结构检查

- 生成后重新加载 MDX，核对模型名、序列、Geoset、骨骼和贴图路径。
- `mdx-m3-viewer` sanity 的 `errors` 和 `severe` 必须为 0。
- 纯点特效出现 `Missing Origin` 通常只是良性警告，但仍要确认不影响挂点和运行时位置。
- `unused` 动画轨道不能直接忽略：确认是校验器误判，还是骨骼没有蒙皮、关键帧没有被任何可见元素使用。

### 视觉检查

- 关闭并重开预览器后再验收，核对修改时间。
- 检查 `Birth / Stand / Death` 三个序列，而不是只看 `Stand`。
- 检查正面、斜侧、侧面、俯视和实际游戏镜头。
- 检查贴图是否丢失、黑底、脏边、条带、闪烁、穿模、过曝、深度排序错误。
- 对需要面向镜头的元素，确认它在游戏内正确朝向；技术预览器不能替代实机。
- 正式验收以游戏内截图和实际技能时序为准。

## 十、性能控制

1. 多个半透明人物、完整球壳和多层大面积 Blend 材质非常耗性能。
2. 军魂群像、镜像和投影应限制数量、错峰出现、降低面数和亮度，禁止同时满亮度叠层。
3. 能用单层凸面解决的主体，不做前后双层透明球体。
4. 能用一张清楚贴图解决的小装饰，不默认复制成三张或更多交叉面。
5. 粒子发射率、寿命和覆盖面积按实际镜头测试，不以预览器“越亮越好”为标准。

## 十一、交付与记录

1. 完成一个模型先让用户看预览或实机，再继续下一个，避免批量生产后整体返工。
2. 用户确认可用后再更新特效库和候选筛选记录；失败版本和不合适候选不写入正式记录。
3. 记录模型路径、用途、叠加关系、运行时改色要求、序列和私有贴图。
4. 生成脚本、迁移脚本和可编辑贴图源要能重现正式 MDX。
5. 不打包地图、不接技能、不修改正式人物动作，除非用户明确要求。

## 十二、问题处理顺序

遇到问题时按以下顺序排查，禁止跳过缓存直接堆补丁：

1. 路径是否正确。
2. 预览器或游戏是否仍在使用缓存。
3. MDX 是否真的重新生成，大小和时间是否变化。
4. 贴图是否存在、路径是否正确、BLP 回读是否保留 alpha。
5. 材质过滤、双面、颜色和透明度是否正确。
6. 面片朝向、枢轴、骨骼蒙皮和深度关系是否正确。
7. 几何结构是否确实需要修改。
8. 最后才考虑重新生图或重做模型。
