---
description: DzAPI UI Frame 类型与避坑（1.27e）
globs: TS/**/表现/**/*.ts, TS/**/*UI*.ts
alwaysApply: false
---

# DzAPI UI Frame 类型与避坑

## 支持的 Frame 类型（DzCreateFrameByTagName）

| 类型 | 用途 |
|------|------|
| BACKDROP | 背景 |
| TEXT | 文本 |
| TEXTAREA | 文字框 |
| GLUETEXTBUTTON | 按钮 |
| GLUECHECKBOX | 复选框 |
| POPUPMENU | 弹出菜单 |
| SCROLLBAR | 滚动条 |
| SPRITE | 电影肖像/小精灵 |
| SLIDER | 滑块 |
| BUTTON | 按钮 |
| EDITBOX | 编辑框/文本框 |
| HIGHLIGHT | 高光模板 |
| MENU | 菜单 |
| DIALOG | 对话框 |
| SIMPLEFRAME | 简单框架 |
| SIMPLESTATUSBAR | 简易工具栏 |
| SIMPLECHECKBOX | 简单复选框 |

## 最小「提示框 / 文本框底」：只建一个 BACKDROP（本图约定，避免乱叠层）

**目标**：提示框、简单边框底图 → **仅一个 `BACKDROP`**，不要默认套 EscMenu 金边切片、不要多条细边拼框（易错位），除非你明确要复杂样式。

**JASS 参考（父级 `DzGetGameUI()` + 绝对点 + 尺寸 + 贴图）：**

```jass
call YDLocal1Set(frame, "主线任务", DzCreateFrameByTagName("BACKDROP", "主线任务", DzGetGameUI(), "template", 0))
call DzFrameSetAbsolutePoint(YDLocal1Get(frame, "主线任务"), 4, 0.20, 0.5573046)
call DzFrameSetSize(YDLocal1Get(frame, "主线任务"), 0.03, 0.03)
call DzFrameSetTexture(YDLocal1Get(frame, "主线任务"), "war3mapImported\\wenbenkuang.blp", 0)
```

- **`DzFrameSetAbsolutePoint(..., 4, x, y)`**：`4` = CENTER（与工程里 `FramePoint.CENTER` 一致）。  
- **边框/底图一体**：用地图已导入的 **`war3mapImported\wenbenkuang.blp`** 一张贴图即可（路径与 `DzFrameSetTexture(..., 0)` 一致）。

**TS 等价流程（`TS/系统/09．表现系统/UI工具.ts`）**：`createFrame({ type: BACKDROP, parent, template: "template" })` → `setFramePosition` 或 `setFramePointRelative`（相对父级时用）→ `setFrameSize` → `setFrameTexture(path)`（内部即 `DzFrameSetTexture`）。需要文字时再 **单独** `createTextLabel` 叠在子级，勿把 `human-options-menu-border` 等 **Edge 切片**当整图拉伸。

**工程示例**：`TS/系统/05．Buff系统/02．BuffUI.ts` 中 Buff 提示框（单 BACKDROP + `wenbenkuang`）。

## 避坑

1. **SPRITE 可能崩溃**：DzAPI 静态文档仅列出 BACKDROP/TEXT/BUTTON/GLUEBUTTON/GLUETEXTBUTTON/FRAME，SPRITE 实测可能导致游戏崩溃。优先用 **BACKDROP + DzFrameSetTexture** 显示头像/图标。

2. **BACKDROP 不接收点击**：BACKDROP 是纯背景，DzFrameSetScriptByCode 点击无效。可点击需用 **BUTTON** 或 **GLUETEXTBUTTON**，再用 DzFrameSetTexture 设贴图。

3. **Layer "ARTWORK" 进游戏直接崩溃**：不要使用。

4. **BUTTON 贴图**：`DzFrameSetTexture(frame, "path.blp", 0)` 可设置按钮贴图。需 `DzFrameSetEnable(frame, true)` 启用。

5. **TSTL 编译**：`japi.XXX` 会编译成 `japi:XXX`，需在 fix-lua-for-pack.js 中把 `japi:` 改为 `japi.`，否则多传 japi 当首参导致参数错位。

6. **template 参数**：DzCreateFrameByTagName 第 4 参用 `"template"` 非 `""`，否则可能创建失败。

7. **GLUETEXTBUTTON + BACKDROP 父子**：BACKDROP 作父，GLUETEXTBUTTON 作子可生效。用 DzFrameSetAllPoints(btn, backdrop) 让按钮覆盖背景。

8. **定位**：DzFrameSetAbsolutePoint(frame, 4, x, y) 绝对坐标，4=CENTER。

9. **`DzFrameSetTextAlignment(frame, integer)` — 文本对齐（以本表为准，勿与旧「0 左、1 居中」混用）**

   | 对齐 | 整数值 |
   |------|--------|
   | 左上 | `0` / `1` |
   | 上 | `16` |
   | 右上 | `32` |
   | 左 | `2` |
   | **居中** | **`18`** |
   | 右 | `34` |
   | 左下 | `4` |
   | 下 | `20` |
   | 右下 | `36` |

   **改对齐前必须先重置**：再次改变同一 frame 的对齐方式前，须先 `DzFrameSetTextAlignment(frame, -1)`，再设目标值；否则新值会与旧状态**叠加**（类似「科技改护甲」类叠加），出现偏移（例如先 `20` 再设 `32` 理论右上却变右下）。**`-1` 会强制回到等价左上基准（0）**，再设表内值才可预期。

   **TEXT 与底图**：实践中宜先给 **TEXT 与父 BACKDROP（或占位区）设好相同或匹配的 `DzFrameSetSize`**，再 `DzFrameSetPoint` / 对齐，再 `DzFrameSetText` / `DzFrameSetFont`，避免只靠锚点而字号区域与底图不一致。

10. **`DzFrameSetFont(frame, path, fontSize, flag)`**

   - 第三参在本图/部分环境下为**小实数**，实测**有效量级上限约 `0.02`**；写 `0.8`、`3` 等与 UI 坐标混淆时**常看不出字号变化或表现异常**。微调可在 `0.001`～`0.02` 间试。
   - 与 **`DzFrameSetSize`** 的关系：TEXT 字形常会随**文本框宽高**适配；**不要**与 **`DzFrameSetScale` 叠在同一目标上重复缩放**（同属尺度，易把字缩没或难调）。

12. **`DzFrameSetTooltip(hostFrame, tooltipFrameInt)`**（`japi.lua`：第二参为 **integer**）  
   - 第二参是 **提示用 Frame 的整型 id**，不是 `|cff...|r` 字符串。常见：`DzFrameSetTooltip(宿主, DzF2I(提示框Frame))`；无 `DzF2I` 时可尝试直接传提示框句柄数值。勿与「字符串 tooltip」用法混用。

**`DzFrameSetScript` / `DzFrameSetScriptByCode` 的 `eventId`（与 `TS/系统/09．表现系统/UI工具.ts` 的 `EventType` 一致）**

| 事件 | ID |
|------|-----|
| 鼠标点击 | 1 |
| 鼠标进入 | 2 |
| 鼠标离开 | 3 |
| 鼠标释放（抬起） | 4 |
| 鼠标按下 | 5 |
| 鼠标滚轮 | 6 |
| Slider 值变化 | 11 |
| 鼠标双击 | 12 |（这个有同行告诉我是无效事件，还没验证过，2026年3月30日17:51:12）

13. **DzFrameSetScript**：`DzFrameSetScript(frame, eventId, funcName, sync)` 用函数名字符串注册。**TS/Lua 下不生效**（引擎通过 JASS ExecuteFunc 查找，无法调用 Lua 函数），需用 `DzFrameSetScriptByCode` + 直接传函数。

14. **为 Frame 注册回调（含本机“异步”、即时 UI）**：一律用 **`DzFrameSetScriptByCode(frame, eventId, funcHandle, sync)`**，直接传 **函数/闭包**；不要用 `DzFrameSetScript`。最后一参 **`sync`**：多数 **本机 UI 反馈** 用 **`false`**（本地即时）；需要 **同步到全房** 时用 **`true`**（按需求选，勿与“必须用 ByCode”混淆）。

15. **【UI 回调 + DisplayTextToPlayer】**：`DzFrameSetScriptByCode` 回调内可直接调用 `DisplayTextToPlayer(DzGetTriggerUIEventPlayer(), 0, 0, msg)`，sync=true 或 sync=false 均可。照搬 JASS 单动作模式即可，无需 TriggerExecute。

## 滚动条 / 自定义滑块（thumb）教训（1.27e）

1. **轨道若是 BACKDROP**：不要对它 **`DzFrameSetMinMaxValue` / `DzFrameSetValue`**（易崩）。数值滚动用 **`scrollOffset` 变量** + 列表偏移；需要原生 Slider 时用 **SCROLLBAR/SLIDER** 隐藏层单独管 value。

2. **圆形 thumb 视觉**：可用 **BACKDROP + `DzFrameSetTexture`**；**BACKDROP 不接点击**，在 thumb 上叠 **透明 `GLUETEXTBUTTON`**（`DzFrameSetAllPoints` 铺满）做点击/拖拽命中。

3. **thumb 位置与「跟手」拖拽**：`syncScrollThumb` 里用的 **竖直可移动行程**（如 `LIST_VIEW_H - thumbSize ± 补偿` 的 **safeRange 归一化高度**）必须与 **拖拽时 `dy`→`scrollOffset` 用的轨道像素高度** 同源；换算：  
   **`trackPx ≈ DzGetClientHeight() * safeRangeNorm / 0.6`**（与工程里竖向 **0.6** UI 坐标一致，同 `PY` 思路）。**禁止**用 `windowHeight * 随意常数`（如 `0.35`）当轨道高，否则会与滑块不同步。

4. **全局鼠标注册（拖拽）**：`DzTriggerRegisterMouseEventByCode` **必须** `japi.DzTriggerRegisterMouseEventByCode(trig, …)` **直接调用**，禁止 `const reg = japi.xxx; reg(trig,…)`（TSTL 易编成 **`reg(nil, trig,…)`** → `key=0` / `errjhw(333)`）。参数顺序与 `war3map.j` 里 **`DzTriggerRegisterMouseEventTrg(trg, status, btn)` → 原生 `(btn, status)`** 一致；左键按下 **`(1,1)`**，左键释放 **`(1,0)`**（以本图 JASS 为准）。

5. **Frame 脚本 eventId**：拖拽起点用 **`MOUSE_DOWN`（工程里常量为 `5`）**，不是 **`MOUSE_CLICK`（`1`）**。参考 `TS/系统/09．表现系统/UI工具.ts` 的 `EventType`。

## 你这次补充的关键记忆（Frame 系统）

1. **Frame 组与 SimpleFrame 组不要混父子**
   - SimpleFrames：`SIMPLEFRAME/SIMPLEBUTTON/SIMPLECHECKBOX/SIMPLESTATUSBAR`。
   - 这两组在父子关系上不稳定，混用常导致“创建成功但行为异常”或直接创建失败。
   - 任务 UI 这条链（`TaskMainPanel -> TaskListContainer -> Tab/Scroll`）统一用 Frame 组更稳。

2. **FrameLevel 规则不同**
   - Frame 组：只和同级 siblings 竞争；子帧在父帧层内排序。
   - SimpleFrame 组：更像全局同层竞争，父子关系影响弱很多。
   - 结论：重叠问题优先先统一组别，再调 `DzFrameSetLevel`。

3. **父子关系的实际效果**
   - 子帧可见性依赖父帧可见性。
   - 子帧默认拷贝父帧的 alpha/scale/level（创建时）。
   - Functional child（FDF 内定义的功能子帧，如 ButtonText/ControlBackdrop）和 Loose child（代码额外创建）行为不同，前者更依赖模板链。

4. **FDF 语法与定位要点**
   - 行注释 `//` 与块注释 `/*...*/` 可用。
   - 非 block action 末尾需逗号。
   - 路径使用单反斜杠（如 `UI\TaskUI.fdf`）。
   - `SetPoint` 规则遵循 0.8x0.6 UI 坐标系；Frame 组默认受 4:3 区域约束，超界会形变。

---

## UI 异步掉线避坑（2026-04-01 BuffUI 修复记录）

### ⚠️ 最关键：计时器必须在所有玩家上启动！
定时器创建	在所有玩家执行 ✅
定时器启动	在所有玩家执行 ✅
定时器回调	在所有玩家执行 ✅
回调内本地判断	if  GetLocalPlayer()✅
计时器在所有玩家创建启动，回调内用 GetLocalPlayer() 隔离✅
UI 操作	只在本地玩家执行 ✅

#### ❌ 错误的做法（会导致 desync）
不要在本地玩家检查内创建和启动计时器 ❌

```typescript
export function init(): void {
  const delayTimer = jass.CreateTimer();
  jass.TimerStart(delayTimer, 1.0, false, () => {
    const lp = jass.GetLocalPlayer();
    if (lp != null && lp !== 0) {
      createUi();
      // ❌ 只在本地玩家启动计时器！
      startRefreshTimer();  
    }
  });
}
```
**后果：**
- 本地玩家：定时器运行 → `syncBuffBar()` 每 0.1 秒执行
- 其他玩家：没有定时器 → `syncBuffBar()` 从不执行
- **不同玩家的执行次数不同 → desync**

#### ✅ 正确的做法（完全同步）
```typescript
export function init(): void {
  const delayTimer = jass.CreateTimer();
  jass.TimerStart(delayTimer, 1.0, false, () => {
    const lp = jass.GetLocalPlayer();
    if (lp != null && lp !== 0) {
      createUi(); // ✅ UI 创建只在本地玩家执行
    }
    startRefreshTimer(); // ✅ 计时器必须在所有玩家上启动！
  });
}

function startRefreshTimer(): void {
  const refreshTimer = jass.CreateTimer();
  jass.TimerStart(refreshTimer, 0.1, true, () => {
    // ✅ 计时器回调内部用 GetLocalPlayer() 隔离 UI 操作
    const lp = jass.GetLocalPlayer();
    if (lp == null || lp === 0) return;
    syncBuffBar();
  });
}
```

### 📌 核心规则
1. **本机 UI 操作**（创建、显示、隐藏、设置纹理/文本）只在 `GetLocalPlayer()` 上执行
2. **同步操作**（创建定时器、触发器注册等）要在**所有玩家**上执行
3. **计时器必须在所有玩家上启动！但回调内部用 `GetLocalPlayer()` 隔离 UI 操作！**
4. 如果有定时器在定期刷新 UI，**直接在定时器里查询选择状态**即可，不需要选择事件触发器（这个可以优化）
5. 在魔兽地图中，任何可能在不同玩家产生不同执行次数/结果的代码，都必须用 GetLocalPlayer() 隔离 UI 操作，或者直接用定时器轮询替代事件触发
### 简单版：
## 异步掉线（Desync）避坑

### 事件 vs 定时器
- ❌ 不要用选中/取消选中事件来触发 UI 刷新
- ✅ 用 0.1 秒定时器轮询选中状态 + `GetLocalPlayer()` 隔离 UI 操作
- 原因：事件在不同玩家触发时机可能不同，导致 UI 状态不同步

### UI 操作规则
- ✅ Frame 创建、显示、隐藏等UI操作必须用 `GetLocalPlayer()` 隔离
- ✅ 定时器回调中操作 UI 前必须判断 `GetLocalPlayer()`
- ✅ 定时器本身要在所有玩家上启动（引擎保证同步）
## 核心原则
**所有玩家必须执行相同次数、相同路径的代码；UI 操作只在本地玩家执行。**
**如果是true，那么它们内部的回调函数全局操作的必须在本地玩家判断之外，异步的操作需要在本地玩家判断内，不可以互相混淆，非常严格**

---

## 【关键新增】`frameSetScriptByCode(..., sync=true)` 硬规则（精简版）

### 一句话原则
**`sync=true` 必须“全链路同步”**：创建帧 → 注册回调 → 回调执行，三步都要所有玩家走同一路径、同次数。

### 必须满足
1. 帧在**所有玩家**上创建（不能在 `GetLocalPlayer()` 内创建）。
2. `sync=true` 回调在**所有玩家**上注册，且建议在初始化阶段一次性注册。
3. 回调函数用**全局函数**，不要匿名闭包。
4. 回调体严格分层：
   - 全局同步操作（状态修改/队列推进/任务逻辑）在本地判断外。
   - 本地异步操作（Frame 显示、文本、音效）在 `GetLocalPlayer()` 判断内。

### 严禁
- 在 `sync=false` 事件链、定时器异步分支、或“仅本地触发”的路径里去注册 `sync=true`。
- 在 `sync=true` 回调里依赖“本地上下文闭包句柄”（如选择事件当时捕获的 `hero/unit/player`）。

### 推荐实践（本项目已踩坑验证）
- 对任务按钮：初始化时全玩家预创建帧并预注册 `sync=true` 回调；点击时仅切换“当前任务回调上下文”。
- 回调内通过稳定 key（如 playerId/同步状态池）重新取对象，不从旧闭包取句柄。

### 层级补充（避免“J 面板盖住对话框”）
- `DzFrameSetPriority` 才是优先级主控制；`DzFrameSetLevel` 仅作同层/局部排序辅助，不能替代 Priority。

