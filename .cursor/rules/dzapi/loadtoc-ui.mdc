---
description: DzLoadToc 使用方法与“何时会崩”的经验
globs: TS/**/*UI*.ts, TS/**/任务UI*.ts
alwaysApply: false
---

# DzLoadToc 使用经验（UI 模块）

## 结论
1. `DzLoadToc` **在 1.27e 是可以用的**；你之前“疑似不能用”的问题，更多是 **TOC/FDF 路径/依赖没加载对** 或 **用 `DzCreateFrame` 创建了不兼容的 FDF 帧类型导致引擎级崩溃**。
2. **有 `DzLoadToc` 成功并不代表所有 FDF 帧都安全**；建议“先保底、再逐步替换”。

## 正确用法（步骤）
1. `imports/UI/*.toc` 的内容填 FDF 路径（地图根相对路径）：
   - 例如：`UI\TaskUI.fdf`（建议末尾带换行）。
2. 初始化 UI 时只加载一次：
   - 用 `pcall` 包住 `japi.DzLoadToc(path)`，避免 Lua 层报错把初始化流程炸掉。
3. `DzCreateFrame` 的名字必须与 FDF 里 `Frame "..." "FRAME_NAME"` 中 **第二个引号名字完全一致**。
4. 失败要兜底：
   - `DzCreateFrame` 可能返回 `0` 或失败；失败就回退到代码里的 `createFrame(...)`。

## 何时会崩（你这次的教训）
1. 你这套项目里，**FDF 创建的某些交互控件帧**（例如按钮/滚动条相关）会在客户端绘制/处理阶段引擎级崩溃。
2. 建议分阶段：
   - 第一步：先只用 FDF 创建“偏静态容器”（如 BACKDROP 外壳：`TaskEntryIcon/TaskMainPanel/TaskListContainer`）。
   - 第二步：确认稳定后，再逐个把 Tab/Close/Scrollbar 等控件替换成 FDF；每替换一个就测一次，定位具体触发崩溃的帧。

## 统一模板（你项目里现在的做法）
- `loadTaskUITocOnce()`：`DzLoadToc("UI\\TaskUI.toc")` + `pcall`
- `tryCreateFromFdf(name, parent, fallback)`：`pcall(DzCreateFrame(...))` 成功才用，否则 fallback
