---
description: Lua/ydLua 运行时安全代码模板：防止异步、闪退、句柄泄漏
alwaysApply: false
---

# Lua 运行时安全代码

## 与 `lua-gc-desync-heuristics.mdc` 的分工

- **前者**：联机异步/GC 的**经验与约束**（同步创建·异步显示、少匿名闭包进 JASS、`pairs`/table key、`jass.runtime.handle_level` 最小化等）。
- **本文件**：仅保留**入口可粘贴的一整段 Lua**（全局替换 `pairs`、`TimerStart`、`TriggerAddAction` 等）；**不重复**前者的原理与示例。

**为何仍保留代码里的行内注释**：便于粘贴后本地维护；与「原则在长文里」不冲突。

**与前者 §5 的差异**：本模板使用 `runtime.handle_level = 1`（与常见 ydLua 入口示例一致）；前者另给出更保守的 `handle_level = 0` 思路。**按项目只选一套**，勿混用两套策略却忘记对齐。

## 当前项目结论

本文件里的“整段全局 hook 模板”现在**不再推荐直接采用**。

原因不是它完全没价值，而是它把风险从“业务层直接传匿名闭包进 JASS”转成了“全局替换 `TimerStart` / `TriggerAddAction` / `ForGroup` / `pairs` 等基础 API”。

当前项目里，这种做法有几个明确问题：

1. `ForGroup / ForForce / Enum*` 共享单个 `func_ref` 槽位，嵌套或重入时会互相覆盖。
2. `TriggerAddAction` 的 action 分发表若靠 `SafePairs` 遍历，执行顺序不稳定。
3. 全局 hook 基础 API 会改变整个工程语义，对 TSTL 产物和第三方模块兼容性要求过高。
4. 它仍然依赖“Lua function → 全局表 → JASS 命名入口转发”模型，不适合作为当前仓库的默认安全方案。

当前仓库的推荐方向是：

- **优先在业务层收敛**，不要把匿名闭包、`Condition`、`Filter`、高频 JASS 回调直接送进引擎。
- **模块级具名函数 + 稳定 key 分发**，按具体子系统局部治理。
- **传给 JASS 的函数必须是长寿的模块级具名函数**，生命周期视为整个地图；不要创建临时闭包再传给 `TimerStart`、`Condition`、`Filter`、`ForGroup`、`Dz*ByCode`、触发器动作/条件等 JASS 注册点。
- **TS/Lua 传给 JASS 的闭包默认按“不释放”处理**，因此要尽量减少这类跨边界回调数量；`Filter` 优先做成单例具名函数，不要手动销毁。
- **不要在项目入口全局重写 `TimerStart` / `TriggerAddAction` / `ForGroup`**，除非专门做运行时实验并完整评估兼容性。

## 历史模板做的事（一览）

| 块 | 作用 |
|----|------|
| 禁用 `pairs` / `SafePairs` | 动机与替代写法见前者 **§4** |
| `handle_level`、`sleep` | 句柄与协程相关；背景见前者 **§1、§5** |
| 禁用 `Condition` / `Filter` | 减少 JASS 侧 code 回调坑 |
| `ForGroup` / `ForForce` / `Enum*` 包装 | 匿名闭包策略见前者 **§3**；此处用固定命名入口转发 |
| `TimerStart` / `DestroyTimer` | 用 handle 映射存真实回调，统一命名 tick |
| `TriggerAddAction` 等 | 同上，减少闭包直挂触发器 |

## 已废弃的整段入口模板

下面这些做法现在只保留为“历史思路”，**不要直接复制进项目入口**：

- 全局替换 `pairs`
- 全局替换 `TimerStart` / `DestroyTimer`
- 全局替换 `TriggerAddAction` / `TriggerClearActions`
- 全局替换 `ForGroup` / `ForForce` / `Enum*`
- 用单槽 `func_ref` 或 `handle -> function` 哈希表统一兜底所有回调

如果确实需要在某个独立实验环境中试验这类运行时 hook，必须额外评估：

1. 是否会破坏 TSTL 生成代码的既有语义。
2. 是否引入新的 `pairs` 顺序问题。
3. 是否在嵌套枚举、重入 timer、重复 trigger action 下覆盖旧引用。
4. 是否与仓库现有的 Dz / UI / STES / YDLocal 封装冲突。

## 使用方式

当前项目**不建议**按“整段入口脚本”接入。

更推荐的使用方式是：

1. 先读 `lua-gc-desync-heuristics.mdc`，按业务层规则整改。
2. 在具体子系统里把匿名闭包、`Condition`、`Filter`、高频 JASS 回调改成模块级具名函数。
3. 只在局部模块内做受控封装，不在入口全局改写基础 API。

## 注意事项

- 不要把这份文件理解成“推荐在入口全局重写 JASS 基础 API”的模板。
- 若只是要规避 desync，优先修业务代码，不要先引入大范围 runtime hook。
- 若未来确实要保留某个局部 runtime 封装，应单独建实验文档，并明确适用边界与兼容性验证结果。
