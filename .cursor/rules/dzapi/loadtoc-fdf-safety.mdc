---
description: DzLoadToc 使用方法、FDF 安全接入策略与引擎崩溃规避（1.27e）
globs: TS/**/*UI*.ts, TS/**/任务UI*.ts, TS/**/表现/**.ts
alwaysApply: false
---

# DzLoadToc 与 FDF 安全使用规则（1.27e）

## 核心结论
1. `DzLoadToc` **在 1.27e 是可以用的**；你之前"疑似不能用"的问题，更多是 **TOC/FDF 路径/依赖没加载对** 或 **用 `DzCreateFrame` 创建了不兼容的 FDF 帧类型导致引擎级崩溃**。
2. **有 `DzLoadToc` 成功并不代表所有 FDF 帧都安全**；建议"先保底、再逐步替换"。
3. 即使 `DzLoadToc("UI\\xxx.toc")` 能成功，`DzCreateFrame` 创建某些 FDF 帧也可能在客户端**绘制/交互阶段触发引擎级 crash**。
4. 这类 crash 不是普通 Lua error，**`pcall` 也拦不住**（所以"止血 pcall"不一定有效）。

## 已定位的崩溃案例
- 在任务 UI 里：启用 FDF 创建 `TaskScrollBar`（以及当时相关的滚动条帧组合）会导致**进图加载阶段崩溃**。
- 将 `TaskScrollBar`/滚动条相关帧切回代码创建（或干脆不从 FDF 创建该帧）后，崩溃消失。

## 正确用法（步骤）
1. `imports/UI/*.toc` 的内容填 FDF 路径（地图根相对路径）：
   - 例如：`UI\TaskUI.fdf`（建议末尾带换行）。
2. 初始化 UI 时只加载一次：
   - 用 `pcall` 包住 `japi.DzLoadToc(path)`，避免 Lua 层报错把初始化流程炸掉。
3. `DzCreateFrame` 的名字必须与 FDF 里 `Frame "..." "FRAME_NAME"` 中 **第二个引号名字完全一致**。
4. 失败要兜底：
   - `DzCreateFrame` 可能返回 `0` 或失败；失败就回退到代码里的 `createFrame(...)`。

## FDF 渐进接入策略（防崩溃）
1. 把所有要从 FDF 创建的帧分组（例如：A 容器/Entry，B Tab/Close 交互，C 滚动条）。
2. 每次只打开其中一组：确认"开一半不崩"后再逐步扩大。
3. 精确到帧名后：
   - 继续用 FDF 的帧：保留它们
   - 会崩的帧：改为 fallback（代码 `createFrame(...)` / 或不创建该控件）

## 推荐的接入阶段
1. **第一步**：先只用 FDF 创建"偏静态容器"（如 BACKDROP 外壳：`TaskEntryIcon/TaskMainPanel/TaskListContainer`）。
2. **第二步**：确认稳定后，再逐个把 Tab/Close/Scrollbar 等控件替换成 FDF；每替换一个就测一次，定位具体触发崩溃的帧。

## 实用代码模板

### 加载 TOC（带兜底）
```typescript
function loadTaskUITocOnce(): boolean {
  const [ok] = pcall(() => japi.DzLoadToc("UI\\TaskUI.toc"));
  return ok;
}
```

### 安全创建 FDF 帧（失败回退）
```typescript
function tryCreateFromFdf(name: string, parent: number, fallback: () => number): number {
  const [ok, frame] = pcall(() => japi.DzCreateFrame(name, parent, 0));
  if (ok && frame && frame !== 0) return frame;
  return fallback();
}
```

## 相关规则
- 联机执行分层、`GetLocalPlayer()` 使用边界、N 槽位模型见 [`n-slot-ui-symmetric-execution.mdc`](n-slot-ui-symmetric-execution.mdc)
