---
description: DzAPI 单管理器本地 UI 规则：最小安全构筑、逐步加回功能、同层闭环
globs: TS/**/*任务UI*.ts, TS/**/*世界地图*.ts, TS/**/*UI*.ts
alwaysApply: false
---

# DzAPI 单管理器本地 UI：先做最小安全构筑，再逐步加回

适用对象：

- 任务 UI
- 世界地图
- 说明面板
- 这类“全局只有 1 套管理入口，但交互主要是本地视觉表现”的 DzAPI UI

相关规则：

- `n-slot-ui-symmetric-execution.mdc`
- `ui-frame-types.mdc`

---

## 1. 默认结构

- 默认使用 `TaskUIManager` / `WorldMapManager` 这类单例管理器
- 系统入口只有 1 份
- 逻辑入口只有 1 份
- frame 句柄集中挂在管理器里
- 不默认拆成 `new TaskUI(pid)` 这种每玩家一整套实例类

推荐形态：

```ts
class TaskUIManager {
  private entryFrame: number | null = null;
  private mainPanel: number | null = null;
  private listContainer: number | null = null;
}
```

---

## 2. 先做最小安全构筑

这类 UI 重构时，默认顺序不是“一次性把所有功能补齐”，而是：

1. 先让主面板显示/隐藏稳定
2. 再让基础列表稳定显示
3. 再加任务展开
4. 再加滚轮翻页
5. 再加滑块联动
6. 最后才加拖拽、热键、额外交互

原则：

- 每次只加一条功能链
- 每加一条，都要基于上一版稳定结构
- 如果某条功能会把结构重新拉复杂，就先不加

不要：

- 一上来就把展开、滚轮、拖拽、热键一起接回去
- 为了“兼容旧结构”不断在主文件外面套补丁层

---

## 3. 生命周期必须同层闭环

这是本规则最重要的约束。

如果一个 UI 子系统走本地链，那么它必须：

- 本地创建
- 本地设置
- 本地显示/隐藏
- 本地点击
- 本地刷新
- 本地关闭

如果一个 UI 子系统走全局对称链，那么它必须：

- 全端创建
- 全端设置
- 全端注册
- 全端刷新
- 本地只做最终 show/hide

禁止混线：

- 这里全局创建，那里本地改，别处又全局刷新
- 这里本地创建，后面公共逻辑又去碰它

一句话：

**同一个 UI 子系统，生命周期必须同层闭环。**

---

## 4. 对本地 UI 的判断

对这类任务 UI / 世界地图 UI，下面这些通常属于本地视觉层：

- `DzFrameShow`
- `DzFrameSetText`
- `DzFrameSetTexture`
- `DzFrameSetPoint`
- `DzFrameSetSize`
- 分类页切换
- 详情展开/收起
- 滚轮切页
- 滑块位置变化

前提是：

- 操作对象已经存在
- 这条链里不再临时创建 frame
- 不再临时注册事件
- 不再启动新的常驻计时器链
- 不再改全局业务状态

也就是说：

**视觉层可以本地，但创建/注册/启动要和它保持同层一致。**

---

## 5. 默认不要保存复杂交互状态

默认不要把这些做成长期状态表：

- 当前展开了哪条任务
- 当前滚动到了哪一条
- 当前切到了哪个标签
- 上次关闭时停留在哪一页

默认行为应该是：

- 打开即默认态
- 关闭即清空现场

允许存在的只是“当前打开期间的临时视图状态”，例如：

- 当前分类
- 当前分页
- 当前展开任务

但它们应该只服务于当前面板会话，不要上升成全局业务状态。

---

## 6. 任务 UI 的推荐实现方式

### 6.1 列表

- 预创建固定槽位
- 预创建分类页
- 预创建展开变体页
- 交互时优先做 show/hide 切换

### 6.2 滚动

默认优先级：

1. 滚轮分页
2. 滑块位置联动
3. 滑块拖拽

先把分页和滑块联动做稳，再加拖拽。

### 6.3 热键

- 键盘硬件事件是否 `sync=true` 取决于平台能力
- 如果平台要求同步注册，就同步注册
- 但同步热键回调内部必须先按触发玩家做本地隔离

不要在热键链里直接塞复杂重绘逻辑。

---

## 7. 重构时的收敛要求

如果重构过程中出现下面这些信号，就说明方向偏了：

- `03．任务UI.ts` 越改越大
- 同一个问题靠一层层补丁绕过去
- 热键、滚轮、展开、显隐逻辑缠到一起
- 为了排查问题把试验代码长期留在主链里

正确做法：

- 回到上一版稳定最小结构
- 只给当前要恢复的那条功能链做最小改动
- 不要顺手把别的链也带着改

---

## 8. 当前建议的功能恢复顺序

任务 UI 按下面顺序逐步加回：

1. `J` 开关主面板
2. 列表基础显示
3. 点击任务展开详情
4. 鼠标滚轮切页
5. 滑块随页数移动
6. 鼠标拖拽滑块
7. `1/2/3` 切换标签页

只有前一步稳定，才进入下一步。

---

## 9. 参考范式

项目内优先参考：

- `JASS/世界地图/`

参考重点不是照抄具体代码，而是学习这几个结构特征：

- 全局只有 1 个入口
- UI 逻辑集中
- 本地视觉链单独闭环
- 功能按小步往回加
- 不为了“功能全”牺牲联机稳定

---

## 10. 联机补充约束（实战结论）

- `sync=false` 的鼠标 / 键盘 / UI 本地链里，不要插入 `DzSyncData`、`DzSyncDataImmediately`
- 也不要在这条本地链里做 `CreateItem`、`RemoveItem` 这类靠 handle 探测异步的动作
- 原因不是“检测不准”，而是探针本身会先破坏本地纯显示链

- 本地 UI 点击音效必须显式指定 `whichPlayer`
- 不要在任务 UI 的本地点击 / 本地热键链里裸调 `SoundUI_ClickPlay()`
- `SoundUI_ClickPlay()` 不传 `whichPlayer` 时，会继续走复用播放链，最终 `StartSound` 会在所有客户端播放

推荐做法：

```ts
const localPlayer = GetLocalPlayer();
SoundUI_ClickPlay(undefined, localPlayer);
```

更稳的做法：

- 在 UI 管理器初始化时缓存一次 `localPlayer`
- 所有按钮、热键、任务行点击都复用同一个 `playLocalClickSound()`
