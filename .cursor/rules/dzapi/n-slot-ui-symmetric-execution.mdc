---
description: DzAPI 联机 desync、对称执行、Timer/GetLocalPlayer 分层、N 槽位 UI、sync 回调与对话框（项目经验单一事实来源）
globs: TS/**/*.ts
alwaysApply: false
---

# DzAPI 联机 UI：Desync、对称执行与 N 槽位模型

本文件是 **Dz 帧 + 联机生命周期 + 同步参数** 的**单一事实来源**（与 `ui-frame-types.mdc` 分工：`ui-frame-types` 偏 Frame 类型、对齐、滚动数值、事件 ID；本文件偏 **全端执行次数、Timer、sync、N 槽**）。

相关：`loadtoc-ui.mdc`、`fdf-crash.mdc`。  
**UI 闭包 / `pcall` / JASS 回调具名函数规则**：统一以 `lua-gc-desync-heuristics.mdc` 为准；本文件不再重复正文，只讨论联机 UI 的执行次数与分层。

涉及同步/本机异步/桥接上下文判断、`pairs()` 顺序、异步报错中断、`math.random` / `math.randomseed` 风险时，先读 `lua-gc-desync-heuristics.mdc` 的「先判断上下文」；随机数细则再读 `war3-tstl/math-randomseed.md`。

---

## 1. 方案 B（N 槽）：设计结论

- 默认优先 **方案 B（N 槽）**：每个客户端都创建同样数量、同样结构的 UI 壳；文本、贴图、布局等内容更新走对称执行，本地差异只放在显隐等表现层。
- 设计目标不是“所有人画面一致”，而是 **所有客户端执行形态一致**。魔兽里的 desync 根因通常是 **全局状态读写的顺序、次数、分支不一致**，不是“不同槽位是否画出同一帧文本”。
- **方案 A（每端仅 1 套本地 UI）维护成本高**：只要后续迭代里让“面板是否存在/是否打开”影响到计时器、注册次数、同步回调链或共享状态读写，就更容易把 UI 问题升级成 desync。
- 方案 B 的价值是把 **创建、注册回调、滚轮、刷新入口** 固定成 **每个客户端永远跑同一套 N 槽流程**；最后才用 **`GetLocalPlayer()` + 可见性/可交互性** 区分谁看见、谁可操作。
- **全局任务数据**仍应是全房一份；各槽位的**纯显示内容**（列表、展开态、滑块位置等）可以不同。B 的保险点不是“允许异步乱写”，而是 **不要让 UI 生命周期把执行路径带偏**。
- 失败模式取向：方案 A 更容易把 bug 放大成 **desync / 掉线**；方案 B 更容易把 bug 降级成 **本地显示异常**，便于继续游戏和排查。

---

## 2. 术语：desync 与纯客户端 UI

| 概念 | 含义 |
|------|------|
| **网络异步 / desync** | 各客户端对**全局游戏状态**的推演不一致（单位、物品、计时器、触发器、`Sync*`、随机路径、**各端执行次数/分支**等）。 |
| **纯客户端 UI** | `DzCreateFrame`、`DzFrameShow`、滚动条、展开态、字体等——**不写全局状态**时，一般不产生同步帧；**少建/多建几套帧本身通常不是 desync 根因**；B 的价值在 **执行路径**，不在「帧数迷信」。 |

---

## 3. 铁律：全端同路径、同次数；像素只在本机改

### 3.1 计时器（BuffUI 2026-04-01 修复记录）

| 步骤 | 要求 |
|------|------|
| 定时器创建 | 在所有玩家执行 ✅ |
| 定时器启动 | 在所有玩家执行 ✅ |
| 定时器回调 | 在所有玩家执行 ✅ |
| 回调内 | `if GetLocalPlayer()` 再改 UI ✅ |
| UI 操作 | 只在本地玩家分支内 ✅ |

**不要在「仅本地玩家」分支里 `CreateTimer` / `TimerStart` 周期性刷新**——否则只有部分客户端跑回调，**执行次数不一致 → desync**。

#### 错误（会导致 desync）

```typescript
export function init(): void {
  const delayTimer = jass.CreateTimer();
  jass.TimerStart(delayTimer, 1.0, false, () => {
    const lp = jass.GetLocalPlayer();
    if (lp != null && lp !== 0) {
      createUi();
      startRefreshTimer(); // ❌ 只有本机分支里启动周期计时器
    }
  });
}
```

#### 正确

```typescript
export function init(): void {
  const delayTimer = jass.CreateTimer();
  jass.TimerStart(delayTimer, 1.0, false, () => {
    const lp = jass.GetLocalPlayer();
    if (lp != null && lp !== 0) {
      createUi(); // ✅ UI 创建仅本机
    }
    startRefreshTimer(); // ✅ 所有玩家都启动
  });
}

function startRefreshTimer(): void {
  const refreshTimer = jass.CreateTimer();
  jass.TimerStart(refreshTimer, 0.1, true, () => {
    const lp = jass.GetLocalPlayer();
    if (lp == null || lp === 0) return;
    syncBuffBar(); // ✅ 周期逻辑里再隔离 UI
  });
}
```

### 3.2 核心原则（一句话）

**所有玩家必须执行相同次数、相同路径的「会进同步推演」的代码；本机 UI（显隐、纹理、文字）只在 `GetLocalPlayer()` 判断内。**

若某 API 的 `sync=true`：其回调里**改全局/共享状态**必须在本地判断**外**且全端一致；**仅本地表现**在判断**内**。二者**不可混在同一分支语义里**。

本机玩家/宿主 API 只适合过滤本地请求、更新本地 UI、镜头、声音、文字、请求发送或诊断；`GetLocalPlayer()` 这类分支不能创建、销毁、移动、奖励或修改同步对象。如果本机操作要影响所有人，先发出或推导出同步事件，再由同步结果推进共享状态。

### 3.3 物编 `EXExecuteScript` 与头像（联机实测）

- `getObjectProperty` / `EXExecuteScript` 是同步调用，但**忌**在全房高频 tick 里反复物编读（易掉线/不同步）。
- 头像 `DzFrameSetTexture`：**建帧时设一次**；周期回调只刷文字等，周期里**不再** `getHeroIcon` / `getObjectProperty`。
- 换英雄改头像：在**业务事件**里单次改，勿挂回高频 tick。

---

## 4. N 槽位 UI（与 §1 呼应：技术落地）

- **N**：地图最大玩家数（常见 4），槽位 `i` ↔ `Player(i)`。
- **每个客户端**对称创建 **N 套**完整壳（帧树、滚轮/刷新注册；子帧名后缀 `_s{i}` 或 `DzCreateFrame` 的实例号/context，避免同名冲突）。
- **数据**：读 `questManager` 等已同步数据时用 `playerId === i`。
- **显示与操作**：仅用 `GetLocalPlayer()`（或触发键玩家 === 本地玩家）决定哪一套可见、可点、可滚；其余槽位隐藏或不接输入，但**创建与注册与本地槽位同路径**。

要点：**对称的是「跑了哪些代码、注册了多少东西」**；**刻意不对称的是最后 `DzFrameShow` / 输入门控**。

与「每端只建 1 套」对比：单套省资源；N 槽用内存换 **§1 所述的路径保险**。新写复杂 Dz 面板时默认评估 N 槽；维持单套则审查所有 Timer/Trigger 是否与「面板是否存在」解耦。

### 4.0 一句话记忆

> **全端同时做同样的事，只有像素在最后一刻分叉。**

等价展开：

- 创建、注册、同步状态修改、文本/贴图/布局更新：**全端同次数、同顺序执行**
- `DzFrameShow` / `DzFrameHide`、本地音效、仅本地消息：**最后再按本机玩家分叉**

### 4.0.1 为什么单套壳经常把自己逼进 `GetLocalPlayer()`

如果一个面板只有 **1 套物理帧壳**，却要同时服务多个玩家，那么：

1. 玩家 A 打开时，面板内容要切成 A 的数据
2. 玩家 B 打开时，又要切成 B 的数据
3. 为了互不覆盖，最容易写成“在 `GetLocalPlayer()` 里 `DzFrameSetText` / `DzFrameSetTexture`”

这正是高风险写法。因为**内容修改**不是纯像素显隐，它会把客户端间的帧状态带偏，后续再被同步回调、刷新链、共享状态引用时，就可能把 UI bug 升级成 desync。

因此：

- **字面 N 槽** 是最稳的默认方案：槽位 `i` 永远服务 `Player(i)`，内容不需要在本地路径里切换
- **若维持单套壳**，则必须额外证明：不会在本地路径里改帧内容、不会让“面板是否存在/打开”影响计时器和同步状态路径

本文件强调的是**执行形态一致**，不是“所有人必须看同一块像素”。

### 4.1 实施清单

1. 本机槽位：`GetPlayerId(GetLocalPlayer())`。
2. 帧名 / FDF context 避免跨槽冲突。
3. **热键**：模块级只注册一份，派发到本机槽位，避免一次按键 N 份逻辑。
4. **多份 `registerUIRefreshCallback`**：回调内用「本槽是否本地 + 是否可见」早退，或集中注册再分发（二选一，保持全端对称）。

### 4.2 本地隔离 vs 全局同步（N槽场景）

**N槽模型**：每个客户端创建N套UI壳（N=玩家数），槽位i服务Player(i)。所有客户端的槽位i状态必须一致，只有显示/隐藏不同。

| 操作类型 | 需要本地隔离 (`GetLocalPlayer`) | 说明 |
|---------|:-------------------------------:|------|
| `DzFrameShow` / `DzFrameHide` | ✅ 必须 | 纯显隐，不改变帧内容，只影响本地渲染 |
| `StartSound` / 音效 | ✅ 必须指定玩家 | 否则全房播放，打扰其他玩家 |
| `DisplayTextToPlayer` | ✅ 必须指定玩家 | 聊天消息可见性，全房可见会暴露他人信息 |
| **以下操作全部禁止放在本地路径，必须所有客户端同步执行** |||
| 全局变量修改 | ❌ 严禁 | 必须所有客户端同步写入，否则状态分叉 → desync |
| 计时器创建/启动/销毁 | ❌ 严禁 | 计时器生命周期必须全端一致 |
| `DzFrameSetText` | ⚠️ 允许，但有前提 | 可以在 `GetLocalPlayer()` 内做最终本地渲染；前提是文本已在本地分支外对称计算完成，且本地分支不再做拼接、排序、选择或共享状态写入 |
| `DzFrameSetTexture` | ⚠️ 允许，但有前提 | 可以在 `GetLocalPlayer()` 内做最终本地渲染；前提是贴图路径已在本地分支外对称计算完成，且本地分支不再做本地选择或共享状态写入 |
| `DzFrameSetPoint` / `DzFrameSetSize` | ❌ 严禁 | 修改帧布局，可能被其他帧锚点依赖，分叉则危险 |
| 滚动条滑块位置 | ❌ 严禁 | 修改帧状态，不同客户端滚动位置不同会分叉 |
| 读取变量 | ❌ 不需要（安全） | 纯读取，不修改任何状态，放哪里都安全 |

**原则**：槽位在所有客户端都存在，只是**显示/隐藏**不同。内容状态要对称执行；本地隔离只留给显隐、音效、文本消息这类最终表现。

### 4.3 本仓库参考（任务面板）

- **入口**：`TS/系统/08．任务系统/10．index.ts`（`ENABLE_QUEST_UI_MODULE` 时对称调用 `04．任务UI拆分/12．任务UI管理器.ts` 的 `init`、`registerHotkey`）。
- **实现目录**：`04．任务UI拆分/*`（主面板 `08`、列表 `09`、滚轮 `10`、热键 `05`、辅助 `02` 等）。
- **与字面 N 槽的关系**：当前任务 UI 为 **每客户端一套对称壳**（各端跑同一套创建/注册流程；源码注释为全局一套、不区分 `slotPid`），**不是**按 `Player(i)` 乘 **N 套物理帧** 的方案 B 最简字面实现；仍遵守本文 **§3「全端同路径、同次数」** 与 **§3.1**（不在仅本机分支里单独起周期 Timer 做主链）。若将来改为 N 槽，须补齐槽位后缀 / `contextId`、热键按槽分发，并同步 `lua-gc-desync-heuristics.mdc` 文末附录。
- **键盘 `sync=true` 与掉线**：`registerKeyUpSync` → `DzTriggerRegisterKeyEvent(..., true)`，**每次松键一条网络同步命令**；非主机玩家连按（如任务面板 `1/2/3` 切页）易灌爆同步队列，约十余秒后异步掉线。**纯本机 UI 导航**须用 `registerKeyUpLocal`（`DzTriggerRegisterKeyEventByCode(..., false)` + 工程内 `runFalseLocalRegistration`）；若确需全房一致再单独走 `Sync`/业务事件，勿用高频键盘 sync 当「状态广播」。
- **属性 UI 等**：「同时要看到多人」可仍用多行展示，文件结构不必与任务 UI 相同，但 **数据全局、显示本地** 分层与本文一致。

### 4.4 滚轮 / 拖拽 / 高频导航

高频 UI 输入不要一概走 `sync=true`。

社区血泪结论：**`MOUSE_MOVE`、`MOUSE_WHEEL`、拖拽跟手移动** 这类高频事件，**不要挂到任何会触发同步的机制上**。这里的“同步机制”不只包括 `sync=true`，也包括 `DzSyncData` 一类显式同步指令。

但要把经验说精确：

- **高频 `false` 本地输入**，不等于一定立刻掉线
- 真正高危的是：**本地输入写出的状态，再被全端 timer / `sync=true` 回调 / 共享分支持续消费**
- 所以“滚轮暂时没复现掉线，拖拽稳定异步”这类现象是可能成立的；前者常见于“本地写了状态，但尚未进入放大链”，后者常见于“本地写出的开关被全端周期逻辑反复读取”

| API / 机制 | 用途一句话 | 高频能用？ |
|-----------|-----------|-----------|
| `sync=true` 帧/键鼠回调 | 触发一次，全房同步一次 | ❌ 严禁 |
| `DzSyncData` | 把数据挂队列，下一帧广播 | ❌ 严禁 |
| `DzSyncDataImmediately` | 立刻广播同步数据 | ❌ 更危险 |
| `DzSyncBuffer` | 读取同步过来的数据 | ❌ 不应用作高频轮询链 |
| `DzExecuteFunc` | 纯本地绕过同步检查 | ⚠️ 无绝对必要不要用 |

#### 何时可以 `sync=false`

滚轮、拖拽、滑块跟手移动这类高频事件，可以用 `sync=false`，但必须同时满足：

1. 写入的是**槽位私有 / 本地 UI 私有**状态
2. 这些状态**不参与全房同步计算**，不反哺共享状态
3. 读取路径有明确的“本机槽位”门控
4. 其他客户端即使状态不同，也只是隐藏槽位或纯本地显示差异

典型例子：

- 本地滚动位置
- 本地当前页偏移
- 本地拖拽中的滑块视觉位置

#### 何时必须避免 `sync=true`

拖拽、快速滚轮、长按连续导航若走 `sync=true`，会形成高密度同步命令，容易把锁步队列灌满。键盘高频切页同理，不要把 `sync=true` 当作“UI 本地导航广播器”。

同理，不要把这类高频输入改走 `DzSyncData` / `DzSyncDataImmediately` 兜底。它们适合**低频、确实需要全房广播**的状态变更，不适合承载个人 UI 交互。

#### 何时仍然需要同步

如果某个操作会改共享可见状态、共享分类/展开态，或任何**后续还会被其他客户端逻辑读取**的字段，它就不再是“纯本地 UI”，必须回到 **`sync=true` 回调里“状态同步、显示本地”分层** 处理。

#### 4.4.1 反模式：本地输入写共享状态，再被全端逻辑消费

这是本仓库后续要**重点规避**的危险模式，也是最容易把“本地 UI 问题”放大成 desync 的链条：

1. `sync=false` / 本地按键 / 本地鼠标 / 本地 frame 回调里写了一份“共享表 / 模块级状态 / 槽位状态”
2. 这份状态随后被全端 `timer`、`sync=true` 回调、对称刷新 tick、共享视图模型或其他客户端都会跑的逻辑继续读取
3. 结果未必“立刻必炸”，但常见后果是：
   - 某些机器只是本地显示异常
   - 某些机器在高频输入后状态分叉
   - 某些机器延迟数秒后才掉线或 desync

一句话判断：

> **本地输入可以改本地显示；本地输入不能偷偷改“随后还会被全端继续读”的状态。**

错误示意：

```ts
registerKeyEventByCode(KEY.V, KEY_STATE.UP, false, onLocalToggle);

function onLocalToggle(this: void): void {
  sharedVisibleState[playerId] = !sharedVisibleState[playerId]; // ❌ 本地输入写共享态
}

function onRefreshTick(this: void): void {
  const visible = sharedVisibleState[playerId] === true; // ❌ 全端 tick 继续消费
  DzFrameShow(panel.root, visible);
}
```

正确收敛：

```ts
registerKeyEventByCode(KEY.V, KEY_STATE.UP, false, onLocalToggle);

function onLocalToggle(this: void): void {
  localVisibleState[playerId] = localVisibleState[playerId] !== true; // ✅ 只写本地态
}

function onRefreshTick(this: void): void {
  const visible = playerId === localPlayerId && localVisibleState[playerId] === true;
  DzFrameShow(panel.root, visible); // ✅ 本地显示只消费本地态
}
```

如果某次输入**真的要**推进共享状态：

- 要么改成 `sync=true` 回调，在本地判断外统一写共享态
- 要么先经由 `Sync` / 业务事件 / 已确认的同步消息，再由同步结果推进共享态

不要折中成“先本地写一份，反正别的地方稍后也会读到”。

#### 4.4.2 UI 同步风险审查清单

遇到 Dz 面板、热键、滚轮、拖拽、分页、开关状态时，至少逐条过一遍：

1. 这条输入链是 `sync=true` 还是 `sync=false`？
2. 写入的是本地态，还是共享态？
3. 这份状态后面会不会被全端 `timer` / 刷新 tick / `sync=true` 回调继续读？
4. `GetLocalPlayer()` 内做的是最终显示，还是已经把共享状态写进去？
5. 是否出现“单套壳 + 本地切内容”的趋势，应该改回 N 槽或对称壳？
6. 是否把高频 UI 交互误接到了 `sync=true`、`DzSyncData`、`DzSyncDataImmediately`？
7. 是否用父帧显隐就能收口，而不是再维护一套额外 visible/page/variant 状态机？

#### 一句话分流

- **全局状态变更**：如 BOSS 刷新、积分更新、共享任务推进 → 可以低频同步
- **个人 UI 交互**：如滚轮、拖拽、翻页、滑块跟手移动 → `sync=false` + N 槽 / 本地门控隔离

### 4.5 来自样本文件的两条经验

#### 4.5.1 本地 `DzFrameSetText` 不是首选，但也不等于“必炸”

这段与上文 4.2、4.4 并不矛盾，补充的只是**排障判断**：

- 某些可行版 JASS 会在 `GetLocalPlayer()` 内直接 `DzFrameSetText`
- 这类写法**不推荐作为长期工程方案**
- 但如果它只改本地显示结果，且后续**没有**被全端 timer / `sync=true` 回调 / 共享状态逻辑继续消费，可能长期“不复现掉线”

工程结论：

- **规则层面**：仍按高风险写法看待，不作为常规方案推广
- **排障层面**：看到本地 `setText` 时，不要立刻武断下结论“必炸”；先追它是否进入后续同步放大链
- **长期维护**：正式模块优先收敛成“文本内容全端一致，最后只做本地显隐”

#### 4.5.2 父帧收口显示控制，优先于散落的显示状态机

样本经验：

- 把子帧全部挂在父帧下
- 父帧隐藏时，整棵帧树一起隐藏
- 显示控制围绕父容器展开，比逐个子帧再叠 `page / variant / visible` 判断更稳

工程结论：

- 能用父容器显隐收口的，就不要再维护额外的子块显隐状态
- `page` / `expanded` / `category` 应尽量只表达业务数据，不要同时承担过多“显示路由”职责
- 若一个 UI 后续出现“翻页只是局部显示，却要层层读取 `page`、算 `variant`、补多个 visible 判断”，应考虑回到父帧树控制，减少状态面

---

## 5. `DzFrameSetScriptByCode(..., sync=true)` 硬规则（精简版）

### 一句话

**`sync=true` 必须全链路同步**：创建帧 → 注册回调 → 回调触发，三步各端同路径、同次数。

### 必须

1. 帧在**所有玩家**创建（不要包在「仅本地」里创建）。
2. `sync=true` 回调在**所有玩家**注册，建议初始化阶段一次性注册。
3. 回调优先**全局具名函数**，避免 sync 链依赖难控的闭包句柄。
4. 回调体分层：**全局同步逻辑**在本地判断外；**帧/音效等**在 `GetLocalPlayer()` 内。
5. JASS / Dz 回调的具名函数约束，统一见 `lua-gc-desync-heuristics.mdc`。

### 严禁

- 在 `sync=false` 链或「仅本机触发」路径里注册 `sync=true`。
- 在 `sync=true` 回调里依赖选择事件当时捕获的 unit/hero 闭包。
- 在 `sync=true` 里做**非幂等共享状态**（如 `queue.shift()` 且各端进入次序不同）、或在 sync=true 里调用**带 GetLocalPlayer 分路且会 `TimerStart`** 的函数（两端 timer 次数可能不一致）。

### 对话框与 `DzClickFrame`（备忘）

- `DzClickFrame` 在 `sync=true` 帧上不是「本机假点击」，会走联机 UI 同步，成本与网页 `click()` 不同。
- **禁止**单帧 `while` 连发多次 `DzClickFrame`（易卡顿掉线）；多句快进用 Lua 裁队列；裁完刷新须先 `showDialogFrames(true)` 再 `applyPortraitFrames`；共享队列对齐用 `DzGetTriggerKeyPlayer` / 事件玩家，勿用本地玩家兜底找对白队列。
- **qipao / 叹号**：勿用「本机是否刚拆掉叹号」这种各端可能不同的条件分叉同步路径；用配置位（如 `removeOverheadMarkerOnOpen`）保证全房一致。

### 推荐实践（任务按钮等）

- 初始化全端预创建帧并预注册 `sync=true`；点击只切换「当前回调上下文」。
- 回调内用 `playerId` 等稳定 key 重新取对象，不从旧闭包取句柄。
- 任务接受/拒绝：队列解析支持队首非任务行；勿只认 `queue[0]`。

---

## 6. 键盘 / 鼠标注册的 `sync`（2026-04-13 实测）

`DzTriggerRegisterKeyEvent` / `DzTriggerRegisterKeyEventByCode` 的 `sync` **有效**：

- **`sync=true`**：全房各客户端都进回调；回调内全局操作可全房一致执行（不必再套一层 DzSyncData 做同一件事）。
- **`sync=false`**：仅本机，只做纯本地 UI。

`DzTriggerRegisterKeyEventTrg(trg, status, key)` 为 JASS 封装，内部 **`sync=true` 硬编码**；参数顺序为 `(trg, status, key)`，与 `DzTriggerRegisterKeyEvent` 参数顺序不同。

**曾误传「平台只对键盘 false 派发」——已证伪，忽略旧说法。**

### 热键处理最佳实践

**使用 `DzGetTriggerKeyPlayer()` 获取真实按键玩家：**

```typescript
registerKeyDown(KEY.J, () => {
  const keyPlayer = DzGetTriggerKeyPlayer();  // 所有客户端返回相同的真实按键玩家
  const pid = GetPlayerId(keyPlayer);
  
  // 修改该玩家的全局状态（所有客户端同步执行）
  g_taskUIVisible[pid] = true;
  
  // UI显示在 GetLocalPlayer 判断内控制（只显示给对应玩家）
  if (GetLocalPlayer() === keyPlayer) {
    showFrame(mainPanel);
  }
});
```

**原则：**
- 状态修改（全局数组）→ 所有客户端同步执行，无需 `GetLocalPlayer` 判断
- UI显示（`showFrame`/`hideFrame`）→ 只在 `GetLocalPlayer() === keyPlayer` 时执行
- 不需要用 `GetLocalPlayer` 来判断"要不要处理"，而是用 `DzGetTriggerKeyPlayer` 确定"处理哪个玩家"

**参考实现：** `04．任务UI拆分/05．任务UI热键.ts`

**Frame 叠放与 `DzFrameSetPriority`**：见 `ui-frame-types.mdc` 文末「Frame 叠放与优先级」。
