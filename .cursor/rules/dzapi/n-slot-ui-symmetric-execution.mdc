---
description: DzAPI 联机 desync、对称执行、Timer/GetLocalPlayer 分层、N 槽位 UI、sync 回调与对话框（项目经验单一事实来源）
globs: TS/**/*.ts
alwaysApply: false
---

# DzAPI 联机 UI：Desync、对称执行与 N 槽位模型

本文件是 **Dz 帧 + 联机生命周期 + 同步参数** 的**单一事实来源**（与 `ui-frame-types.mdc` 分工：`ui-frame-types` 偏 Frame 类型、对齐、滚动数值、事件 ID；本文件偏 **全端执行次数、Timer、sync、N 槽**）。

相关：`loadtoc-ui.mdc`、`fdf-crash.mdc`。

---

## 1. 方案 B（N 槽）：设计结论

- 默认优先 **方案 B（N 槽）**：每个客户端都创建同样数量、同样结构的 UI 壳；本地差异只放在显隐等表现层。设置UI文本/贴图等全局同步
- 设计目标不是“所有人画面一致”，而是 **所有客户端执行形态一致**。魔兽里的 desync 根因通常是 **全局状态读写的顺序、次数、分支不一致**，不是“不同槽位是否画出同一帧文本”。
- **方案 A（每端仅 1 套本地 UI）维护成本高**：只要后续迭代里让“面板是否存在/是否打开”影响到计时器、注册次数、同步回调链或共享状态读写，就更容易把 UI 问题升级成 desync。
- 方案 B 的价值是把 **创建、注册回调、滚轮、刷新入口** 固定成 **每个客户端永远跑同一套 N 槽流程**；最后才用 **`GetLocalPlayer()` + 可见性/可交互性** 区分谁看见、谁可操作。
- **全局任务数据**仍应是全房一份；各槽位的**纯显示内容**（列表、展开态、滑块位置等）可以不同。B 的保险点不是“允许异步乱写”，而是 **不要让 UI 生命周期把执行路径带偏**。
- 失败模式取向：方案 A 更容易把 bug 放大成 **desync / 掉线**；方案 B 更容易把 bug 降级成 **本地显示异常**，便于继续游戏和排查。

---

## 2. 术语：desync 与纯客户端 UI

| 概念 | 含义 |
|------|------|
| **网络异步 / desync** | 各客户端对**全局游戏状态**的推演不一致（单位、物品、计时器、触发器、`Sync*`、随机路径、**各端执行次数/分支**等）。 |
| **纯客户端 UI** | `DzCreateFrame`、`DzFrameShow`、滚动条、展开态、字体等——**不写全局状态**时，一般不产生同步帧；**少建/多建几套帧本身通常不是 desync 根因**；B 的价值在 **执行路径**，不在「帧数迷信」。 |

---

## 3. 铁律：全端同路径、同次数；像素只在本机改

### 3.1 计时器（BuffUI 2026-04-01 修复记录）

| 步骤 | 要求 |
|------|------|
| 定时器创建 | 在所有玩家执行 ✅ |
| 定时器启动 | 在所有玩家执行 ✅ |
| 定时器回调 | 在所有玩家执行 ✅ |
| 回调内 | `if GetLocalPlayer()` 再改 UI ✅ |
| UI 操作 | 只在本地玩家分支内 ✅ |

**不要在「仅本地玩家」分支里 `CreateTimer` / `TimerStart` 周期性刷新**——否则只有部分客户端跑回调，**执行次数不一致 → desync**。

#### 错误（会导致 desync）

```typescript
export function init(): void {
  const delayTimer = jass.CreateTimer();
  jass.TimerStart(delayTimer, 1.0, false, () => {
    const lp = jass.GetLocalPlayer();
    if (lp != null && lp !== 0) {
      createUi();
      startRefreshTimer(); // ❌ 只有本机分支里启动周期计时器
    }
  });
}
```

#### 正确

```typescript
export function init(): void {
  const delayTimer = jass.CreateTimer();
  jass.TimerStart(delayTimer, 1.0, false, () => {
    const lp = jass.GetLocalPlayer();
    if (lp != null && lp !== 0) {
      createUi(); // ✅ UI 创建仅本机
    }
    startRefreshTimer(); // ✅ 所有玩家都启动
  });
}

function startRefreshTimer(): void {
  const refreshTimer = jass.CreateTimer();
  jass.TimerStart(refreshTimer, 0.1, true, () => {
    const lp = jass.GetLocalPlayer();
    if (lp == null || lp === 0) return;
    syncBuffBar(); // ✅ 周期逻辑里再隔离 UI
  });
}
```

### 3.2 核心原则（一句话）

**所有玩家必须执行相同次数、相同路径的「会进同步推演」的代码；本机 UI（显隐、纹理、文字）只在 `GetLocalPlayer()` 判断内。**

若某 API 的 `sync=true`：其回调里**改全局/共享状态**必须在本地判断**外**且全端一致；**仅本地表现**在判断**内**。二者**不可混在同一分支语义里**。

### 3.3 事件 vs 定时器（简单版）

- 不要用「选中/取消选中」等**各端触发时机可能不同**的事件驱动 UI 刷新作为主路径。
- 可用短周期定时器轮询 + `GetLocalPlayer()` 内更新 UI（与 BuffUI 思路一致）。

### 3.4 物编 `EXExecuteScript` 与头像（联机实测）

- `getObjectProperty` / `EXExecuteScript` 是同步调用，但**忌**在全房高频 tick 里反复物编读（易掉线/不同步）。
- 头像 `DzFrameSetTexture`：**建帧时设一次**；周期回调只刷文字等，周期里**不再** `getHeroIcon` / `getObjectProperty`。
- 换英雄改头像：在**业务事件**里单次改，勿挂回高频 tick。

---

## 4. N 槽位 UI（与 §1 呼应：技术落地）

- **N**：地图最大玩家数（常见 4），槽位 `i` ↔ `Player(i)`。
- **每个客户端**对称创建 **N 套**完整壳（帧树、滚轮/刷新注册；子帧名后缀 `_s{i}` 或 `DzCreateFrame` 的实例号/context，避免同名冲突）。
- **数据**：读 `questManager` 等已同步数据时用 `playerId === i`。
- **显示与操作**：仅用 `GetLocalPlayer()`（或触发键玩家 === 本地玩家）决定哪一套可见、可点、可滚；其余槽位隐藏或不接输入，但**创建与注册与本地槽位同路径**。

要点：**对称的是「跑了哪些代码、注册了多少东西」**；**刻意不对称的是最后 `DzFrameShow` / 输入门控**。

与「每端只建 1 套」对比：单套省资源；N 槽用内存换 **§1 所述的路径保险**。新写复杂 Dz 面板时默认评估 N 槽；维持单套则审查所有 Timer/Trigger 是否与「面板是否存在」解耦。

### 4.1 实施清单

1. 本机槽位：`GetPlayerId(GetLocalPlayer())`。
2. 帧名 / FDF context 避免跨槽冲突。
3. **热键**：模块级只注册一份，派发到本机槽位，避免一次按键 N 份逻辑。
4. **多份 `registerUIRefreshCallback`**：回调内用「本槽是否本地 + 是否可见」早退，或集中注册再分发（二选一，保持全端对称）。

### 4.2 本地隔离 vs 全局同步（N槽场景）

**N槽模型**：每个客户端创建N套UI壳（N=玩家数），槽位i服务Player(i)。所有客户端的槽位i状态必须一致，只有显示/隐藏不同。

| 操作类型 | 需要本地隔离 (`GetLocalPlayer`) | 说明 |
|---------|-------------------------------|------|
| `DzFrameShow` (显隐) | ✅ 必须 | 只有本地玩家看到自己的UI |
| `StartSound` / 音效 | ✅ 必须指定玩家 | 否则全房播放 |
| `DisplayTextToPlayer` | ✅ 必须指定玩家 | 否则全房可见 |
| 全局变量修改 | ❌ 不需要 | 所有客户端同步修改 |
| 计时器状态 | ❌ 不需要 | 所有客户端同步修改 |
| `DzFrameSetTexture` | ❌ 不需要 | 贴图内容全房一致 |
| `DzFrameSetText` | ❌ 不需要 | 文本内容全房一致 |
| `DzFrameSetPoint` | ❌ 不需要 | 位置全房一致 |
| 滚动条滑块位置 | ❌ 不需要 | 位置状态全房一致 |

**原则**：4个槽位在所有客户端都存在，只是**显示/隐藏**不同。修改槽位的**内容**（贴图、文本、滑块位置）需要所有客户端同步知道，只有**显示/隐藏**、**音效**、**文本消息**需要本地隔离。

### 4.3 本仓库参考

`TS/系统/08．任务系统/03．任务UI.ts` 与 `04．任务UI拆分/*`（`slotPid`、`contextId`、帧名后缀）。属性 UI「同时要看到多人」可仍用多行展示，不必同一文件结构，但**数据全局、显示本地**分层一致。

---

## 5. `DzFrameSetScriptByCode(..., sync=true)` 硬规则（精简版）

### 一句话

**`sync=true` 必须全链路同步**：创建帧 → 注册回调 → 回调触发，三步各端同路径、同次数。

### 必须

1. 帧在**所有玩家**创建（不要包在「仅本地」里创建）。
2. `sync=true` 回调在**所有玩家**注册，建议初始化阶段一次性注册。
3. 回调优先**全局具名函数**，避免 sync 链依赖难控的闭包句柄。
4. 回调体分层：**全局同步逻辑**在本地判断外；**帧/音效等**在 `GetLocalPlayer()` 内。

### 严禁

- 在 `sync=false` 链或「仅本机触发」路径里注册 `sync=true`。
- 在 `sync=true` 回调里依赖选择事件当时捕获的 unit/hero 闭包。
- 在 `sync=true` 里做**非幂等共享状态**（如 `queue.shift()` 且各端进入次序不同）、或在 sync=true 里调用**带 GetLocalPlayer 分路且会 `TimerStart`** 的函数（两端 timer 次数可能不一致）。

### 对话框与 `DzClickFrame`（备忘）

- `DzClickFrame` 在 `sync=true` 帧上不是「本机假点击」，会走联机 UI 同步，成本与网页 `click()` 不同。
- **禁止**单帧 `while` 连发多次 `DzClickFrame`（易卡顿掉线）；多句快进用 Lua 裁队列；裁完刷新须先 `showDialogFrames(true)` 再 `applyPortraitFrames`；共享队列对齐用 `DzGetTriggerKeyPlayer` / 事件玩家，勿用本地玩家兜底找对白队列。
- **qipao / 叹号**：勿用「本机是否刚拆掉叹号」这种各端可能不同的条件分叉同步路径；用配置位（如 `removeOverheadMarkerOnOpen`）保证全房一致。

### 推荐实践（任务按钮等）

- 初始化全端预创建帧并预注册 `sync=true`；点击只切换「当前回调上下文」。
- 回调内用 `playerId` 等稳定 key 重新取对象，不从旧闭包取句柄。
- 任务接受/拒绝：队列解析支持队首非任务行；勿只认 `queue[0]`。

---

## 6. 键盘 / 鼠标注册的 `sync`（2026-04-13 实测）

`DzTriggerRegisterKeyEvent` / `DzTriggerRegisterKeyEventByCode` 的 `sync` **有效**：

- **`sync=true`**：全房各客户端都进回调；回调内全局操作可全房一致执行（不必再套一层 DzSyncData 做同一件事）。
- **`sync=false`**：仅本机，只做纯本地 UI。

`DzTriggerRegisterKeyEventTrg(trg, status, key)` 为 JASS 封装，内部 **`sync=true` 硬编码**；参数顺序为 `(trg, status, key)`，与 `DzTriggerRegisterKeyEvent` 参数顺序不同。

**曾误传「平台只对键盘 false 派发」——已证伪，忽略旧说法。**

### 热键处理最佳实践

**使用 `DzGetTriggerKeyPlayer()` 获取真实按键玩家：**

```typescript
registerKeyDown(KEY.J, () => {
  const keyPlayer = DzGetTriggerKeyPlayer();  // 所有客户端返回相同的真实按键玩家
  const pid = GetPlayerId(keyPlayer);
  
  // 修改该玩家的全局状态（所有客户端同步执行）
  g_taskUIVisible[pid] = true;
  
  // UI显示在 GetLocalPlayer 判断内控制（只显示给对应玩家）
  if (GetLocalPlayer() === keyPlayer) {
    showFrame(mainPanel);
  }
});
```

**原则：**
- 状态修改（全局数组）→ 所有客户端同步执行，无需 `GetLocalPlayer` 判断
- UI显示（`showFrame`/`hideFrame`）→ 只在 `GetLocalPlayer() === keyPlayer` 时执行
- 不需要用 `GetLocalPlayer` 来判断"要不要处理"，而是用 `DzGetTriggerKeyPlayer` 确定"处理哪个玩家"

**参考实现：** `04．任务UI拆分/05．任务UI热键.ts`

**Frame 叠放与 `DzFrameSetPriority`**：见 `ui-frame-types.mdc` 文末「Frame 叠放与优先级」。
