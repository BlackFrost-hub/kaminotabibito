---
description: Lua/ydLua 联机异步安全规范（社区经验优化版，按致命风险排序）
globs: TS/**/*.ts, TS/**/*.lua
alwaysApply: false
---

> **⚠️ 最高优先级（Dz / 任务面板等一切联机 UI）**  
> UI 路径是社区经验里**最容易**因闭包、GC、`pcall` 体、回调注册路径与 `GetLocalPlayer()` 混写而诱发掉线或 desync 的环节，**不得**按「调用频率低、只是包一层」在此放宽。  
> - **必须**较下文普通条目更严：进 JASS/Dz 的回调、`pcall` 包裹体、帧点击/滚轮/键盘注册 → **模块顶层具名函数 + 单列或 `mgr` 分发**；禁止把 **`() => …`** 传到上述注册点（细则见下 §「回调铁律」及仓库内 `04．任务UI拆分` 实现）。**`pcall`** 用 **`pcall(模块顶层 void 具名体)`** + 槽位写结果 + **`local ok = pcall(…)`** 读 boolean（`TS/global.d.ts` 对 `pcall` 的 `@noSelf`）；禁止 **`(pcall as any)(具名)`**（TSTL→`pcall(nil,fn)`）、**匿名 `function` 仅转发具名体**，以及 **`pcall(…)[n]` / 解构元组** 取 ok（Lua 多返回值与 TSTL 易错）。若遇到 **`pcall` 返回后后续逻辑疑似不执行**、导致 `uiInitialized=true` 但实例未落表这类现象，UI 初始化链优先改为 **直接调用 + `try/catch`**，不要继续在 `const [ok] = pcall(...)` 这类形态上加补丁。  
> - **必须**与 `n-slot-ui-symmetric-execution.mdc` 一致：**全端对称的执行次数与创建路径**；纯显隐、本机音效、仅本地可见的改帧再在本地门控内做。  
> - **审查快照**：同文件文末 **「附录：任务 UI（`04．任务UI拆分`）符合性」**；代码迭代后应复验。

# Lua GC / 闭包异步经验（优先级排序版）

> 适用：TSTL → Lua、DzAPI UI、任何频繁 JASS↔Lua 交互的系统。  
> 这不是理论，而是从社区血泪教训中提炼的稳定优先准则。  
> **越靠前的条目，越要无条件遵守。**（**例外不存在于 UI 回调链**：UI 一律按上文「最高优先级」执行，不降级。）

## 0. 先判断上下文

修改任何回调或函数前，先判断它属于哪种上下文；不要只看函数名。叫 `sync_timer`、`ready`、`callback` 的函数不一定同步安全，必须确认是谁触发、是否所有客户端都触发、是否收到同样数据、是否按同样顺序执行。

| 上下文 | 含义 | 能否安全修改同步游戏状态 |
|--------|------|--------------------------|
| 同步上下文 | 来自 Warcraft 3 同步执行、同步事件、已确认的房间/屏障结果，或所有客户端一致消费的同步消息 | 可以，但逻辑本身必须确定 |
| 本机异步上下文 | 只在当前客户端、当前本机玩家、本机 UI、平台/宿主回调、本地计时器、本地请求结果、本地文件或缓存读取中发生 | 不可以；只做本地显示、日志、提示、缓存预热、请求拦截或诊断 |
| 桥接上下文 | JASS、触发器、编辑器包装出的 Lua 调用、STES/同步键鼠/Dz 同步回调等桥接入口 | 取决于底层 Lua 来源是否同步安全；必须继续追调用来源和触发次数 |

总规则：不要让本机异步数据直接决定同步游戏行为。如果异步信息必须影响游戏状态，先让它经过明确的同步通道，再从同步后的结果决定游戏状态。

高危模式包括：在本机回调里创建单位、改技能、发奖励、写 hashtable、写游戏变量或其他同步状态；在本机回调里启动随机玩法；同步收口时读取本地请求、缓存或错误结果；把本机失败当成房间级失败；本机模式、宿主能力或平台状态差异改变同步推进分支。

## 🚨 重要程度速查（从高到低）

1. **创建 / 删除 / 修改 → 必须同步**  
   对象生命周期操作绝不能进入 `GetLocalPlayer()` 分支。唯一允许异步的只有显示/隐藏、读变量/非冥等性操作。

2. **禁止异步全局引用**  
   在本地分支中给全局表写 handle（单位、帧、物品等）会在 GC 后导致各端句柄不一致，直接掉线。
   对 `sync=false` / 本地异步路径里临时创建或拿到的 handle（trigger、timer、frame、unit、item 等），**不要跨帧持有引用**；若只是为了当前帧注册/调用，使用后立即清空局部或模块槽位引用。禁止把这类 handle 挂进本地缓存表、实例字段或模块级变量后继续跨帧保留。

3. **JASS / Dz 回调铁律：只用长寿具名函数**  
   凡是会进入 JASS 引擎的回调——计时器、触发器、单位组、帧事件——一律使用**模块顶层长寿具名函数**。  
   禁止：
   - `() => ...`
   - `function() ... end`
   - 仅转发具名体的浅包装闭包  
   动态上下文统一通过模块级槽位、映射表或稳定 key 分发。

4. **强制禁用 `pairs()`**  
   `pairs` 对哈希表的遍历顺序不保证跨客户端一致，可直接导致掉线。入口处全局禁用，改用数组下标或 `sort` 后有序遍历。

5. **字符串 / table 只在同步路径创建**  
   所有字符串拼接、新建表必须在所有玩家代码路径中执行，本地分支只做最终显示赋值。

6. **最小化 `GetLocalPlayer()` 内部逻辑**  
   本地玩家分支内除纯显示和读取外不要做任何事，包括数学运算、字符串操作等。

7. **全局绝对禁止“表表索引”**  
   禁止任何二级表索引写法与读取写法（如 `a[b][c]`、`stateByType[typeId][hid]`、`unitToBuffs[hid].buffs[buffId]`），与同步/异步场景无关。  
   统一改为：
   - 扁平 key 的单层表
   - 数组
   - 单层结构体容器  
   禁止新增、禁止保留、禁止在新旧代码中继续传播。

---

必须同步（非幂等）的操作
创建帧、创建字符串、创建 table

销毁句柄、修改全局表结构

任何对运行时对象的写入

DzFrameSetText（修改帧的内容）

DzFrameSetTexture（修改帧的内容——除非贴图路径是预定义常量且该帧不会被同步逻辑引用）

DzFrameSetPoint / DzFrameSetSize（修改帧的布局——除非该帧的布局不影响其他帧的锚点计算）

可以异步（幂等）的操作
DzFrameShow / DzFrameHide

读取变量

播放本地音效（指定玩家）

### UI 系统标准模式
```lua
-- ✅ 正确：内容同步创建，异步显示
local text = "伤害: " .. damage  -- 所有玩家都执行，创建相同字符串
if GetLocalPlayer() == Player(0) then
    DzFrameSetText(frame, text)  -- 只在本地显示
end

-- ❌ 错误：字符串在本地分支创建
if GetLocalPlayer() == Player(0) then
    local text = "伤害: " .. damage  -- 仅本地创建，其他玩家没有
    DzFrameSetText(frame, text)
end
```

### 📘 回调铁律（本文件唯一正文）

默认禁止把匿名闭包传递给：

- `DzFrameSetScriptByCode`、键盘/鼠标注册
- `TimerStart`、`ForGroup`、`Filter`、触发条件

正确做法：

- 所有回调使用模块顶层命名函数
- 动态参数通过稳定 key 的模块级表或数组传递
- `ForGroup` 等直接在 Lua 层用循环替代，不让闭包进入 JASS

补充社区经验：
- 单位组遍历优先 `FirstOfGroup` + `while`，不要继续把业务 action 挂到 `ForGroup` / `ForForce`。能在 Lua 层手动取第一个单位循环，就不要再走 JASS 侧 action 回调。
- 独立 `TimerStart` 只留给一次性延迟或确有独立生命周期的对象；常规周期逻辑优先接入项目中心计时器做分发，减少“每个系统各起一条 timer”。
- 数学运算优先使用 JASS 数学函数，不要把联机关键路径建立在 Lua `math` 库之上。社区长期经验认为 `math` 存在低概率异步风险；项目层面的默认策略是“业务代码直接少用/不用 `math`”，而不是先在入口全局 monkey patch 整个数学库。

```lua
-- ✅ 正确：命名函数，单列模式
local callbacks = {}
function MyTimerCallback()
    local data = callbacks[this]  -- 通过预存数据获取上下文
    -- ...
end

-- ❌ 错误：每触发一次就创建新闭包
TimerStart(t, 0.1, false, function()
    -- 每次不同的匿名闭包，大数据量下极易异步
end)
```

### 📘 table 与迭代器铁律

入口强制禁用 pairs

```lua
-- 项目入口文件
SafePairs = pairs
pairs = function()
    error("pairs 遍历顺序不确定，可能导致异步！请使用 SafePairs 或数组索引")
end
```

替代方案
数组优先：for i = 1, #arr do ... end

需要遍历非数组 table 时：先收集 key → sort → 再遍历

```lua
local keys = {}
for k in SafePairs(t) do table.insert(keys, k) end
table.sort(keys)
for _, k in ipairs(keys) do
    local v = t[k]
    -- ...
end
```

### Map / 哈希表的联机安全使用条件
- Lua 表遍历顺序不是游戏协议，`pairs()` 的顺序不能用于锁步决策。
- 不要用 `pairs()` 顺序决定赢家、奖励、随机消耗顺序、同步消息顺序、序列化内容、哈希或校验值。
- 不要默认相信插入顺序，除非数据结构明确定义并保证顺序。
- 需要确定顺序时，先把 key 收集到数组，显式排序，再按数组顺序遍历；同步敏感逻辑里不要遍历历史 hashtable，优先构造新的有序列表。
- **key 必须是数字或字符串**，绝不用 unit、frame、player 等 handle/userdata
- **所有客户端同步读写**，绝不在 `GetLocalPlayer()` 分支里写入
- **如果需要遍历**，必须先 `sort` keys 再按序遍历
- **优先使用数组替代 Map**：数组的遍历顺序天然确定，Lua 的数组部分（`for i=1,#arr`）顺序在各端一定一致，不需要额外 `sort`
- **二级哈希索引禁令**：不允许 `table[k1][k2]` 结构；必须改为一层索引结构（扁平 key / 数组 / 单层对象）

### 📘 全局状态与引用

任何对全局表的写操作（GlobalTable[x] = y）必须发生在所有玩家共有的代码路径中

绝不在 GetLocalPlayer() 内创建或写入全局引用

UI 控件引用、缓存数据等均受此约束

```lua
-- ✅ 正确：所有玩家都写入，结构一致
local data = ComputeAllPlayerData()  -- 同步计算
GlobalTable["LastResult"] = data

if GetLocalPlayer() == Player(0) then
    ShowResultUI(data)  -- 只做显示
end

-- ❌ 错误：只本地写入
if GetLocalPlayer() == Player(0) then
    GlobalTable[myUnit] = true  -- 只有玩家0记录了该单位，GC后其它端数据不同
end
```

### 📘 其他硬性约束

数学库：使用 JASS 提供的数学函数，Lua 的 math 可能异步。

禁止等待：用计时器代替 TriggerSleepAction。

条件判断放动作里：不要把条件写在 TriggerAddCondition 的返回布尔中，那个隐式闭包也会进 JASS。

音效：本地 UI 触发的 StartSound 必须传入本地玩家参数，不能全局播放。

运行时保护：

```lua
local runtime = require("jass.runtime")
runtime.handle_level = 0
```

单例复用：避免频繁创建销毁，把常用对象挂到全局变量，降低 GC 压力。

Lua 与 JASS 交互：尽量用全 Lua 逻辑，减少混用。

### ⚠️ 心态与概率

Lua 异步是几率性的，无法做到 100% 绝对安全。

严格按照上述规范，异步概率可控制在 0.1% 以下。

不要为追求完美写出更危险的并行逻辑；当无法避免时，接受极小概率，不要过度恐慌。

测试参考：10 万次 JASS 回调 + 修改操作未异步；1 万字符串 key 的 SafePairs 顺序未见异常（但 handle key 极度危险）。

口诀
本地玩家睁大眼，除了显示不要浪
匿名闭包频繁创，不如写为动作行
创建删除修改皆同步，查看显示可异步

## 异步报错中断规则

本机异步回调里不能让错误自然冒泡后继续影响房间状态。这里的回调包括本机事件监听器、宿主/平台回调、请求回调、队列回调和本地计时器回调。

- 异步回调报错通常只发生在当前客户端；如果它打断后续流程，其他客户端可能继续推进，状态从这一刻开始分叉。
- 高危后果包括：后续同步消息没发、事件没继续分发、队列不再推进、缓存没写完、状态机停在中间态、某些客户端少消耗一次随机数。
- 异步边界应捕获错误，并把错误变成本地诊断、错误事件、明确失败结果或可观察日志；不要静默吞掉，也不要把本机失败直接改写成房间级终态。
- 捕获到的本地错误不能反向改写同步状态；若错误结果确实要影响游戏状态，仍必须经过同步通道。

---

## 附录：任务 UI（`TS/系统/08．任务系统/04．任务UI拆分`）规则符合性快照

> 随代码变更请复验；以源码为准。与 `n-slot-ui-symmetric-execution.mdc` §4.3 交叉维护。

| 规则域 | 结论 |
|--------|------|
| **创建 / 生命周期（§1–2）** | `10．index` 在 `ENABLE_QUEST_UI_MODULE` 下对各客户端对称调用 `12．任务UI管理器` 的 `init()` / `registerHotkey()`；帧创建在 `runInitBodyInPcall` 等**全房共有路径**，非「仅本地分支才建 UI」。 |
| **匿名闭包 → JASS/Dz（§3）** | 行点击 `handleTaskRowClick`、分类 Tab 具名表、`12` 模块级分发；`01`/`02`/`09`/`10`/`12` 的 `pcall`：**void 具名体写模块槽位** + **`local ok = pcall(具名)`** 读 boolean（`TS/global.d.ts` 对 `pcall` 的 `@noSelf`）；**禁止** `(pcall as any)(具名)`（TSTL→`pcall(nil,fn)`）及对 `pcall` 返回值做 **下标/元组解构** 取 ok（Lua 多返回值与 TSTL 易错）。若某个 UI 初始化点出现 **函数体已执行，但 `pcall` 返回后的赋值/落表没发生**，直接改为 **`try { body(); } catch { return false; }`**，并在 build 后核对生成 Lua。 |
| **`pairs`（§4）** | 分类遍历走 `questTypes()` 等**固定顺序数组**，热路径未见 `pairs(` 哈希遍历。 |
| **本机分支（§5–6）** | `05．任务UI热键`：先全端 `onSwitchCategoryState`，再在 `player === GetLocalPlayer()` 内做分类 UI；与「状态同步、显示本地」分层一致。 |
| **`n-slot` §3 计时器** | 任务 UI 不以「仅本机 `TimerStart`」做主刷新链；数据变更走管理器刷新回调 → 全端 `rebuildPages` 路径。 |
| **与字面「N 槽」（`n-slot` §4）** | 当前为**每客户端一套对称壳**（源码注释：全局一套、不区分 `slotPid`），**不是**按 `Player(i)` 乘 N 套物理帧；仍满足 **「各端执行同套创建/注册逻辑」** 的铁律。若将来改为 N 槽，须同步帧名/`contextId`/热键分发及本附录与 `n-slot` §4.3。 |

**残留关注点**：新增 `setFrameClickEvent` / 滚轮 / 同步键注册时，继续保持具名函数 + 分发，避免回退为箭头传入注册点。

## 已废弃的运行时整段 hook 模板

- `lua-runtime-safety` 里那种“全局替换 `pairs` / `TimerStart` / `TriggerAddAction` / `ForGroup`”历史模板，当前不作为默认方案
- 如果真要做运行时实验，只能放在独立实验文档里评估，不要当成项目入口规范直接复制
- 当前默认方向仍然是业务层收敛、模块级具名函数、减少跨边界回调，而不是全局 monkey patch
