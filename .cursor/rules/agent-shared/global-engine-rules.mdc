---
description: 全局高优先级：jass/japi/BJ/本地库边界、require、封装与 STES 约定
alwaysApply: true
---

# Global Engine Rules

## 1. 命名空间（必读，勿混）

| 来源 | 含义 | 用法 |
|------|------|------|
| **jass** | `require("jass.common")`，**common.j 原生** | 仅调表里有的原生；常量用 `jass.EVENT_*` 等，勿手写魔数 |
| **japi** | `require("jass.japi")`，**扩展（Dz/EX 等）** | 与 jass 区分，勿混名 |
| **BJ** | **Blizzard.j**，Lua 里**不一定**挂在 `jass` 上 | 用项目封装：`require("lib.扩展函数.BJ函数…")`，**禁止** `(jass as any).IsUnitIllusionBJ` 这类冒充 |
| **本地库** | `TS/lib`、`系统/*` 等 | 只通过 **require 调本地导出**；**禁止**挂到 `jass`/`japi` 表冒充原生，也禁止让引擎 API「反向依赖」本地实现 |

- 查表：根目录 `jass表.txt`、`japi表.txt`（运行时日志为准）。
- **BJ ≠ jass ≠ japi**；需要 BJ 语义时走 BJ 封装模块，不要当原生用。

## 2. require

- 绝对模块路径：`require("lib.…")`、`require("系统.…")`；**禁止** `./` 相对路径（Lua 无法解析）。

## 3. 复用 `TS/lib/扩展函数`

- 新增逻辑前先搜是否已有封装；缺口可补或注明。

## 4. 全局对象

- **勿用 `globalThis` 代替 jass 全局**（`udg_*` 等按引擎约定取）。

## 5. 目录与体量

- 通用逻辑放 `自定义扩展函数` 或既有封装；新目录需 `index` 并在父级登记；单文件不宜过长（建议不超过约 500 行），过长拆分。

## 6. 新系统文档

- 新完整系统补简短 `.md`，便于接手。

## 7. STES / YDLocal 参考

- `系统/02．物品系统/07．装备提取.ts`、`系统/12．测试系统/STES事件测试.ts`、`YDLocal返回值测试.ts`

## 8. `.cursor` 经验库

- 遇坑先查 `.cursor/rules` 与历史说明。

## 9. STES 桥接

- jass **不能**直接调 Lua 函数；需 STES/事件桥接。需求若写「jass 可触发/传参」，按 STES 可桥接方式设计。

## 10. 变通不越界

- 实现可变通，但不违反上述边界。

## 11. 拆分与注释

- 拆文件/新模块时：跨模块入口、核心流程、难读函数给**短注释**；不必给每个小函数写注释。

## 12. 安全检查（默认少写）

- 默认**不要**为 jass/japi **加** `typeof === "function"` / 可选链式「API 存在性」判断。
- **业务判空保留**（句柄、配置、索引、玩家/单位是否有效）；勿与「API 是否存在」混谈。
- **已在 jass/japi 表确认的原生**：直接点号调用。
- 巡检时同时搜 `=== "function"` 与 `!== "function"`。
- **例外**：动态字符串取表、规则要求多运行时、已复现不确定且用户同意保留。

## 13. 玩家单位事件：禁止业务代码直连 `TriggerRegisterPlayerUnitEvent`

**目的**：同一 `(玩家, eventId)` 重复原生注册会浪费句柄、难审计；统一走事件中心可减少重复注册、与现有系统一致。

**✅ 新代码必须这样接**

| 场景 | 入口 |
|------|------|
| 通用玩家单位事件（拾取/丢弃/使用物品/死亡/选中/升级等） | `require("系统.00．核心系统.01．事件中心.01．玩家单位事件")` 的 `registerPlayerUnitEvent` / `registerPlayerUnitEventForPlayerIds` / `registerDefaultPlayerUnitEvent` / `registerPlayerUnitEventById` |
| 区域进入 | `require("系统.00．核心系统.01．事件中心.02．区域事件中心")` 的 `registerEnterRegionTrigger` |
| 单位特定事件、单位+范围 | `require("系统.00．核心系统.01．事件中心.03．单位特定事件中心")` 的 `registerUnitEventTrigger` / `registerUnitInRangeTrigger` |
| **仅** `EVENT_PLAYER_UNIT_SPELL_CHANNEL` / `EVENT_PLAYER_UNIT_SPELL_EFFECT` | `require("系统.03．技能系统.00．技能事件.01．核心功能")` 的 `registerSpellChannelListener` / `registerSpellEffectListener`（由该模块 `init` 统一注册原生事件） |

**❌ 禁止**（除非下面「例外」）

- 业务/系统模块里手写 `jass.TriggerRegisterPlayerUnitEvent(...)`，或自写 `for i=0..n` 对每个玩家循环注册同一事件（应改用上表 API）。

**例外（不视为违规）**

- **`01．事件中心/01．玩家单位事件.ts` 内部**：实现 master 分发时的原生注册。
- **`01．事件中心/02．区域事件中心.ts` 内部**：`TriggerRegisterEnterRegion`（语义非「玩家单位事件」，且已集中管理）。
- **`01．事件中心/03．单位特定事件中心.ts` 内部**：`TriggerRegisterUnitEvent` / `TriggerRegisterUnitInRange` 等单位实例级事件入口。
- **`03．技能系统/00．技能事件/01．核心功能.ts` 的 `init`**：对 SPELL_CHANNEL/EFFECT 的注册（唯一入口）。
- **Timer 到期、STES 子触发、Dz 键鼠等**：与「多玩家同一 event」不是同一类；能收进中心计时器/事件中心的按对应中心入口走，不能收的按子系统既有方式保留。
- **仅作封装的 lib**（如 `lib/扩展函数/BJ函数/01．触发与事件.ts`）：新增功能仍应优先走事件中心，避免再叠一层独立注册习惯。

## 14. 重复语义的单一事实来源

以下规则以后**不要**在别的规则文件里大段重复展开，只保留一句引用：

| 主题 | 单一事实来源 |
|------|--------------|
| JASS / Dz 回调必须使用长寿具名函数、禁止匿名闭包、`pcall` 体收束、表表索引禁令 | `dzapi/lua-gc-desync-heuristics.mdc` |
| Dz 联机 UI 的全端执行次数、`GetLocalPlayer()` 分层、N 槽对称执行 | `dzapi/n-slot-ui-symmetric-execution.mdc` |
| no-self / `nil` 首参 / 依赖环 / 缺失 API / 机制混用等近期 TSTL 复现坑 | `tstl-recent/recent-pitfalls.mdc` |

本文件只保留全局入口与边界，不再复制上述规则正文。

## 15. UTF-8 读写是硬规则

- 读取或写入仓库文本文件时，默认按 **UTF-8** 处理。
- 使用 shell / PowerShell / 脚本读写文件时，**必须显式指定 UTF-8**；禁止依赖终端、PowerShell、Node、系统代码页的默认编码。
- 中文路径、中文文件、`.ts`、`.lua`、`.md`、`.txt`、规则文件，全部按这条执行。
- 终端显示乱码时，先假设是显示层或代码页问题；**不要**因为看到乱码就整文件重写。
- 除非任务明确要求，禁止使用“读完整文件再原样写回”的方式处理中文文件；优先局部补丁。
