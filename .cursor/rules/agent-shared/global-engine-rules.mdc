---
description: 全局高优先级工程规则，覆盖 jass/japi/BJ 边界、模块 require、封装复用、目录组织与 STES 桥接约定
alwaysApply: true
---

# Global Engine Rules

以下规则为全局高优先级规则，适用于整个仓库。

## 1. 先看 jass 表和 japi 表
- 先看项目根目录里的 `jass表.txt` 和 `japi表.txt`
- 不要把 BJ 函数当成 jass 或 japi 函数使用
- 不要把 jass、japi、BJ 三者互相混淆
- 不要把本地函数库当成 jass / japi 原生函数使用

## 2. require 必须使用绝对模块路径
- 正例：`require("lib.扩展函数.YDWE函数.04．YDWE_trigger")`
- 正例：`require("系统.05．Buff系统.00．Buff系统")`
- 反例：`require("./06．XX函数")`
- 相对路径在 Lua 运行时无法正确解析

## 3. 优先复用封装库
- `TS/lib/扩展函数` 是项目封装函数库
- 在新增封装、抽通用逻辑、优化源文件前，先检查这里是否已有可复用实现
- 如果缺少通用封装，应补齐或明确报告缺口

## 4. 不要把 globalThis 当成 jass 全局
- 对于 jass 全局变量，不要使用 `globalThis`
- `globalThis` 是 TS / Lua 侧全局，不是 jass 全局

## 5. 通用函数与目录组织
- 可复用通用函数优先放入 `TS/lib/扩展函数/自定义扩展函数` 或更合适的现有封装文件
- 新建文件夹时必须带 `index` 统一导入导出，并在父级 `index` 中补入口
- 单文件不应超过 800 行，最好不超过 500 行；过长就拆分

## 6. 新系统要补文档
- 新建完整系统时，需要补充对应的 `.md` 说明，方便后续 AI 或人接手

## 7. STES / YDLocal 示例入口
- `TS/系统/02．物品系统/07．装备提取.ts`
- `TS/系统/12．测试系统/STES事件测试.ts`
- `TS/系统/12．测试系统/YDLocal返回值测试.ts`

## 8. .cursor 目录是坑点与经验库
- `.cursor` 目录中记录了一些项目坑点和经验，遇到相关问题优先回看

## 9. STES 桥接边界
- jass 端不能直接调用 lua 端函数
- 但可以通过 STES 事件桥接，实现被 jass 触发、传参、回调等能力
- 如果需求明确要求“能被 jass 端调用 / 触发 / 传参”，就必须按 STES 可桥接方式设计

## 10. 走不通时要变通，但不能越界
- 一个方法走不通时可以变通
- 但前提是不违反这里的边界规则

## 11. 拆分 / 新建文件时的关键函数注释
- 当智能体拆分文件、构建新模块、或重组系统时，不要只写文件头部注释
- 对于跨模块入口、核心流程、或不易直接看懂的关键函数，需要补充简短注释
- 不需要给每个小工具函数都加注释；优先保证“关键函数有注释”

## 12. 安全检查默认策略（全局覆盖）
- 新建文件和常规改代码时，默认**不要**添加“函数存在性/可选链/返回值兜底”安全检查（如 `if (typeof xxx === "function")`、`if (typeof xxx !== "function")`、`xxx?.(...)`、以及为 jass/japi 写的 `typeof jass.X / typeof japi.X` 存在性判断）。
- **业务判空/判合法应保留**（如句柄 `null/0`、配置缺失、数组越界、玩家/单位是否有效）；不要把“对象是否存在”与“API 函数是否存在”混为一谈。
- **已用运行日志 / `jass表.txt` / `japi表.txt` 确认存在的原生函数**：直接调用，不要再包一层 `typeof`。
- **巡检漏网提醒**：清理或审查时务必同时搜 `=== "function"` **与** `!== "function"`（以及 `typeof jass.` / `typeof japi.`），只搜一种容易漏。
- **允许的少数例外**（不属于“无意义样板”）：
  1) **动态名字解析**：例如按字符串从 `japi`/`jass` 上取函数、桥接注册等，必须用 `typeof` 判断再调用；
  2) 规则或需求明确要求跨平台 / 多运行时兼容；
  3) 调用点已复现运行时不确定且用户同意保留防御式写法。
- 目标：保持代码简洁，减少冗余与 token 噪音；默认**非必要不加**安全检查。
