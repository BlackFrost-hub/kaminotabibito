---
description: 技能测试规则：Boss 技能与玩家英雄技能测试场景、测试单位血量、测试靶与运行时安全约定
globs: TS/系统/12．测试系统/**/*.ts, TS/系统/03．技能系统/00．技能模板+函数/03．技能测试/**/*.ts, TS/系统/03．技能系统/05．单位技能/**/*.ts
alwaysApply: false
---

# 技能测试规则

## 通用测试场景

- 技能测试不只覆盖 Boss，后续玩家英雄技能、装备技能、召唤物技能也按本规则处理。
- 输入测试命令创建测试场景时，默认准备：
  - 地图上预设的玩家1大法师作为玩家侧测试基准英雄：`jass.globals.gg_unit_Hamg_0002`。
  - 2 个敌对步兵作为通用测试靶。
  - 如测试对象是 Boss，再额外创建对应 Boss 单位。
- 本项目当前技能测试里，“大法师”默认指 `gg_unit_Hamg_0002`，不要误判为“可能不存在”或优先猜注册表里的其他英雄。
- 代码可以保留注册英雄或玩家英雄扫描作为极端兜底，但测试规则、测试说明和排查口径都必须优先按地图预设玩家1大法师处理。
- 如果某个测试明确要用当前玩家注册英雄，必须在测试命令说明里单独写明；否则默认使用预设玩家1大法师。
- 测试靶优先用普通步兵 `hfoo`，除非技能机制明确需要英雄、召唤物、建筑或特殊单位。

## 测试血量

- 测试场景中的玩家英雄和通用步兵默认设为 `999999` 最大生命值。
- 改最大生命值后，必须立刻把当前生命值也设为同样数值，保证生命百分比为 100%。
- 不要只改最大生命值；只改最大生命会导致当前生命百分比很低，测试高伤技能时仍然会被秒杀。
- 推荐顺序：
  - 先用 JAPI `SetUnitState(unit, UNIT_STATE_MAX_LIFE, 999999)` 改最大生命。
  - 再用 JASS `SetUnitState(unit, UNIT_STATE_LIFE, 999999)` 回满当前生命。

## 测试命令

- 测试命令应该可以重复输入，不要无限刷单位；优先缓存并复用最近创建的测试 Boss / 测试靶。
- 复用测试单位时，也要重新执行一次满血初始化，避免上次测试残留低血量。
- 测试命令应该在聊天提示里写清楚可用子命令和技能映射。
- 不要为纯被动、开局自动触发、周期自动触发的机制额外添加手动测试指令；这类机制应通过创建测试场景后自然运行来验证。
- 指令只保留需要玩家主动触发观察的技能或资源生成，例如主动技能、投掷物、召唤、拾取物。删除被动/自动机制指令后应重新压紧编号，不要留下无意义空洞。

## 阵营与目标

- Boss 技能测试中，Boss 可以创建为敌对玩家单位，以便目标选择、Buff、伤害、仇恨等逻辑接近正式战斗。
- 如果为了手动施法需要玩家控制版 Boss，应单独写测试命令，不要混用正式敌对 Boss 测试入口。
- 玩家英雄技能测试中，玩家英雄保持玩家所属，测试靶使用敌对阵营。

## TSTL 与异步规则

- 测试系统也必须遵守 TSTL 硬规则：JASS/JAPI API 使用局部别名，回调用模块顶层具名函数。
- 不要在测试命令里使用 `GetLocalPlayer`、本地分支、异步 UI 回调或本地随机数来改变共享状态。
- 运行时敏感路径不使用 `Math.*`，距离、随机、三角函数按项目封装或 JASS 对齐函数处理。
