---
description: 英雄技能施法距离修正规则，约束施法距离、运行距离、提示范围同步
globs: TS/系统/03．技能系统/**/*.ts
alwaysApply: false
---

# 英雄技能施法距离修正规则

## 核心原则

- 施法距离加成不是只改物遍 `Rng`。如果技能实际运行距离不变，玩家只是能点得更远，但命中、位移、弹幕或提示仍然短，会形成割裂。
- 英雄技能需要施法距离增加/减少时，统一走 `TS/系统/03．技能系统/00．技能模板+函数/04．机制组件/11．技能属性修正/01．施法距离修正.ts`。
- 默认只给英雄技能使用。Boss、装备、传送门、剧情移动、测试摆位、移动别人不要自动吃英雄施法距离加成。
- 规则里的距离用途是常见示例，不是穷举。只要某个实际战斗距离语义上来自英雄主动技能施法距离，就必须走同一套修正 API；遇到新形态用 `施法距离派生距离` 或显式传 `吃施法距离加成: true`。

## 必须同步的三层

- 物遍施法距离：用 `登记英雄技能施法距离配置` 记录基础施法距离，再用 `刷新英雄技能物遍施法距离` 同步单位身上的技能施法距离。
- 实际运行距离：技能逻辑里不要直接用配置距离，改用 `取英雄技能修正距离(hero, abilityId, 基础距离, 用途)`。
- 提示范围：提示圈、直线预警、扇形预警、弹幕预警必须读取同一个修正后的距离。
- 通用底层入口优先传 `英雄技能距离修正: { 单位: hero, 技能ID: abilityId, 用途?: "..." }`。底层不自动猜英雄技能，只有调用方显式传上下文才吃施法距离修正。

## 哪些距离默认吃加成

- `施法距离`
- `施法距离派生距离`：用于没有更精确标准用途的新形态。
- `自身位移距离`，例如冲锋、闪烁、跳跃、穿梭、贴近目标、脱离等施法者主动移动自己。
- `直线长度`、`矩形长度`、`路径长度`、`路径总长度`、`胶囊长度`
- `扇形半径`
- `弹幕飞行距离`、`飞行最大距离`、`投射物最大距离`、`追踪最大距离`
- `目标点最大距离`、`落点最大距离`

## 哪些距离默认不吃加成

- `效果半径`、`爆炸半径`：落点 AOE 可以点得更远，但爆炸半径不应该默认变大。
- `击退距离`、`牵引距离`、`移动别人距离`：这是移动目标或敌人，不是施法者自身位移。
- 传送门、剧情移动、复活回场、测试摆位、机制单位位置同步。

## 技能配置要求

- 英雄技能模块落地后，每个英雄主动技能要登记基础施法距离，不要运行时读取已经被修改过的当前距离当基础值，否则重复刷新会叠加错误。
- 如果某个技能的效果半径确实要随施法距离变化，必须在技能配置中显式声明，并调用 `取英雄技能修正距离` 时传 `吃施法距离加成: true`。
- 如果技能是位移技能，只给施法者自身位移距离吃加成；击退、吸附、牵引敌人不跟随。
- 提示范围不单独决定是否吃加成。提示圈、直线预警、扇形预警、弹幕预警应复用实际运行距离的修正结果，或者传同一个用途重新计算，不要出现提示和实际结算两套距离。

## 接入检查

- 改直线、扇形、矩形、弹幕、冲锋、闪烁、跳跃底层时，先确认是否需要读施法距离修正。
- 改路径、胶囊、环形扩散、链式追踪、飞行物、落点最大距离等新形态时，同样先判断它是不是施法距离派生距离；是就接修正 API，不是就保持固定配置。
- 同一个技能的物遍施法距离、实际结算距离、提示圈距离必须来自同一份基础配置和同一个修正 API。
- 不要在单个英雄技能文件里自己维护 `施法距离加成` 表。
- Boss、装备、传送门、剧情移动、测试摆位、移动别人默认不传 `英雄技能距离修正`。击退、牵引、吸附敌人不因为施法距离提高而变远，除非设计明确写成特殊效果。
