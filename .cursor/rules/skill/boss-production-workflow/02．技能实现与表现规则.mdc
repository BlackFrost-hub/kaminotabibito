---
description: Boss 技能数值配置、目标选择、触发方式、提示圈、单技能质量门槛
globs: TS/系统/03．技能系统/05．单位技能/03．Boss技能/**/*.ts, TS/系统/03．技能系统/00．技能模板+函数/**/*.ts, TS/系统/01．单位系统/06．仇恨系统/**/*.ts
alwaysApply: false
---

# Boss 技能数值配置、目标选择、触发方式、提示圈、单技能质量门槛

## Boss 数值与表现配置

- Boss 规划文档可以描述机制方向，不要求把所有伤害、冷却、特效、动作、颜色、模型都写死；实现时必须把这些落到具体 Boss 的配置表里，避免散落在技能函数内部。
- Boss 技能凡是涉及“单人/多人”数量、倍率或惩罚分支，必须使用 `TS/系统/00．核心系统/00．玩家系统/00．英雄注册联动/06．玩家人数.ts` 的 `取当前有效玩家人数()`；不要用敌对英雄扫描数量、地图槽位数量或测试模板里开了几个槽来推断玩家人数。空槽、未在玩玩家、电脑玩家都不应算入在线玩家数。
- 推荐在具体 Boss 目录中建立 `02．数值与表现配置.ts`，按技能分组维护：
  - 伤害：优先写成比例表达，不要默认写死固定点数。常规技能/小技能以 Boss 攻击力倍率为主，例如 `Boss攻击力×120%`、`Boss攻击力×150% + 目标最大生命值×2%`；核心技能、大招、致命惩罚、阶段机制最好带目标最大生命值、目标已损失生命值、Boss 最大生命值等百分比项，例如 `Boss攻击力×200% + 目标最大生命值×8%`、`目标已损失生命值×25%`、`Boss最大生命值×5%`。固定值只作为低压补充、召唤物基础数值或特殊机制门槛，不作为 Boss 技能伤害默认写法。
  - 伤害分层：常规小技能偏“攻击力倍率 + 少量目标 MaxHP 百分比”；核心技能偏“攻击力倍率 + 明确 MaxHP/已损失 HP 百分比”；大招/灭团惩罚偏“MaxHP/已损失 HP 百分比 + 可规避机制”，避免纯堆固定伤害导致后续难度成长失真。
  - 伤害字段必须同时写清属性类型/伤害类型、是否触发攻击效果、是否影响仇恨、是否可被减免/护盾/抗性处理。
  - 伤害调参必须方便。实际进图后如果伤害过高或过低，应优先只改 `02．数值与表现配置.ts` 里的倍率字段；技能文件只负责读取配置并计算公式，不要把伤害数字、临时补正、难度修正散写进技能逻辑。字段命名要让人一眼看出可调项，例如 `Boss攻击力比例`、`目标最大生命比例`、`目标已损失生命比例`、`全局伤害倍率`。
  - 时间：冷却、持续时间、前摇、引导、延迟、tick 间隔、生命周期。
  - 范围：施法距离、判定半径、安全区半径、召唤距离、锁链断裂距离。
  - 表现：特效路径、模型路径、挂点、缩放、颜色、透明度、动画编号、动画速率、飞行高度、闪电类型。
  - 召唤物：单位壳 raw id、运行时模型文件、生命/攻击/护甲/攻速、持续时间、数量、出生位置规则。
- 技能实现文件只读取配置并执行逻辑；不要把同一技能的伤害倍率、特效路径、动画编号等硬编码到多个函数里。
- 如果规划文档故意留白伤害或特效，AI 可以先按配置表给出一版保守默认值，但必须集中在配置表，方便后续平衡。
- 召唤物优先复用 `系统.03．技能系统.00．技能模板+函数.01．技能函数.11．召唤物.04．对外接口` 的 `创建召唤物`；该 API 支持 `模型文件/模型路径/ModelFileID` 运行时改模型，所以只有“需要进入物编、拥有独立基础属性或被多个系统识别”时，才必须新增完整单位物编。

## 技能目标选择

- Boss 技能目标选择不要在单个 Boss 目录里私有复制一份；应优先复用并扩展 `TS/系统/01．单位系统/06．仇恨系统/` 下的公共目标选择能力，让后续 Boss、AI 和技能都能共用。
- 现有 `02．目标选择.ts` 已负责从仇恨表中选择“应攻击目标”并处理目标粘性；后续如果技能需要更多选择策略，应在仇恨系统内新增面向技能的封装，而不是写进具体 Boss。
- 技能常用目标策略包括：
  - 优先指定目标：例如被 Boss 标记、被锁链链接、被阶段机制点名的玩家。
  - 最高仇恨目标：从仇恨表读取，适合点名、追击、单体控制。
  - 当前攻击目标：适合作为最高仇恨不可用时的兜底。
  - 最近/最远玩家：适合范围压制、安全区、远程惩罚。
  - 随机有效玩家：适合开局点名、全场机制随机点人。
  - 全部有效玩家：适合全场 AOE、全员脚下法阵。
- 技能目标选择封装应支持过滤死亡、无效句柄、敌我关系、距离、区域、是否已有指定 Buff/标记等条件。
- 如果技能伤害会影响仇恨，应通过统一伤害/仇恨入口维护仇恨；不要在技能里手写一套与仇恨系统脱节的目标表。

## 技能实现优先复用

- Boss 技能伤害如果需要被后续系统识别为 `单体` / `AOE`，必须走封装伤害入口（如 `TS/系统/04．伤害系统/08．技能伤害系统.ts` 的 `造成技能伤害` / `造成单体技能伤害` / `造成AOE技能伤害`）并显式传 `伤害形态`。不要靠本次命中单位数量倒推 AOE；旧的裸 `UnitDamageTarget` 或未传形态的伤害只视为 `"未知"`。
- Boss / 英雄主动技能如果需要“本次施法第一次伤害命中敌人”这类机制，必须先用 `创建技能伤害实例` 生成本次施法唯一 `技能实例ID`，后续所有封装伤害都传同一个 `技能实例ID`，并复用 `00．技能模板/12．技能伤害触发模板/01．首次伤害命中模板.ts`。技能流程有明确收尾时调用 `结束技能伤害实例`；没有收尾时依赖实例 TTL 兜底清理。
- 多段前摇、引导、生效帧、后摇、收尾优先看：
  - `00．技能模板/01．多阶段技能编排`
- 选目标优先看：
  - `00．技能模板/02．选目标模板`
- 路径、地面持续区域优先看：
  - `00．技能模板/03．路径技能模板`
- 弹幕、冲锋、击退、跳跃、牵引、区域、护盾、落点打击、跳链、召唤物、控制、光环等优先看：
  - `01．技能函数/**`
- 单位/范围、Buff、动画等待、提示特效、命中规则等优先看：
  - `02．通用函数/**`
- Boss 专属但可复用的运行机制优先看：
  - `04．机制组件/**`
  - 适合放这里的内容包括：机制清理篮子、Boss 层数状态集、战场地点位/安全区组、持续危险区/线段危险区/多波延迟 AOE、可攻击机制单位、限时摧毁目标组、持续单位连线、血量节点触发器。
  - 这些组件只负责通用生命周期、判定和表现骨架；不要写入具体 Boss 名称、台词、伤害倍率或剧情语义。
- 如果同类逻辑在两个以上技能/Boss 中出现，或逻辑涉及施法时间线、硬直、吟唱条、目标选择、特效生命周期、召唤物行为、联机同步安全等易错点，AI 可以主动在 `TS/系统/03．技能系统/00．技能模板+函数` 或 `TS/lib/扩展函数` 中新增小型通用封装；新增前必须先搜索已有能力，接口要小、命名清楚、生成 Lua 可验证，不要把明显可复用的底层能力长期堆在具体 Boss 文件里。
- 通用封装是工具，不是万能框架。复杂技能如果需要多段动作、十几个时点、多个伤害/效果/特效链，可以使用多阶段编排或自由代码组织；不要为了套 `施法时间线` 之类的轻量封装而牺牲语义和可读性。
- Boss 开战、转阶段、释放关键技能、死亡等台词必须使用 `系统.09．表现系统.06．广播提示消息.index` 的 `广播单位提示(来源单位, 文本, 持续时间Ms)`，让头像、队列、停留时间和表现风格统一。
- 不要在 Boss 技能文件里直接散写原生大触发器；玩家单位事件统一走事件中心。

## 技能提示圈归属规则

- 同一个技能时点只能有一个提示圈拥有者。不要在 Boss 技能里手动画一份提示圈，同时又让底层区域/落点/路径系统自动再画一份。
- 新增或维护技能预警时，优先走 `TS/系统/03．技能系统/00．技能模板+函数/02．通用函数/16．技能提示圈工厂.ts` 的 `创建技能提示圈` 或底层暴露的 `提示圈` 配置。
- 已经支持提示圈的底层系统，例如区域效果、持续危险区、落点打击、地面路径持续区域，应由调用方把 `提示圈` 配置传给底层；底层负责创建提示圈，技能文件不再额外手动创建同一时点的提示圈。
- 如果技能文件确实需要自己手动创建提示圈，必须关闭底层提示圈：优先传 `提示圈: false` 或 `{ 类型: "无" }`，旧接口可传 `显示提示圈: false` / `提示特效启用: false`。
- 动态扩张/收缩范围、动态扇形这类需要每 tick 更新尺寸/朝向的提示圈，默认继续使用底层句柄式动态提示；如果另行手动画提示圈，必须显式关闭底层动态提示，避免重复和不同步。
- 代码评审时看到“提示圈 + 区域效果/落点打击/路径持续区域”同时出现，要先判断是否重复创建；不能为了补表现随手在技能文件里再加一圈。
- 凡是延迟后才结算的伤害，必须给玩家可见预警。包括延迟爆炸、落点打击、扇形喷吐、场地脉冲、锁定当前位置后延迟结算等；预警应落在真实伤害坐标/范围/目标脚下，持续时间要覆盖从锁定到结算的等待期。
- 吟唱条只能算全局/文字提示，不能替代落点或范围预警。只要玩家需要通过移动、站位或离开区域来规避伤害，就必须有地面提示圈、法阵、冒泡、目标点特效或等价清晰表现。
- 持续 Dot/周期伤害如果来自已经可见的 Buff、锁链、污染区、连接线或残留区域，不要求每一跳重新创建提示圈；但首次施加前如果存在延迟生效窗口，仍要在目标或区域上提前预警。
- 使用提示特效底层时必须先读函数注释，不要只凭函数名猜参数。尤其 `09．提示特效.ts` 里的矩形/扇形提示：
  - 矩形提示圈只适合宽长比 `1:1` 到 `1:6`，更长会出现菱形或视觉错误；超长路径应拆成多段矩形、走路径持续区域，或换成更合适的表现。
  - 扇形提示圈的 `size` 是模型缩放，不是实际半径；当前模型 `size = 1.0` 约等于外圈半径 `512`，按技能半径换算时应使用 `半径 / 512`，不要直接把 600、900 这种半径当 size 传入。
  - 矩形 `width/long/fac`、扇形 `fac/size/time/speed` 的参数顺序必须按注释来；改提示圈前先检查视觉锚点、朝向、持续时间和实际伤害范围是否一致。

## Boss 技能触发方式

- Boss 技能不一定必须通过物编主动技能或 AI 自动施法触发；只要机制本质是“满足运行时条件后生效”，就可以在代码侧直接驱动。
- 适合代码侧直接触发的典型场景：
  - 被动技能：例如近身光环、持续压制、攻击/受击附加效果。
  - 周期机制：例如每 tick / 每 N 秒检测全场 AOE、场地伤害、全场点名、持续召唤。
  - 阶段机制：例如开战触发、血量 70% / 40% 转阶段、阶段内循环审判。
  - 累计条件：例如累计受到 XX 伤害后破盾、锁链被攻击累计 1500 伤害后打断、Boss 连续击中若干次后触发惩罚。
  - 状态条件：例如玩家进入/离开区域、距离过远、目标身上有某 Buff、Boss 处于月光灌注状态。
  - 召唤物逻辑：例如召唤物出生后自动链接玩家、死亡时爆炸、存活期间周期射击。
- 适合物编主动技能 / AI 自动施法的场景：
  - Boss 需要真实“施法动作”，并且需要 AI 按冷却、距离、目标选择去释放。
  - 技能依赖魔兽原生施法事件、目标点/目标单位、命令 ID、施法前摇或打断判定。
  - 需要让物编技能壳承载施法距离、魔法消耗、冷却组、施法动画等表现。
- 纯机制类、阶段类、被动类、周期类技能优先按代码运行时条件组织，不要为了登记而强塞主动技能槽。
- 代码侧直接触发仍然要走项目已有封装：中心计时器、事件中心、Buff 系统、伤害/治疗入口、技能模板函数和表现系统；不要在 Boss 文件里散写原生大触发器。

## 单技能质量门槛

每个 Boss 技能实现后，至少检查这些问题：

- 触发是否正确：主动技能不是只写了 AI 表；代码侧技能不是被塞进无意义物编技能壳。
- 朝向是否正确：有目标/目标点/飞行方向的技能，动作开始前和真正发射/结算前都要面向目标。
- 时间线是否一致：吟唱条、硬直、动作、飞行物、命中、爆炸、伤害不能各走各的。
- 表现是否完整：读条、提示圈、法阵、飞行、冲击、爆炸、Buff 图标不能缺规划文档里的关键节点。
- 锚点是否正确：默认不要把 Boss 身上特效放玩家身上，也不要把玩家脚下法阵放 Boss 脚下。
- 数值是否真实：增伤、减攻、减速、攻速、最大生命百分比、召唤物生命等必须真的接入对应系统。
- 描述是否同步：Buff 描述、测试提示、配置注释要写清楚具体数值，不能只写“提高/降低/短时间”。
- 清理是否完整：特效、Buff、召唤物、连接、计时器、状态表、硬直和动作速率都要有结束路径。
- 测试是否覆盖：至少有一个测试方式能观察主动技能全链路；被动/周期机制要能在测试场景自然验证。
