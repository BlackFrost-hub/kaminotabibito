---
description: Boss、装备、奖励、测试与生成 Lua 的最终验收清单
globs: TS/系统/03．技能系统/05．单位技能/03．Boss技能/**/*.ts, TS/系统/12．测试系统/**/*.ts, TS/系统/02．物品系统/**/*.ts, objediting/**/*.lua
alwaysApply: false
---

# Boss、装备、奖励、测试与生成 Lua 的最终验收清单

## 验收清单

- `npm run build` 通过。
- 若改 TS 回调/事件/计时器/主动技能核心，build 后检查生成 Lua 的 `self/nil`、回调参数和调用形态。
- 新装备能通过物品名反查到 raw id。
- 新 Boss 目录被父级 `index.ts` 接入。
- 新 Boss 自动施法被对应 AI 配置表接入。
- 新 Boss 走 Boss 战启动桥接后，运行层必须能同步当前 Boss 到 `udg_Boss`，死亡/结束时只能清理仍指向自己的 `udg_Boss`，避免脱战回血和当前 Boss 判断失效。
- 新 Boss 死亡奖励在通用死亡结算或首领奖励池中可追踪。
- 新主动装备在物编、装备数据、主动装备名、主动物品 ID、通用物品技能槽位配置、使用效果核心中保持一致。
