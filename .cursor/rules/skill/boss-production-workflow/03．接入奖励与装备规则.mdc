---
description: Boss AI 登记、死亡结算、首领奖励、装备物编与主动装备登记
globs: TS/系统/03．技能系统/06．AI自动使用技能/**/*.ts, TS/系统/02．物品系统/**/*.ts, objediting/Equipment/**/*.lua
alwaysApply: false
---

# Boss AI 登记、死亡结算、首领奖励、装备物编与主动装备登记

## Boss AI 和技能登记

- 如果 Boss 需要自动施放主动技能，配置在：
  - 普通 Boss：`TS/系统/03．技能系统/06．AI自动使用技能/03．BossAI配置表.ts`
  - 英雄 Boss：`TS/系统/03．技能系统/06．AI自动使用技能/06．英雄BossAI配置表.ts`
  - 异界 Boss：`TS/系统/03．技能系统/06．AI自动使用技能/07．异界BossAI配置表.ts`
- `08．全部AI配置索引.ts` 会合并各类 AI 配置表；通常不用手动改索引，除非新增配置分类文件。
- 同一个 Boss 的多个主动技能必须在物编里挂不同技能槽位，并在 AI 表里分别登记；物编槽位够用时，不要让多个主动技能共用一个 `AN00`/`ATxx` 再靠 TS 运行时路由。
- 每个通过 AI 自动施放的物编技能壳，都必须有对应 `registerSpellEffectListener` 入口或项目已有等价入口接到真实 TS 技能效果；不能只登记 AI 表和物编 abilities。
- Boss 单位物编必须显式挂难度生命科技，例如 `upgrades = 'R001,R002'`。虽然 `createBossHeroUnit` 当前有默认值，但具体 Boss 文件里也要写清楚，避免后续覆盖或他人误判导致 Boss 生命不吃难度加成。
- Boss 主动技能如果复用通用通魔技能壳，运行时必须给该 Boss 单位实例设置真实 `Tip/UberTip`，避免命令卡显示“通用模板”语义。`Tip` 作为技能按钮名，`UberTip` 作为扩展说明；当前 1.27 + DzAPI 不要求运行时修改物编 `Name`。优先在 Boss `00．配置.ts` 维护 `主动技能提示`，并在创建运行时上下文时调用 `设置单位技能壳普通提示`；如果新增技能壳，也要同步更新提示配置。
- Boss 战启动桥接登记运行上下文在：
  - `TS/系统/03．技能系统/06．AI自动使用技能/09．Boss战启动桥接/01．Boss自动技能注册表.ts`
- Boss 开战属性、护卫、血条弱点韧性、死亡结算分别在 `09．Boss战启动桥接/**` 对应子目录配置，不要混到具体技能文件里。
- Boss 战启动属性的胜利音乐默认使用变量名：主线 Boss 填 `gg_snd_shengliBgm`，隐藏 Boss 填 `gg_snd_shengliBgm2`。除非该 Boss 有专属胜利音乐，否则不要留空 `胜利音乐路径` 或另填路径。
- Boss 战启动属性里的属性弱点字段是精简后的系统口径：文档或设计里写“木弱”时填 `风弱`，写“水弱”时填 `冰弱`，写“电弱”时填 `雷弱`。不要为了木/水/电再新增平行字段，除非底层弱点系统明确扩展属性维度。
- 说“Boss 已接入 Boss 战”至少要同时确认：
  - Boss 单位/模型/物编存在。
  - Boss 物编显式挂了难度生命科技 `R001,R002` 或当前 Boss 所需等价科技。
  - Boss 技能实现目录被父级 index 接入。
  - 需要自动施法的技能在对应 Boss AI 配置表中登记。
  - 物编技能壳、AI 配置、`registerSpellEffectListener` 或等价入口三者能对上。
  - 通用技能壳在运行时被改成 Boss 技能真实 `Tip/UberTip`，不显示通用模板语义。
  - 战斗启动属性配置表登记了 Boss raw id、启动属性、区域音乐、护卫/来源主人等需要的上下文。
  - 死亡结算或首领奖励入口按设计接入；如果该 Boss 不需要死亡结算，也要明确不是遗漏。

## Boss 死亡结算

- Boss 死亡结算允许保留旧固定掉落语义：`直接掉落物品名列表` 表示 Boss 死亡后把列表里的物品全部直接掉出来。
- 旧宝箱库存、随机候选这类“多件装备里 pick 1”的语义统一迁入首领奖励选择系统；死亡结算只填 `首领奖励池ID` 触发，不再创建宝箱单位库存。
- 死亡结算还处理清理旧 YD 表、剧情承接、全员数值奖励、击杀者奖励、提示、非装备批量掉落：
  - `TS/系统/03．技能系统/06．AI自动使用技能/09．Boss战启动桥接/05．Boss死亡结算/02．Boss死亡非UI掉落与清理配置表.ts`
- 该死亡结算层通过单位死亡事件中心接入；不要在业务代码里再散写 `TriggerRegisterPlayerUnitEvent`。
- 直接掉落和非装备批量掉落的物品名优先写物品名，由 `系统.02．物品系统.13．物品名反查` 反查 raw id；不要手写 4 位 raw id。
- 如果 Boss 死亡仍需保留旧剧情演出，配置可使用 `保留原剧情执行: true`，不要把剧情流程硬抽进通用死亡结算。

## 首领奖励选择系统

- 首领奖励选择系统和 Boss 死亡结算是互相独立的系统；首领奖励负责“从奖励池中选择并发放装备”，死亡结算只是可能触发它的入口之一。
- 新 Boss 若需要“让玩家从多件装备中选择”，优先接入首领奖励选择系统，而不是直接掉一堆装备让玩家抢；触发时机可以是 Boss 死亡、剧情节点、挑战完成、指令测试或其他业务条件。
- 奖励池配置在：
  - `TS/系统/02．物品系统/18．首领奖励选择/01．奖励配置表/`
- 奖励配置表已经拆成目录结构，完整入口是 `01．奖励配置表/index.ts`。TS `import "./01．奖励配置表"` 可以由 TSTL 处理成 `.index`；但手写 Lua/TS `require("系统...01．奖励配置表")` 不会自动解析目录，必须写 `require("系统...01．奖励配置表.index")` 或改用 ES import。
- 配置表目录拆分后，如果旧代码还依赖某个导出常量，例如 `瑟兰迪尔奖励池ID`，必须在 `01．奖励配置表/index.ts` 重新导出，不能只在单 Boss 配置文件里导出。
- Boss 死亡只在死亡奖励与清理配置里填 `首领奖励池ID` 触发选择界面；不要把首领奖励池数据复制进死亡结算配置。
- 每个 Boss 奖励池需要：
  - 稳定 `奖励池ID`
  - `标题`
  - `可选数量`
  - `选项`，每项含装备名、排序、图标、描述、特效
- 奖励装备发放走：
  - `08．奖励物品发放.ts`
- UI 打开入口是：
  - `打开首领奖励选择界面(奖励池ID, 玩家)`
- 若玩家暂不选择，系统会记录待选择；下一个奖励池出现时通过 `自动随机发放旧待选择首领奖励` 自动随机补发旧奖励。
- 如果选择“Boss 死亡时自动打开首领奖励”，可以把打开入口接入 Boss 死亡流程；如果该 Boss 不需要死亡奖励或奖励由剧情/挑战系统触发，不要为了首领奖励强行扩展死亡结算。
- 完整奖励池以 `01．奖励配置表/` 下的单 Boss 文件为准，`index.ts` 只负责聚合和查找。

## 装备物编

- Boss 掉落装备的物编统一在：
  - `objediting/Equipment/**`
- 任务物品统一放在 `objediting/Equipment/QuestItem/`，物编 `classification` 写 `Charged`（可充）；不要写 `Campaign` / `Miscellaneous`。
- 分类按现有目录放置，例如：
  - `Item`
  - `Clothes`
  - `MainWeapon`
  - `SubWeapon`
  - `Shoes`
  - `Pants`
  - `Helmet`
  - `Soul`
  - `TwoHandedWeapon`
- 新分类必须接入：
  - `objediting/Equipment/Equipment.lua`
- 物编创建优先用：
  - `objediting/Equipment/EquipmentItemTemplates.lua` 的 `createEquipmentItem`
- 图标路径写去掉 `imports\` 后的路径，例如 `Equipment\\Icon\\Item\\xxx.blp`。
- 装备图标尺寸按用途区分：会显示在首领奖励 UI 里的 Boss 奖励装备用 `100x100`，保证选择界面清晰；不会进入首领奖励 UI 的普通装备、任务物品、消耗品、材料等默认用 `80x80`，避免无意义增加地图体积。
- 图标默认使用原版 Warcraft 3 物品 BTN 风格，最终保留 `.blp`；PNG 只作中间产物，转换完成后删除。
- 主动使用装备的物编要设置：
  - `abilities`
  - `cooldownGroup`
  - `activelyUsed = true`

## 装备数据同步

- 每次制作一个装备，或变更装备属性/评分/等级/分类，都必须同步更新：
  - `TS/系统/02．物品系统/01．装备数据.ts`
- `01．装备数据.ts` 是装备属性、评分、分类等运行时数据源；物编只负责地图物品对象和 UI 描述。
- 首领奖励详情会复用装备数据查询，不要在首领奖励资料里复制一份装备属性。

## 装备技能

- 单件装备效果文件放在：
  - `TS/系统/02．物品系统/15．装备技能/00．物品`
- 一个物品/装备一个独立文件，入口接入对应 `index.ts` 或主动技能核心。
- 装备效果优先复用：
  - `TS/系统/03．技能系统/00．技能模板+函数/01．技能函数/20．物品辅助`
  - 攻击效果、光环、反击、控制、Buff、伤害/治疗事件等已有系统
- 持有型效果优先用获取/丢弃监听注册与注销，不要每 tick 扫玩家背包。

## 主动物品技能登记顺序

主动物品技能有专门的“通用物品技能槽位”登记，目的是避免玩家身上两个物品使用同一个基础命令 ID 导致冲突。

### 1. 物编通用槽位来源

- 通用 Channel 槽位由这里生成：
  - `objediting/Equipment/EquipmentAbilityTemplates.lua`
- 槽位按目标类型分组：
  - `IU00-IU14`：单位目标
  - `IP00-IP14`：点目标
  - `IN00-IN14`：无目标
- 这里同时维护每个槽位的基础命令 ID 顺序，例如 `thunderclap / warstomp / roar ...`。
- 分配装备主动技能时必须按同目标类型数组顺序取下一个空槽，不要凭记忆复用已有槽位。
- 例：第一个无目标主动装备用 `IN00`，第二个无目标主动装备用 `IN01`，第三个用 `IN02`；单位目标和点目标同理使用 `IUxx` / `IPxx`。
- 这样做的核心目的，是避免同一名英雄身上同时拥有两个使用相同基础命令 ID 的主动物品，导致使用命令冲突。

### 2. TS 主动装备名称

- 新增主动装备名：
  - `TS/系统/02．物品系统/15．装备技能/03．主动技能/00．公共/00．主动技能装备名.ts`
- 名称必须与 `01．装备数据.ts` 的 `name` 完全一致，包括颜色代码。

### 3. TS 主动物品 ID

- 新增物品 ID 常量：
  - `TS/系统/02．物品系统/15．装备技能/03．主动技能/00．公共/01．主动技能物品ID.ts`
- 这里通过装备名反查 raw id，不要手写 raw id 进入业务逻辑。

### 4. TS 通用物品技能槽位配置

- 必须登记：
  - `TS/系统/02．物品系统/15．装备技能/03．主动技能/00．公共/02．通用物品技能槽位配置.ts`
- 每项必须写：
  - `装备名称`
  - `物编ID`
  - `技能ID`
  - `目标类型`
  - `命令ID`
  - `冷却间隔组`
  - `冷却时间`
  - `魔法消耗`
  - `施法距离`
  - `说明`
- `技能ID`、`命令ID`、`冷却间隔组` 必须和 `objediting/Equipment/EquipmentAbilityTemplates.lua` 以及装备物编里的 `abilities/cooldownGroup` 对上。
- 当前项目暂不实现运行时命令 ID 冲突兜底；如果 `TS/系统/02．物品系统/15．装备技能/03．主动技能/00．公共/02．通用物品技能槽位配置.ts` 中登记的主动物品超过 15 个，且代码里仍没有拾取/装备时扫描同英雄主动装备并调用 `DzSetUnitAbilityOrderId` 的兜底逻辑，AI 必须先主动询问用户是否需要加兜底，不要直接继续增加主动装备配置。

### 5. 装备物编绑定

- 在对应 `objediting/Equipment/<分类>.lua` 里给装备写：
  - `abilities = "IN00"` 等
  - `cooldownGroup = "IN00"` 等
  - `activelyUsed = true`
- 同一单位可能同时拥有多个主动装备时，不要让两件装备共用同一个通用技能壳子和基础命令 ID。

### 5.1 命令 ID 冲突兜底

- 常规分配已经应当避免冲突；兜底只处理运行时意外冲突。
- 英雄获取/装备主动装备时，检测该英雄身上现有主动装备技能的基础命令 ID。
- 如果发现新获得物品的技能命令 ID 和同一英雄已有的物品主动技能冲突，优先为新物品换到同目标类型的空闲通用槽位。
- 若已经绑定到某个槽位但只需要临时改命令 ID，可用 `DzSetUnitAbilityOrderId` 修改该英雄身上的技能实例命令 ID。
- `DzSetUnitAbilityOrderId` 只影响当前单位上的技能实例，不会改全地图所有同 raw id 技能。
- 物品从英雄身上移除后，物品技能命令会恢复物编默认；因此只需要在获取/装备时检测与兜底，不需要在丢弃时手动复原命令 ID。
- 兜底修改后，必须同步更新该单位的“物品 -> 技能槽位/命令 ID”运行时映射，后续物品使用事件才能正确分发到对应装备效果。

### 6. 使用效果处理

- 物品使用触发配置和核心在：
  - `TS/系统/02．物品系统/15．装备技能/03．主动技能/03．物品使用触发`
- 新增真正效果处理时，通常还需要：
  - 在 `00．物品/<装备文件>.ts` 写处理函数
  - 在 `03．物品使用触发/02．物品使用主动技能核心.ts` import 并调用
- 如果是施法触发或治疗触发类主动效果，分别接入：
  - `02．施法触发`
  - `01．治疗触发`
