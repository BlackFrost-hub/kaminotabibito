---
description: UnitState 读写时区分 JASS 与 JAPI，避免动态最大值和扩展状态走错 API
globs: TS/**/*.ts
alwaysApply: false
---

# UnitState 的 JASS / JAPI 边界

`GetUnitState` / `SetUnitState` 在 `jass.common` 和 `jass.japi` 里都有同名入口，读写前必须按 GUI 的 `[JAPI]` 标记确认来源：

- 当前生命、当前魔法、攻击类型 `[1-6]`、护甲类型 `[0-7]` 没有 `[JAPI]` 标记，使用 JASS 读写。
- 骰子数量、骰子面数、基础伤害、附加伤害、攻击范围、攻击间隔、攻击速度、护甲、最大生命、最大魔法标记为 `[JAPI]`，必须使用 JAPI 读写。
- `UNIT_STATE_MAX_LIFE`、`UNIT_STATE_MAX_MANA` 的读取和写入都按 `[JAPI]` 处理。
- `ConvertUnitState(0x10-0x16)` 伤害/攻击范围、`0x20` 护甲、`0x25` 攻击间隔、`0x51` 攻击速度等扩展状态读写必须走 JAPI。
- `SetUnitAcquireRange` 是 JASS 主动索敌/主动攻击范围 API，不属于 `SetUnitState`。

推荐写法：先绑定本地别名，避免 TSTL 生成 `japi:GetUnitState(...)` / `japi:SetUnitState(...)` 这类错误调用形态。

```ts
const jass = require("jass.common") as any;
const japi = require("jass.japi") as any;

const GetUnitStateJass = jass.GetUnitState as (this: void, unit: any, state: number) => number;
const GetUnitStateJapi = japi.GetUnitState as (this: void, unit: any, state: number) => number;
const SetUnitStateJass = jass.SetUnitState as (this: void, unit: any, state: number, value: number) => void;
const SetUnitStateJapi = japi.SetUnitState as (this: void, unit: any, state: number, value: number) => void;

const maxLife = GetUnitStateJapi(unit, jass.UNIT_STATE_MAX_LIFE);
const life = GetUnitStateJass(unit, jass.UNIT_STATE_LIFE);
SetUnitStateJapi(unit, jass.UNIT_STATE_MAX_LIFE, maxLife + bonus);
SetUnitStateJass(unit, jass.UNIT_STATE_LIFE, newLife);
```

审计清单：

1. 搜索 `GetUnitState(`、`SetUnitState(`、`jass.GetUnitState(`、`jass.SetUnitState(`、`ConvertUnitState(`。
2. 看到 `ConvertUnitState(0x10-0x16/0x20/0x25/0x51)` 或最大生命/最大魔法读写，检查是否使用 JAPI。
3. 当前生命/当前魔法以及攻击类型/护甲类型的读写保持 JASS，避免把无 `[JAPI]` 标记的状态误改成 JAPI。
4. 改完必须构建，并抽查生成 Lua，确认是普通函数调用而不是冒号调用。
