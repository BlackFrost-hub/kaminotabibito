---
description: Warcraft3 + TypeScriptToLua + jass 绑定的常见坑与写法约束
alwaysApply: true
---

# War3 + TSTL + jass 常见坑（必读）

## 1) **禁止**对 jass API 使用可选链调用 `?.(...)`

TSTL 会把 `obj.fn?.(x)` 编译成“取出函数后再用 self 调用”的形态，容易把 **table** 当成第一个参数传给需要 **jhandle_t** 的函数，导致：

- `bad argument #1 ... (jhandle_t expected, got table)`

**✅ 正确写法**

- 先判断 `typeof (jass as any).Fn === "function"`，再直接 `(jass as any).Fn(arg)` 调用。

## 2) **禁止**把 `jass.xxx` 赋给局部变量再调用

例：`const f = (jass as any).GetOwningPlayer; f(unit)`  
TSTL 可能编译成 `f(nil, unit)` 或 `f(jass, unit)` 之类，造成参数错位。

**✅ 正确写法**

- 直接调用：`(jass as any).GetOwningPlayer(unit)`

## 3) Lua 真假值：`""` 在 Lua 里也为真

TS 里常见写法：`if (!msg) ...`  
编译到 Lua 后 `msg=""` 依然为真，逻辑会错。

**✅ 正确写法**

- 用 `msg === ""` / `msg !== ""` 判断空字符串。

## 4) 遍历 Lua table：避免 `Object.keys(table)`

Lua table 在 TSTL 环境中用 `for (const k in t)`（pairs）最稳。

**✅ 正确写法**

- `for (const k in itemsData) { ... }`

## 5) 随机数：避免 `math.random()` 无参浮点假设

不同环境下 `math.random()` 无参可能不返回 `[0,1)` 浮点。

**✅ 正确写法**

- `const idx = (math as any).random(1, list.length);`
- `return (list as any)[idx];`

## 6) 对“绑定签名不一致”的少数 API 才做兜底

若确实存在“需要 self/不需要 self”差异，兜底顺序建议：

1. 直接 `(jass as any).Fn(x, y)`
2. 再尝试 `fn(jass, x, y)`
3. 最后 `fn(undefined as any, x, y)`

优先保证第一种能工作（最不容易被 TSTL 改写成 self 调用）。

## 7) Lua 表 1-based：`math.random(1, n)` 后不要用 `arr[idx - 1]`

TS 里习惯写：`idx = math.random(1, arr.length)` 再 `arr[idx - 1]`（0-based）。TSTL 原样编译到 Lua 后，Lua 表是 **1-based**，`idx - 1` 在 idx=1 时为 0，`arr[0]` 为 **nil**，会导致漏项或报错。

**✅ 正确做法二选一**

- **写法 A（推荐）**：在 TS 里用 `(arr as any)[idx]`，并保证该处只给 Lua 用；在 `fix-lua-for-pack.js` 里对该文件做替换：`arrName[idx - 1]` → `arrName[idx]`（见规则 16）。
- **写法 B**：避免“随机 1..n 再取 [idx-1]”。改用 `const i = (math as any).random(1, arr.length); return (arr as any)[i];` 且**仅在**该表在 Lua 中确认为 1-based 时，在 fix 脚本里把 `[i-1]` 改为 `[i]`。

凡新增“用 random(1, len) 从 Lua 表里抽元素”的代码，要么走写法 A 并在 fix-lua-for-pack 加对应替换，要么用其他方式避免 0/1-based 混用。

