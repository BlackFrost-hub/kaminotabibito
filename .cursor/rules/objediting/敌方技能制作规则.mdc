---
description: ObjEditing 敌方/Boss 技能制作规则，约束 Boss 通魔技能模板、目标类型和命令 ID
globs: objediting/Boss/**/*.lua
alwaysApply: false
---

# 敌方技能制作规则

适用范围：`objediting/Boss/**/*.lua` 中制作 Boss / 敌方单位对象编辑技能。

## Boss 通魔技能目标类型

- 本图 Boss 技能只使用通魔技能。
- Boss 通魔技能只分两类：单位目标、无目标。
- 锁定单位技能和点地面技能，都用单位目标通魔。
- 对自身周围造成伤害、以自身为中心 AOE、对自己使用的技能，都用无目标通魔。
- 不新增点目标通魔作为 Boss 技能模板。

## 通用模板与具体 Boss 文件边界

- 通用 Boss 技能模板、通用系统技能，放在 `objediting/Boss/BossAbilityTemplates.lua`。
- 通用 Boss 单位模板，放在 `objediting/Boss/BossUnitTemplates.lua`。
- 具体 Boss 自己的技能，放在对应 Boss 文件，例如 `objediting/Boss/Thranduil.lua`。
- 具体 Boss 自己的单位定义，也放在对应 Boss 文件，并优先调用通用 Boss 单位模板。
- 不要把 `[系统]` 通用技能写进具体 Boss 文件。
- ObjEditing 阶段优先复用通用通魔技能模板 ID，例如 `AT00`、`AT01`、`AN00`。
- 具体 Boss 文件通常只把通用技能 ID 挂到单位技能列表，不为每个 Boss 新建专属通魔物编技能。
- 后续具体技能行为、数值、语义，可在运行时代码或配置层绑定到这些通用技能 ID。
- 单位目标通魔按槽位组复用：`ATxx`、`BTxx`、`CTxx` 分别是一组 400~2000 施法距离模板。
- 同一个 Boss 如果需要多个相同施法距离的单位目标技能，使用不同槽位组，例如 3 个 1000 距离技能用 `AT06,BT06,CT06`。
- 新建具体 Boss 结构时，默认同步创建该 Boss 的专属 Buff 表文件；即使暂时没有 Buff，也保留空表并接入 Buff 聚合入口，避免后续制作技能时漏建。

## Boss 英雄母单位

- 远程英雄 Boss 单位走 `Nbrn`。
- 近战英雄 Boss 单位走 `Npbm`。
- 具体 Boss 文件中优先调用模板快捷函数区分近战/远程，不要手写重复字段。

## imports 资源路径

- ObjEditing 中填写模型、图标、特效等导入资源路径时，默认去掉 `imports\` 前缀。
- 例如本地文件 `imports\Boss\Thranduil\Thranduil.blp`，物编路径写 `Boss\Thranduil\Thranduil.blp`。
- 例如本地文件 `imports\Boss\Thranduil\Archangel_IncarnationofRedemption.mdx`，物编路径写 `Boss\Thranduil\Archangel_IncarnationofRedemption.mdx`。
- `imports` 下资源目录和资源文件名优先使用英文，避免 Warcraft 资源路径不支持中文导致加载失败。
- 多系统复用资源放 `imports\Common\`。
- 通用特效先按父级分类，再按英文子类放入。
- 属性特效放 `imports\Common\Effect\Element\`：
  - `imports\Common\Effect\Element\Thunder`
  - `imports\Common\Effect\Element\Wind`
  - `imports\Common\Effect\Element\Ice`
  - `imports\Common\Effect\Element\Fire`
  - `imports\Common\Effect\Element\Light`
  - `imports\Common\Effect\Element\Dark`
  - `imports\Common\Effect\Element\Fantasy`
- 表现形态特效放 `imports\Common\Effect\Form\`：
  - `imports\Common\Effect\Form\MagicCircle`
  - `imports\Common\Effect\Form\Spread`
  - `imports\Common\Effect\Form\Line`
  - `imports\Common\Effect\Form\RiseFall`
  - `imports\Common\Effect\Form\Rotate`
  - `imports\Common\Effect\Form\Explosion`
- 每个特效分类目录下可建 `Texture` 子目录，放该分类特效专属贴图，例如 `imports\Common\Effect\Element\Light\Texture`。
- 用户给出模型、图标、特效、贴图路径时，默认主动规范为物编/TS 可用路径：去掉开头的 `imports\`，保留后续英文目录。
- 如果用户给出 `imports\Common\Effect\Element\Light\Texture\Mark_Checkable.blp`，物编/TS 中应写 `Common\Effect\Element\Light\Texture\Mark_Checkable.blp`。
- 添加或整理 `.mdx` / `.mdl` 模型资源时，应同时检查模型内部引用的贴图路径；如果自定义贴图散落在模型同级目录，默认移动到对应分类的 `Texture` 子目录，再把模型内部贴图引用同步改成实际导入路径，例如 `Common\Effect\Element\Light\Texture\Mark_Checkable.blp`。
- 模型贴图引用中确认是 Warcraft 自带路径的不要修改，例如 `Textures\...`、`ReplaceableTextures\...`、`UI\...`、`Units\...`、`Abilities\...`。
- 如果只是讨论技能需求，用户没有特别声明“不要改路径”，后续默认自动完成上述资源路径规范。
- 通用图标、模型、音效分别放：
  - `imports\Common\Icon`
  - `imports\Common\Model`
  - `imports\Common\Sound`
- 生成或整理图标资源时，默认尺寸约定：
  - Buff / 技能图标默认输出 `80x80`。
  - 单位头像、Boss 头像默认输出 `64x64`；如需保留高清源图，单独保存为源图，不作为默认导入头像尺寸。
- Buff / 技能图标、单位头像、Boss 头像默认带 Warcraft / BTN 风格边框；除非用户明确要求无边框，不要输出纯裸图。
- 图标可先生成 PNG 源图，再按需要转成 `.tga` 或 `.blp`；如果用户没有指定格式，优先保留可编辑 PNG 源图并同步准备最终引用路径。
- Boss 专属 Buff 图标优先放 `imports\BuffIcon\Boss\<Boss英文名>\`，TS/物编引用路径去掉 `imports\` 前缀，例如 `BuffIcon\Boss\Thranduil\yueguangguanzhu.tga`。

## ObjEditing Lua 命名

- ObjEditing Lua 文件中的函数名、变量名使用英文标识符，避免 VSCode / Lua 解析报错。
- 中文可以用于注释、技能名称、提示文本、编辑器说明。

## 通魔命令 ID

- 通魔技能真正用于施法命令、会互相冲突的是 `数据 - 基础命令ID`。
- ObjEditing setter 对应 `setBaseOrderID(...)`。
- 同一个单位可能同时拥有多个通魔技能时，必须给每个技能分配不同基础命令 ID。
- 不要只改 `文本 - 命令串 - 使用/打开` 等文本命令字段来规避冲突。

## 通用模板默认清理

通用通魔模板默认应尽量保持空白，避免复制后带出视觉或动画残留：

- `EffectArt` 为空。
- `TargetArt` 为空。
- `Targetattachcount` 为 `0`。
- `Targetattach` 为空。
- `CasterArt` 为空。
- `Casterattachcount` 为 `0`。
- `Casterattach` 为空。
- `Animnames` 为空。
- `DataA` / `DataC` 等模板数据默认用 `0`，除非具体技能明确需要。
