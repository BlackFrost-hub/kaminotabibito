# Global Agent Prompt for This Project

你在维护一个 Warcraft 3 + TypeScriptToLua + Lua + JASS/DzAPI 项目。

这份文件只保留总纲，不重复子规则正文。具体细则优先看：

- `README.md`
- `agent-shared/global-engine-rules.mdc`
- `agent-shared/tstl-hard-rules.mdc`
- `agent-shared/中文命名.mdc`
- `tooling/encoding-and-patch-safety.mdc`
- `dzapi/lua-gc-desync-heuristics.mdc`
- `dzapi/n-slot-ui-symmetric-execution.mdc`
- `equipment/物品系统与装备技能规则.md`
- `equipment/buff经验/00．Buff经验规则.mdc`

## 核心原则

- 先看生成 Lua 的调用形态，再相信 TS 源码
- 涉及 `jass` / `japi` / 事件中心 / timer / frame / bridge 的改动，优先检查 `self`、`nil`、参数错位
- 涉及 Lua/JASS/Dz 回调、timer、sync/async、本机玩家分支时，先判断上下文：同步 / 本机异步 / 桥接
- 运行时逻辑不要用 Lua/TS `Math.*`
- JASS / Dz 回调只用长寿命具名函数
- 不要在入口全局 monkey patch 基础 API
- `GetLocalPlayer()` 只做最终本地展示，不做共享逻辑计算
- 物品、装备、Buff、音效、STES、YDLocal 都按各自子规则处理

## 工作顺序

1. 先读本文件
2. 再读具体子系统规则
3. 先改 TS 源码，后 build
4. 涉及异步回调时，检查错误处理是否会让单客户端中断并造成状态分叉
5. 需要时检查生成 Lua 的调用形态
6. 再报告改了什么，验证了什么，还剩什么风险

## 记录习惯

- 调试输出优先用 `print`
- 规则有重复时，只保留一个 canonical source
- 需要补规则时，优先补到最相关的子目录，不要再复制一份全局说明
