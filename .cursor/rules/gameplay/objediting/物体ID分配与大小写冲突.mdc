---
description: 物体 ID 分配硬规则：魔兽引擎 ID 不区分大小写，新 ID 必须做大小写冲突校验，改名需全引用同步并验证构建产物
globs: objediting/**/*.lua, TS/系统/01．单位系统/08．单位配置表/**/*.ts, TS/系统/01．单位系统/00．单位初始化创建/**/*.ts
alwaysApply: false
---

# 物体 ID 分配与大小写冲突规则

适用范围：在 `objediting/` 新建单位/物品/技能/增益物编、给英雄或 Boss 分配 Rawcode、修改 `总单位配置表` 单位 ID 的所有任务。

## 硬规则

1. **魔兽引擎的物体 ID 不区分大小写**：`E00C` 与 `e00C` 在游戏内是同一个物体。物编文件、World Editor、ObjEditing.exe 都允许两者并存，但游戏加载时会按大小写不敏感合并，文件中靠后的条目覆盖靠前条目的同名字段（名称、模型、属性等）。
2. 分配任何新 ID 前，必须确认它与项目内所有已有 ID 的**大写形式**都不相同。只检查"完全相同的字符串"不够——`E00C` 与 `e00C` 字面不同但冲突。
3. 症状特征：编辑器里物编名字正常、构建产物 w3u 逐字节检查也正确，但游戏内单位名字/模型/属性被另一个不相干物体覆盖——优先排查大小写孪生 ID，不要往运行时脚本方向空转。

## 已知事故（2026-09-02）

英雄 20~25（爱蜜莉雅等）分配了 `E00C/E00G/E00H/E00I/E00J/E00K`，与旧地图里咲夜弹幕马甲 `e00C/e00G/e00H/e00I/e00J/e00K` 只差大小写。构建产物正确、编辑器显示正确，游戏内英雄名字全部被马甲名覆盖。最终把六个英雄改到空闲 ID `E0L0~E0L5` 解决。

## 新 ID 分配校验流程

ID 有三个来源，缺一不可：

1. `objediting/**/*.lua` 中 `createXxx('XXXX', ...)` 与 `DefinitionXxx:new('XXXX', ...)` 的定义；
2. `TS/系统/01．单位系统/08．单位配置表/04．总单位配置表.ts` 的键（该表由 `lua生成物遍表格.xlsm` 自动生成，含大量小写马甲 ID）；
3. `.build/objediting.source/war3map.w3u`（旧地图原始物编，二进制，需脚本提取条目 ID）。

三个来源的全部 ID 收集后按 `toUpperCase()` 分组；候选新 ID 的大写形式不得命中任何一组。可用空间参考：`E0L0~E0L9/E0LA~E0LD`、`E0M0~` 段在 2026-09-02 时点为空闲。

## 改 ID 的同步范围

改一个物体 ID 必须同步所有引用位置，漏一处就是运行期 nil 或创建错单位：

- `objediting/` 物编定义（如 `Unit/PlayerHero/*.lua`）；
- `TS/系统/01．单位系统/00．单位初始化创建/01．玩家英雄/00．玩家英雄配置.ts`（英雄注册表，选择流程按此表按 ID 直查）；
- `01．玩家英雄/02．英雄升级系统/01．升级配置表.ts`；
- `02．世界地图单位初始化/11．英雄选择场景初始化.ts`（展示批次列表）；
- 英雄自己的 `TS/系统/03．技能系统/05．单位技能/04．英雄技能/<英雄>/00．配置.ts` 及该英雄目录下硬编码 ID 的文件；
- `TS/系统/12．测试系统/` 中的创建测试。

同步完成后必须重建并验证：

1. 运行 ObjEditing（Pack Objects）重建 `.build/objediting/war3map.w3u`；
2. `npm run build` 重新生成 src；
3. 用脚本解析新 w3u，确认新 ID 条目的 `unam` 字段值正确，且其大小写孪生 ID 不存在。w3u 变体格式：条目 = parentId(4) + sourceId(4) + modCount(int32 LE)，mod = field(4) + type(int32 LE，3=字符串) + 值（字符串为 null 结尾、无长度前缀、无条目结束标记），直接按 null 截断读取 `unam` 即可。

## 当前仍存在的冲突对（截至 2026-09-02 未修）

以下对在构建产物中已被验证会互相覆盖（小写孪生在文件后部、字段获胜），进游戏会显示错名字并被部分覆盖属性，修复方式与英雄相同，改哪一侧需按内容归属决策：

| 大写侧（物编 + 配置表） | 小写侧（配置表 + 旧地图） |
|---|---|
| U007 安兹·乌尔·恭 | u007 深渊之影 |
| U008 雅儿贝德 | u008 熔岩药剂师 |
| U009 夏提雅·布拉德弗伦 | u009 双翼恶魔蝙蝠群 |
| N03J 熔岩破坏者·格鲁姆 | n03J 恶魔邪恶巫师（精英） |
| N03H 巴尔扎罗斯火焰锁链 | n03H 熔岩恶魔护卫 |
| N03I 不稳定地核 | n03I 恶魔巫师 |
| N03K 冰焰巫师·塞拉 | n03K 恶魔地狱犬骑士 |
| N03Z 地精商人111 | n03Z 赤尾 |
