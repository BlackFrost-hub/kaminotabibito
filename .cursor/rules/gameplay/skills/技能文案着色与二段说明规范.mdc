---
description: 技能文案着色与二段说明规范：结构色/伤害类型色框架、二段机制双覆盖写法；六英雄及后续所有英雄的技能 tooltip 必须遵守
globs: TS/系统/03．技能系统/05．单位技能/04．英雄技能/**/*.ts, objediting/HeroAbility/*.lua, TS/lib/扩展函数/封装函数/01．通用工具/06．技能文案着色.ts
alwaysApply: false
---

# 技能文案着色与二段说明规范

适用范围：英雄技能说明（TS `00．配置.ts` 的 `说明` 字段、物编壳 `tooltipExtended`、限时二段壳 `二段说明`）。禁止在技能文案里裸写颜色码之外的随意颜色，统一走着色工厂 `TS/lib/扩展函数/封装函数/01．通用工具/06．技能文案着色.ts`。

## 一、着色框架（唯一来源：着色工厂）

技能说明 = 结构色 + 伤害类型色，全部通过工厂函数生成，禁止手写字符串拼接颜色码（物编壳 lua 除外——它是静态串，从 TS 生成结果同步）。

### 结构色

| 元素 | 函数 | 颜色 |
|---|---|---|
| 段落标题（技能说明/伤害/二段/强化X） | `标题("伤害")` → 金黄「伤害：」 | `|cffffcc00` |
| 机制行标签（施法距离/冷却时间/魔法消耗/护盾/减速/持续时间/蓄力时间 等） | `机制("冷却时间", "5秒")` | 青绿 `|cff00ff99` |
| 关键数值（120%、5秒、900、350） | `数值("120%")` | 淡蓝 `|cff87ceeb` |
| 正文 | 不包色码 | 白 |

### 伤害类型色

伤害词必须按实际伤害类型着色（`伤害("冰魔法伤害")`）——以代码里的 `DAMAGE_TYPE_*` 常量为准，不按设定猜。纯魔法伤害（无属性、`DAMAGE_TYPE_MAGIC`/`DAMAGE_TYPE_UNIVERSAL` 类）也是合法类型，使用淡紫白色；属性伤害（冰/火/暗/光/雷等）必须写明属性词并按表上色。类型色表（登记在工厂 `伤害类型色表`，新增类型先加表再使用）：

| 伤害类型 | 颜色 | 色码 |
|---|---|---|
| 冰魔法伤害 | 冰蓝 | `|cff66ccff` |
| 水魔法伤害 | 水蓝 | `|cff3399ff` |
| 火魔法伤害 | 火红 | `|cffff5533` |
| 暗/暗影魔法伤害 | 暗紫 | `|cffaa66ff` |
| 光魔法伤害 | 金色 | `|cffffd700` |
| 雷/电魔法伤害 | 电紫 | `|cffb388ff` |
| 强化伤害（破防/符号强化） | 橙 | `|cffff9900` |
| 自然/风 | 浅绿 | `|cff99ff66` |
| 毒 | 绿 | `|cff66ff66` |
| 魔法（纯魔法/未指定属性，同样必须包色） | 淡紫白 | `|cffc0c0ff` |
| 物理/普通伤害 | 棕（暗金褐） | `|cffb8860b` |
| 精神伤害（=真实伤害，无视抗性护盾） | 白 | `|cffffffff` |

写法示例：`"命中造成攻击力" + 数值("120%") + "的" + 伤害("冰魔法伤害")`。

### 标题长度硬规则

标题（金黄段）在 tooltip 面板宽度内不得折行：标题词不超过 4 个汉字，按键说明写进正文（如 `标题("二段") + "再次按 W，…"`，不要写 `标题("二段（再次按 W）")`）。

## 二、二段机制说明（强制双覆盖）

凡技能含二段窗口（`创建限时二段技能壳`），两处说明缺一不可：

1. **一段 tooltip**：`标题("二段")` 段落放在「伤害」段之前，用工厂 `二段(操作, 触发效果, 不操作后果)` 生成。写明：再次按什么键、触发什么效果与数值、不做任何操作的后果（窗口消失/自然结束的伤害差异）。
2. **二段窗口内按钮**：`创建限时二段技能壳` 必传 `二段说明` 参数（机制组件会自动给二段技能设实例提示）。结构：`标题("技能说明")` 首行写效果概述 → `标题("伤害")` 或强化行写数值 → `机制色"不做任何操作"+复位` 行写窗口结束的后果。名称用「技能名·二段动作（热键）」格式（如「冰花绽放·引爆（W）」）。

二段说明的数值必须与实际运行逻辑一致；一段与二段两处对同一数值的描述不得矛盾。

## 三、同步要求

- TS `00．配置.ts` 的 `说明`（运行时实例 tooltip）与物编壳 `tooltipExtended` 必须同步修改，内容一致；TS 为唯一事实源，物编壳是复制。
- 新英雄技能说明从第一条就使用本框架；存量技能改文案时顺手迁移。
- 物编壳 lua 中的颜色码是最终串（静态），更新后必须重跑 Pack Objects。
