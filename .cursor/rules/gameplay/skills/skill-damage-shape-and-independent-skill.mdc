---
description: 通用技能制作的 AOE/单体/独立技能/位移技能判定规则
globs: TS/系统/03．技能系统/**/*.ts, TS/系统/04．伤害系统/**/*.ts, TS/系统/02．物品系统/15．装备技能/**/*.ts
alwaysApply: false
---

# 通用技能制作：伤害形态、独立技能与位移判定规则

## 制作前必判

制作或复查任意英雄主动、Boss 主动、单位技能、装备技能、机制技能时，先回答这四个问题，再写伤害或移动逻辑：

- 这次最终伤害语义是 `AOE` 还是 `单体`？只看最终打出来的伤害，不看选目标、表现、锁定或生成过程。
- 这次释放是否需要 `独立技能实例ID`？只有英雄主动、Boss 主动、单位技能需要“本次释放首次命中/对每个目标首次命中/本次释放累计命中”这类语义时才创建；装备技能这一项固定为否。
- 这次技能是否包含 `战斗自身位移`？只要施法者/技能拥有者在战斗技能中主动移动自己，就按位移技能处理。
- 这次逻辑是否只是移动别人、地图传送、剧情移动、机制摆位或测试摆位？这些不要误判为战斗自身位移。

## 核心口径

- `AOE` / `单体` 只看最终打出来的这次伤害语义，不看选目标过程、表现过程、锁定过程。
- 不要因为技能“先锁了一个目标”“从一个目标脚下生成”“追踪一个目标”就判成单体。
- 也不要因为技能“扫描过多个候选”“画了范围提示圈”“创建了多个机制单位”就判成 AOE。
- 判定问题只有一个：这次伤害最终是范围/路径/区域/多目标结算，还是只对一个目标结算。

## AOE 判定

满足任意一条，伤害形态应为 `AOE`：

- 最终伤害会对一个区域、路径、扇形、矩形、圆形、环形、全屏、波次区域内的多个单位结算。
- 最终伤害以锁定目标或落点为中心，对周围单位造成范围伤害。
- 弹幕、冲锋、线段、路径技能可以沿途命中多个单位，即使它有一个追踪目标或初始目标。
- 同一次技能效果按范围枚举目标后逐个造成伤害。
- 持续危险区、地面区域、路径残留、扩散波、分摊/扩散类机制，本质影响多个单位。

例子：锁定 A 玩家脚下爆炸一圈是 `AOE`；追踪 A 但弹幕沿途能打到 B/C 也是 `AOE`。

## 单体判定

满足以下口径，伤害形态应为 `单体`：

- 这次伤害最终只对一个明确目标结算。
- 技能可以先筛选多个候选，但每一次伤害调用只打其中一个目标。
- 可以有目标脚下提示圈、追踪弹道、召唤物围绕目标、距离校验、命中半径校验，只要最终只伤害这个目标，就是 `单体`。
- 一个技能循环多个目标并分别调用单体伤害时，每一次独立伤害仍可标 `单体`；除非这段逻辑语义上是范围/区域多目标结算。

例子：锁定 A 后只伤害 A 是 `单体`；画了 A 脚下预警圈但只保存并伤害 A 也是 `单体`。

## 独立技能判定

- `独立技能` 表示一次主动技能释放实例，不等于 AOE，也不等于单体。
- 英雄主动、Boss 主动、单位技能如果需要“本次释放第一次命中”“本次释放对每个目标首次命中”“本次释放累计命中次数”这类机制，应创建同一个 `技能实例ID` 并传给后续所有伤害。
- 同一个主动技能实例中的多段伤害、延迟伤害、区域 tick、弹幕多次命中，应该共享同一个 `技能实例ID`，直到技能结束或 TTL 清理。
- 装备技能全部不算独立技能，包括物品技能、装备主动、装备被动、装备持续伤害；它们统一属于装备技能伤害，不创建 `独立技能实例ID`。
- 全局首次命中：同一个 `技能实例ID` 第一次造成伤害。
- 对目标首次命中：同一个 `技能实例ID` 对某个目标第一次造成伤害；A 和 B 各自第一次被命中都应该能判定。

## 位移技能判定

- `战斗自身位移` 指战斗技能中，施法者/技能拥有者主动移动自己，例如冲锋、闪烁、跳跃、穿梭、贴近目标、脱离、短距离传送到战斗落点。
- 战斗自身位移必须走统一封装入口，例如 `开始冲锋`、`开始跳跃`、`开始闪烁`、`执行战斗自身位移到坐标`、`执行战斗自身传送到坐标`。不要在技能文件里直接散写 `SetUnitPosition`、`DzSetUnitXY`、`SetUnitX/Y` 来完成战斗自位移。
- 统一封装负责检查禁锢/束缚/纠缠根须等 `禁止位移` Buff，失败时跳过本次位移，并在玩家提示位置显示“当前无法使用位移技能”。
- `移动别人` 不算战斗自身位移：击退、牵引、吸附、推拉目标、拖拽敌人、机制强制移动其他单位，都不应被施法者身上的禁锢拦截。
- `非战斗传送` 不算战斗自身位移：地图传送、传送门、回城、剧情摆位、测试摆位、出生/复活/回场、机制单位移动、陷阱/弹幕/护盾条/进度条位置同步，不要接入位移封锁。
- 看到 `SetUnitPosition`、`DzSetUnitXY`、`SetUnitX/Y` 时，先判断语义：如果是战斗技能让自己位移，改走位移封装；如果是传送门、剧情、测试、机制坐标同步或移动别人，保持对应系统语义，不要强行套位移技能规则。

## 实现要求

- 需要形态识别的伤害必须走封装入口，并显式传形态：
  - `造成单体技能伤害(...)`
  - `造成AOE技能伤害(...)`
  - 或 `造成技能伤害({ 伤害形态: "单体" | "AOE", ... })`
- 原生弹幕系统不允许底层按 `碰撞消失`、`最大命中次数`、`轨迹类型` 等参数默认推断伤害形态；只要弹幕自身传 `伤害值` 并可能造成技能伤害，调用 `创建原生弹幕` 的地方必须显式传 `伤害形态`。漏传只能落为 `"未知"`，不能自动补成 `单体` 或 `AOE`。
- 不要用本次实际命中了几个单位倒推形态。一个 AOE 可能只命中 1 人，一个单体技能也可能在循环中打多个不同目标。
- 新增或复查技能时，先看最终伤害调用点，再看这次调用背后的结算语义。
