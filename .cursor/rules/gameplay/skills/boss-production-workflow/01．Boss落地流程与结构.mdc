---
description: Boss 制作总流程、目录结构、护卫目录约定
globs: TS/系统/03．技能系统/05．单位技能/03．Boss技能/**/*.ts, TS/系统/03．技能系统/05．单位技能/03．Boss技能/**/*.md
alwaysApply: false
---

# Boss 制作总流程、目录结构、护卫目录约定

## 总原则

- 先读具体 Boss 规划文档，例如 `TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/02．瑟兰迪尔/瑟兰迪尔.md`，再写实现。
- 读规划文档时先拆成“机制语义清单”，逐项标注：触发方式、目标选择、时间线、表现锚点、伤害/控制/Buff、清理、测试方式、奖励入口；实现完必须逐项回查，不要只按关键词做一版。
- 新逻辑优先复用项目已有函数，不要重造底层：
  - `TS/lib/扩展函数/**`
  - `TS/系统/03．技能系统/00．技能模板+函数/**`
  - 事件中心、中心计时器、伤害/治疗/Buff/表现系统已有入口
- JASS / Dz 回调遵守全局 TSTL 规则：模块顶层具名函数、`this: void`、build 后按需检查生成 Lua。
- 单文件接近 500 行时拆分；Boss 技能按“配置 + 单技能文件 + index”组织。

## AI 落地流程

后续制作 Boss 技能时按这个流程推进，除非用户明确要求只做其中一部分：

1. 先读 Boss `.md`、本规则、运行时规则和测试规则，列出技能清单和缺失信息。
2. 先落配置表：伤害、范围、时间、动作、特效、颜色、Buff、召唤物、台词都集中写进 `02．数值与表现配置.ts` 或 Boss 配置文件。
3. 再选触发方式：被动/阶段/周期机制走代码侧驱动；真正主动施法才接物编技能壳、AI 配置和施法事件。
4. 再实现技能：按“朝向 -> 动作/硬直/吟唱条 -> 前摇表现 -> 生效表现 -> 伤害/控制/Buff -> 清理”组织时间线。
5. 再接入系统：Boss index、Buff 表、AI 表、战斗启动桥接、死亡结算或首领奖励池，缺一项就不能说 Boss 已完整接入。
6. 再做测试命令：只给主动观察技能留指令，被动/开局/周期技能通过测试场景自然运行验证。
7. 最后验证产物：`npm run build`，必要时检查生成 Lua；进图测试前确认 `.build/war3map.lua` 和测试 `.w3x` 已刷新。

每个技能至少要有一张心智对照表：

- 语义：文档原句实际想表达什么。
- 代码入口：由 AI、事件、计时器、阶段还是测试命令触发。
- 时间线：读条、硬直、飞行、命中、爆炸、Buff、清理的具体时点。
- 表现锚点：Boss 身上、Boss 脚下、目标身上、玩家脚下、场地中心还是安全区。
- 真实效果：伤害、属性变化、控制、仇恨、召唤、掉落是否真的生效。
- 测试观察点：玩家应该在游戏里看见什么，什么时候看见。

## Boss 技能落地结构

- 具体 Boss 目录放在：
  - `TS/系统/03．技能系统/05．单位技能/03．Boss技能/<分类>/<编号>．<Boss名>/`
- 分类按剧情定位判断，不按强度、模型或技能风格判断：
  - `01．主线Boss`：章节主线必经 Boss。
  - `02．挑战与隐藏Boss`：可选挑战、隐藏支线与特殊守卫 Boss。
  - `03．异界Boss`：异界来源或异界挑战体系 Boss。
- 常规文件结构：
  - `00．配置.ts`：单位 ID、技能壳 ID、广播台词、数值常量。
  - `01．运行时.ts` / `01．被动效果.ts`：开战、阶段、周期驱动、公共上下文。
  - `02．数值与表现配置.ts`：技能伤害、范围、冷却、持续时间、特效、模型、动画、颜色等配置。
  - `04．xxx.ts` / `05．xxx.ts`：具体技能实现。
  - `index.ts`：只做 `export * from "./xxx"`。
  - `<Boss名>.md`：设计资料、阶段、技能、掉落规划。
- Boss 台词文本可以集中放在 `00．配置.ts`，但播放必须走广播提示消息系统，不要在 Boss 技能里散写普通消息、电影对白或临时 UI 文本。
- 新 Boss 目录必须先接入所属分类的 `index.ts`；Boss 根 `index.ts` 只汇总 `00．公共` 和三个分类，不直接逐个导出 Boss。
- 新 Boss 默认同步创建 Boss 专属 Buff 表，见 [Boss 运行时与 Buff 表规则](../boss-skill-runtime.mdc)。
- 新建 Boss 物编单位后，必须同步登记单位配置表，不能只写 ObjEditing 或 Boss 技能目录：
  - 常规 Boss：加入 `TS/系统/01．单位系统/08．单位配置表/02．Boss配置表.ts`
  - 异界 / 隐藏等使用独立分类的 Boss：按语义加入 `03．异界Boss配置表.ts` 或对应分类表
  - 所有新单位最终必须接入 `TS/系统/01．单位系统/08．单位配置表/04．总单位配置表.ts`
  - 若缺少单位配置表登记，不能视为 Boss 已完整落地。

### Boss 护卫技能目录

- 如果 Boss 有独立护卫、分身、核心、召唤出来长期存在并拥有自己技能组的单位，不要把这些单位的技能长期堆在 Boss 根目录的单个 `护卫Axxx.ts` / `护卫Bxxx.ts` 文件里。
- 护卫应在 Boss 目录下按护卫建立子文件夹，序号与 Boss 根目录文件序号互相独立，例如：
  - `01．护卫A格鲁姆/index.ts`
  - `01．护卫A格鲁姆/01．熔岩重锤.ts`
  - `01．护卫A格鲁姆/02．熔岩火径.ts`
  - `01．护卫A格鲁姆/03．技能驱动.ts`
  - `02．护卫B塞拉/index.ts`
  - `02．护卫B塞拉/01．冰焰双星.ts`
  - `02．护卫B塞拉/02．绝对零度领域.ts`
  - `02．护卫B塞拉/03．元素转换.ts`
  - `02．护卫B塞拉/04．技能驱动.ts`
- 护卫目录的 `index.ts` 只负责导出本护卫技能入口和必要公共能力；单个技能文件只写该技能的时间线、表现、真实效果和清理；公共目标选择、形态状态、伤害修正、冷却表等可放在本护卫目录的 `00．公共.ts` 或 `00．状态.ts`。
- Boss 根目录只保留 Boss 本体技能、全局阶段机制、护卫生成/死亡/归属机制和公共配置；根目录入口通过 `./01．护卫Axxx/index`、`./02．护卫Bxxx/index` 接入护卫，不直接引用护卫内部单技能文件。
- 如果护卫只有一个非常小的被动效果，可以暂时放在 `index.ts` 内；一旦护卫拥有 2 个以上主动/周期技能，必须拆成“护卫目录 + 单技能文件 + 驱动入口”，避免后续继续扩大成单文件。
