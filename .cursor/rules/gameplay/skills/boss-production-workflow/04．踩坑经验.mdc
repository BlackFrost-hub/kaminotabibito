---
description: Boss 技能制作累计踩坑经验，覆盖表现、场地、召唤物、Buff、测试、构建等
globs: TS/系统/03．技能系统/05．单位技能/03．Boss技能/**/*.ts, TS/系统/12．测试系统/**/*.ts, TS/系统/03．技能系统/00．技能模板+函数/**/*.ts
alwaysApply: false
---

# Boss 技能制作踩坑经验

## 使用方式

本文是 Boss 制作、表现、奖励、装备与测试联动的长期经验正文。先按问题定位章节，不要求每次完整通读；新增经验放进对应主题，不继续堆到文件末尾的杂项区。

| 问题 | 章节 |
|------|------|
| 规划、时间线、动作、飞行和命中表现 | §1-§3 |
| 特效、场地、目标选择、召唤物 | §4-§7 |
| Buff、控制和技能测试 | §8-§9 |
| 奖励 UI、贴图和主动装备 | §10-§12 |
| 构建、生成 Lua、测试图和复杂场地 | §13-§14 |

### 1. 规划文档必须逐条落地

- 先逐条读 Boss `.md` 规划，不要只抓关键词实现。规划里的“施法动作、硬直时间、飞行/投掷、法阵位置、持续时间、伤害、Buff、掉落、测试方式”都要逐条对应到代码或配置。
- 如果文档写“投掷、飞行、弹幕、飞过来、链条、箭矢”等字样，不能用“原地创建特效 + 延迟计时器命中”冒充飞行。
- 如果文档写“施法硬直”，必须实际调用项目硬直/暂停封装，例如 `开始硬直` / `GS_Suspend`，不能只写动画或只写延迟。
- 如果文档写“施法动作：动画编号 N / 速率 X / N 秒后停顿”，必须实际播放对应动画、设置时间流速，并处理原生施法事件可能抢回动画的问题。必要时短延迟重播一次动作。
- 如果规划中伤害、特效、范围故意没有写死，实现时必须先补进 `02．数值与表现配置.ts`，不要散写到函数里。

### 2. 技能表现必须和机制同步

- 视觉和结算不能割裂。玩家看到飞行物命中，才应该触发命中结算；玩家没看到法阵/飞行/蓄力时，不能只在延迟后突然吃控制或伤害。
- 点名、投掷、追踪弹、箭矢、锁链等需要真实运动的技能，优先使用 `TS原生弹幕` 或已有弹幕模板：
  - `系统.03．技能系统.00．技能模板+函数.01．技能函数.01．弹幕.01．TS原生弹幕`
  - 追踪目标优先用 `创建追踪插值轨迹`
  - 命中结算放在 `on命中` / `on命中单位` / 必要的 `on到达目标点` 兜底里
- 如果规划文档明确写“往目标方向射出”“不跟踪”“直线弹幕”，只能在发射瞬间锁定方向，不能使用 `指定目标`、`创建追踪插值轨迹` 或任何持续修正目标位置的轨迹。
- 不要把“飞行特效”挂在 caster 武器点一段时间，然后用距离计算延迟命中；这只是原地特效，不是飞行。
- 如果弹幕只允许命中指定目标，要使用 `目标筛选` 比较目标句柄，避免误中旁边单位。
- 弹幕的速度、命中半径、最大距离、生命周期、模型、飞行高度都要放进配置表。
- 对“投掷后命中”类技能，旧的 `SquareRoot(distance) / speed` 延迟命中只能作为极特殊兜底，不应作为主机制。
- 大招、蓄力、引导、终末审判这类技能必须有完整表现链路：Boss 施法动作、硬直、蓄力条、Boss 身上的蓄力法阵或蓄力特效、玩家可反应的提示圈、蓄力完成冲击、爆炸/命中表现、伤害或控制结算。
- Boss 技能的蓄力条/吟唱条统一走表现系统 `TS/系统/09．表现系统/08．吟唱条`，不要再临时使用单位头顶进度条替代玩家视野里的读条提示。
- 吟唱条按四类通道区分：普通 Boss 小技能使用 `常规技能` 或 `显示常规技能吟唱条`；终末审判、长蓄力爆发等大招使用 `大招` 或 `显示大招吟唱条`；审判之环这类整场常驻轮转机制使用 `场地常驻AOE` 或 `显示场地常驻AOE吟唱条`；临时但极重的秒杀/灭团惩罚倒计时使用 `致命惩罚` 或 `显示致命惩罚吟唱条`，例如“请在 3 分钟内击杀，秒杀惩罚倒计时”。旧 `场地AOE` / `显示场地AOE吟唱条` 只作为大招通道兼容别名，不要再给常驻机制使用。
- `常规技能`、`大招`、`场地常驻AOE`、`致命惩罚` 四个吟唱条通道允许同时存在；实现或调整 UI 时必须保持 Y 坐标分离，不能重叠或部分重叠，也不能让一个通道覆盖另一个通道。
- 同一通道内部是单槽覆盖语义：新读条会顶掉同通道旧读条。普通 Boss 主动小技能如果都使用 `常规技能` 通道，必须由施法硬直、AI 冷却或技能调度互斥保护，不能同时启动两个常规技能读条；场地周期技能需要长期倒计时的，优先放 `场地常驻AOE`，不要抢常规技能槽。
- 吟唱条、硬直、动作、飞行物和实际效果必须共用同一条时间线。读条没有结束时，不能提前发射飞行特效、提前命中、提前造成控制或伤害；飞行物类技能应在吟唱完成回调里再次面向目标后再发射。
- 常见“一段施法动作 + 一段硬直/吟唱条 + 一个生效点”的 Boss 技能，优先使用 `02．通用函数/13．施法时间线.ts` 的基础施法时间线封装；它负责台词、朝向、硬直、动作、吟唱条、生效回调和恢复动作。复杂多段技能不要硬套这个封装，可以只复用其中的目标选择、特效、吟唱条或硬直工具。
- 有明确目标、目标点或飞行方向的技能，施法动作开始前至少要让施法者面向目标；如果目标会移动，实际发射/结算前还要重新朝向一次，避免背对目标施法。
- 文档写“蓄力法阵”“气势扩散冲击”“高举长剑蓄力”等以 Boss 为主体的表现时，默认锚点是 Boss 自身或 Boss 脚下；只有明确写“玩家脚下”“目标身上”“场地中央”时，才把对应特效放到玩家、目标或场地中心。
- 如果某个爆炸模型自身有出生延迟、动画前摇或肉眼可见慢半拍，必须用配置提前创建特效，或把伤害结算延后到玩家实际看到爆炸的时刻；不能让伤害先出、爆炸 1 秒多以后才出现。
- 文档写“每 tick 特效”“每 0.26 秒播放动作”时，必须实现周期 tick，不要只创建一次特效或只播放一次动作。

### 3. 施法动作和硬直

- `SetUnitAnimationByIndex` 只代表播放动作，不等于硬直；硬直必须走控制系统。
- 原生技能生效事件前后可能覆盖单位动作。Boss 技能如果反馈“没看到施法动作”，优先检查：
  - 是否调用了 `开始硬直`
  - 是否设置了正确动画编号
  - 是否设置了正确时间流速
  - 是否被原生施法/命令覆盖，需要短延迟重播动作
  - 是否在硬直结束后恢复 `SetUnitTimeScale(unit, 1)` 和待机/0 动画
- 不要擅自改规划里的动画速率。例如文档写“默认速率”，就不要改成 `1.5` 或 `2`。
- 被动、开局自动、阶段自动触发的技能，不要为了测试硬塞手动指令；测试命令只保留需要玩家主动观察的技能。

### 4. 特效创建、缩放、绑定和销毁

- Dz 绑定特效要注意顺序：先创建特效，再设置大小，再 `DzBindEffect`。绑定后再 `EXSetEffectSize` 可能无效或表现不稳定。
- Dz 绑定特效销毁时，优先走封装：解绑、移动到安全坐标、缩放到 0、再销毁，避免残留。
- 如果一个绑定特效会长期存在，配置变化或测试热刷新时不能只判断“已有就 return”；已有特效也要能同步配置，或检测变化后销毁重建。
- 光环类特效如果绑定在 Boss 自身，不能每 tick 创建一个新特效；应创建一次，死亡/清理时销毁。
- 光环表现要分清“挂在 Boss 自己身上”还是“挂在敌人身上”。秩序领域是 Boss 自身光环，不是敌人身上的特效。
- 常驻光环/法阵不要直接使用一次性特效模型；落地前要检查 MDX/MDL 动画序列，`Stand` 只有十几毫秒的模型会看起来像“刚出现就消失”。优先选有较长/循环 `Stand` 的模型，例如 `Common\\Effect\\Element\\Light\\protectionaura.mdx` 这类常驻光环模型。
- 如果某个法阵模型只有一段 3-5 秒的一次性动画，但机制需要 10 秒以上持续可见，优先使用 `lib.扩展函数.封装函数.01．通用工具.03．特效` 的 `创建循环点特效` 按间隔重建；每次重建都要重新应用缩放、顶点颜色、动画速度和高度，不要只在第一次创建时设置。
- 一次性点特效、单位脚下点特效、持续法阵优先使用 `lib.扩展函数.封装函数.01．通用工具.03．特效` 的 `创建点特效` / `创建单位脚下点特效` / `创建持续点法阵`，不要在具体 Boss 技能里反复散写 `AddSpecialEffect + EXSetEffectSize + EXSetEffectZ + 定时销毁`。
- 本项目目标魔兽版本是 1.27 / 1.27e，默认没有任何 `Blz*` 原生函数；不要写 `jass.Blz...`，也不要用 `typeof Blz... === "function"` 当兜底来包装不存在的接口。需要平台扩展能力时，必须先搜索项目里已经存在且可用的 Dz/JAPI/EX 封装或真实导出，再使用。特效矩阵旋转已在 `.build/war3map.j` 确认存在 `EXEffectMatRotateX/Y/Z`，业务代码优先调用 `lib.扩展函数.封装函数.01．通用工具.03．特效` 的 `设置特效XYZ轴旋转`，不要在具体 Boss 技能里散写 `EXEffectMatRotateX/Y/Z`。Z 轴用于平面朝向，X/Y 可用于俯仰/横滚倾斜；当前咆哮波模型已验证 `X轴角度=40` 表现为特效前端下翘。高度用 `EXSetEffectZ`，缩放用 `EXSetEffectSize`。不同模型本地轴可能不同，X/Y 倾斜必须做成配置项并进图验证，不要硬编码到技能逻辑里。
- 锁链、牵引、导管连线、能量连接、束缚线、两单位之间的持续连接默认使用闪电效果，不要用普通模型特效硬拉长度/角度/缩放。优先使用 `01．技能函数/10．跳链/单位绑定闪电.ts` 的 `创建单位绑定闪电` 或牵引/独占连接底层；闪电 code 优先从 `02．通用函数/17．闪电效果代码.ts` 的 `闪电效果代码`/推荐常量读取，不要散写字符串。
- 如果连接线需要表现为锁链，优先选 `LEAS`、`ROP`、`蓝色链条` 等闪电效果；如果表现为稳定导管/能量束，优先选 `蓝色细束(BLSB)`、`蓝色光束(BLBM)`、`蓝色粒子束(BLPA)`。只有无法用闪电效果表达的粗大实体墙/地面路径，才考虑模型特效。
- 模型查看器能显示不代表游戏内导入链完整。遇到“查看器正常、游戏内报错/不显示”的 MDX，必须先用二进制字符串检查内部贴图、`PREM` 粒子模型和子模型路径；如果内部写死 `war3mapImported\\xxx.mdx`，就要补对应导入路径或重修模型内部路径，不能只检查外层 `AddSpecialEffect` 的路径。
- 点特效、场地法阵、玩家脚下法阵要区分坐标来源；不要把所有法阵都默认放 Boss 脚下。

### 5. 场地、Boss 战矩形和测试回退

- 需要“场地中央”的技能，正式 Boss 战优先读取 `Boss战.地点` 矩形中心；没有 Boss 战矩形的测试环境再回退到 Boss 坐标。
- 终末审判这类全场机制，要同时检查：
  - Boss 蓄力表现
  - Boss 身上的蓄力条、蓄力法阵和蓄力完成冲击
  - 场地中心法阵表现
  - 玩家脚下法阵表现
  - 安全区判定位置
  - 爆炸表现和伤害时机
- 安全区机制不要随便因为表现中心变化而改语义。若文档写“安全区是 Boss 脚下”，伤害判定仍按 Boss 位置。

### 6. 仇恨、点名和目标选择

- 技能侧目标选择应优先复用仇恨系统或其技能目标选择封装，不要在 Boss 技能里复制私有目标选择。
- 常见 Boss 技能目标选择优先使用 `系统.01．单位系统.06．仇恨系统.05．技能目标选择`，包括最高仇恨、应攻击目标、敌对英雄列表、随机敌对英雄、范围内敌对英雄、最近/最远/带权重目标等；缺少策略时优先扩展这个公共文件。
- “执法印记”这类强点名效果不能和仇恨系统互相打架；若文档写“无脑攻击标记单位”，AI/攻击目标选择必须明确优先标记目标。
- 增伤、受击修正、Buff 描述必须同步。不能只加 Buff 图标或特效，忘了实际伤害修正。

### 7. 召唤物和物编壳子

- 通用召唤物优先使用 `创建召唤物` 封装，并使用 `objediting/units.lua` 中的通用召唤物物编壳子。
- 召唤物不是默认飞行单位。本项目不允许单位越过地形；需要浮空只设置飞行高度，不要给飞行移动类型。
- 远程召唤物除模型外，还需要明确：
  - 攻击范围 / 主动攻击范围
  - 普攻弹道模型、弧度、速度
  - 攻击力、攻击间隔
  - 护甲、生命、持续时间
- 如果召唤物语义是“站桩用弹幕系统模拟普攻”，不要让它走原生攻击；应清掉原生攻击/弹道/索敌并固定站桩，用公共封装按“攻击动作 → 出手点 → TS 原生弹幕 → 命中结算”驱动，同时在起手前和出手前检查是否可以攻击。死亡、暂停、眩晕、冰冻、硬直、施法硬直、睡眠、变形、缴械、飓风等控制状态期间不能起手，也不能继续完成已排队出手。
- 站桩弹幕召唤物优先使用 `01．技能函数/25．站桩弹幕射击单位`，不要在具体 Boss 技能里再写一套固定站桩、攻击动作、出手延迟、弹幕发射和控制状态检查。
- 站桩直线弹幕的目标选择应以射手自身为中心实时找附近敌人；起手时选目标用于转身/播攻击动作，出手帧必须重新找一次附近敌人并锁定当前方向，避免朝旧目标位置或空气射击。
- 站桩/禁止移动语义不要使用 `SetUnitMoveSpeed(unit, 0)`；这会污染移速系统、属性读取和后续恢复逻辑。应使用 `SetUnitPropWindow(unit, 0)`，项目内优先调用 `lib.扩展函数.Star扩展函数.Star扩展库.06A．X库函数安全版` 的 `X_FixUnitStandingSafe` / `X_SetUnitMovableSafe`。
- 召唤物/单位如果需要“每个目标只能被一个单位连接、无目标时重试、连接后周期距离惩罚/伤害”，优先使用 `01．技能函数/10．跳链/独占单位连接.ts`，不要在 Boss 技能里复制连接、重试、过期清理和距离判定。
- JAPI 扩展单位状态读写必须成对使用 JAPI，例如 `ConvertUnitState(0x15)` 攻击力读取应走 `japi.GetUnitState`，不能用 `jass.GetUnitState` 读取扩展状态。
- 区分 JASS 和 JAPI：没有标注 JAPI 的按 JASS；标注 JAPI 的按 JAPI。`SetUnitState` 有些状态是 JASS 常规用法，有些扩展状态依赖 JAPI/平台，最容易混淆，必须查项目先例。
- ObjEditing API 不能凭猜测传数字枚举；例如攻击类型要用 ObjEditing 期望的枚举值，不要直接传 `1`。
- 通用召唤物壳子的默认护甲类型用普通，默认飞行高度可为 10，但移动类型不要飞行。

### 8. Buff 和控制

- Boss 专属 Buff 统一进 Boss 专属 Buff 表；说明、图标、效果值要和真实逻辑同步。
- 控制效果需要区分原生控制、扩展控制、Buff 展示和硬直暂停。不要只上 Buff 而没有控制，也不要只控制而没有 UI/Buff 描述。
- DOT / Tick 伤害必须和控制持续时间对齐，并在目标/来源无效时停止。
- 可以被打断、累计伤害、掉落碎片的机制，要从一开始设计状态表和清理路径，不要只先做“定身 + DOT”。

### 9. 技能测试规则

- 测试场景默认使用地图上预设的玩家 1 大法师 `gg_unit_Hamg_0002`，不要猜“大法师可能不存在”。
- `thtest` 初始化时，玩家英雄和测试步兵要设置最大生命值 `999999`，并立刻把当前生命值也回满到 `999999`。
- 测试靶优先用普通步兵 `hfoo`，除非机制需要英雄、建筑、召唤物或特殊单位。
- 测试命令不要给纯被动/开局自动/周期自动机制额外开手动指令；这类机制应在测试场景自然运行。
- 手动测试命令只保留需要主动观察的技能：投掷、召唤、主动法阵、主动点名等。删除无意义指令后要重新压紧编号。
- 测试一个技能时，要同时确认：
  - 是否有施法动作
  - 是否有硬直
  - 是否有可见前摇/飞行/法阵
  - 命中/爆炸/控制是否发生在视觉抵达后
  - Buff 描述和实际效果一致
  - 伤害、增伤、减速、攻速等数值实际生效

### 10. 首领奖励选择 UI

- 首领奖励系统的配置只保留一份完整数据源，不要在奖励配置、奖励详情资料、装备数据查询之间复制同一份装备属性。
- 装备属性、百分比、评分、分类等优先复用 `TS/lib/扩展函数/物品相关函数/装备数据查询.ts` 和 `TS/系统/02．物品系统/01．装备数据.ts`，不要在首领奖励详情里手写一份。
- 首领奖励选择系统和 Boss 死亡结算互相独立；死亡结算只是可能触发奖励选择，不是奖励池的数据源。
- 奖励 UI 支持 N 槽位，联机下除本地显示/隐藏外，逻辑、选择、发放、随机补发都不要放进 `GetLocalPlayer`。
- UI 点击如果会改变共享状态、发物品、起 timer、改奖励池，必须按联机同步规则处理。普通 UI 显示隐藏可以本地分支，发奖逻辑不可以本地分支。
- 之前“暂不选择”按钮命中区问题说明：不要用一堆小按钮拼命中区域；应创建与美术按钮等量大小的单个真实按钮帧，并确认点击区域和绿色调试区域一致。
- UI 常量改了没效果时，要先确认真实命中帧是哪一个、是否被模板/子帧/旧句柄覆盖、是否测试图已重新打包，不要只反复改同一组无效常量。
- 调试 UI 命中区时可以临时改成明显颜色和打印 frame id、坐标、大小；问题修复后必须删除调试输出并恢复透明。
- 选中奖励的边框和勾选图标要盖在装备图标之上，不能被装备图标遮住；勾选图标应放右下角且足够明显。
- 装备图标应在内嵌框中严格居中，注意上下/左右间隙，不要肉眼明显偏移。
- 属性/特效标题、分割线和正文不要互相冲突；长属性和长特效必须用模拟极端文本测试，不要只看 2-3 条短文本。
- 一个装备可能有 10 条以上属性，UI 要么单列支持滚动，要么明确每列容量；不能把本来能放一列的属性随意拆散到多列导致冲突。
- 特效文本可能很长，要测试换行、边界、左偏移和行容量；不要为了“看起来整齐”把一行文字容量压得过小。

### 11. UI 资源和贴图

- 如果属性/特效标题需要好看字体，可以做成透明贴图资源，不要在代码里硬画所有字。
- 透明贴图即使大部分透明，只要尺寸很大，TGA 仍可能很大；需要控制尺寸、格式和压缩方式，避免小 UI 资源变成数 MB。
- 修改 UI 叠加图时，要检查装备评分、属性图标、特效图标、线条是否互相重叠；不要只看单个元素。
- 资源位置调整要按用户指向的轴向理解，例如“避开左侧竖线”是横向右移，不是整体下移。

### 12. 主动物品技能和命令 ID

- 主动物品技能使用通用模板槽位，必须按目标类型顺序登记，避免同一英雄持有两个相同基础命令 ID 的物品。
- 当前常规分配应避免冲突；兜底逻辑如果未实装，登记超过 15 个同类通用槽位前必须先问用户是否加运行时兜底。
- 运行时兜底思路：英雄获取装备时检查已有主动装备命令 ID；冲突时用同目标类型空闲槽位或 `DzSetUnitAbilityOrderId` 临时调整当前单位上的技能实例。
- `DzSetUnitAbilityOrderId` 只改当前单位技能实例；物品从英雄身上移除后会恢复物编默认命令，因此主要在获取/装备时检测。
- 点目标物品如果需要施法区域提示，可以使用目标选取图像并在获取物品时动态修改 `DzSetUnitAbilityArea` / `DzSetUnitAbilityRange` 等平台 API，但要先查项目封装和生成 Lua 调用形态。

### 13. 构建、生成 Lua 和测试图

- 局部或完整 TS 构建通过都不等于测试地图已经更新；它们会更新相应的 `src/**/*.lua`，但不一定刷新 `.build/war3map.lua` 和测试 `.w3x`。
- 修改 TS 后必须：
  - 按 [TS 构建验证分流](../../../tooling/build/map-packaging-after-ts-build.mdc#ts-构建验证分流) 构建；普通业务 TS 只对本次修改文件运行 `build:files`
  - 对敏感回调/导出函数/JASS/JAPI 调用检查生成 Lua
  - 普通进图测试交给用户执行 Warcraft VSCode `Run Map`；只有用户明确要求手动打包或正在排查地图未更新时，才确认 `.build/war3map.lua` 并重新打包测试图
- 如果运行时报 `module 'xxx' not found`，不要只看 `src`，还要搜：
  - `src/**/*.lua`
  - `.build/war3map.lua`
  - `.build/_warcraft_vscode_test.w3x`
- 对目录入口文件，Lua require 必须写到 `.index`；`warcraft.json` 的 package path 当前只包含 `./?.lua` 和 `./?/init.lua`，不包含 `./?/index.lua`。
- 仅在用户明确要求手动打包或正在排查打包问题时，如果插件没有自动刷新测试图，可用 `MopaqPack-rs.exe generate -i .build/packlist.json -o .build/_warcraft_vscode_test.w3x` 重新生成测试地图；执行前要确认 `.build/war3map.lua` 已经是最新内容。
- 如果打包报 `ErrorCode(32)`，优先检查 `War3` / `WorldEditKKWE` 是否正在占用测试图。
- 生成 Lua 检查重点：
  - 不能出现多余 `nil/self`
  - 回调参数不能整体右移
  - JASS/JAPI 调用必须是局部 alias 后正常函数调用
  - 旧错误路径必须消失，例如假飞行的 `SquareRoot(distance) / speed` 延迟命中
- 不要因为终端显示中文乱码就整文件重写；先确认文件编码和生成 Lua。

### 14. 闪电宫格与网格场地

- 需要用横竖线表现九宫格、3×3、4×4、长条分区或类似场地网格时，优先使用 `TS/系统/03．技能系统/00．技能模板+函数/04．机制组件/02．战斗区域/05．闪电宫格区域.ts` 的 `创建闪电宫格区域`。不要在具体 Boss 技能里手写固定 3 条横线、4 条竖线和 9 个 Rect。
- 网格语义拆成两层：闪电线只负责玩家可见边界表现，Rect 格子负责逻辑判定。默认 3×3 时应创建 `行数 + 1` 条横线、`列数 + 1` 条竖线，并创建 `行数 * 列数` 个矩形格子。
- 闪电效果不要散写字符串；优先从 `02．通用函数/17．闪电效果代码.ts` 读取 `闪电效果代码` 或推荐常量。组件允许传中文枚举名，也允许传底层原始 code，例如 `DRAL`、`DRAM`、`MBUR`、`LEAS`、`BLSB`。
- Boss 死亡、测试结束、阶段重置或机制取消时，必须销毁闪电句柄并移除 Rect；优先接入机制清理篮子，不要只清理视觉线而遗漏矩形区域。
