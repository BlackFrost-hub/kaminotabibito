---
description: 玩家英雄制作规则：配置驱动、暂停与动作先后、表现时点、伤害时点和验收
globs: TS/系统/03．技能系统/05．单位技能/04．英雄技能/**/*.ts, TS/系统/01．单位系统/00．单位初始化创建/01．玩家英雄/**/*.ts
alwaysApply: false
---

# 玩家英雄制作规则

本规则适用于玩家英雄的被动、Q/W/E/R/D 技能、英雄升级技能和英雄技能公共封装。制作前先读：

1. `.cursor/rules/gameplay/skills/skill-damage-shape-and-independent-skill.mdc`
2. `.cursor/rules/gameplay/skills/boss-production-workflow/02．技能实现与表现规则.mdc`
3. 本规则

## 英雄物编与属性预算

- 玩家英雄物编统一使用可复用的英雄物编构造函数（当前为 `objediting/Unit/UnitTemplates.lua` 的 `createPlayerHeroUnit`），英雄文件只填写该英雄的差异配置；不要在每个英雄文件重复复制截图中的通用字段。
- 每个英雄必须显式填写 `attackMode`：`ranged` 或 `melee`。远程英雄必须配置有效的普攻弹道模型和大于 0 的弹道速度，并使用投射物武器；近战英雄必须使用即时武器，攻击距离按近战配置，弹道模型为空且弹道速度为 0。构造函数应在物编生成阶段直接拒绝不符合模式的配置。
- 初始力量、敏捷、智力之和必须 `>= 70`。
- 力量成长、敏捷成长、智力成长之和必须 `<= 7.0`。
- 主属性成长必须 `>= 3.5`；主属性必须明确登记为 `STR`、`AGI` 或 `INT`。
- 上述属性约束是制作规则和生成校验，不得通过运行时属性、隐藏加成或其他字段绕过。任何不满足条件的英雄都应在物编生成阶段失败并修正配置。

## 英雄技能物编与初始化流程

- 先在 `objediting/HeroAbility/` 建立可复用的玩家英雄通魔壳模板；模板只负责产生可监听的施法事件，不承载最终 TS 伤害、控制、联动或表现。除非英雄已经进入实际接入阶段，不要为规划中的英雄提前创建真实技能对象。
- Q/W/E/R 正式接入时才写入英雄单位的 `heroAbilities`，顺序必须是 Q、W、E、R；被动 D 如果只用于状态/被动逻辑，不得强行占用主动英雄技能槽。
- 玩家英雄通魔壳必须使用英雄技能模板，`setHeroAbility(true)`；Boss 通魔模板的 `setHeroAbility(false)` 不能直接复用到玩家英雄单位。被动 D 需要显示按钮时，另按已确认的截图模板建立独立的被动显示壳，不将其当作 QWER 主动技能。
- 每个技能入口必须同时登记 Rawcode、快捷键、按钮位置、目标类型、施法距离、基础冷却、名称、图标和说明。未确认的最终数值可以使用明确标注的壳占位值，但不得让壳配置冒充最终玩法数值。
- 在 `TS/系统/01．单位系统/00．单位初始化创建/01．玩家英雄/00．玩家英雄配置.ts` 同步登记英雄 Rawcode、`heroAbilList`、主体模型、属性、攻击模式和普攻弹道；该表必须与 ObjEditing 单位入口逐字段核对。
- 在 `TS/系统/01．单位系统/00．单位初始化创建/01．玩家英雄/04．英雄技能成长系统/00．配置表.ts` 登记已经确认的技能成长效果。没有确认成长数值时只登记空属性入口或暂不登记，禁止为了填表虚构属性增量。
- 运行时技能显示信息使用统一安全封装修改单位技能实例的名称、图标和说明；物编壳的默认文字只作为未初始化或非本地 UI 的回退值。动态显示封装只改变当前单位技能实例，不等于创建真实技能玩法。
- 接入顺序固定为：确认是否需要真实技能物编 -> 确认 Rawcode/技能槽位 -> 复用或扩展通魔/被动显示模板 -> 正式接入 ObjEditing 技能壳 -> 玩家英雄配置 -> 技能成长配置 -> TS 英雄技能配置与事件监听 -> 构建 -> 进图验收。仅做运行时动态名称、图标、说明时，不得借此提前创建真实 QWER 技能物编。

## 制作前确认

- 先读 `00．配置.ts`、技能目录 `index.ts`、对应公共封装和现有实现；确认英雄 Rawcode、技能 Rawcode、技能槽位和已有实现后再改代码。
- 英雄技能固定按配置顺序对应：`01` 被动，`02` Q，`03` W，`04` E，`05` R；需要 D 技能时按项目现有固定槽位配置接入。不要通过目录顺序、遍历顺序或运行时猜测快捷键。
- 技能动作、动作速度、音效、音效播放点、特效模型、挂点、缩放、颜色、持续时间和所有时点必须写在英雄配置或共享表现配置中。技能实现只读取配置并执行，不在技能函数里硬编码这些值。
- 技能配置必须明确：前摇、动作开始点、伤害点、控制点、命中特效点、持续特效开始/结束点、音效点、后摇和收尾点。没有资料时先标记待确认，不得填一个看起来合理的编号或时间冒充源行为。
- 当前生命值和当前魔法值使用项目 JASS 普通接口；最大生命值、攻击力等属性按项目约定使用已有 JAPI 封装。不要在英雄技能里直接混用裸 `jass`、`BJ`、`japi` 调用。
- 百分比魔耗统一由项目魔耗系统处理。英雄技能文件不得自行重复扣除百分比魔法值；技能配置中的百分比魔耗只作为数据来源，不能在技能逻辑中再写一套扣魔，避免双重扣除、时点不一致和本地异步修改真实资源。

## 强制时序：先暂停，再播放动作

英雄技能涉及施法者、被命中目标或其他需要表现硬直的单位时，严格执行以下顺序：

1. 校验单位、目标、技能状态和可释放条件。
2. 使用项目硬直暂停系统添加暂停来源。
3. 在暂停成功后，立即播放配置中的动作或动画。
4. 按配置时点播放音效、创建特效、结算控制和造成伤害。
5. 在动作/硬直结束或技能中断时，移除同一暂停来源、恢复动画速度和状态。

禁止以下顺序：

- 先播放动作，再暂停单位。
- 先造成伤害，再补暂停或补动作。
- 用 `TriggerSleepAction(0)`、零秒眩晕或零秒暂停模拟硬直。
- 直接调用 `PauseUnitBJ`、裸暂停状态或临时布尔值覆盖项目暂停系统。
- 把永久暂停当作动作播放的替代方案。

“先暂停”不是“暂停后等待一帧才动作”。暂停调用成功后，动作调用应在同一阶段紧接执行；后续等待使用项目计时器/动画等待/多阶段技能编排。暂停时长必须为明确的正数，零秒控制在本项目可能留下永久控制状态，禁止使用。

暂停来源必须可追踪、可清理，并且只能移除本技能添加的来源。单位原本已经暂停时，不得在技能结束时解除其他系统的暂停。

## 标准技能时间线

每个主动技能实现前先写出实际时间线，时间以 `t0 = SPELL_EFFECT` 或代码侧技能启动时刻为基准；不要把所有动作堆在一个回调里。

| 阶段 | 必须确认的内容 | 约束 |
|---|---|---|
| `t0` 校验 | 施法者、目标、目标点、方向、技能等级、独立技能实例 | 先保存本次释放需要的快照；不要让后续异步回调重新猜目标 |
| `t0` 暂停 | 施法者或被控单位的暂停来源 | 必须先添加暂停，成功后才能播动作 |
| `t0` 动作 | 配置动作、动作编号/名称、动作速度 | 动作播放必须紧跟暂停；不允许硬编码动作编号 |
| 前摇 | 预警圈、蓄力特效、起手音效、读条 | 预警范围和持续时间必须覆盖真实可规避窗口 |
| `t命中` | 命中特效、控制、伤害、命中音效 | 以配置时点为准，不能因为实现方便提前/延后 |
| 持续阶段 | 周期特效、周期伤害、持续控制 | 每个 tick 使用同一技能实例和配置间隔；不要重复创建本应持续的特效 |
| 后摇 | 收尾动作、尾音、残留特效 | 后摇期间仍需正确保持或恢复暂停、动画速度和朝向 |
| `t结束` | 移除暂停、恢复动作/速度、销毁特效、结束技能实例 | 所有正常结束、打断、死亡、目标失效路径都必须清理 |

表中的时点不是默认模板值。实际技能必须按技能配置填写；音效、特效、控制和伤害按照同一份时间线执行，不能因实现方便改变顺序。

### 蓄力/吟唱读条

- 后续制作或迁移的所有玩家英雄，只要技能存在蓄力、吟唱、持续引导、锁定等待或延迟生效窗口，就必须使用 `TS/系统/09．表现系统/15．世界坐标进度UI` 显示世界坐标蓄力条；这是强制验收项，不得省略。
- 世界坐标蓄力条必须绑定施法者实时跟随，并作为技能实例的一部分保存；统一通过 `06．施法·蓄力·充能` 的世界坐标进度 UI 参数接入，不得为单个英雄复制私有读条实现。
- 单位头顶模型进度条、动作、飘字、全局文字或局部玩家 UI 只能作为附加表现，不能替代世界坐标进度 UI；使用世界坐标进度 UI 时应明确是否关闭模板默认模型进度条，避免重复显示两套蓄力条。
- 施法者死亡、单位失效、技能被打断、重复释放、场景清理和正常结束时，读条必须与技能实例同时销毁；不能遗留在原坐标或只依赖临时隐藏。

## 动作、音效和特效

- 动作播放必须经过项目已有动画封装或动画等待/动画守护封装；需要限时动作时必须配置动作时长、动画速度和恢复动作，不能只调用一次 `SetUnitAnimation` 后不管。
- 动作开始前先确定单位朝向。目标单位技能要面向目标，目标点技能要面向目标点；真正发射、位移、命中前若方向会变化，按技能配置和当前目标状态再次校准。
- 音效播放点必须与技能配置和已有音效封装的时间线对齐。音效路径、音量、距离、是否 3D 和播放时长放进配置或已有音效封装，不在技能函数中散写。
- 施法特效、飞行特效、命中特效和持续特效分别记录创建点、挂点/坐标、朝向、缩放、生命周期和销毁点。不要因为特效创建成功就提前创建所有后续特效。
- 特效资源必须遵循单项筛选流程：先根据具体英雄、技能和阶段确认适配。若原始文件名是乱码、纯编号、代号或哈希式导出名，确认用途后先把原始名称、来源、哈希、贴图依赖和用途登记到 `特效模型命名与去重备忘录.md`，再按单项流程导入；未确认用途或仅凭文件名收集的候选不得登记。完成导入、路径、贴图和序列检查并正式纳入项目资源库后，才允许把这类原始名改成正常项目名，同时保留原始名和改名映射。语义清晰的模型不进入该乱码索引，只记录在对应英雄资源文件或视觉特效目录。
- 禁止把外部“投射物”或“特效库”目录整体扫描、批量迁入项目。普攻弹道和技能特效必须分别按英雄、技能、阶段建立导入项；没有明确归属的资源留在源目录。
- 同一模型已存在于 `imports` 时，先核对路径、字节数和哈希，禁止重复导入副本；只有确认是不同版本且用途不同，才建立版本记录。
- 特效锚点必须与真实语义一致：施法者特效挂施法者，目标特效挂目标，落点特效放落点，命中特效放实际命中位置；不能只因视觉方便全部挂在施法者身上。
- 模型或动作损坏时先更换配置资源或确认资源导入，不要用播放 `Death` 等无关动画掩盖动作缺失。目标单位不需要死亡表现时，不得主动播放 `Death`。
- 同一时点的提示圈只能有一个拥有者。底层区域/落点/路径系统已经创建提示圈时，英雄技能不得再手动画第二个同范围提示圈。

## 英雄技能设计规划规则

本节约束英雄规划文档和开始实现前的设计确认。规划不是技能已经落地的证明；数值、状态、表现和联动最终都必须在代码侧接入真实系统。

### 联动要求

- Q/W/E/R/D 不能设计成互相独立的按钮；每个技能必须先有不依赖联动的基础效果，再提供可验证的联动增强。
- 规划文档必须写技能联动矩阵，至少覆盖一个主动技能到主动技能、技能到普攻、技能到天赋或 D 的联动。
- 每条联动必须写清触发条件、读取对象、消耗次数或资源、持续时间、内部冷却、重复触发限制和清理路径。
- 联动必须进入真实的技能、伤害、Buff、冷却、控制或资源系统；不能只修改 Buff 名称、技能说明或特效来伪造联动已经生效。
- 联动失败时技能仍要执行基础效果。除非设计明确要求消耗失败，否则不能因目标失效、没有前置状态或窗口错过而让技能永久空转。
- 技能伤害联动必须沿用同一个技能实例和正确的伤害形态，不能因为追加特效或回响就偷偷创建无法追踪的第二套伤害流程。

### 操作感要求

- 规划文档必须列出玩家的主动决策点，例如方向瞄准、目标选择、二段窗口、蓄力时机、走位、普攻穿插和资源分配。
- 禁止整套英雄只有固定顺序连招。至少保留两种可行的技能衔接，或允许玩家根据目标位置、前置状态和资源数量改变下一步选择。
- 高频技能可以使用短冷却制造频繁输入，但必须有资源上限、目标限制、内部冷却或收益递减，防止无限循环。
- 二段或多段技能必须写明输入窗口、窗口超时、重复输入、打断、死亡、目标失效和状态清理处理。
- 失败时技能应保留基础收益；不要把完整技能的有效性全部押在一次精确联动上。
- 召唤或协战效果默认不增加独立宠物操作。只有当人物玩法确实需要单位控制时，才另行设计宠物指令、同步和生命周期。

### 操作复杂度分级

规划文档必须记录目标星级、星级来源和限制复杂度的边界：

| 星级 | 判定标准 |
| --- | --- |
| 1 星 | 单一效果、单一输入、几乎没有联动 |
| 2 星 | 一个短窗口，或一次简单的两技能联动 |
| 3 星 | 多个技能联动，同时需要走位、时机或目标选择 |
| 4 星 | 多资源、分支选择、二段/多段窗口和多目标决策并存 |
| 5 星 | 多阶段、多资源、复杂窗口，或强依赖高频操作、宠物/单位控制 |

星级不是技能数量评分。若目标为三星半，应限制资源层数、读取次数、窗口数量和召唤物控制，避免设计在实现中自然膨胀到五星。

### 表现记录要求

规划文档中的每个被动、Q、W、E、R、D 至少要记录以下内容；资料未确认时明确写“待资源库核对”，不得猜填路径或动画编号：

- 动作/动画名称或待确认标记。
- `t0`、前摇、动作开始、施法硬直、实际生效/伤害时点、后摇和结束时间。
- 特效语义：模型类别、挂点或坐标、朝向、缩放、颜色、生命周期、创建点和销毁点。
- 音效语义：起手、蓄力、发射、命中、爆炸、持续和结束；同时记录音量、3D/2D、播放时长和时间点。
- 联动触发后新增或变化的动作、特效、音效和清理点。
- 表现与真实判定的锚点关系：施法者、目标、落点、路径或弹道不能只为方便而全部挂在施法者身上。

同一技能的动作、硬直、控制、伤害、音效和特效必须来自同一份时间线配置；实现文件只读取配置执行，不在多个技能函数内重复硬编码表现参数。

### 英雄图标的人物轮廓与招式规则

- 生成技能图标或 Buff 图标前，先判断图标语义是否依赖人物的身形、姿态或武器动作。拔刀、居合、招架、突进、施法、飞行、变身、蓄势和反击等动作型语义，优先使用角色轮廓加正确招式姿势；单纯的层数、标记、护盾、减益和资源状态，仍可使用清晰的状态符号。
- 需要人物动作时，人物轮廓、服装、发型、武器类型、握持方式、身体朝向和动作语义必须以已确认的角色参考图或规划资料为硬约束。不能为了增强“战斗感”擅自加入铠甲、肩甲、头盔、战场、军旗、战争女神/女武神、现代军装或其他不属于角色世界观的元素。
- 武器姿势必须符合角色原始武器和招式：太刀/和装角色保持和装、袖摆、腰带与合理的拔刀/收刀/招架姿势；不能把和装角色改成重甲战士，也不能把角色的刀替换成陌生武器。参考图不足以确认服装、武器或姿势时，停止生成并先向用户说明缺少的参考信息。
- 人物动作图标必须先生成约 `1K` 正方形源图，再等比例缩放为 `100x100`；普通状态符号图标按项目默认尺寸缩放为 `80x80`。两种尺寸都必须单独检查，确认缩小后人物轮廓、武器方向和技能语义仍可辨识。
- 每个动作型图标先生成一张预览确认风格，再继续同一英雄的其他图标。失败候选不得登记到正式资源库；用户确认前只保留在临时预览目录。
- 图标提示词或制作记录必须显式写出“保留项”和“禁止项”：保留角色服装、武器、动作方向和主色；禁止新增铠甲、战争场景、额外角色、错误武器和不符合原著的身份变体。

### 设计验收清单

开始实现前和完成后都要检查：

- 每个技能有独立基础效果，联动不是技能正常运行的唯一条件。
- 技能联动矩阵已写，且每条联动有上限、消耗、内部冷却和清理路径。
- 有普攻或天赋联动；若人物定位不适合普攻，则在规划中明确说明原因。
- 有玩家主动操作决策，且不存在唯一固定连招。
- 已评定 1～5 星复杂度，并说明如何阻止复杂度继续上升。
- 动作、特效、音效和所有生效/清理时点均已记录。
- 正常结束、打断、死亡、目标失效、重复释放路径都能清理表现、状态、计时器、暂停来源和技能实例。
- 原著能力、已确认资料和本项目游戏化扩展有明确边界。

## TS Buff 表达规则

- 源技能中凡是玩家需要持续识别的增益、减益、标记、蓄力、锁定、易伤、减速、沉默、控制或阶段状态，必须判断是否需要 TS Buff。只要状态会持续一段时间、影响玩家决策、需要显示剩余时间，或后续逻辑需要按状态查询，默认应登记 TS Buff。
- 原生控制与 TS Buff 是两套生命周期。眩晕、沉默、减速等真实控制仍走项目控制封装；需要图标表达时，再登记同持续时间的 TS Buff，并确保提前解除、驱散、死亡、技能中断和自然到期时两边同步清理。
- Buff 图标暂未制作时可以先使用语义接近的现有图标或明确的临时图标，但 Buff ID、名称、类型、可驱散性、持续时间、来源和描述必须真实。后续替换图标不得改变运行逻辑。
- 登记 Buff 不等于实现效果。攻击、移速、承伤、治疗、护盾、控制等数值必须接入对应属性、伤害、治疗、护盾或控制系统；验收时必须从运行时入口反查，禁止用 Buff 文案反推效果已经生效。
- 瞬时命中特效、一次性伤害、零持续时间状态和纯内部实现标记通常不创建玩家可见 Buff；确需内部状态时使用技能上下文或专用状态表，避免污染 Buff UI。

## 伤害、控制和独立技能

- 伤害必须通过项目技能伤害封装，禁止裸 `UnitDamageTarget`。AOE、单体、路径、多段和持续伤害必须显式传递正确的伤害形态。
- 先按最终结算语义判定 `AOE` 或 `单体`，不能按本次实际命中人数猜测。范围、路径、扇形、矩形、环形、波次或持续危险区统一按 AOE 规则处理。
- 英雄主动技能需要“本次释放首次命中”“每个目标首次命中”或累计命中时，创建一个独立技能实例；多段、延迟、周期和弹道伤害共享同一个实例 ID，直到技能结束或 TTL 清理。
- 控制时点必须与技能配置的时间线对齐。暂停、眩晕、定身、沉默、击退等不能在 `SPELL_EFFECT` 入口无条件提前施加，也不能用零秒持续时间模拟“瞬间控制”。
- 延迟伤害必须在真实伤害位置和真实范围提供玩家可见预警；预警持续时间覆盖从锁定到伤害结算的窗口。
- 伤害前需要特效时，顺序固定为“创建/播放配置特效 -> 到达配置伤害时点 -> 通过技能伤害封装结算”。不允许用伤害回调反过来补创建本应提前出现的特效。

## 异步与清理

- 每一个延迟回调、计时器、动画等待、特效句柄、暂停来源、Buff、技能实例和临时目标都必须有所有结束路径：正常结束、打断、死亡、目标失效、技能重复释放和场景清理。
- 回调使用模块顶层具名函数或本地具名函数，禁止用匿名闭包作为 JASS 回调；需要携带上下文时使用项目已有计时器/技能上下文封装。
- 不准使用 Lua 数学库；距离、角度、随机、三角函数优先复用 `TS/lib` 或技能模板封装。
- 不要主动查看或修改生成 Lua；只有排查 TSTL `self/nil` 错位时，才检查与 TS 对应的生成结果。禁止为解决 TSTL 问题修改 `fix-lua-for-pack.js`。
- 运行时调试输出统一使用 `TS/lib/扩展函数/自定义扩展函数/03．调试输出.ts`，临时诊断完成后删除埋点。

## 英雄技能验收

每个英雄技能完成后，至少逐项验证：

- 物编技能 ID、英雄 Rawcode、技能槽位和快捷键顺序正确；`01` 是被动，`02-05` 对应 QWER。
- 动作、动作速度、音效、特效和时点来自配置，技能文件没有重复硬编码。
- 暂停发生在动作之前；动作播放后单位不会永久暂停；重复释放不会叠加无法清理的暂停来源。
- 施法者/目标朝向正确，特效挂点和落点正确，命中特效不会提前出现或延迟到伤害之后。
- 伤害、控制和命中特效发生在同一份时间线定义的时点；AOE/单体形态和独立技能实例正确。
- 延迟伤害有对应预警，提示范围与实际判定范围、持续时间和坐标一致。
- 所有蓄力、吟唱、持续引导、锁定等待和延迟生效窗口均显示跟随施法者的世界坐标蓄力条；正常完成、打断、死亡和重复释放后均无残留，也没有误叠两套进度条。
- 技能结束后特效、动作速度、暂停、Buff、计时器、目标引用和技能实例全部清理。
- 至少测试一次正常施法、连续施法、目标失效/死亡和技能被打断；确认没有永久眩晕、永久暂停、重复特效、提前伤害或漏伤害。

## 构建与交付

## 特效朝向与矩阵旋转

- 技能特效方向必须遵循技能配置与当前技能方向；点/目标技能按施法者到施放点或目标的方向计算，不得用猜测的固定 `180`/`270` 度替代实际方向。
- 先确认当前 TS 封装和 `EXEffectMatRotateZ` 的角度单位。项目的 `两点角度()` 返回角度制，当前特效旋转调用按项目既有约定传角度制。
- 特效创建时设置一次最终朝向即可。特效跟随单位或跟随位移的周期回调只能更新坐标、高度和生命周期，不得重复调用 `EXEffectMatRotateZ` 或再次叠加同一旋转矩阵；需要改变方向时必须先重置矩阵，再设置绝对朝向。

- 修改 TS 后优先执行：

```powershell
npm run build:files -- "本次修改的TS文件1" "本次修改的TS文件2"
```

- 参数只传本次修改或新建的 `TS` 目录下 `.ts` 文件；文档、规则和资源变更不运行 TS 构建。
- 不要为了验证英雄技能擅自完整打包地图。需要最终地图验证时，先确认所有 TS 生成物已经同步，再按项目地图打包规则执行。

## 子任务协作

- 批量英雄开发需要并行协作时，优先继续向已经存在且上下文匹配的子代理派发后续批次，不为每个英雄或每个技能重复新建子代理。
- 主代理负责拆解时间线、架构决策、公共封装边界和最终代码审核；子代理只能按已确认的配置与时点表实现、修错和运行测试，不得自行改写动作、特效、伤害或控制时点。
