---
description: 技能测试规则：Boss 技能与玩家英雄技能测试场景、测试单位血量、测试靶与运行时安全约定
globs: TS/系统/12．测试系统/**/*.ts, TS/系统/03．技能系统/00．技能模板+函数/03．技能测试/**/*.ts, TS/系统/03．技能系统/05．单位技能/**/*.ts
alwaysApply: false
---

# 技能测试规则

## 通用测试场景

- 技能测试不只覆盖 Boss，后续玩家英雄技能、装备技能、召唤物技能也按本规则处理。
- 输入测试命令创建测试场景时，默认准备：
  - 地图上预设的玩家1大法师作为玩家侧测试基准英雄：`jass.globals.gg_unit_Hamg_0002`。
  - 2 个敌对步兵作为通用测试靶。
  - 如测试对象是 Boss，再额外创建对应 Boss 单位。
- 本项目当前技能测试里，“大法师”默认指 `gg_unit_Hamg_0002`，不要误判为“可能不存在”或优先猜注册表里的其他英雄。
- 代码可以保留注册英雄或玩家英雄扫描作为极端兜底，但测试规则、测试说明和排查口径都必须优先按地图预设玩家1大法师处理。
- 如果某个测试明确要用当前玩家注册英雄，必须在测试命令说明里单独写明；否则默认使用预设玩家1大法师。
- 测试靶优先用普通步兵 `hfoo`，除非技能机制明确需要英雄、召唤物、建筑或特殊单位。

## Boss 测试空地坐标

- 后续制作 Boss 测试时，默认测试空地中心坐标固定为 `(-540.6, -2495.2)`；除非用户明确提供新的测试空地坐标，否则不要改用其他坐标。默认约定该坐标周围 1800 码内没有会干扰测试的单位、可破坏物或场景对象。
- AI 不要要求用户逐个提供测试安全区、导管、机制核心、凤凰蛋、地核、场地矩形等测试坐标。应根据 Boss 正式场地中心和默认测试空地中心 `(-540.6, -2495.2)` 自动计算 `偏移X/偏移Y`，把所有测试用点位与矩形整体平移过去；用户明确给出新测试中心时才覆盖默认值。
- 正式 Boss 场地配置不能被测试坐标污染。测试系统只能创建测试坐标副本，或在测试初始化期间临时覆盖本次测试上下文读取的场地配置；正式 Boss 战启动配置、正式规划坐标和正式场景矩形保持不变。
- 测试通用辅助函数优先放在 `TS/系统/12．测试系统/00．测试系统辅助函数.ts`。坐标平移、矩形平移、测试中心映射这类能力不是 Boss 专属逻辑，不要长期放在 Boss 测试系统内部。
- Boss 测试优先复用 `TS/系统/12．测试系统/00．Boss测试系统/`，目录结构固定为：
  - `00．Boss测试类型.ts`：测试场地、技能命令、注册配置等通用类型。
  - `01．Boss测试场地.ts`：Boss 测试侧的场地封装/兼容转导出；底层坐标与矩形平移应来自 `../00．测试系统辅助函数`。
  - `02．Boss测试单位.ts`：预设大法师、固定中立敌对步兵、测试单位满血、存活/归属判断和禁止移动等单位准备逻辑。
  - `03．Boss测试上下文.ts`：基于正式中心和测试空地中心创建测试场地上下文。
  - `04．Boss测试命令注册.ts`：统一管理当前 Boss 测试会话，并注册 `Boss单位名`、技能数字与公共清理命令。
- 测试场地映射优先复用 `00．测试系统辅助函数.ts`：
  - `创建测试中心平移映射(正式中心X, 正式中心Y, 测试中心X, 测试中心Y)`
  - `按测试映射平移坐标`
  - `按测试映射平移XY坐标`
  - `按测试映射平移矩形`
  - `根据测试中心平移坐标`
  - `根据测试中心平移矩形`
  - `复制平移测试坐标数组`
  - `复制平移测试矩形数组`
- 测试时必须整体平移的内容包括但不限于：
  - Boss/玩家/测试靶初始点位
  - 战斗场地中心和战斗矩形
  - 固定安全区、危险区、路线区、象限区
  - 导管、核心、凤凰蛋、召唤节点、地核节点、宝箱或可交互机制单位
  - 任何依赖“场地中心/矩形中心”的读条、提示圈、法阵、落点和周期机制
- 如果某个 Boss 的技能直接读取 Boss 目录里的 `01．场地配置.ts`，测试命令必须在创建机制单位、创建提示圈、启动周期机制之前完成测试场地覆盖；不要等导管/安全区已经创建后才平移。
- 如果某个技能通过运行时上下文读取场地矩形组，测试命令应销毁正式矩形句柄副本后创建测试矩形句柄副本；不要复用正式矩形句柄再只移动 Boss。
- 重复输入测试命令时，测试坐标映射必须基于“正式原始坐标快照”重新计算，不能基于上一次已经平移过的坐标继续叠加偏移。

## 测试血量

- 测试场景中的玩家英雄默认设为 `999999` 最大生命值。
- 统一 Boss 测试的两只固定步兵靶明确使用 `99999` 最大生命值；该约定覆盖旧的通用步兵 `999999` 约定。
- 改最大生命值后，必须立刻把当前生命值也设为同样数值，保证生命百分比为 100%。
- 不要只改最大生命值；只改最大生命会导致当前生命百分比很低，测试高伤技能时仍然会被秒杀。
- 推荐顺序：先用 JAPI `SetUnitState(unit, UNIT_STATE_MAX_LIFE, 目标值)` 改最大生命，再用 JASS `SetUnitState(unit, UNIT_STATE_LIFE, 目标值)` 回满当前生命。

## 测试命令

- 统一 Boss 手动测试的选择命令使用 `Boss单位名`，例如 `Boss巴尔扎罗斯`；选择后只输入 `1`、`2` 等数字触发当前 Boss 的对应技能。
- 公共命令固定为 `Boss列表`、`Boss重置`、`Boss清理`、`BossAI开启`、`BossAI关闭`；切换、重置或清理时必须调用当前 Boss 的清理钩子。
- 统一 Boss 手动测试命令只允许红色玩家（`Player(0)`）且玩家名精确为 `WorldEdit` 或 `WorldEdit:` 时生效。
- 测试命令应该可以重复输入，不要无限刷单位；优先缓存并复用最近创建的测试 Boss / 测试靶。
- 复用测试单位时，也要重新执行一次满血初始化，避免上次测试残留低血量。
- 测试命令应该在聊天提示里写清楚可用子命令和技能映射。
- 主动技能存在 P2/P3 阶段强化时，必须同时提供 `技能序号-阶段号` 测试命令，例如技能 `2` 的 P2/P3 强化分别使用 `2-2`、`2-3`；纯数字命令测试该技能的基础阶段，通常是 P1，P2 才解锁的技能则测试 P2 基础形态。
- 阶段强化命令必须设置技能实际读取的阶段来源（例如 Boss 当前生命比例），不能只改一个随后会被刷新覆盖的上下文字段；每次执行命令都重新准备目标阶段，保证测试可重复。
- 只为确实存在行为差异的阶段登记强化命令，不给没有阶段强化的技能补空命令；聊天提示中要明确标出 `P1基础`、`P2阶段/强化`、`P3强化`。
- 统一 Boss 手动测试创建场景时默认关闭自动施法，技能只由数字命令触发；输入 `BossAI开启` 后才允许调用 `记录Boss自动技能启动` 验证正式 AI，输入 `BossAI关闭` 可再次暂停自动施法。
- 不要为纯被动、开局自动触发、周期自动触发的机制额外添加手动测试指令；这类机制应通过创建测试场景后自然运行来验证。
- 指令只保留需要玩家主动触发观察的技能或资源生成，例如主动技能、投掷物、召唤、拾取物。删除被动/自动机制指令后应重新压紧编号，不要留下无意义空洞。

## 阵营与目标

- 统一 Boss 手动测试中的 Boss 归触发命令的玩家所有，方便选中和控制；两个固定测试靶必须是中立敌对 `hfoo` 步兵。
- 固定步兵通过项目现有 `X_FixUnitStandingSafe` 禁止移动；复用时仍要恢复位置、朝向、`99999` 最大/当前生命并再次固定。
- Boss 技能测试中，Boss 可以创建为敌对玩家单位，以便目标选择、Buff、伤害、仇恨等逻辑接近正式战斗。
- 如果为了手动施法需要玩家控制版 Boss，应单独写测试命令，不要混用正式敌对 Boss 测试入口。
- 玩家英雄技能测试中，玩家英雄保持玩家所属，测试靶使用敌对阵营。

## TSTL 与异步规则

- 测试系统也必须遵守 TSTL 硬规则：JASS/JAPI API 使用局部别名，回调用模块顶层具名函数。
- 不要在测试命令里使用 `GetLocalPlayer`、本地分支、异步 UI 回调或本地随机数来改变共享状态。
- 运行时敏感路径不使用 `Math.*`，距离、随机、三角函数按项目封装或 JASS 对齐函数处理。
