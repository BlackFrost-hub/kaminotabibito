---
description: 项目全局游戏平衡数据，包括战斗公式、单位档位、章节基线与数值维护口径
globs: objediting/**/*.lua, TS/系统/01．单位系统/**/*.ts, TS/系统/02．物品系统/**/*.ts, TS/系统/03．技能系统/**/*.ts, TS/系统/04．伤害系统/**/*.ts
alwaysApply: false
---

# 游戏平衡数据

## 使用范围

- 本文件是全项目共用战斗公式和数值基线的统一入口，不是某个 Boss、装备或护甲系统的局部规则。
- 后续确认的生命值、攻击力、攻击间隔、射程、护甲、章节倍率、普通/精英/Boss 档位等全局数据，按主题继续追加到本文件。
- 单个 Boss、技能或装备的特殊数值仍写在对应配置和规划文档中，不把个例抬升为全局规则。
- 未经用户确认的数据不要为了补全表格而推测；可以记录待确认项，但不能把推测值写成正式基线。

## 护甲公式

- 正护甲 `N` 的物理减伤率固定为：`N / (N + 50)`。
- 对应的物理伤害承受倍率为：`50 / (N + 50)`。
- 本项目不是 `N / (N + 100)`，也不能直接套用 Warcraft III 默认护甲因子。
- 设计护甲、护甲穿透、护甲削减和装备评分时，统一按本公式核算。

| 护甲 | 物理减伤率 | 物理伤害承受倍率 |
|------|------------|------------------|
| 10 | 16.67% | 83.33% |
| 25 | 33.33% | 66.67% |
| 35 | 41.18% | 58.82% |
| 50 | 50.00% | 50.00% |

## 敌方单位护甲档位

| 单位档位 | 默认护甲 | 使用范围 |
|----------|----------|----------|
| 普通单位 | 10 | 常规敌人、普通召唤物；机制单位不需要承伤时另按机制处理。 |
| 主线 Boss | 25 | 章节主线必经 Boss。 |
| 精英单位 | 35 | 精英敌人、隐藏挑战、异界挑战以及按精英强度设计的长期护卫。 |

- 隐藏 Boss、异界 Boss 默认按精英档 `35` 起步，不因名称中存在“Boss”就套用主线 Boss 的 `25`。
- Boss 阶段减伤、护盾、伤害分摊和临时增甲属于机制层，不应通过随意抬高基础护甲替代。
- 需要偏离基准时，必须在对应配置和规划文档中写明原因。
- 装备评分中的“敌人平均护甲”是估值采样值，不是所有敌方单位的物编默认值。

## 数据同步

修改单位基础战斗数据时至少同步：

1. `objediting/` 中的对象数据源。
2. 对应分类单位配置表。
3. `04．总单位配置表.ts`。
4. 涉及设计语义或例外时，同步 Boss/单位规划文档。
5. 运行 ObjEditing、TypeScript build，并检查生成对象数据。
