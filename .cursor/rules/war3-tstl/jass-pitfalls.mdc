---
description: 
alwaysApply: true
---

# War3 + TSTL + jass 常见坑（必读）

**分层**：`jass` / `japi` / **BJ（Blizzard.j）** / **项目本地库** 勿混用；BJ 与本地封装边界见 `.cursor/rules/agent-shared/global-engine-rules.mdc` §1。

## 0) 目标客户端：**魔兽争霸 3 补丁 1.27**

- 勿默认存在 **1.29+** 的 `Blz*` 伤害读接口（如 `BlzGetEventDamage`）；即便能 `type(...) == "function"`，返回值也可能与 **YDWE `EXSetEventDamage` 改写后的实际结算** 不一致。
- 在 `EVENT_UNIT_DAMAGED` 同步阶段读取「当前事件伤害」时，优先 **`jass.japi`**：`GetEventDamage`（若 LuaEngine 提供）、`EXGetEventDamageData(…)` 中与引擎约定一致的 **伤害数值槽**，最后才是 `jass.GetEventDamage`（多为改写前初始值）。**1.27 无** `BlzGetEventDamage`。
- 若 `japi.GetEventDamage` 仍不可靠：在伤害修正流程末尾用 **`registerAppliedFinalDamageListener`**（`系统.04．伤害系统.04．伤害计算.04．主计算流程`）把最终伤害推给飘字/UI/统计，勿再叠一层快照表。

## 1) **禁止**对 jass API 使用可选链调用 `?.(...)`

TSTL 会把 `obj.fn?.(x)` 编译成"取出函数后再用 self 调用"的形态，容易把 **table** 当成第一个参数传给需要 **jhandle_t** 的函数，导致：

- `bad argument #1 ... (jhandle_t expected, got table)`

**✅ 正确写法**

- 直接 `(jass as any).Fn(arg)` 调用，不要默认添加 `typeof === "function"` 安全检查。
- 仅在规则或需求明确要求跨平台兼容时，才允许补存在性检查。

## 2) **禁止**把 `jass.xxx` 赋给局部变量再调用

例：`const f = (jass as any).GetOwningPlayer; f(unit)`  
TSTL 可能编译成 `f(nil, unit)` 或 `f(jass, unit)` 之类，造成参数错位。

**✅ 正确写法**

- 直接调用：`(jass as any).GetOwningPlayer(unit)`

## 3) Lua 真假值：`""` 在 Lua 里也为真

TS 里常见写法：`if (!msg) ...`  
编译到 Lua 后 `msg=""` 依然为真，逻辑会错。

**✅ 正确写法**

- 用 `msg === ""` / `msg !== ""` 判断空字符串。

## 4) 遍历 Lua table：避免 `Object.keys(table)`

Lua table 在 TSTL 环境中用 `for (const k in t)`（pairs）最稳。

**✅ 正确写法**

- `for (const k in itemsData) { ... }`

## 5) 随机数：避免 `math.random()` 无参浮点假设

不同环境下 `math.random()` 无参可能不返回 `[0,1)` 浮点。

**✅ 正确写法**

- `const idx = (math as any).random(1, list.length);`
- `return (list as any)[idx];`

## 6) Lua 表 1-based：`math.random(1, n)` 后不要用 `arr[idx - 1]`

TS 里习惯写：`idx = math.random(1, arr.length)` 再 `arr[idx - 1]`（0-based）。TSTL 原样编译到 Lua 后，Lua 表是 **1-based**，`idx - 1` 在 idx=1 时为 0，`arr[0]` 为 **nil**，会导致漏项或报错。

**✅ 正确做法**

- 在 TS 里用 `(arr as any)[idx]`，并在 `fix-lua-for-pack.js` 里对该文件加替换：`arrName[idx - 1]` → `arrName[idx]`。

## 7) 回调注册：禁止用冒号调用模块导出的函数

**❌ 错误写法**
```typescript
damageEvent:registerDamageCallback(callback);
```
TSTL 编译后会把模块表本身当成第一个 self 参数传给注册函数，导致回调列表里存的是模块表而不是回调函数，调用时报错 `attempt to call a table value`。

**✅ 正确写法**
```typescript
damageEvent.registerDamageCallback(callback);
```

## 8) 回调验证：不要对 TSTL 编译后的回调做 `typeof === "function"` 检查

**❌ 错误写法**
```typescript
if (typeof cb !== "function") return;
```
TSTL 编译后的 Lua 回调是一个带 `__call` 元方法的 table，`type()` 返回 `"table"` 而非 `"function"`，这个检查会把有效回调挡掉。

**✅ 正确写法**
```typescript
if (cb == null) return;
```

## 9) 导出函数的 self 参数坑：TSTL 给所有导出函数加 `(self, ...)` 签名

TSTL 把每个 `export function foo(x)` 编译成 `function ____exports.foo(self, x)`，外部调用时参数会错位。

**✅ 解决方案**

### TypeScript 端（07．技能函数.ts / 08．伤害函数.ts）
所有导出函数都加 `self` 作为第一个参数，内部直接调用 japi：

```typescript
export function EXGetUnitAbility(self: any, u: any, abilcode: number): any {
  return japi.EXGetUnitAbility(u, abilcode);
}

export function YDWESetUnitAbilityDataString(self: any, u: any, abilcode: number, level: number, data_type: number, value: string): boolean {
  const a = EXGetUnitAbility(self, u, abilcode);
  return EXSetAbilityDataString(self, a, level, data_type, value);
}
```

### fix-lua-for-pack.js 端
已内置规则 6g 会自动清理：
- 去掉导出函数签名里的 `self/____self`
- 去掉调用时传递的 `nil`

这样既保证性能（不使用 EXExecuteScript），又正确处理了参数错位。

## 10) japi / 外部 API 的冒号调用坑：TSTL 对 require 进来的模块生成冒号调用

TSTL 对 `japi.EXGetEventDamageData(edd_type)` 可能编译成 `japi:EXGetEventDamageData(edd_type)`（冒号），Lua 冒号语法把 `japi` 表作为第一参数传入，`edd_type` 实际收到的是 `japi` 表，函数参数完全错位。

同理，外部对本模块用冒号调用（`mod:YDWEIsEventDamageType(dt)`）也会把模块表当 self 吃掉。

**✅ 修复方案（在 `fix-lua-for-pack.js` 规则 6b 里）**

对 **整个 `japi` 表**统一把冒号调用改成点号（`DzXxx`、`EXXxx` 等都会中招）：

```javascript
content = content.replace(/\bjapi:/g, "japi.");
```

若仍有对某模块的 `编码名:` 冒号调用，再按文件单独替换为 `编码名.`（例如规则 6h 对 `08．伤害函数` 编译名的处理）。仅改冒号**不够**时，还须配合规则 9 去掉被调模块导出函数签名里的 `self`。

## 11) 回调调用：TSTL 对回调变量调用可能多传 self / nil

**现象**：依编译形态不同，可能出现 `cb(nil, su, sd, ...)` 或 `cb(nil, nil, su, sd, ...)` 等，使 `unit`/`damage` 等参数错位。

**✅ 修复方案**：在 `fix-lua-for-pack.js` 的全局表 `WC3_NIL_FIRST` 中处理（原仅 `伤害事件` 路径，现与 BJ/原生多传 `nil` 一并全局应用）。例如 DamageCallbacks 去掉多余的第二个 `nil`：

```javascript
[/cb\s*\(\s*nil\s*,\s*nil\s*,\s*su\s*,/g, "cb(nil, su,"],
```

若其它文件出现同类错位，需对照生成 Lua 增删对应替换，不可假设全局只有一种 `cb(nil, …)` 形态。

## 12) `jass.xxx` 常量/全局变量：不依赖 `udg_*` 全局，改从事件入参传递

## 13) 减少 `fix-lua-for-pack.js`：`self` 从源头消掉

**现象**：TSTL 默认给模块导出函数生成 Lua 首参 `self`，点号单参调用时与 JAPI 期望错位（如 `YDWESetEventDamage(65)` → `EXSetEventDamage(nil)`），靠打包脚本「事后修」会越来越臃肿。

**更稳的长期做法（按投入递增）**

1. **单文件**：在纯工具模块首行加 `/** @noSelfInFile */`（须在首条语句之前），该文件内导出不再带 `self`，与点号调用一致。见 [The Self Parameter](https://typescripttolua.github.io/docs/the-self-parameter/)。
2. **全项目**：`tsconfig.json` 里 `"tstl": { "noImplicitSelf": true }`，等价于所有实现文件都带 `@noSelfInFile`。需配合 `strict` / `noImplicitThis` 处理「把方法当回调」处：用箭头包一层，或把接口里的回调声明为 `this: void`，否则 TSTL 报 `Unable to convert function with a 'this' parameter...`。
3. **`require("…")` 单参（本仓库已做）**：在 `global.d.ts` 对 `declare function require` 使用 `/** @noSelf */`，并在 `tstl.noResolvePaths` 中加入 `jass.*`、`lib.*`、`系统.*`（picomatch），使 TSTL 直接生成 `require("…")` 且不因点分路径报 `Could not resolve`；`fix-lua-for-pack.js` **不再**替换 `require(nil, …)`。
4. **`fix-lua` 仍会保留**：`jass:` / `japi:` 冒号、其它多传 `nil`、Lua 1-based 随机下标、STES 注册等**引擎与 TSTL 缝隙**往往仍要脚本或自定义 TSTL 插件处理；目标是**把「self 剥离」类规则收进编译期**，脚本只留环境特化。

**跨模块解构边角**：若 A 文件为默认导出（Lua 带 `self`），`const { f } = require("B")` 且 B 已 `@noSelfInFile`，TSTL 仍可能对 `f(x)` 生成 `f(nil, x)`。本仓库例：`02．伤害修正` → `06．伤害函数` 的 `calcArmorReduction`，由 `fix-lua-for-pack.js` 规则 **6k** 去掉多余 `nil`；或给 A 也加 `@noSelfInFile`（需无隐式 `this`），或避免解构、改用 `require("B").f(x)` 并确认生成 Lua 为点号而非冒号。

**工程组织**：若脚本过长，可按主题拆成 `scripts/fix-lua/rules/*.js`，由入口遍历 `src` 串行应用，逻辑不变、可维护性更好。

## 14) `DzTriggerRegisterKeyEvent(Trg)`：键码不要用 `string.char(VK)` 冒充

Lua 里 `string.char(113)` 是字母 **`q`**，不是 **F2**；`string.char(9)` 是 **制表符**，不是 JASS 里用的 **VK=9（Tab）**。用字符路径注册会把热键绑到错键上，表现为 **F2–F6 / Tab 全失效**。

**✅ 做法**：与 `属性查看.j` 一致，对 **F1–F12（112–123）**、**控制键（1–31，含 Tab=9）** 传 **数字 VK**；**OEM 区（如 `~` / VK_OEM_3 = 192，以及常见 186–192、219–222）** 同样须传 **数字 VK**，勿用 `string.char(192)`。实现见 `lib/扩展函数/封装函数/04．硬件输入/04．键盘函数` 的 `registerKeyBindToTrigger`。

仅对明确可打印字母/数字键再考虑字符分支。

## 15) `DzSetUnitModel` 与 `AddSpecialEffectTarget(overhead)` 的先后

**现象**：`CreateUnit` 后立刻 `AddSpecialEffectTarget(..., "overhead", …)`，若随后又 **`japi.DzSetUnitModel`**（常见延后 0.01s 避免 hook 问题），换模往往会 **清掉或弄丢** 已绑定的头顶特效，表现为 **开局 NPC 叹号/问号不出现**。

**✅ 做法**：头顶任务标记放在 **换模完成之后**（例如与 `DzSetUnitModel` 同策略：再延后一帧/0.02s），或先换模再挂特效；无自定义模型时也可 **短延时 0.01s** 再挂，避免与 CreateUnit 同栈绑定失败。

## 16) `TriggerRegisterPlayerUnitEvent`：业务代码勿手写直连

在业务/系统 TS 里对每个玩家循环 `TriggerRegisterPlayerUnitEvent`，会与已有监听 **重复注册** 同一原生事件，浪费且难查。

**✅ 做法**：统一走 **`系统.00．核心系统.01．事件中心.01．玩家单位事件`**（区域进入用 `02．区域事件中心`，单位实例事件/范围用 `03．单位特定事件中心`）；**施法通道/效果** 走 **`系统.03．技能系统.00．技能事件`**。细则与例外见 **`.cursor/rules/agent-shared/global-engine-rules.mdc` §13**。
