## 朱雀院椿 A1-A11 全英雄技能实现（B1-B6 批次，WorkBuddy）
- 新增/重写（22．朱雀院椿/）：02 被动（VF 场 registerDamageModifier priority40 真实吸收+残缺阈值0.3+恢复VF 统一冷却1s；反击准备 1.2s 普攻/Q/E 各消费一次；一刀/二刀姿态互斥；决斗距离；普攻反击斩 isNormalAttack&&!isWrappedSkillDamage 追加实际伤害×0.6 一刀恢复VF/二刀追加斩光；清理朱雀院椿状态 幂等）、03 Q（基础居合 150ms 前摇；返刃=反击准备消费→一段输入方向+二段防守方向回身斩；一刀恢复VF/二刀交叉斩）、04 W（0.6s 招架窗口 单位是否在来源正面扇区 90°；普通/完美=进入≤0.25s 且方向差≤60°；修改器 priority60 先于 VF40；addDelayedCallback(0) 延迟收尾；未受击收刀斩+恢复VF；R 蓄力禁止开 W）、05 E（开始冲锋+位移特效 t3_effect_k4+az_yin_q1；先标记结束再停止位移；终点横斩+决斗距离 2.5s；精确回锋=消费反击准备+位移取来源方向+设置回锋方向给Q）、06 R（开始充能 指令中断+世界坐标UI+显示进度条特效:false；终式仅完成回调；一刀蓄势受击记录→后之先反击分支；二刀交错斩+VF代价；锁定姿态；椿R蓄力中 供W）、07 D（姿态切换 设置姿态互斥；R 锁定禁切；二刀 12s+addPeriodicCallback 每秒扣 VF 归零回一刀；切回恢复VF）。
- Buff 表：E017 VF场/E018 VF残缺/E019 反击准备/E01A 一刀/E01B 二刀/E01C 决斗距离（22．朱雀院椿.ts+英雄 index 聚合）；图标占位 BTNSelectHeroOn。
- 正式资源 16 个全部迁入（VF完整/残缺、Q主备、W普通/完美、E冲锋主层/终点主备、R主斩/交错主备、D一刀/二刀）；A8 动作索引 Q/W/E/R/D=0 待实机；A9 物编 Tsubaki.lua 冷却魔耗目标类型与 TS 完全一致。
- 构建：全项目 npm run build exit 0（fix-lua 389）；Lua 抽查 7 文件关键形态；A10 待进图验收。
- 报告：执行计划文件第 8 节 R001-R006 批次报告。

## 朱雀院椿第七轮验收反馈修复（R007，6 高优先级 + 3 其他）
- 初始 VF：取英雄状态 懒创建补 刷新VF表现+初始一刀 Buff（初始化VF 原无调用方）。
- E→Q 回锋联动：Q 运行时 require 获取椿回锋方向（E 改读取即消费 delete 回锋表[id]，每次 Q 最多一次）；E→R 决斗距离：R 充能完成回调 有决斗距离→获取决斗距离方向→清除决斗距离（02 新增 清除决斗距离 导出）。
- 正式特效接入：Q 结算Q斩 建 Q主斩；E 结算E终点横斩 建 E终点主斩；R 配置 主斩/交错斩特效 补正式路径（TsubakiPhoenixFinalSlash/TsubakiCrossSlash）。
- 去双层特效：02 删手动 createUnitEffect/destroyUnitEffect（VF 完整/残缺、一刀/二刀），特效全由 Buff 表 effect 管理（此前手动+Buff 两层重叠）。
- W：删 水镜BuffID（复用 VF场 会误删常驻 VF Buff）与两处移除；完美恢复合并单次调用（恢复VF 1s 冷却挡第二次）。
- E 撞墙：撞墙回调 GetUnitX/Y(移动单位) 更新终点再结算（此前在未达目标点结算）。
- 伤害形态：Q/E/R/收刀 →AOE，W 对单来源反击+两侧刀光保持单体；R 受击记录加 正伤害+非自身敌对来源 条件。
- catalog charge.md 补 t3_effect_k4/az_yin_q1/DashSlashWindBackup 椿 E 冲锋用途。
- 构建：全项目 npm run build exit 0（fix-lua 389）；Lua 抽查 5 处形态确认。

## 朱雀院椿第八轮验收反馈修复（R008，4 项 + W 叠层）
- 初始 VF：注册朱雀院椿被动 内注册 registerPlayerHeroListener（玩家英雄获取桥接，英雄创建/选择即初始化VF）；VF 修改器改 取英雄状态(单位) 懒创建兜底。
- E→Q：获取椿回锋方向 返回 number|null（0° 合法）；Q 已消费反击 = 反击!=null || 回锋!=null（E 提前消费反击准备后回锋存在仍进返刃）；反击方向 回锋 优先。
- 刀光朝向/动画：Q/E/R 点特效补 面向角度（角度制直接传方向角）+ 动画索引:0（冰焰双星先例）；时长按 catalog 序列：Q 主斩 Stand 1000-1755→0.9s、E 横斩 Death 233-667→0.8s、R 主斩 death 1000-3000→2.2s（原0.6s截断）、R 交错 Death 0-333→0.5s。
- R 决斗距离释放前快照（蓄力 0.7s 期间过期不丢联动），完成回调消费快照+清除决斗距离。
- W 删 VF完整 招架窗口叠层（常驻 VF Buff 特效已显示防护立场）。
- 构建：全项目 npm run build exit 0（fix-lua 389）；Lua 抽查 02/03/05/06/04 形态确认；动画索引 0 待实机核对模型序列。

## 朱雀院椿第九轮修复（R009：W 招架窗口独立持续表现）
- W 窗口期 createUnitEffect(英雄,"origin",W普通招架 TsubakiParryFlash, 招架窗口秒, 招架特效键)：独立白金半圆闪光弧（项目无独立招架窗口模型，catalog Birth/Stand/Death 可循环）；不叠加 VF 常驻特效，VF=0 窗口仍可见；结束/成功/死亡/中断统一 destroyUnitEffect(招架特效键)。成功反馈点特效在来源位置独立播放不冲突。
- 构建：npm run build exit 0（fix-lua 390）；Lua 确认窗口弧创建与两处销毁路径。

## 芙莉莲 A1-A11 全英雄技能实现（B1-B6 批次，WorkBuddy）
- 新增/重写（25．芙莉莲/）：01A 动作表现（播放限时动作+循环守护，恢复动画速度1.0，02清理句柄）、02 被动（隐匿静默延迟回调期满进入+花田静止倍率缩短；唯一重点目标+三类解析到期+解析完成原子消费；registerAppliedFinalDamageListener 演算普攻追加伤害不消耗完成+Q/W冷却缩减；registerPlayerHeroListener注册初始化+死亡监听清理）、03 Q（发射弹道窄直线+on命中统一结算不传伤害值防双扣；解析完成破防/防御穿透分支；花田修正射程）、04 W（正面扇区一次防御+修改器addDelayedCallback(0)延迟收尾；开始护盾自然结束基础护盾；花田加持续）、05 E（开始冲锋+SetUnitFlyHeight起飞+CWBM AddLightningEx/MoveLightningEx周期观察闪电上限5条；先结束再停位移；撞墙安全终点；恢复高度）、06 R（开始充能指令中断+世界坐标UI单套+显示进度条特效false；t0解析快照；充能完成回调主炮/解析消费/花田盛开；窄线判定+分支加成）、07 D（友方准备区+FogModifier视野+静止检测注入02隐匿加速；Q/W/E修正单次锁+R盛开单次；重复释放清理旧花田；自然消散/打断不播盛开）。
- Buff表：E020魔力隐匿/E021解析中/E022解析完成/E023演算魔弹（25．芙莉莲.ts+英雄index聚合）；图标占位BTNSelectHeroOn。
- 公共封装：护盾系统07（开始护盾）；闪电17（CWBM青蓝白光束）；方位判定08（单位是否在来源正面扇区）；弹道编排工厂（发射弹道）；充能系统（开始充能）；区域效果（创建区域效果）；AddLightningEx/MoveLightningEx（塞莉亚先例）；SetUnitFlyHeight（克劳德先例）；FogModifierRadius（英雄选择场景先例）；平台API取值/动作（冷却缩减）；registerAppliedFinalDamageListener（主计算流程）；registerPlayerHeroListener（玩家英雄获取桥接，英雄注册初始化）。
- 动作槽：00配置 芙莉莲模型动作配置（15序列完整登记原始区间/时长/循环/截图关键帧）+芙莉莲技能动作槽{索引,播放速度,持续秒}（3W保持/9Q-R发射/7R蓄力/11E-D送杖/4E观察保持；待截图映射true）。
- 物编Frieren.lua冷却魔耗目标类型与TS完全一致（Q4/45 W10/75 E12/70 R75/150 D18/60）。
- 构建：全项目npm run build exit 0（fix-lua 401=390+11）；中途01A引用名错误(芙莉莲技能动作配置→芙莉莲模型动作配置)已修；Lua抽查7文件关键形态。
- 已知：花田主体模型未齐（仅环境粒子）；E观察保守实现（无统一反隐接口）；动作待截图映射；图标占位；数值待平衡；A10待进图验收。

## 芙莉莲第九轮验收反馈修复（R002.1，9 项运行时机制错误）
- E 阻断：开始冲锋补 角度:数据.方向角（双缺返回0不移动）+ 位移ID==0 立即收尾清理。
- 解析惰性化：施加解析/目标解析完成/尝试消费/取重点目标 全部按未到期类型数动态判定（到期不计入）；状态.解析完成 同步惰性值。
- D 导出 在花田内（R/E require 读取，此前只注入 02 接口）；静止检测状态变化调 重新安排隐匿计时（02 新增导出）；E 也消费花田修正（修正E落点倍率加成）。
- W/E/R 动作收口：持有循环守护句柄，成功/自然/中断/死亡统一 停止循环守护；E 去起飞限时（观察守护承载）；R 完成先停蓄力守护再播发射。
- 特效参数配置化：表现配置.特效参数 全量登记（缩放/高度/持续秒；D花瓣 基准半径/基准缩放；E闪电 高度）；消除 Z:40/缩放:0.8/半径÷100 硬编码。
- W 正面判定用快照方向角+角度差绝对值≤正面角度/2（不用公共扇区函数读当前面向）。
- R 改窄矩形：半径枚举+投影/垂距过滤（固定命中半宽）+投影排序；隐匿首击只加第一个目标。
- 目标死亡清理 传 状态.芙莉莲（状态新增句柄字段）。
- 构建：npm run build exit 0（fix-lua 401）；4处前向引用已修；Lua 抽查 6 文件关键形态确认。

## 芙莉莲第十轮验收反馈修复（R002.2，3 高 + 7 中低）
- 高1 普攻惰性：按未到期有效类型数动态判定+选倍率（过期不触发演算）。
- 高2 R 充能中断：保存充能ID；结束回调 原因~="完成" → 控制器.中断()。
- 高3 E 位移原因过滤：仅 "完成" 结算落点；中断/死亡/主单位死亡只清理；撞墙独立结算。
- 中：R蓄力预警参数化（特效参数.R蓄力预警）；E位移特效 位移特效缩放/高度/持续秒（E起落风压参数）；Q弹道 特效参数.Q弹道 缩放/高度；解析标记/W护壁/W公式层 createUnitEffect 后 设置特效缩放（挂载无高度入口）；E观察持续秒生效（观察截止后统一收尾）；E观察死亡目标清理+闪电创建失败不登记；E起飞动作槽接入（起飞限时→延迟0.6s观察守护）；01A 动作槽名称/原始时长从模型配置权威带出。
- 构建：npm run build exit 0（fix-lua 401）；Lua 抽查确认。

## 芙莉莲第十一轮验收反馈修复（R002.3，6 项残留 + 观察时序对齐）
- E 观察期纳入实例生命周期：到达/撞墙不再立即 控制器.完成()，改 E安排观察期收尾（观察持续秒 延迟回调 完成E收尾+完成实例，登记 控制器.登记延迟回调）；实例持续秒 = 位移时长+观察持续秒+2。观察期死亡由 H-01 收束即时清理；观察期再释放 E 被活跃实例阻止。
- E 观察动作起算点：删除固定 600ms 起飞延迟回调；观察守护改由位移到达/撞墙时（结束E 自然到达分支）启动，持续秒=观察持续秒 与观察截止同起算点对齐（不再提前覆盖起飞、不再有保持动作提前恢复的间隔）。
- R 预警补 持续秒: 特效参数.R蓄力预警.持续秒（配置驱动）。
- 挂载特效高度：解析标记/W护壁/W公式层 改 创建单位坐标跟随特效(scale+height)（lib 已有，瑟兰迪尔先例）+ 销毁单位坐标跟随特效；00 配置补 高度（40/45/45）。
- 解析最多两种：施加解析 已有类型数>=2 且新类型未持有 → return 忽略第三种（刷新已持有允许）。
- 构建：npm run build exit 0（fix-lua 401）；Lua 抽查 05 登记延迟回调/无600、02 第三种拒绝 195 行、02/04 坐标跟随特效各 2 处、06 R蓄力预警持续秒。

## 20-25 英雄表现配置化整改（B1-B8，WorkBuddy）
- 用户要求：配置表里该放的（缩放/高度/持续秒/动作速度/飞行高度/装饰物）全部配置化，业务不硬编码。执行 8 批次完成。
- 新增物编配置（模型+模型缩放 1.0）：20 爱蜜莉雅/21 红叶/22 椿（23/24/25 原有）。
- 动作槽完整元数据 {索引/名称/原始时长秒/播放速度/持续秒/语义/阶段}：20 新增 5 槽（吸收 0.8/0.9/1.0/1.2 硬编码）；21 新增 10 槽（从模型序列派生，吸收 0.8/1.0/1.2）；22 新增 11 槽（含 R 姿态条件，吸收 0.8/0.9/1.2）；23 补 语义/阶段 12 槽；24 动作槽 1→7 槽（Spell 2/3 待实机）；25 补 名称/原始时长/语义/阶段 7 槽。
- 播放函数签名统一改 槽对象：播放爱蜜莉雅动作/播放红叶动作/播放椿动作 (英雄, 槽{索引,持续秒})；业务调用 25 处全部改槽（20:5、21:10、22:9、R条件式）。
- 特效参数：20/21/22 已有参数子结构；23/24/25 表现子配置/特效参数全条目（含基准半径/基准缩放/D花海网格间距旋转）。挂载特效用 创建单位坐标跟随特效(scale+height)；24 闪电 z 读 节点配置.连接Z高度；25 D花簇 DzDoodad 全配置化。
- 复扫：20-25 业务硬编码残留 0（唯一允许 01A 内部恢复动画速度 1.0）；全量 npm run build exit 0（3 次瞬时文件锁重跑即过）；Lua 抽查 5 处槽对象读取确认。
- 报告：04．英雄技能/20-25英雄表现配置化整改报告.md（逐英雄 已配置/已接入/未确认/待补资源）。
- 未确认项：20/21/22 动作索引候选待实机；24 Q/W/E/D 业务动作接入缺口（Spell 2/3 未确认）；23 动作时点待实机校准。
