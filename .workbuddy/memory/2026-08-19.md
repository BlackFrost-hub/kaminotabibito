# 2026-08-19 工作日志

## 克劳德英雄技能 debugLogForce 埋点（定位 bug）
- 给 `TS/系统/03．技能系统/05．单位技能/04．英雄技能/01．克劳德/` 下 6 个 TS 文件加了调试日志，用于一次性定位技能 bug：
  - `00A．联动状态.ts`（模块名"克劳德-联动"）：凶斩命中标记/读取、空牙Q联动设置/获取/消耗/清理
  - `01．被动效果.ts`（模块名"克劳德-被动"）：暴击回调进入、触发Buff判断、溅射目标数
  - `02．Q技能.ts`（模块名"克劳德-Q"）：释放三段、蓄力、发射弹幕、命中结算、到达终点、魔耗、清理、死亡
  - `03．W技能.ts`（模块名"克劳德-W"）：三段释放、冲锋创建、Tick 前后置、窗口超时、魔耗、清理、死亡
  - `04．E技能.ts`（模块名"克劳德-E"）：三段释放、冲锋、斩击次数、窗口/强化结算、魔耗、清理、死亡
  - `05．R技能.ts`（模块名"克劳德-R"）：三段释放、接近、一二刀、旋风命中、魔耗、清理、死亡
- 统一用 `debugLogForce`（无条件输出），require 路径 `lib.扩展函数.自定义扩展函数.03．调试输出`。
- 构建：`npm run build:files` 传 6 个文件，编译通过；生成 Lua 抽查确认 `debugLogForce` 为局部别名直接调用，无 self/nil 错位。
- 日志为临时诊断埋点，定位完成后需删除。
