# 2026-09-09

## 昨日收尾：祖地守门单位坐标改回计划值

用户核对原始计划后确认：守门单位 e06O（虔诚的高等精灵骑士）坐标改为 (-16355.1, -11711.0) 面向 0°（原实现误放 -9821.1/-8637.8/118°）。已同步 3 处：`03．路人NPC出生配置.ts:111`、`09．世界地图单位缓存.ts:35`、`01．祖地双灵卫副本配置.ts` 守门单位（守门范围矩形中心自动跟随）。缓存匹配按 分组名+单位名+X+Y 数字严格相等，出生表与缓存表必须一起改。计划文档流程第 1 条已补坐标并更正 e060→e06O 笔误（e060 数字零是阿伦劳特残影马甲）。用户进游戏已确认看到守门单位。

## 米亚Boss战复活落水矫正（已按用户要求撤回，未上线）

用户反馈：米亚（污染之猫米亚，战场为水面/浅滩地形）Boss 战英雄复活点会落在水里（截图：英雄躺浅滩）。关键前提：**浅水在地形层面 WALKABILITY 可通行**，所以 `寻找可通行复活点` 的步进判定（X_IsTerrainWalkable = 物品法 + WALKABILITY）一路畅通直接停在水里。

曾实现「矫正水域复活点」（FLOATABILITY 判水 + 环形搜最近陆地，双重过滤 WALKABILITY 防地形相斥），**用户随后说"算了你撤回吧"，已完整撤回**：`04．英雄复活系统.ts` 恢复原状并重新编译，生成 Lua 无残留。此方案留档备用：判水 = `!IsTerrainPathable(x,y,PATHING_TYPE_FLOATABILITY)`；可站立陆地 = FLOATABILITY 阻挡 且 WALKABILITY 未阻挡。若日后重启此需求，按此口径实现即可。

## 花灵守护：花灵改玩家7 + 模型放大4倍

用户反馈花灵任务两个缺失项：① 花灵模型太小（缩放1）→ 改 4（"扩大300%"按字面 +300%=4倍，嫌大可回调）；② 花灵原属 `所属玩家ID: 15`（中立被动，魔物不打它，防守不成立）→ 改 `6`（玩家7 绿色）。改动仅 `01．花灵守护/00．入口配置.ts` 守护目标两行。

链路（改前已核对）：通用防守 `创建守护目标` → `Player(所属玩家ID ?? 中立被动)` → `创建召唤物` 直接 `CreateUnit(owner,...)`；缩放 `>0` 时 `SetUnitScale(u,s,s,s)`。波次敌人属 `敌对玩家ID ?? 中立敌对(12)`，创建后强制 `IssueTargetOrder(敌人,"attack",花灵)`——花灵属玩家7 后正常挨打。构建通过，生成 Lua `所属玩家ID=6 / 缩放=4` 已核对。
## -跟踪花 传送命令（41．跟踪花传送命令.ts）
- 新增 TS/系统/12．测试系统/41．跟踪花传送命令.ts：聊天输入 -跟踪花 → 取注册英雄 → 遍历 06．植物配置表 随机物品的去重刷新区域（荧光草.刷新区域/精灵王城道中区域1/2，经 获取矩形区域 惰性注册）→ safeEnumItemsInRect 收集可见花物品（shwd/I0H4/I0H5/I0H6 = 荧光草/星露花/晨曦花/月影花，排除聚灵花单位 nsea）→ 随机选一株 → SetUnitX/Y 到"花→英雄原方向"偏移120 落点 + PanCameraToTimedForPlayer → 文本反馈株数/坐标；找不到花提示"可能已被采集"。
- 权限走 是允许测试玩家；接线：12．测试系统/index.ts ENABLE_TRACK_FLOWER_COMMAND=true + loadTests require。
- 验证（用户全量构建后）：41 lua 产物完整；index.lua 46/179-180 接线；06．植物配置表.lua 月影花已同步为 5（旧产物 3 的滞后一并解决）；safeEnumItemsInRect(nil,rect,nil,fn) 4参形态经 normalizeEnumArgs maybeAction 分支确认正确。
- 环境注意：当日本会话跑 npm run build 连续 5 次卡在 src 写盘 UNKNOWN open（不同文件、tstl emit/fix-lua 两阶段均有，fix-lua 自带 1.5s 重试不够）；用户侧构建正常 → 疑似本会话环境干扰，构建最好由用户执行或重试。
- 背景结论（植物创建核查）：植物创建链路完整（main→单位系统 init→初始化其他世界地图配置→初始化世界地图植物），聚灵花=nsea"植物"，7 条随机物品区域/物品ID 全部有效；"找不到花"主因是分布广（荧光草区域 X∈[-25344,-23456] 南北 4.3 万码长条）+ 采集消耗，非未创建。

### -跟踪花 反馈"没找到花"排查（续）
- 静态已排除：创建链路完整（main→单位系统init→初始化其他世界地图配置→初始化世界地图植物→CreateItem）；4 个花 rawcode 均在地图（I0H4/I0H5/I0H6 在 objediting/Equipment/Item/Item.lua，shwd 荧光草在 XG_LUA_item.lua）；装备排泄只在物品死亡时 RemoveItem（不定时清场）；登记采集物品实例只建记录；SetItemVisible(false) 仅 X库函数地形检测临时用（不持久隐藏）；区域配置合并正常（场地区域 01A 也并入 动态矩形区域配置表，按需注册可用）。
- 新增诊断/兜底命令（需构建）：42．花诊断命令（-花诊断：逐区域输出 rect 有效性/物品总数/可见数/各花命中数 + 4 个 rawcode 数值）；43．花生成命令（-种花：英雄身边 120-220 码随机点生成随机一种花并登记采集）。均在 12．测试系统/index.ts ENABLE_TRACK_FLOWER_COMMAND 分支内 require。
- 判读：区域"物品总数=0"→从未生成（初始化未跑/被清空，可查 启用世界地图单位TS初始化 断言时序）；"总数>0 花命中无"→物品ID/物编问题；"矩形无效"→区域配置问题。
- 环境：本会话跑 npm run build 已连续 6 次卡在 src 写盘 UNKNOWN open（每次不同文件，tstl emit 与 fix-lua 阶段均有，fix-lua 自带 1.5s 重试不足），用户侧构建正常 → 后续构建交给用户执行。
