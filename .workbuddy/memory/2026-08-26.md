# 2026-08-26 工作日志

## 爱蜜莉雅英雄（20）A1 公共状态与冰晶节点
- 入口文件：`TS/系统/03．技能系统/05．单位技能/04．英雄技能/20．爱蜜莉雅/爱蜜莉雅执行计划与模型报告.md`（A0 已完成，从 A1 开始）。
- 新增 `02．公共状态与冰晶.ts`：英雄句柄索引状态容器（冰晶≤3/替换最旧、D 强化状态、世界坐标进度UI列表、技能清理表）；冰晶用 `Common\Effect\Element\Ice\EmiliaIceCrystalNode.mdx`（imports 已迁入，游戏路径去 `imports\` 前缀）；统一回收=冰晶+D强化+进度UI+技能清理表+摘表；死亡监听懒注册；地图清理不依赖句柄反查（TSTL 无 handle 反查）。
- 修改 `00．配置.ts`（+冰晶配置/读条配置）、`index.ts`（导入 02）。
- 关键公共封装：世界坐标进度 UI = `系统.09．表现系统.15．世界坐标进度UI`（创建/更新/销毁 + 跟随单位偏移）；充能高层封装 = `06．施法·蓄力·充能/充能系统.ts`（参数含 世界坐标进度UI 开关）。
- 构建：build:files exit 0；进图未验证（无 War3）。报告状态 A1=待验收（R001），已按 5.1~5.6 格式写入执行计划文件。
- 用户确认：本项目玩家不会换英雄 → A1 验收项"玩家换英雄"按不适用处理，死亡监听即完整覆盖英雄离场清理（报告 5.6 已更新）。

## 爱蜜莉雅 A2-A11 全英雄技能实现（用户指示"直接全部完成，问题写报告"）
- 新增文件（20．爱蜜莉雅/）：03 被动（寒意/冻结具名暂停+冰壳/霜裂/碎冰+统一命中结算）、04 普攻联动（契约应和+帕克追击追踪冰弹+QWE冷却缩减）、05 Q（穿晶 onTick 检测+分裂冰刃+终点冰晶+D强化冰弹）、06 W（持续危险区域+二段扇形冰片）、07 E（registerDamageModifier 护盾+冲锋位移+撞墙短惩罚冷却+落点冰晶）、08 D（环绕+3次强化+结束爱蜜莉雅D）、09 R（蓄力世界坐标进度UI+BLSB连接光冰晶读取+最终冰爆+D强化R）、10 表现接入（注册总入口+动作辅助）。
- Buff 表：系统/05．Buff系统/03．Buff表/02．英雄/20．爱蜜莉雅.ts（E00D-E012 六 Buff）+ 英雄 index 聚合。
- 公共封装关键：registerManualBuff/移除单位指定Buff/获取单位Buff层数；添加单位暂停/移除单位暂停（具名冻结）；造成技能伤害/造成批量AOE技能伤害；平台扩展API取值.技能_获取技能当前冷却时间 + 动作.技能_设置技能冷却时间（冷却缩减）；registerDamageModifier/unregisterDamageModifier（护盾吸收）；开始冲锋（撞墙回调→短惩罚冷却）；createUnitEffect/destroyUnitEffect（按key护盾/环绕特效）；施加快速减速Buff（C007 控制Buff）；创建世界坐标进度UI（跟随单位+登记A1统一销毁）；弹道编排工厂 onTick 透传 + 新增 获取弹道当前位置 导出。
- 普攻判定范式：snapshot.isNormalAttack===true && !isWrappedSkillDamage && originalAttacker===attacker（安斯艾尔先例）。
- 构建：全项目 npm run build 通过（exit 0，fix-lua 372）；中途多次瞬时文件锁重跑即过；一次真错（函数名含空格"减少最长QW E冷却"）已修。
- 报告：执行计划文件更新 R002（A2-A9 待验收、A10 未执行-无进图环境、A11 已交付）；数值全部标 待平衡；动作索引 4/5/8/9/13 候选；Buff 物编/图标 BLP 未做登记已知问题。

## 爱蜜莉雅验收反馈修复（R002.1，6 项运行时逻辑问题）
- Q：主冰矢去掉 弹道伤害值（由 on命中 统一结算）→ 消除双重扣血。
- R：读条加 100ms 周期更新世界坐标进度UI + 蓄力完成 销毁爱蜜莉雅进度UI（A1 摘除）；冰晶爆发延迟回调登记到 H-01 控制器（打断/死亡不再结算）。
- E：护盾吸收返回 currentDamage - 吸收（护盾100/伤害300 → 剩余200 继续结算）。
- W：区域创建后延迟 0.25s 结算创建伤害（登记实例）；引入 最后单位列表（on周期刷新）供 on销毁/二段结算（区域销毁后当前区域内单位不可靠）。
- D：D到期回调表（句柄→ID），重复开启先取消旧回调；结束爱蜜莉雅D 统一取消挂起回调。
- 构建：build:files 5 文件 + 全项目 npm run build 通过（exit 0，fix-lua 372）。

## 爱蜜莉雅第二轮验收反馈修复（R002.2，6 项机制缺失）
- R 蓄力改用通用 开始充能（指令中断/硬控中断/死亡中断/进度UI 由充能系统管理）；领域只在 充能完成回调 创建。
- D 强化补全：W 自然结束/二段后保留小冰晶（"D强化"来源）；E 落点保护脉冲（友军 registerDamageModifier 短护盾）。
- Q 帕克冰弹：先判发射条件再消费（无目标→直线弹），点地穿晶不再白扣强化。
- W/R 结束伤害改 结束时点实时快照（GroupEnumUnitsInRange+敌人过滤），删除最后单位列表依赖。
- 物编 Emilia.lua 补 cooldown/manaCost（Q5/60 W9/80 E10/70 R70/140 D15/40，待平衡）；TS 配置同步。
- W/E 二段：复用 通用二段壳 ASW2/ASE2（物编已全局创建）+ 25．限时二段技能壳（按钮切换+窗口自动恢复）；注册 ASW2/ASE2 技能壳监听 → 二段引爆/提前结束。
- 构建：build:files 5 文件 + 全项目 npm run build 通过（exit 0，fix-lua 372）。

## 爱蜜莉雅第三轮验收反馈修复（R002.3，5 项运行时机制错误）
- R 指令中断：充能系统 `指令中断: !强制硬直 && 参数.指令中断 === true` → 强制硬直会强制关闭指令中断。R 移除 `强制硬直: true`，保留 指令中断: true（移动/点/目标指令可中断）；硬控中断由充能系统「单位是否处于硬控制效果合集」tick 检测，与强制硬直无关。停止充能仅 `强制硬直 && 来源非空` 才移除暂停 → 无硬直安全。
- W 二段方向：ASW2 是 DivineShield 瞬发输入壳（duration 0.001）无目标点，GetSpellTargetX/Y 无效 → 改 `jass.GetUnitFacing(施法者) * 0.01745329252`（英雄朝向）。
- W 自然结束 vs 打断/死亡：数据加 `结束原因`（H-01 结束回调写入原因+销毁区域置空）；on销毁 仅 `!已二段 && 结束原因==null`（区域自然到期）才结算绽放/寒意/D强化小冰晶；打断/死亡只清理。
- E 实例结束回调补全（与 结束E护盾分支 幂等）：已结束置位、停止位移(位移ID,"中断")、清理 ASE2、移除冰晶护身 Buff。
- E 破盾注销延迟：回调内 unregisterDamageModifier 会 splice 扰乱 applyDamageModifiers 遍历（下一项左移跳项）→ 破盾收口改 addDelayedCallback(0, ...) 下一 tick 执行。
- 构建：build:files 3 文件 + 全项目 npm run build exit 0（fix-lua 372）；Lua 抽查确认 5 处形态（指令中断无强制硬直键/GetUnitFacing/结束原因判断/停止位移/延迟收口）。
- 报告：5.9 节 R002.3 修复记录。仍需 A10 进图实机验收。

## 爱蜜莉雅第四轮验收反馈修复（R002.4，5 项运行时错误）
- W 二段方向：弹道编排工厂 发射方向角 与 GetUnitFacing 同为角度制（`参数.发射方向角 ?? GetUnitFacing(所有者)`）→ 直接 `GetUnitFacing(施法者)`，去掉上轮误加的弧度换算。
- E 结束回调顺序：先 `已结束 = true` 再 `停止位移`（停止位移同步触发位移结束回调，已结束 必须先置位，否则落点冰爆/冰晶/D强化被错误执行）。
- W/R 区域标记泄漏：区域效果 销毁() 只调 onDestroy 后清空集合、不触发 onLeave → onDestroy 开头先遍历 `区域.区域效果.当前区域内单位`（底层清空前有效）逐个取消标记，任一分支都执行。
- R 双蓄力条：充能系统 显示进度条特效 默认 true → 补 `显示进度条特效: false`；补传 `世界坐标进度UI高度偏移: 读条配置.跟随Z偏移`（默认 180，233 此前未生效）。
- R 死亡触发最终冰爆：R领域数据 加 结束原因；R结束 回调写原因+销毁区域置空；onDestroy 结束原因 != null 直接 return 不结算。
- 构建：build:files 3 文件 + 全项目 npm run build exit 0（fix-lua 372）；Lua 抽查 5 处形态确认。
- 报告：5.10 节 R002.4。仍需 A10 进图实机验收。

## 爱蜜莉雅第五轮验收反馈修复（R002.5，4 项运行时问题）
- W/R 区域所有者：区域效果 是否影响目标 在 所有者 为空时对"敌方"也接受全部单位（`if (!所有者) return true`）→ W/R 创建持续危险区域补 `所有者: 施法者`，友军不再被纳入。
- E 主动二段重复结算：结束E护盾分支 已结束=true 统一提前到 停止位移 前（停止位移同步触发位移结束回调，未置位则位移回调落点结算+公共收尾冰爆双份）。
- W/R 区域标记计数：a) 区域首次扫描写集合但不触发 on进入（`!是首次` 条件）→ 创建后 50ms（>默认检测间隔 0.02s）补 标记+减速（登记实例，区域已销毁跳过）；b) W 二段引爆删除取消标记（on销毁 统一一次），标记只由 on进入/on离开/on销毁 管理。
- 视觉随实例清理：W 冰花/寒气、R 领域主体 特效句柄登记 控制器.登记自定义清理（jass.DestroyEffect）；R 蓄力预警法阵句柄 + 充能参数 结束回调（原因≠完成 销毁）。自然到期特效自毁，DestroyEffect 对失效句柄幂等无害。
- 构建：build:files 3 文件 + 全项目 npm run build exit 0（fix-lua 372）；Lua 抽查：所有者/初始标记 50ms/取消标记仅 on离开+on销毁/法阵结束回调/主体登记。
- 报告：5.11 节 R002.5。仍需 A10 进图实机验收。

## 爱蜜莉雅第六轮验收反馈修复（R002.6，2 项问题，含公共组件扩展）
- 计数方案重构：弃用 50ms 补标（竞态：首扫后-50ms 前进入单位被 on进入+补标双 +1）。改为公共区域组件原生支持：区域效果参数新增可选 `首次扫描触发进入`（首次扫描对站桩目标也触发 on进入，默认 false 旧行为不变）；持续危险区域参数新增 `防抖间隔`/`首次扫描触发进入` 透传；W/R 传 `首次扫描触发进入: true, 防抖间隔: 0`（进出防抖条件 >=0 恒真 → 严格对称），删除 50ms 补标回调。
- 特效双销毁：创建点特效传持续秒 → EC_CreateEffect 内置定时器到时销毁 + H-01 DestroyEffect 双销毁。改常驻句柄（不传持续秒=-1）：W 冰花/寒气、R 领域主体 生命周期由实例清理统一管理；R 法阵去持续秒 + 充能结束回调无条件销毁（完成/打断/死亡都会调用）。
- 公共文件改动向后兼容（可选参数默认不变），全项目回归通过。
- 构建：build:files 4 文件 + 全项目 npm run build exit 0（fix-lua 372）；Lua 抽查：区域效果 `(not 是首次 or 参数.首次扫描触发进入==true)` 条件、W/R 参数表、主体/法阵无持续秒。
- 报告：5.12 节 R002.6。仍需 A10 进图实机验收。
