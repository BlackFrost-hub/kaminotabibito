interface:
  display_name: "Syzl Project Rules"
  short_description: "Route work through repo rules and War3 shared UI boundaries."
  default_prompt: "Use $syzl-project-rules to work inside this repository while following its project-specific rules and Warcraft III shared UI constraints first."
