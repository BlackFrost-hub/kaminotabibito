# 联机安全回调整改跟踪

## 文档目的

- 记录最近几轮围绕 `ForGroup`、`TimerStart(..., () => ...)`、联机安全封装做过的代码改动。
- 明确哪些改动 **已 build**、但 **尚未实机测试**。
- 方便后续接手的人或 AI 在出现新 bug 时，先排查这批改动，而不是盲猜问题来源。
- 从本文件开始，**后续每推进一步，都继续追加更新**。

## 当前状态

- 状态：**代码已修改，已通过 `npm run build`，尚未系统实机回归测试**
- 时间：`2026-04-27`
- 背景：本轮主要是在收敛两类联机风险
  - `ForGroup / Enum*` 直接把业务闭包塞进 JASS 回调
  - `TimerStart(..., () => ...)` 直接把匿名闭包塞进 JASS 回调

## 本轮改动总目标

1. 在 `lib` 中提供稳定的 Lua 层遍历封装，减少 `ForGroup` 闭包直进 JASS。
2. 用 `safeTimerStart / safeDestroyTimer` 收敛一发式 timer 回调，减少匿名闭包直进 JASS。
3. 不做全局 monkey patch，不改项目入口运行时语义。
4. 先收主业务链和常用封装，周期性 timer 的中心计时器化单独处理。

## 已完成改动

### 1. 单位组遍历封装

- 新增封装：
  - `TS/lib/扩展函数/封装函数/01．通用工具/04．单位工具.ts`
- 新增能力：
  - `forEachUnitInGroup(group, action)`
- 语义：
  - 使用 `FirstOfGroup + while`
  - 不再把业务 action 直接挂到原生 `ForGroup`
  - 遍历期间用 scratch group 暂存，再恢复原 group

### 2. 已替换的 `ForGroup / EnumItemsInRect` 主链

- `TS/系统/00．核心系统/00．玩家系统/00．英雄注册联动/02．脱战计时.ts`
- `TS/系统/01．单位系统/01．多面板属性/01．核心功能.ts`
- `TS/系统/01．单位系统/02．恢复系统/04．恢复执行.ts`
- `TS/系统/04．伤害系统/01．伤害事件.ts`
- `TS/系统/04．伤害系统/00．伤害计算/03．吸血吸魔.ts`
- `TS/系统/08．任务系统/09．主线配置驱动.ts`
- `TS/lib/扩展函数/BJ函数/08．单位BJ扩展.ts`
- `TS/lib/扩展函数/自定义扩展函数/00．单位相关.ts`
- `TS/lib/扩展函数/Star扩展函数/Star扩展库/06．X库函数.ts`

### 3. 联机安全工具收敛

- 文件：
  - `TS/系统/00．核心系统/07．联机安全工具.ts`
- 已做：
  - 旧的 `safeForGroup` 已删除
  - `ForForce / Enum / Trigger` trampoline 改成模块级具名函数
  - 保留 `safeTimerStart / safeDestroyTimer / safeTriggerAddAction` 等受控封装

### 4. 已替换为 `safeTimerStart / safeDestroyTimer` 的一发式 timer 主链

#### 通用封装

- `TS/lib/扩展函数/封装函数/01．通用工具/02．计时器.ts`
- `TS/lib/扩展函数/封装函数/02．音效系统/02．音效池.ts`
- `TS/lib/扩展函数/封装函数/02．音效系统/04．MP3音效播放.ts`
- `TS/lib/扩展函数/封装函数/07．镜头函数/02．震动计时器.ts`

#### 核心 / 业务系统

- `TS/系统/00．核心系统/00．玩家系统/00．英雄注册联动/00．玩家英雄获取桥接.ts`
- `TS/系统/01．单位系统/00．单位初始化创建/00．主线NPC/01．主线NPC.ts`
- `TS/系统/01．单位系统/01．多面板属性/01．核心功能.ts`
- `TS/系统/02．物品系统/03．物品加工.ts`
- `TS/系统/02．物品系统/04．装备成长.ts`
- `TS/系统/02．物品系统/07．装备提取.ts`
- `TS/系统/03．技能系统/07．技能吟唱条/03．输入.ts`
- `TS/系统/05．Buff系统/01．控制抗性/04．系统初始化.ts`
- `TS/系统/05．Buff系统/02．BuffUI.ts`
  - 这里只收了 **1 秒延迟初始化** 那条一发式 timer
  - **没有**动 `0.1s` 周期刷新主链
- `TS/系统/06．经济系统/00．宝箱系统/02．事件注册.ts`
- `TS/系统/08．任务系统/00．配置表/04．NPC生成器.ts`
- `TS/系统/08．任务系统/02．任务管理器/04．QuestManager.ts`
- `TS/系统/08．任务系统/09．主线配置驱动.ts`
- `TS/系统/09．表现系统/02．对话框系统/17．对话框渲染-Dz与状态.ts`

#### Star 扩展库

- `TS/lib/扩展函数/Star扩展函数/Star扩展库/03．硬直暂停系统.ts`
- `TS/lib/扩展函数/Star扩展函数/Star扩展库/04．快速Buff系统.ts`
- `TS/lib/扩展函数/Star扩展函数/Star扩展库/05．移动速度突破系统.ts`

## 当前明确未做的部分

### 1. BuffUI 周期 timer 还没重构

- 文件：
  - `TS/系统/05．Buff系统/02．BuffUI.ts`
- 当前残留：
  - `jass.TimerStart(_refreshTimer, 0.1, true, () => { ... })`
- 原因：
  - 这不是简单替换 `safeTimerStart` 就完事的问题
  - 它涉及 `BuffUI` 本地刷新链、N 槽/半 N 槽设计、以及后续是否要接中心计时器
- 结论：
  - **这是已知残留，不属于漏改**

### 2. 测试系统文件还没统一收

- 当前扫描残留主要在：
  - `TS/系统/12．测试系统/**`
- 这些文件不在本轮优先范围

## 回归测试重点

如果后续测试出问题，优先检查下面几类：

1. **一发式延迟逻辑不再触发**
- 重点看：
  - 英雄注册桥接延迟重试
  - 主线 NPC 延迟创建
  - 任务时间限制到期
  - NPC 头顶任务标记延迟挂载
  - 装备成长的临时属性回滚

2. **计时器销毁时机变化**
- 因为一批逻辑从 `DestroyTimer(timer)` 改成了 `safeDestroyTimer(timer)`
- 如果有遗漏的外部直接 `DestroyTimer`，可能出现 registry 不一致或重复销毁判断问题

3. **旧逻辑依赖匿名闭包捕获上下文**
- 当前替换大多仍保留了 Lua 侧闭包，但闭包已不再直接进 JASS
- 如果某逻辑本来依赖“原生 timer 回调里特定的捕获时机”，要重点看

4. **对话框 / 音效 / 镜头等表现层**
- 这些系统改动本质是“计时器入口替换”
- 正常情况下不应改业务结果
- 如果出现表现不触发、延迟不对、音效池复用异常，优先排查这轮

## 当前已知结论

- 这轮改动 **已经通过 `npm run build`**
- 当前生产代码里，raw `jass.TimerStart(..., () => ...)` 残留基本只剩：
  - `BuffUI` 的周期刷新主链
  - `TS/系统/12．测试系统/**`

## 后续维护规则

从这份文档开始，后续每一步都补：

1. 改了哪些文件
2. 为什么改
3. 哪些部分仍未测试
4. 当前残留点是什么
5. build 是否通过

## 本轮记录

### 记录 2026-04-28 / 第 14 轮

- 完成：
  - `BuffUI` 相关的玩家选中单位事件中心，改成“英雄注册桥接派发注册”
  - 不再在事件中心初始化时一次性为 `0..15` 全量注册选中/取消选中监听
- 本轮已改文件：
  - `TS/系统/00．核心系统/01．事件中心/05．玩家选中单位事件中心.ts`
  - `TS/系统/00．核心系统/00．玩家系统/00．英雄注册联动/00．玩家英雄获取桥接.ts`
  - `TS/系统/05．Buff系统/02．BuffUI.ts`
- 本轮处理内容：
  - 事件中心改为“按玩家注册”，由英雄注册桥接在拿到 `owner` 后派发
  - 保留桥接层原有 UI 注册保护，同时在事件中心内部按 `playerId` 做重复保护
  - `BuffUI` 自身不再主动全量初始化选中事件中心
- 当前风险提醒：
  - 这一步改变了选中事件监听的注册路径
  - 如果后续出现“某玩家选中单位但 BuffUI 不刷新”，优先检查该玩家是否已经走到英雄注册桥接

### 记录 2026-04-27 / 第 1 轮

- 完成：
  - `ForGroup` Lua 层遍历封装
  - 主业务链 `ForGroup` 替换
  - 一批一发式 timer 改为 `safeTimerStart / safeDestroyTimer`
  - 对话框层 `dzTimerStart` 改走联机安全工具
- 未完成：
  - `BuffUI` 周期 timer
  - 测试系统 timer 收敛
- 验证：
  - `npm run build` 已通过
- 风险状态：
  - **已 build，未系统回归测试**

### 记录 2026-04-27 / 第 2 轮

- 完成：
  - 开始收生产代码中的高风险随机路径
  - 先不做全局 `math` monkey patch，改走业务层显式 JASS 数学/随机函数
  - 已根据根目录 `jass表.txt` 确认可直接使用：
    - `GetRandomInt`
    - `GetRandomReal`
    - `Sin`
    - `Cos`
    - `Atan2`
    - `SquareRoot`
    - `Pow`
- 本轮已改文件：
  - `TS/系统/01．单位系统/单位狂暴.ts`
  - `TS/系统/02．物品系统/03．物品加工.ts`
  - `TS/系统/02．物品系统/04．装备成长.ts`
  - `TS/系统/02．物品系统/05．装备掉落.ts`
  - `TS/系统/02．物品系统/07．装备提取.ts`
  - `TS/系统/06．经济系统/00．宝箱系统/01．宝箱掉落配置.ts`
  - `TS/系统/08．任务系统/00．配置表/05．NPC初始化动作.ts`
- 本轮处理内容：
  - 删除了 `装备掉落.ts` 里的 `math.randomseed(...)` 初始化块
  - 把掉落、提取、加工、成长、库存随机移除、概率刷怪等逻辑改成 `jass.GetRandomInt / GetRandomReal`
  - 把宝箱掉落里 `sort(() => math.random() - 0.5)` 改成显式 Fisher-Yates 洗牌
- 未完成：
  - 三角 / 距离 / 对数类关键路径还没继续统一审计
  - `BuffUI` 周期 timer 仍未处理
- 当前风险提醒：
  - 这一步直接影响随机掉落结果、随机提取结果、宝箱掉落顺序和随机库存移除结果
  - 如果后续测试出现“掉落/抽取结果和之前不一致”，优先回看这一轮

### 记录 2026-04-27 / 第 3 轮

- 完成：
  - 开始收联机关键路径中的 `Math.cos / sin / atan2 / sqrt`
  - 仍然不做全局 `math` 重载，只改高风险业务调用点
- 本轮已改文件：
  - `TS/lib/扩展函数/Star扩展函数/GS扩展库/00．极坐标投影.ts`
  - `TS/lib/扩展函数/Star扩展函数/Star扩展库/05．移动速度突破系统.ts`
  - `TS/lib/扩展函数/Star扩展函数/Star扩展库/06．X库函数.ts`
  - `TS/系统/03．技能系统/06．AI自动使用技能/02．工具函数.ts`
  - `TS/系统/06．经济系统/00．宝箱系统/03．宝箱核心.ts`
  - `TS/系统/06．经济系统/01．杀敌金币平分/01．核心功能.ts`
- 本轮处理内容：
  - 把极坐标投影、强制位移中的三角运算改成 `jass.Sin / jass.Cos`
  - 把通用距离、AI 距离、金币平分范围判断改成 `jass.SquareRoot`
  - 把宝箱朝向角计算改成 `jass.Atan2`
- 未完成：
  - `BJ` 数学封装库整体仍有 `Math.*` 残留，本轮没有全量改
  - `Math.log` 还没统一处理，因为 `jass表.txt` 里未确认对应原生函数
  - `BuffUI` 周期 timer 仍未处理
- 当前风险提醒：
  - 这一步直接影响位移方向、极坐标投影、AI 距离判断、宝箱朝向角和金币分配范围判断
  - 如果后续测试出现这些方向或范围异常，优先回看这一轮

### 记录 2026-04-27 / 第 4 轮

- 完成：
  - 继续收基础数学库中的明确残留，把 `BJ` 数学封装里已经确认可替换的 `Math.*` 改到 JASS 数学/随机函数。
- 本轮已改文件：
  - `TS/lib/扩展函数/BJ函数/12．数学函数.ts`
- 本轮处理内容：
  - `CosBJ` 改成 `jass.Cos`
  - `SinBJ` 改成 `jass.Sin`
  - `DistanceBetweenPoints` 改成 `jass.SquareRoot`
  - `GetRandomPercentageBJ` 改成 `jass.GetRandomReal`
- 未完成：
  - `BJ` 数学库里的 `tan / acos / asin / atan` 暂未统一处理
  - `Math.log` 仍未处理
  - `BuffUI` 周期 timer 仍未处理
- 当前风险提醒：
  - 这一步会影响所有复用 `CosBJ / SinBJ / DistanceBetweenPoints / GetRandomPercentageBJ` 的旧逻辑
  - 如果后续测试出现角度、距离、随机百分比异常，优先回看这一轮

### 记录 2026-04-27 / 第 5 轮

- 完成：
  - 继续收 `lib` 层数学封装和角度换算残留，统一到 JASS 数学函数或固定常量。
- 本轮已改文件：
  - `TS/lib/扩展函数/BJ函数/00．BJ全局兜底.ts`
  - `TS/lib/扩展函数/BJ函数/12．数学函数.ts`
  - `TS/lib/扩展函数/YDWE函数/03．YDWE_Base.ts`
  - `TS/lib/扩展函数/Star扩展函数/GS扩展库/00．极坐标投影.ts`
  - `TS/lib/扩展函数/Star扩展函数/GS扩展库/01．通用GS函数.ts`
  - `TS/lib/扩展函数/Star扩展函数/Star扩展库/05．移动速度突破系统.ts`
  - `TS/lib/扩展函数/Star扩展函数/Star扩展库/06．X库函数.ts`
  - `TS/lib/扩展函数/物品相关函数/物品叠加函数.ts`
  - `TS/lib/扩展函数/自定义扩展函数/00．单位相关.ts`
  - `TS/lib/扩展函数/封装函数/01．通用工具/06．格式化工具.ts`
  - `TS/lib/扩展函数/封装函数/07．镜头函数/01．镜头震动.ts`
- 本轮处理内容：
  - `BJ` 常量从 `Math.PI` fallback 改成固定常量 fallback
  - `TanBJ / AcosBJ / AsinBJ / AtanBJ` 改为基于 JASS 数学函数实现
  - 多处 `180 / Math.PI`、`Math.PI / 180` 角度换算改成 `bj_*` 常量或固定 fallback 常量
  - `Math.pow` 改成 `jass.Pow`
- 未完成：
  - `TS/lib/扩展函数/YDWE函数/06．护甲获取.ts` 的 `Math.log` 仍未处理
  - `BuffUI` 周期 timer 仍未处理
- 当前风险提醒：
  - 这一步影响 `lib` 层大量复用数学工具、角度换算和部分表现层震动强度计算
  - 如果后续测试出现朝向、距离、扇形判定、格式化精度或镜头震动异常，优先回看这一轮

### 记录 2026-04-27 / 第 6 轮

- 完成：
  - 继续清理 TS 全仓中剩余的非 `log` 数学残留，把零散 `Math.PI / 180`、`180 / Math.PI` 和测试侧 `Math.sqrt` 收掉。
- 本轮已改文件：
  - `TS/系统/01．单位系统/00．单位初始化创建/00．主线NPC/01．主线NPC.ts`
  - `TS/系统/08．任务系统/00．配置表/04．NPC生成器.ts`
  - `TS/系统/06．经济系统/00．宝箱系统/03．宝箱核心.ts`
  - `TS/系统/12．测试系统/STES事件测试.ts`
- 本轮处理内容：
  - NPC 朝向弧度换算改成固定 `BJ_DEGTORAD` fallback
  - 宝箱角度换算 fallback 改成固定 `BJ_RADTODEG`
  - 测试代码里的 `Math.sqrt` 改成 `jass.SquareRoot`
- 当前唯一保留项：
  - `TS/lib/扩展函数/YDWE函数/06．护甲获取.ts`
  - 原因：`Math.log` 在 `jass表.txt / japi表.txt` 未确认可替代原生函数，当前不做近似替换，避免把护甲反算公式改坏。
- 当前风险提醒：
  - 这一步影响主线 NPC 朝向、NPC 配置生成朝向、宝箱角度换算和 STES 测试公式
  - 如果后续测试出现这些方向或角度异常，优先回看这一轮

### 记录 2026-04-27 / 第 7 轮

- 完成：
  - 继续补齐 `BJ` 数学库里漏掉的整数取模实现，去掉该处 `Math.floor`。
- 本轮已改文件：
  - `TS/lib/扩展函数/BJ函数/12．数学函数.ts`
- 本轮处理内容：
  - `ModuloInteger` 从 `Math.floor` 改为 `jass.R2I`
- 当前唯一保留项：
  - `TS/lib/扩展函数/YDWE函数/06．护甲获取.ts` 的 `Math.log`
- 当前风险提醒：
  - 这一步只影响复用 `ModuloInteger` 的逻辑
  - 如果后续测试出现取模异常，优先回看这一轮

### 记录 2026-04-27 / 第 8 轮

- 完成：
  - 开始系统收 `Math.floor`，先处理 `lib` 里明确属于“整数运算语义”的一批。
- 本轮已改文件：
  - `TS/lib/扩展函数/BJ函数/12．数学函数.ts`
  - `TS/lib/扩展函数/Star扩展函数/02．GS单位属性.ts`
  - `TS/lib/扩展函数/Star扩展函数/Star扩展库/06．X库函数.ts`
  - `TS/lib/扩展函数/YDWE函数/03．YDWE_Base.ts`
  - `TS/lib/扩展函数/YDWE函数/05．STES子触发公共工具.ts`
- 本轮处理内容：
  - `IAbsBJ / ISignBJ` 改成 `jass.R2I`
  - 英雄属性增量的整数转换改成 `jass.R2I`
  - `X_R2I2` 改成 `jass.R2I(r + 0.5)`
  - `YDWE` 整数除法和整数三元表达式改成 `jass.R2I`
  - `STES` 整数参数读取改成 `jass.R2I`
- 未完成：
  - 仍有一批 `Math.floor` 留在格式化、FourCC 拆分、中心计时器显示、业务文本展示里
  - `Math.log` 仍是唯一数学原语保留项
- 当前风险提醒：
  - 这一步影响 `lib` 层整数转换、属性加点、整数表达式求值和 STES 整数参数读取
  - 如果后续测试出现这些整数结果异常，优先回看这一轮

### 记录 2026-04-27 / 第 9 轮

- 完成：
  - 继续收 `lib` 层基础数学调用，把一批 `Math.abs / Math.max / Math.floor` 改成 `BJ` 数学封装或 `jass.R2I`。
- 本轮已改文件：
  - `TS/lib/扩展函数/BJ函数/12．数学函数.ts`
  - `TS/lib/扩展函数/BJ函数/07．杂项.ts`
  - `TS/lib/扩展函数/Star扩展函数/GS扩展库/01．通用GS函数.ts`
  - `TS/lib/扩展函数/Star扩展函数/Star扩展库/03．硬直暂停系统.ts`
  - `TS/lib/扩展函数/封装函数/04．硬件输入/03．鼠标函数.ts`
  - `TS/lib/扩展函数/封装函数/03．漂浮文字/03．创建漂浮文字.ts`
  - `TS/lib/扩展函数/封装函数/01．通用工具/01．FourCC转换.ts`
  - `TS/lib/扩展函数/封装函数/01．通用工具/06．格式化工具.ts`
- 本轮处理内容：
  - `RAbsBJ / IAbsBJ` 不再依赖 `Math.abs`
  - `OrderIdToString / fourCCToString` 的字节拆分改成 `jass.R2I`
  - `GS` 扇形判定改用 `RAbsBJ`
  - `硬直暂停`、`鼠标滚动条行程`、`漂浮文字 ticks` 改用 `RMaxBJ`
  - 格式化工具中的取整改成 `jass.R2I`
- 未完成：
  - 仍有业务层和中心计时器里的 `Math.floor / max / min`
  - `Math.log` 仍是唯一数学原语保留项
- 当前风险提醒：
  - 这一步影响字节拆分、GS 扇形判定、暂停时间裁剪、鼠标滚动条行程、漂浮文字生命周期和数字格式化
  - 如果后续测试出现这些显示或计算异常，优先回看这一轮
### 记录 2026-04-27 / 第 10 轮
- 完成：
  - 继续把系统层和任务 UI 层残留的 `Math.max / min / abs / floor`、`math.floor` 全部收掉。
  - 当前 `TS/**/*.ts` 复扫结果里，这几类调用已经清零。
- 本轮已改文件：
  - `TS/系统/03．技能系统/05．动态技能说明/02．公式解析器.ts`
  - `TS/系统/03．技能系统/06．AI自动使用技能/01．核心功能.ts`
  - `TS/系统/03．技能系统/07．技能吟唱条/02．渲染.ts`
  - `TS/系统/05．Buff系统/01．控制抗性/02．控制时间计算.ts`
  - `TS/系统/05．Buff系统/02．BuffUI.ts`
  - `TS/系统/06．经济系统/01．杀敌金币平分/01．核心功能.ts`
  - `TS/系统/06．经济系统/01．杀敌金币平分/02．金币获取回调.ts`
  - `TS/系统/06．经济系统/01．杀敌金币平分/02．金币获取率加成.ts`
  - `TS/系统/08．任务系统/01．任务数据.ts`
  - `TS/系统/08．任务系统/09．主线配置驱动.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/03．任务UI列表与滚动.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/09．任务UI列表控制.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/10．任务UI滚动与滚轮.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/11．任务UI列表控制辅助.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/12．任务UI管理器.ts`
  - `TS/系统/09．表现系统/03．UI属性系统/01．属性工具.ts`
  - `TS/系统/12．测试系统/STES事件测试.ts`
  - `TS/系统/02．物品系统/11．装备系统.ts`
- 本轮处理内容：
  - 整数取整统一改成 `jass.R2I`。
  - 夹逼和上下限逻辑统一改成显式比较或局部 `clamp` helper，不再依赖 `Math.max / min`。
  - 任务 UI、属性 UI、BuffUI 的显示格式化和分页夹逼逻辑一起收口。
- 当前唯一保留项：
  - `TS/lib/扩展函数/YDWE函数/06．护甲获取.ts` 的 `Math.log` 两处。
  - 原因：`jass表.txt / japi表.txt` 未确认可替代原生对数函数，暂不做近似替换。
- 当前风险提醒：
  - 这一步影响 AI 周期检查间隔、吟唱条倒计时显示、Buff 剩余时间文本、金币平分、任务 UI 分页夹逼、属性 UI 数字格式化。
  - 如果后续测试出现这些界面的数字显示、分页滚动或收益结算异常，优先回看这一轮。
### 记录 2026-04-27 / 第 11 轮
- 完成：
  - 修复进图即报的 `critical dependency` 循环依赖。
- 本轮已改文件：
  - `TS/lib/扩展函数/封装函数/01．通用工具/04．单位工具.ts`
- 本轮处理内容：
  - `isSpecialUnit` 不再通过 `BJ函数.index` 间接调用 `IsUnitIllusionBJ`，改为直接使用 `jass.IsUnitIllusion`。
  - 切断依赖链：`08．单位BJ扩展 -> 通用工具.index -> 04．单位工具 -> BJ函数.index -> 08．单位BJ扩展`。
- 当前风险提醒：
  - 这一步直接影响进图初始化时的模块加载顺序。
  - 如果后续仍有类似 `critical dependency`，优先继续排查 `index.ts` 聚合导出和跨层互相 `require`。
### 记录 2026-04-27 / 第 12 轮
- 完成：
  - 在本地通用数学库里新增 `ceil` 和 `round`，并把全仓 `Math.ceil / Math.round` 主链调用收掉。
- 本轮已改文件：
  - `TS/lib/扩展函数/封装函数/01．通用工具/07．数学运算.ts`
  - `TS/lib/扩展函数/YDWE函数/00．YDWE函数.ts`
  - `TS/系统/02．物品系统/04．装备成长.ts`
  - `TS/系统/03．技能系统/05．动态技能说明/02．公式解析器.ts`
  - `TS/系统/03．技能系统/07．技能吟唱条/02．渲染.ts`
  - `TS/系统/04．伤害系统/04．伤害显示/02．核心功能.ts`
  - `TS/系统/06．经济系统/00．宝箱系统/03．宝箱核心.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/10．任务UI滚动与滚轮.ts`
  - `TS/系统/09．表现系统/03．UI属性系统/01．属性工具.ts`
- 本轮处理内容：
  - `ceil(value)`：用 `jass.R2I` + 进位逻辑实现。
  - `round(value)`：用 `jass.R2I(value + 0.5)` / 负数对称逻辑实现。
  - 所有 tick 计算、分页 round、量化 round 均改为走本地库，不再依赖 Lua `math.ceil / math.round`。
- 当前风险提醒：
  - 这一步影响周期 tick 计算、装备成长金币变化、公式解析器格式化、任务 UI 拖拽分页、属性 UI 量化显示。
  - 如果后续测试出现这些数值边界异常，优先回看这一轮。
### 记录 2026-04-28 / 第 13 轮
- 完成：
  - 把重复出现的 `clampMin / clampRange / max / min` 收进本地数学库。
  - 开始删业务层和 UI 层的本地重复 helper，统一走 `07．数学运算.ts`。
- 本轮已改文件：
  - `TS/lib/扩展函数/封装函数/01．通用工具/07．数学运算.ts`
  - `TS/系统/00．核心系统/05．中心计时器.ts`
  - `TS/系统/03．技能系统/07．技能吟唱条/02．渲染.ts`
  - `TS/系统/05．Buff系统/02．BuffUI.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/03．任务UI列表与滚动.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/09．任务UI列表控制.ts`
  - `TS/系统/08．任务系统/04．任务UI拆分/10．任务UI滚动与滚轮.ts`
  - `TS/系统/09．表现系统/03．UI属性系统/01．属性工具.ts`
  - `TS/系统/12．测试系统/STES事件测试.ts`
- 本轮处理内容：
  - 新增 `clampMin(value, minValue)`
  - 新增 `clampRange(value, minValue, maxValue)`
  - 新增 `max(a, b)` / `min(a, b)`
  - 删除上述文件里的本地重复实现，改成统一导入
- 未完成：
  - `BuffUI` 周期链整改仍未开始
  - `Math.log` 保留项仍在 `TS/lib/扩展函数/YDWE函数/06．护甲获取.ts`
- 当前风险提醒：
  - 这一轮影响分页裁剪、倒计时下限裁剪、属性显示数值格式化、中心计时器延时裁剪和 STES 测试公式
  - 如果后续测试出现这些边界数值异常，优先回看这一轮

### 记录 2026-04-28 / 第 14 轮
- 完成：
  - 把“玩家选中单位”收成真正的事件中心模式：一个 selected trigger、一个 deselected trigger、一个监听器列表、顺序派发。
  - 对话框系统不再自己创建选择 trigger，改为订阅公共选择事件中心。
- 本轮已改文件：
  - `TS/系统/00．核心系统/01．事件中心/05．玩家选中单位事件中心.ts`
  - `TS/系统/09．表现系统/02．对话框系统/10．选择触发入口.ts`
- 本轮处理内容：
  - 选择事件中心新增 `addSelectionListener/removeSelectionListener`
  - 选择状态维护与事件派发合并到同一中心
  - BuffUI 继续只读中心状态；对话框系统改为监听中心派发，不再重复挂选择 trigger
- 当前风险提醒：
  - 这一轮影响 BuffUI 和对话框系统的“选中单位”来源
  - 如果后续测试出现“选中 NPC 不开对话”“多选后 BuffUI 行为异常”“取消选中后状态不清”等问题，优先回看这一轮

### 记录 2026-04-28 / 第 15 轮
- 完成：
  - `BuffUI` 联机整改主线已实际落地，不再处于“待施工”状态。
  - `BuffUI` 当前结构已经收成：选择事件中心 + `BuffUIViewModel` + `BuffUI` 本地渲染壳。
- 当前落地状态：
  - 选中单位来源已改为公共选择事件中心：
    - `TS/系统/00．核心系统/01．事件中心/05．玩家选中单位事件中心.ts`
  - `BuffUI` 主链已不再本地扫选中组：
    - `TS/系统/05．Buff系统/02．BuffUI.ts`
  - Buff 行数据与 tooltip 文本已迁到 ViewModel 层：
    - `TS/系统/05．Buff系统/04．BuffUIViewModel.ts`
  - 本地路径中的 `CreateGroup / GroupEnumUnitsSelected / DestroyGroup` 已移出主链。
- 对 `TS/系统/05．Buff系统/规划-BuffUI联机整改.md` 的结论：
  - 该文件不再适合作为“施工文档”继续执行。
  - 现在更适合作为历史整改记录保留，或改名归档后保留。
  - 如果仓库倾向只保留当前有效文档，可以删除；删除前提是以后以本跟踪文档和现代码为准。
- 当前风险提醒：
  - BuffUI 这条线后续再出问题，优先回看：
    - 选择事件中心是否正确维护“恰好单选”
    - `BuffUIViewModel` 是否对目标单位正确产出 `visible/icon/remain/tooltip`
    - `BuffUI` 的 tooltip 是否继续保持“手动 hover show/hide”，不要再混用引擎 tooltip 机制
