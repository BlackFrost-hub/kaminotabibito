# 主线配置兼容报告

更新时间：自动生成初版

## 已实现

- 配置驱动入口：`10．主线配置驱动.ts`
- 条件判定：按 `condition` 动态求值
- 阶段推进：`fromStage` / `toStage` 写入 `YDUserData(剧情进度/整数)`
- 对话发送：`TransmissionFromUnitWithNameBJ`，并带自动等待时长
- 时间轴执行：`actionTimeline` 按 `X. 指令` 调度，支持 `call` / `set` 基础语法
- UI联动：实时刷新 `main_story_runtime` 任务描述

## 需要补齐（项目内无明确实现或无同功能封装）

以下函数在主线 `actionTimeline`/`condition` 中出现频率高，但项目内暂无稳定封装或无法确认可用：

- `YDLocalSet`
- `SDR_DebugTimer`
- `EC_CreateEffect`
- `StarOther_PanCameraToTimedUnitForPlayer`
- `GS_PolarProjectionBJ`
- `TriggerRegisterUnitInRangeSimple`

## 说明

- `QuestMessageBJ`、`CreateTimer`、`TimerStart`、`CreateUnit` 等大量函数属于引擎/JASS侧函数，通常可直接调用。
- 上述“需要补齐”项建议逐个补桥接函数，补完后可降低运行期 `action执行失败` 日志。
