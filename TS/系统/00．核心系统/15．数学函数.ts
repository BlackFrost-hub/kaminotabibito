// ========== 虚拟分区：基础数学 ==========
export function IMaxBJ(a: number, b: number): number {
  return a >= b ? a : b;
}

export function IMinBJ(a: number, b: number): number {
  return a <= b ? a : b;
}

export {};

