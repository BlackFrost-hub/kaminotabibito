/**
 * 主线引导 UI 入口
 *
 * 迁移自 JASS\世界地图\主线剧情\主线引导.j（ZXRW01 + ZXRW02）
 *
 * 结构：
 *  - 00．主线引导类型.ts：类型定义
 *  - 01．主线引导配置表.ts：剧情进度 -> 文本/镜头配置表
 *  - 02．主线引导框架.ts：DzFrame UI 创建、frame 句柄模块变量
 *  - 03．主线引导执行.ts：点击回调与执行逻辑
 *
 * 规则：
 *  - UI 全局创建，不放 GetLocalPlayer 内创建
 *  - 按钮点击注册必须 sync=true
 *  - Dz 回调必须是模块级具名函数，不允许匿名闭包/箭头函数
 *  - 配置表驱动"剧情进度 -> 提示文本 / 镜头目标"
 *  - 文本来源：文本常量3.wts STRING 293~317
 */

import { 创建主线引导帧, 创建主线引导按钮 } from "./02．主线引导框架";
import { on主线引导按钮点击 } from "./03．主线引导执行";

/**
 * 初始化主线引导 UI
 * 创建 UI 帧并注册按钮点击回调（sync=true）
 */
export function 初始化主线引导UI(this: void): void {
  创建主线引导帧();
  创建主线引导按钮(on主线引导按钮点击);
}
