# 47 -> 48 封印核心场景落地记录

> 用途：记录本轮已确认的 47 -> 48 场景关系、坐标和实现边界，后续制作或审查先读本文件。
>
> 当前状态：47 已接入；47 死亡后通往 48 的内层墓门传送点、传送落点一次性抵达对白、A 入口监听、48 纯对白和七色光束演出已接入 TS。三锚点稳定机制仍未实现。

## 一、进度边界

- 本记录只覆盖 `47 -> 48`，不要在本轮擅自扩展到 49。
- 47 为沉睡英魂·亚伦柯斯 Boss 战。
- 亚伦柯斯死亡后才进入 48；死亡承接需要先清理 47 的死亡监听、范围监听、暂停/无敌状态和运行时记录。
- 48 进入真正封印核心，但本节点不启动里科特 Boss 战，只进行里科特、教皇和奥斯特利一世的剧情演出。

## 二、47 结束位置

| 对象 | X | Y | Z | 面向 |
| --- | ---: | ---: | ---: | ---: |
| 亚伦柯斯死亡点 | 8322.9 | -14452.7 | 未记录 | 270° |

死亡后的后续动作：

1. 释放亚伦柯斯的死亡事件和进入范围事件，不再继续监听。
2. 清理本段剧情创建的定时器、暂停/无敌状态和运行时引用。
3. 开启通往 48 的内层墓门。

实现状态：已由亚伦柯斯死亡监听调用 `第三章_开启亚伦柯斯战后传送门`，不会在地图初始化时永久注册。

## 三、墓门与传送

| 对象 | X | Y | Z | 面向 | 备注 |
| --- | ---: | ---: | ---: | ---: | --- |
| 进入 48 的墓门 | 8389.6 | -12280.9 | 未记录 | 270° | 玩家从此处进入内层 |
| 玩家传送落点 | 10641.8 | -9804.5 | 256.0 | 90° | 传送后仍需沿右侧道路步行 |

- 使用现有传送门特效：`imports\Common\Effect\Form\Portal\RicketSecretRoomShift.mdx`。
- 已接入 `TS\系统\07．地形系统\03．区域传送.ts` 的 `注册剧情玩家组传送`，由地形系统统一创建区域、监听玩家英雄进入、传送并清理区域/触发器。
- 入口半径：`420`；只允许玩家英雄组单位触发，触发一次后立即取消监听并销毁传送门特效。
- 传送完成后玩家抵达传送落点，不会立即到达封印核心，必须继续沿道路走到 48 场景入口 A。
- 传送完成后地形系统将玩家英雄组统一设置为 `90°`，随后玩家仍需沿右侧道路步行到 A。
- 传送完成后只播放一次抵达感知对白：`这里就是封印核心吧……我能感觉到前方传来强烈的能量波动。里科特和分离教派，应该就在前面。`；对白结束或 Esc 后清理玩家运行时引用。

## 四、48 场景固定点位

用户已确认 A-E 的对应关系如下。坐标已确认；面向依据场景图和坐标按 Warcraft 角度约定推算，并集中维护在运行时站位表。

| 标记 | X | Y | Z | 语义与参与者 |
| --- | ---: | ---: | ---: | --- |
| A | 2480.1 | -9696.2 | 221.0 | 入口，面向 E：178.32° |
| B | 1635.4 | -10330.4 | 260.6 | 玩家队伍对白站位，面向 E：134.20° |
| C | 443.5 | -9340.2 | 236.3 | 里科特待战站位，面向 B：320.28° |
| D | 1314.7 | -9198.5 | 244.4 | 教皇待战站位，面向 B：285.82° |
| E | 975.9 | -9652.2 | 250.8 | 奥斯特利一世与中央封印核心，面向 B：314.20° |

场景进入顺序：

1. 玩家从传送落点沿道路步行到 A。
2. 到达 A 后，触发封印核心入场对白。
3. 对白中的说话者必须已经在当前场景存在、存活，并处于对应站位；不能让地图其他区域的单位隔空说话。
4. 里科特、教皇和奥斯特利一世只参与剧情，不在本节点开启战斗。

## 四-A、49 机制战锚点（坐标已记录，暂不实装）

49 是封印核心内的普通机制战，不是 Boss 战。三个锚点位于同一封印核心大厅，按用户最新三张截图的顺序记录如下：

> 后续已确认的五分钟守卫战、精英波次、锚点自动修复与 49 -> 50 原地收束规则，统一维护在 [49-50 封印守卫战落地记录](07．49-50封印守卫战落地记录.md)。本节只保留坐标来源，不再继续追加节点 49 的实现细节。

| 锚点 | X | Y | Z | 当前语义 | 面向 |
| --- | ---: | ---: | ---: | --- | --- |
| 1 | 959.6 | -9016.9 | 230.3 | 北侧内环 | 待确认 |
| 2 | 396.7 | -10096.3 | 252.5 | 西南内环 | 待确认 |
| 3 | 1588.6 | -10146.2 | 264.4 | 东南内环 | 待确认 |

- 三点围绕中央 E 封印核心组成三角形；E 本身不作为锚点。
- 七色晶体仍是外环能量来源，不改成锚点。
- 本段只记录坐标，不在本轮新增单位、触发器或机制代码；待机制规则和面向确认后再落地。

## 五、作废标注

- 旧标注中的 F“钥匙共鸣晶体台”作废，不得继续作为唯一晶体台配置。
- 图 6、图 7 是上下拼接的同一场景，编辑器截图无法一次显示完整区域；周围存在多座晶体台。
- 中央白色能量核心仍对应 E。后续若制作七色水晶或同魂晶事件，应以完整场景为准，不要把图 6/7 的单个黄色晶体台误当成 F。

参考标注图：

- `C:\Users\Administrator\Desktop\syzl\tmp\48场景标注\48封印核心场景标注图1.png`
- `C:\Users\Administrator\Desktop\syzl\tmp\48场景标注\48封印核心场景标注图2.png`

## 六、七色晶体坐标与闪电演出记录

用户按图片顺序确认七座晶体的颜色和坐标。表中的原始 Z 是地图中晶体锚点高度；演出发射点统一使用 `Z=1700.0`，不改变晶体本体的位置。

| 编号 | 颜色 | X | Y | 原始 Z | 演出发射 Z |
| --- | --- | ---: | ---: | ---: | ---: |
| 1 | 红 | 2630.6 | -8719.3 | 180.9 | 1700.0 |
| 2 | 粉 | 1148.2 | -7998.1 | 131.3 | 1700.0 |
| 3 | 黄 | -337.3 | -8510.6 | 125.1 | 1700.0 |
| 4 | 黑 | -838.9 | -9768.8 | 118.7 | 1700.0 |
| 5 | 绿 | -377.1 | -10973.8 | 136.3 | 1700.0 |
| 6 | 金 | 1141.3 | -11431.3 | 178.9 | 1700.0 |
| 7 | 青蓝白 | 2639.2 | -10680.0 | 191.2 | 1700.0 |

### 闪电连接方案

- 每座晶体以自己的 X/Y 和 `Z=1700.0` 作为高空发射端。
- 七条闪电都指向中央 E 点：`975.9, -9652.2, 250.8`。
- 当前版本使用一次创建后持续存在的七个闪电句柄保持演出，不额外启动 Tick；不要把七个坐标改写成一个圆周估算值。
- 红、粉、黄、黑、绿、金、青蓝白保持独立颜色语义；如果现有资源没有纯黑或金色变体，先记录资源缺口，不用相近颜色偷偷替代。
- 剧情开始时统一创建闪电句柄和计时器；对白结束、Esc 跳过、场景离开或封印事件结束时统一销毁，不能让计时器或特效永久运行。
- 这七个坐标是晶体与演出锚点，不是旧标注 F；F“钥匙共鸣晶体台”仍然作废。

### 闪电代码资源确认

资源表位于 `TS\系统\03．技能系统\00．技能模板+函数\02．通用函数\17．闪电效果代码.ts`。每组代码按“新闪电 / 细束 / 粒子束 / 光束 / 瞄准激光 / 链条”排列：

| 颜色 | 可直接使用的代码组 |
| --- | --- |
| 红 | `RENL` / `RESB` / `REPA` / `REBM` / `RETL` / `RECH` |
| 黄 | `YENL` / `YESB` / `YEPA` / `YEBM` / `YETL` / `YECH` |
| 绿 | `GRNL` / `GRSB` / `GRPA` / `GRBM` / `GRTL` / `GRCH` |
| 蓝 | `BLNL` / `BLSB` / `BLPA` / `BLBM` / `BLTL` / `BLCH` |
| 白 | `WHNL` / `WHSB` / `WHPA` / `WHBM` / `WHTL` / `WHCH` |
| 粉 | `PNBM`（新增 Beam 贴图） |
| 黑 | `BKMB`（新增可见深黑 Beam 贴图） |
| 金 | `GDBM`（新增 Gold Beam 贴图） |
| 青蓝白 | `CWBM`（新增 Cyan White Beam 贴图） |

- 新增贴图统一从 `SBL_WhiteBeam.blp` 派生，保留原始 256×64 Beam 的核心亮线、边缘衰减和 alpha/混合结构；正式资源位于 `imports\Common\Effect\Lightning\Texture`。
- 黑色使用可见深黑版本，不使用纯黑染色，避免核心完全消失；其余三色为独立贴图，不依赖运行时 `SetLightningColor`。
- 七色长距离连接优先使用光束变体：红 `REBM`、粉 `PNBM`、黄 `YEBM`、黑 `BKMB`、绿 `GRBM`、金 `GDBM`、青蓝白 `CWBM`。

## 七、待补确认项

以下内容没有可靠的现有配置，制作前必须确认或补齐：

- 教皇：正式 TS 物遍和模型仍待补齐；当前纯对白版本使用“精灵审判官”占位单位，并在场景结束时移除临时创建的占位单位。
- 奥斯特利一世：正式 TS 物遍和模型仍待补齐；当前纯对白版本使用“血精灵守护者”占位单位，并在场景结束时移除临时创建的占位单位。
- A-E 面向：根据用户提供的场景图和坐标按 Warcraft 角度约定推算并写入运行时配置；后续若编辑器实测有偏差，只调整站位表，不复制到对白配置。
- 48 场景中七色水晶的闪电特效资源与 rawcode 已确认；纯对白版本已在对白 10 结束后创建，场景结束或 Esc 时统一清理。

## 八、实现检查清单

- [x] 读取本记录和 `05．封印核心与第三章收束剧情草案.md` 后再修改代码。
- [x] 复用现有主线节点、主线引导、小地图、传送和剧情动作封装。
- [x] 进入 48 时同步任务目标、提示文本、主线引导和进度，不复制出另一套坐标。
- [x] 里科特、教皇、奥斯特利一世在对白开始前完成创建/读取、移动、面向、暂停和无敌设置。
- [x] 中央封印核心和奥斯特利一世保持不可攻击的剧情状态；本节点不启动 Boss 战。
- [x] 支持 Esc 跳过：停止对白和运行时监听，清理临时对象，并把玩家恢复到正常控制。
- [x] 48 对白结束后清理本段运行时对象；后续 49 不在本轮实现。
