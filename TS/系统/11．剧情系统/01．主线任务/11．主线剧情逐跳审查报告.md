# 主线剧情逐跳审查报告

本报告按剧情进度跳转逐轮追加；无剧情进度变化但会影响主线时机的前置事件，单独标为“前置”。

## 前置A：精灵村门口放行（无剧情进度变化）

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村001`
- 触发条件：玩家英雄触发 `JLC精灵村001`，且触发单位位置在 `gg_rct______________077` 内。
- 事件类型：矩形/范围共用触发器内的无进度前置分支，不写 `剧情进度`。

TS对应：
- 入口：`主线剧情矩形入口配置表` 中 `精灵村门口放行 -> gg_rct______________077 -> jlc_elven_village_gate_release`。
- 额外入口：`自然守护者范围入口`、`精灵村初始区域` 也指向同一片段。
- 精灵村守卫复核：旧 JASS 的自然守护者 500 范围入口只负责注册到同一个 `JLC精灵村001` 触发器，真正放行分支仍要求 `RectContainsLoc(gg_rct______________077, 触发单位位置) == true`；因此 TS 的 `自然守护者范围入口 -> jlc_elven_village_gate_release` 不是等价迁移。
- 剧情片段：`jlc_elven_village_gate_release`。
- 动作ID：`JLC精灵村_村口放行前置`、`JLC精灵村_村口任务指引`、`JLC精灵村_解锁村内视野`。
- 专用处理器/通用执行器：`村口放行前置` 有专用处理器；任务指引和视野走通用执行器。

顺序核对：
1. JASS：停止背景音乐 -> 删除 `gg_rct______________077` -> 选取暗夜村区域内自然守护者 -> 自然守护者面向 210 度 -> 玩家英雄停止并面向守护者 -> 三段电影对白 -> 打开 `gg_dest_LTe3_0298` -> 再次停止背景音乐 -> 任务更新消息 -> 小地图 ping。
2. TS：进入 `jlc_elven_village_gate_release` -> `beforeDialog(1)` 删除门禁矩形、停止门卫 -> 三段对白 -> `afterDialog(3)` 打开 `gg_dest_LTe3_0298`、任务消息、小地图 ping -> 添加村内视野。
3. 差异说明：TS 保留了核心放行、开门、任务提示、小地图与视野；已补自然守护者 210 度朝向，并按 JASS 角度让玩家英雄停止且面向守护者；`StopMusic(false)` 未实际迁移；TS 额外给 `自然守护者范围入口` 和 `精灵村初始区域` 也绑定同一片段，触发条件明确比 JASS 的 `rect077` 更宽。

结论：
- 状态：有缺失，但暂不属于阻断级。
- 已修：`JLC精灵村_村口放行前置` 已设置自然守护者固定面向 210 度，并让玩家英雄停止且面向守护者；`JLC精灵村_村口任务指引` 已有小地图闪光点，坐标 `(-29109.7, -27625.6)`，持续 `10` 秒。
- 缺失：`StopMusic(false)`；入口条件比 JASS 更宽，存在重复/提前播放风险。
- 修复验证：`npm run build` 通过；生成 Lua 中玩家英雄遍历使用具名函数回调。
- 建议修复：给通用执行器或专用处理器补 `停止背景音乐`；将 `自然守护者范围入口` 改为不直接播放放行片段，或给入口追加 `gg_rct______________077` 二次条件；复核 `精灵村初始区域` 是否也应移除或改为非剧情触发。

## 进度 0 -> 1 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村001`
- 触发条件：`剧情进度 < 1`，触发单位在 `主线NPC.精灵村长老` 800 范围内，且触发单位属于 `玩家英雄.单位组`。
- 事件类型：单位范围触发，正式推进主线到地精调查任务。

TS对应：
- 入口：`主线剧情单位范围入口配置表` 中 `精灵村长老发布任务 -> 精灵村长老 -> 800 -> jlc_elven_village_elder_quest`，`最高剧情进度: 0`，等价于 JASS 的 `< 1`。
- 剧情片段：`jlc_elven_village_elder_quest`。
- 动作ID：`JLC精灵村_长老对话前置`、`JLC精灵村_帝国勋章共鸣特效`、`JLC精灵村_远古波动奖励`、`JLC精灵村_长老任务物品生成`、`JLC精灵村_发布地精任务`、`JLC精灵村_地精区域显视野`、`JLC精灵村_创建地精祭祀Boss预备`。
- 专用处理器/通用执行器：长老对话前置、远古波动奖励、长老任务物品生成、发布地精任务、地精区域显视野、创建地精祭祀 Boss 预备均有专用处理器；帝国勋章共鸣特效走通用执行器的 `模型路径`。

顺序核对：
1. JASS：村内 `gg_rct________________QY` 显视野 -> 自然传送门 `gg_unit_n025_0372` 归属玩家 6 -> 删除并重设 `udg_FHD=(-26218.60,-28632.40)` -> 在随机点创建金光戒指 -> `StopMusic(false)` -> 设置剧情进度为 1 -> 触发英雄停止并面向长老 -> 长老归属玩家并面向英雄 -> 15 段电影对白，中途播放觉醒特效 -> 全属性 +3 与任务获得提示 -> 创建医疗剂与魔法药水 -> 更新任务描述、Ping 小地图、任务更新提示 -> 地精区域两块视野 -> `StopMusic(false)` -> 创建并冻结地精祭祀 -> 注册 750 范围触发。
2. TS：入口命中后播放 `jlc_elven_village_elder_quest`；`beforeDialog(1)` 执行长老对话前置，能设置剧情进度 1、停止触发单位、触发单位面向长老、长老归属玩家 6、长老面向触发单位；对白 1-4 后，`beforeDialog(5)` 播放觉醒特效；`afterDialog(5)` 给触发英雄全属性 +3 并发获得提示；对白 6-15 后，`afterDialog(15)` 创建两件补给物品、刷新任务 UI/任务消息、小地图信号、地精区域视野、创建并冻结地精祭祀并注册范围触发。
3. 差异说明：TS 主干可推进，不会卡在 `0 -> 1`；对白、奖励、补给、任务提示、小地图、地精区域视野、地精祭祀预创建与 750 范围触发都存在。审查时发现长老对话前置缺村内视野、自然传送门归属、`udg_FHD`、随机金光戒指与两处 `StopMusic(false)`；本轮已补齐。

结论：
- 状态：已修复，通过构建。
- 已修：村内 `gg_rct________________QY` 显视野；自然传送门 `gg_unit_n025_0372` 归属玩家 6；`udg_FHD` 删除旧点并重设到 `(-26218.6, -28632.4)`；按真实物品名随机创建金光戒指；长老对话前置与地精祭祀预创建前各执行一次 `StopMusic(false)`。
- 不缺：入口条件、剧情进度写入、触发单位停止与转向、长老归属与转向、对白主干、觉醒特效、全属性 +3、补给物品、任务 UI/任务消息、小地图信号、地精区域 `085/032` 视野、地精祭祀预创建/暂停/无敌/范围触发。
- 会不会卡流程：不会。玩家可正常从 0 推进到 1，并能继续触发地精洞窟入口。
- 验证结果：`npm run build` 通过；抽查生成 Lua，`StopMusic(false)`、`GetRandomReal(-1800, 1800)`、`Location(x, y)` 均为直接函数调用，未出现 `self/nil` 错位。
- 本轮审查状态：`0 -> 1` 已审查、已落地、已验证。

## 进度 1 -> 2 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村01`
- 触发条件：`剧情进度 == 1`，玩家英雄进入 `gg_rct______________020`
- 事件类型：矩形进入演出

TS对应：
- 入口：`00．主线剧情\02．地精洞窟进入演出.ts` 的核心矩形触发
- 剧情片段：`jlc_goblin_cave_intro`
- 动作ID：`JLC精灵村_地精洞窟演出前置`
- 专用处理器：有；入口核心也已独立实现

顺序核对：
1. JASS：写地区名 UI -> 设置 `剧情进度 = 2` -> 半夜/电影模式/黑场 -> 多段洞窟演出 -> 切镜头、生成临时单位、音效、黑幕收放 -> 关闭 `JQBGM01` 并开启 `BGM002`
2. TS：命中矩形后播放 `jlc_goblin_cave_intro`；`beforeDialog(1)` 只做 `SetTimeOfDay(0)`、`CinematicModeBJ(true)`、黑场滤镜；片段配置里保留了“1 秒后关滤镜、切区域音乐”，但运行层没有把 JASS 那组临时单位/镜头链单独落到底层
3. 差异说明：TS 保住了 `1 -> 2`、黑场、电影模式和区域音乐切换主干，但地区名 UI、玩家英雄暂停/无敌、镜头、洞内临时单位、更多音效与特效链都明显简化

结论：
- 状态：主干可推进，但演出等价度低
- 已对齐：入口矩形、进度 `1 -> 2`、基础电影模式、黑场、区域音乐切换
- 缺失：地区名 UI、玩家英雄控制、镜头/临时单位/音效细节、跳过分支后的完整收束
- 会不会卡流程：大概率不会卡到 `2`

## 进度 2 -> 3 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村01`
- 触发条件：`剧情进度 == 2`，`Boss.地精巫师` 仍归中立被动且存活，玩家英雄进入其 `755` 范围
- 事件类型：Boss 战前导

TS对应：
- 入口：`00．主线剧情\03．地精祭祀Boss前导.ts`
- 剧情片段：`jlc_goblin_boss_intro`
- 动作ID：`JLC精灵村_创建地精祭祀Boss预备`、`JLC精灵村_地精祭祀Boss战正式注册`
- 专用处理器：有；范围触发核心已独立实现

顺序核对：
1. JASS：写进度 `3` -> 加入血条 Boss 组 -> 关门 -> 将 Boss 改归敌对但保持暂停/无敌 -> 多段对白 -> 注册技能事件 -> 写入 `Boss战` 绑定并执行 Boss 战
2. TS：范围命中后播放 `jlc_goblin_boss_intro`；专用处理器会把 Boss 改归敌对、暂停、无敌并写进度 `3`；后续注册 `Boss战` 绑定也有独立动作承接
3. 差异说明：TS 保住了“进入前导 -> 进度到 `3` -> Boss 战注册”的骨架，但关门、对白数量、技能演出、镜头、即时战斗承接仍比 JASS 薄很多

结论：
- 状态：主干可用
- 已对齐：触发条件、进度 `2 -> 3`、Boss 归敌对前导、Boss 战绑定主链
- 缺失：关门、更多对白、镜头/音效、前导中的细节动作
- 会不会卡流程：大概率不会卡到 `3`

## 进度 3 -> 4 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`剧情进度 == 3` 且死亡单位类型为 `'N00C'`
- 事件类型：Boss 死亡演出

TS对应：
- 入口：Boss 死亡剧情索引 -> `jlc_goblin_boss_death`
- 剧情片段：`jlc_goblin_boss_death`
- 动作ID：`JLC精灵村_地精祭祀死亡演出前置`
- 专用处理器：有；已拆到 `00．主线剧情\04．地精祭祀死亡演出.ts`

顺序核对：
1. JASS：写进度 `4` -> 开门 -> 迁移玩家英雄 -> 创建残血地精、选择宝箱与库存 -> 清理 `Boss.地精巫师` -> 多段神秘人演出
2. TS：死亡承接已落到独立文件；会写进度、开门、迁移玩家英雄、创建残血地精、清 Boss 引用，并保留神秘人演出的关键生成
3. 差异说明：奖励箱、神秘人黑洞链和主干都在，但 JASS 里更细的计时器循环、镜头和收尾细节没有完全等价

结论：
- 状态：主干可推进
- 已对齐：进度 `3 -> 4`、开门、残血地精、Boss 清理、核心死亡演出
- 缺失：部分计时器演出细节、镜头、对白节奏
- 会不会卡流程：不会卡到 `4`

## 进度 4 -> 5 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村01`
- 触发条件：`剧情进度 == 4`，玩家英雄回到精灵村长老 `800` 范围
- 事件类型：单位范围承接

TS对应：
- 入口：`00．主线剧情\05．击败地精返回长老.ts`
- 剧情片段：`jlc_goblin_defeated_return_elder`
- 动作ID：`JLC精灵村_击败地精回村前置`、`JLC精灵村_赠送沙漠之靴`、`JLC精灵村_沙漠任务刷新`
- 专用处理器：前置有；赠礼与任务刷新主要依赖通用执行器

顺序核对：
1. JASS：写进度 `5` -> 玩家英雄 stop 并面向长老 -> 多段对白 -> 发放 `沙漠之靴` -> 刷新沙漠任务、ping 小地图、给视野
2. TS：前置会写进度、stop、双向转向；片段配置保留赠送沙漠之靴和任务刷新参数
3. 差异说明：主干承接完整，但对白被压缩，JASS 里更显式的区域视野与赠礼时机没有逐条展开核对

结论：
- 状态：可推进
- 已对齐：进度 `4 -> 5`、回长老承接、赠送沙漠之靴、任务刷新主干
- 缺失：更细的对白/提示节奏与可能的显式视野补充
- 会不会卡流程：不会卡到 `5`

## 进度 5 -> 6 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村01`
- 触发条件：`剧情进度 == 5`，玩家进入 `gg_rct______________098`
- 事件类型：矩形进入承接

TS对应：
- 剧情片段：`jlc_desert_arrival`
- 动作ID：`JLC沙漠_抵达沙漠外缘`、`JLC沙漠_调查目标刷新`
- 运行层：`00．主线剧情\06．沙漠与线索.ts` 复用通用执行能力

顺序核对：
1. JASS：写进度 `6` -> stop -> 一句到达对白 -> 更新任务并给视野
2. TS：片段与动作参数都在；主干依赖通用执行器推进 `目标进度`、任务文本和提示
3. 差异说明：TS 保住了到达沙漠后的主线切换，但没有单独实现 JASS 那种“到点即停、即播、即给视野”的更紧收束

结论：
- 状态：可推进
- 已对齐：进度 `5 -> 6`、到达沙漠、任务切换
- 缺失：更细的视野/提示等价核对
- 会不会卡流程：不会卡到 `6`

## 进度 6 -> 7 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村01`
- 触发条件：`剧情进度 == 6`，分别靠近沙漠年轻佣兵、沙漠年长者、沙漠情报商人
- 事件类型：线索分支，其中真正推进主线的是情报商人

TS对应：
- 剧情片段：`jlc_desert_young_mercenary`、`jlc_desert_elder_hint`、`jlc_desert_intelligence_merchant`
- 动作ID：三段都已配置，运行层在 `00．主线剧情\06．沙漠与线索.ts`
- 专用处理器：有，但主要是“归属 Player(6) + stop + 特定破坏物处理”

顺序核对：
1. JASS：年轻佣兵与年长者给线索和任务提示，但不推进进度；情报商人对话后写进度 `7`，并清理去蛇人族路上的障碍
2. TS：三段线索片段都存在；情报商人分支会推进 `目标进度: 7`，并在专用处理器里移除指定破坏物
3. 差异说明：TS 主干方向对了，但三段对白都比 JASS 短，年轻佣兵/年长者的提示节奏和附加引导弱于 JASS

结论：
- 状态：主干可用
- 已对齐：三段线索结构、真正推进的是情报商人、`6 -> 7`、障碍清理
- 缺失：更完整的线索对白、提示消息密度
- 会不会卡流程：不会卡到 `7`

## 进度 7 -> 8 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\蛇人族.j`
- 触发器：`SRZ蛇人族002`
- 触发条件：`剧情进度 == 7`，玩家进入 `gg_rct______________106`
- 事件类型：矩形进入承接

TS对应：
- 入口：`主线NPC初始化配置表.ts` 中的 `蛇人族入口`
- 剧情片段：`jlc_snake_territory_entry`
- 动作ID：`SRZ蛇人族_领地入口`
- 专用处理器：有

顺序核对：
1. JASS：删矩形 -> 写进度 `8` -> stop 并短暂停顿 -> 三段对白 -> 恢复控制 -> 扣 `233` 金币 -> 开视野、ping 小地图、刷新任务
2. TS：专用处理器会 stop 并移除矩形；通用执行器可处理 `目标进度`、扣金币、视野、任务 UI、小地图
3. 差异说明：JASS 里显式 `PauseUnit(true/false)` 和进门节奏更强，TS 当前更偏“对话驱动 + 通用参数执行”

结论：
- 状态：主干可推进
- 已对齐：进度 `7 -> 8`、删矩形、扣金币、任务刷新、视野与小地图
- 缺失：显式暂停/恢复与更细对白节奏
- 会不会卡流程：不会卡到 `8`

## 进度 8 -> 9 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\蛇人族.j`
- 触发器：`SRZ蛇人族002`
- 触发条件：`剧情进度 == 8`，玩家靠近 `主线NPC.蛇人族藏品管家` `400` 范围
- 事件类型：单位范围剧情

TS对应：
- 入口：`主线NPC初始化配置表.ts` 中的 `蛇人族藏品管家初见`
- 剧情片段：`jlc_snake_keeper_first_meet`
- 动作ID：`SRZ蛇人族_藏品管家初见`、`SRZ蛇人族_食人魔任务刷新`
- 专用处理器：初见有；任务刷新主要依赖通用执行器

顺序核对：
1. JASS：写进度 `9` -> stop -> 多段对白 -> 刷新任务 -> 向管家商店加入 `I0D0` -> 2 秒后发出 Boss 战警告提示
2. TS：专用处理器会把管家归属 Player(6)；通用执行器能处理任务刷新、商店新增物品和延迟提示
3. 差异说明：主干能力都具备，但 TS 片段对白数量更少，是否完全对齐 JASS 的提示时机仍需后续细补

结论：
- 状态：可推进
- 已对齐：进度 `8 -> 9`、任务刷新、商店上架食人魔任务物品、延迟警告
- 缺失：对白数量与演出节奏
- 会不会卡流程：不会卡到 `9`

## 进度 9 -> 10 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\蛇人族.j` 相关物品桥接，README 也已注明由 `剧情传送CS` 承接
- 触发条件：`剧情进度 == 9`，玩家英雄拾取“接受任务-狩猎食人魔”物品
- 事件类型：物品拾取桥接

TS对应：
- 入口：`01．主线剧情入口\03．主线剧情物品事件初始化.ts`
- 剧情片段：`jlc_snake_ogre_task_accept`
- 动作ID：`SRZ蛇人族_接受食人魔任务`
- 专用处理器：有；已拆到 `00．主线剧情\09．狩猎食人魔任务接取.ts`

顺序核对：
1. TS 当前逻辑：校验玩家英雄与任务物品 -> `RemoveItem(item)` -> 写进度 `10` -> 播放 `jlc_snake_ogre_task_accept`
2. 片段动作会继续创建次元裂缝、预创建沙漠食人魔、注册 Boss 范围、生成尸骨圈，并刷新任务
3. 差异说明：这一步本来就更像桥接事件；TS 已有独立拾取入口，不再依赖大文件集中触发

结论：
- 状态：已落地
- 已对齐：拾取物品推进 `9 -> 10`、移除任务物品、生成食人魔战前预置与任务刷新
- 缺失：更细的提示对白仍偏压缩
- 会不会卡流程：不会卡到 `10`

## 进度 10 -> 11 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\蛇人族.j`
- 触发器：`SRZ蛇人族002`
- 触发条件：`剧情进度 == 10`，`Boss.沙漠食人魔` 仍归中立被动，玩家进入其 `1000` 范围
- 事件类型：Boss 战前导

TS对应：
- 剧情片段：`jlc_desert_ogre_boss_start`
- 动作ID：`SRZ蛇人族_沙漠食人魔Boss启动`
- 专用处理器：有；已拆到 `00．主线剧情\10．沙漠食人魔Boss启动.ts`

顺序核对：
1. JASS：写进度 `11` -> 加入血条组 -> 开视野 -> 先把 Boss 改敌对但保持暂停/无敌 -> 拉镜头、转向、对白、咆哮特效、血迹圈和音效 -> 最后才解锁 Boss 并执行 `Boss战`
2. TS：专用处理器会把 Boss 加入血条组、改归敌对、直接解除暂停和无敌、写入 `Boss战` 绑定并播放音效后立即启动 Boss 战
3. 差异说明：TS 把这一步压成“能开打”的最小实现，缺 JASS 的镜头、对白、特效圈、开场停顿和真正的解锁时机

结论：
- 状态：主干可开战，但演出压缩明显
- 已对齐：进度 `10 -> 11`、血条组、Boss 战绑定与启动
- 缺失：开战前对白、镜头、特效、音效链、Boss 解锁时机
- 会不会卡流程：大概率不会卡到 `11`，但战前承接不等价

## 进度 11 -> 12 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`剧情进度 == 11` 且死亡/触发单位类型为 `'N05J'`（沙漠食人魔）。
- 事件类型：Boss 死亡承接。

TS对应：
- 入口：`02．剧情步骤\06．Boss死亡剧情索引.ts` 中 `Boss单位名: "沙漠食人魔" -> jlc_desert_ogre_first_death`。
- 剧情片段：`jlc_desert_ogre_first_death`。
- 动作ID：`SW01死亡事件_沙漠食人魔一阶段死亡`。
- 专用处理器/通用执行器：`04．主线剧情动作注册表.ts` 中有专用处理器 `执行沙漠食人魔一阶段死亡()`。

顺序核对：
1. JASS：设置进度 `12` -> 保留尸体 -> 记录击杀者与死亡点 -> 停区域音乐 `gg_snd_shengliBgm @ gg_rct______________047` -> 创建次元裂缝 -> 雷鸣/黑洞类特效 -> 暂停附近玩家英雄并转向裂缝、镜头拉向裂缝 -> 创建蜥蜴人并移动/转向 -> 多段对白与警告消息 -> 创建并冻结二阶段 `杀戮食人魔` -> 播放音效 `gg_snd_GWSY07` -> 最终解锁 Boss、写入 `Boss战.绑定单位` 并执行 `gg_trg_Boss____________u`。
2. TS：Boss 死亡索引先写进度 `12` 并播放 `jlc_desert_ogre_first_death`；`beforeDialog(1)` 的 `SW01死亡事件_沙漠食人魔一阶段死亡` 会保留尸体、创建裂缝、创建蜥蜴人、创建二阶段 `杀戮食人魔`、写入 `Boss/杀戮食人魔` 与 `Boss战/绑定单位`、暂停并无敌后直接 `启动Boss战运行(bossUnit)`；片段中再播 4 段对白。
3. 差异说明：TS 保住了“裂隙 -> 蜥蜴人 -> 二阶段 Boss -> 进度到 12”的主干，但缺 JASS 的大部分镜头、暂停玩家英雄、朝向、过场特效链、停止 `shengliBgm`、播放 `gg_snd_GWSY07`、Boss 初次转向/首个攻击目标等细节；而且 TS 是在 Boss 仍暂停无敌时直接 `启动Boss战运行`，和 JASS “过场后再解锁/接战”并不等价。

结论：
- 状态：主干可推进，但演出和战斗承接明显简化。
- 已对齐：Boss 死亡索引承接；进度 `11 -> 12`；裂缝、蜥蜴人、二阶段 Boss、对白片段。
- 缺失：停区域音乐、播放提示/警告音效、暂停并转向玩家英雄、镜头拉向裂缝、部分特效链、Boss 解锁时机。
- 会不会卡流程：大概率不会卡 `11 -> 12`，但 Boss 战开始时机与 JASS 不一致，存在“过场味道不足/战斗状态提前启动”的风险。
- 最小修复方向：后续落地时先补 `停止区域音乐 / 播放音效 / 玩家英雄暂停与恢复 / Boss 解锁时机`，再决定是否补镜头和特效。
- 修复结果：本轮已把 `SW01死亡事件_沙漠食人魔一阶段死亡` 抽到 `00．主线剧情/11．沙漠食人魔一阶段死亡.ts` 独立处理器，保持裂缝、蜥蜴人、二阶段 Boss 与 Boss 战绑定逻辑。

## 进度 12 -> 13 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`剧情进度 == 12` 且 `YDUserData(string,"Boss","杀戮食人魔", unit) == 死亡Boss`。
- 事件类型：Boss 死亡承接。

TS对应：
- 入口：`Boss死亡剧情索引.ts` 中 `Boss单位名: "杀戮食人魔" -> jlc_slaughter_ogre_death`。
- 剧情片段：`jlc_slaughter_ogre_death`。
- 动作ID：`SW01死亡事件_杀戮食人魔死亡`。
- 专用处理器/通用执行器：当前没有专用处理器；会先由 Boss 死亡索引写进度并播放片段，动作阶段只剩通用执行器处理通用参数。

顺序核对：
1. JASS：设置进度 `13` -> 击杀者发言 -> 更新任务描述/任务提示 -> 清理 `Boss.杀戮食人魔` -> 在死亡点创建 `食人魔头颅` -> 创建选择宝箱并加入 3 个库存物品 -> 始终提示消息 -> 对玩家英雄组执行后续逻辑。
2. TS：Boss 死亡索引会先把进度写到 `13` 并播放 `jlc_slaughter_ogre_death`；片段 `beforeDialog(1)` 配了动作 `SW01死亡事件_杀戮食人魔死亡`，但运行层没有同名专用处理器；通用执行器能吃到 `目标进度`，但这一步已被索引提前写入。片段只保留 1 句“带上凭证回蛇人族”对白。
3. 差异说明：TS 当前没有把 JASS 的 `清理 Boss 引用 / 生成食人魔头颅 / 选择宝箱 / 宝箱库存 / 提示消息` 真正落到运行层；Boss 死亡剧情索引仅保证进度推进与片段播放。

结论：
- 状态：进度能推进，但奖励与承接掉落缺失明显。
- 已对齐：`12 -> 13` 的进度跳转；回蛇人族的剧情口头引导。
- 已修：`SW01死亡事件_杀戮食人魔死亡` 已落到 `00．主线剧情/12．杀戮食人魔二阶段死亡.ts`，补了 `Boss.杀戮食人魔` 清理、`食人魔头颅` 掉落、选择宝箱与三件库存物品。
- 会不会卡流程：当前主风险已解除，后续 `13 -> 14` 不再因为头颅缺失直接卡死。
- 剩余缺失：提示消息与更细的 JASS 死亡收束仍未完全复刻。

## 进度 13 -> 14 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\蛇人族.j`
- 触发器：`SRZ蛇人族002`
- 触发条件：触发单位在 `主线NPC.蛇人族藏品管家` `400` 范围内，`剧情进度 == 13`，且持有 `'I0D4'`（食人魔头颅）。
- 事件类型：单位范围交付事件。

TS对应：
- 入口：`主线NPC初始化配置表.ts` 中 `蛇人族交还食人魔凭证 -> 蛇人族藏品管家 -> 350 -> jlc_snake_keeper_return_item`。
- 剧情片段：`jlc_snake_keeper_return_item`。
- 动作ID：`SRZ蛇人族_交还食人魔凭证`。
- 专用处理器/通用执行器：有专用处理器 `执行蛇人族交还食人魔凭证()`，同时通用执行器可处理 `需要物品 / 物品名 / 停止区域音乐 / 开始音乐 / 目标进度`。

顺序核对：
1. JASS：设置进度 `14` -> 触发单位停止并短暂停顿 -> 删 `食人魔头颅` -> 多段对白 -> 给玩家 `夜光翡翠` -> 关闭 `BGM019` 并开启 `JQBGM02` -> 创建蛇人族卫队长并令其走入场 -> 继续对白 -> 恢复触发单位控制 -> 卫队长改回中立被动并摆正位置 -> 刷新试炼任务。
2. TS：入口命中后播放 `jlc_snake_keeper_return_item`；`beforeDialog(1)` 的 `SRZ蛇人族_交还食人魔凭证` 专用处理器只负责创建卫队长并下达移动指令；随后通用执行器会处理 `目标进度: 14`、移除 `需要物品: 食人魔头颅`、给触发单位 `物品名: 夜光翡翠`、停止 `BGM019`、开始 `JQBGM02`；片段只保留 2 段对白。
3. 差异说明：TS 主干交付链路基本在，但缺 JASS 的“触发单位停止/短暂停顿”“卫队长最后改回中立被动并摆正”“额外多段对白细节”；同时入口半径 `350` 比 JASS 的 `400` 更窄。

结论：
- 状态：主干基本可用，但仍有行为差异。
- 已对齐：需要物品检查、进度 `13 -> 14`、回收头颅、发放夜光翡翠、切音乐、创建卫队长。
- 缺失：触发单位控制停顿、卫队长最终归属/站位收尾、对白数量与节奏、JASS 原半径 `400`。
- 会不会卡流程：如果 `12 -> 13` 已正常掉落头颅，则这里大概率不会卡；真正风险仍来自上一跳掉落未落地。
- 最小修复方向：后续补“触发单位暂停/恢复”和卫队长收尾归位；半径是否改回 `400` 需再对照实际体感。
- 修复结果：`SRZ蛇人族_交还食人魔凭证` 已抽到 `00．主线剧情/13．蛇人族交还食人魔凭证.ts`；卫队长创建与行进已独立落地。

## 前置B：蛇人族卫队长试炼宣告（已并入 13 号片段）

JASS来源：
- 文件：`JASS\世界地图\主线剧情\蛇人族.j`
- 触发器：同 `SRZ蛇人族002` 的后续对白分支。
- 触发条件：承接 `13 -> 14` 交付后的同一段剧情流程，已并入 `13．蛇人族交还食人魔凭证.ts`。
- 事件类型：同轮剧情对白前置，不单独写新进度。

TS对应：
- 剧情片段：已并入 `jlc_snake_keeper_return_item`。
- 动作ID：`SRZ蛇人族_卫队长入场`、`SRZ蛇人族_护卫对战目标刷新`。
- 专用处理器/通用执行器：这两个动作当前都没有注册到专用动作表；会退回通用执行器，仅能吃到通用任务参数。

顺序核对：
1. JASS：卫队长进场 -> 卫队长/管家/玩家多段对白 -> 刷新“护卫试炼”任务目标。
2. TS：这段对白已并入 `13．蛇人族交还食人魔凭证.ts`，`beforeDialog(3)` 的 `SRZ蛇人族_卫队长入场` 和 `afterDialog(6)` 的 `SRZ蛇人族_护卫对战目标刷新` 仍由运行层动作表承接。
3. 差异说明：试炼宣告现在不再是独立片段，而是 `13 -> 14` 合并流程的一部分。

结论：
- 状态：已并入 13 号片段，不再单列。
- 已对齐：对白片段、任务目标刷新文本、卫队长入场动作。
- 已修：`SRZ蛇人族_卫队长入场` / `SRZ蛇人族_护卫对战目标刷新` 仍由运行层动作表承接。
- 剩余缺失：试炼玩法本体的胜负桥接仍需单独补查。
- 运行风险：若将来拆回单独片段，需要再核对动作与对白边界。

## 进度 14 -> 15 审查结果

当前情况：
- JASS 来源已补查：`JASS/世界地图/主线剧情/14-15.j`。
- 事件类型：受伤血线承接，不是普通对白入口，也不是技能命中触发。
- 触发条件：剧情进度 `14`、受击单位类型为 `N05L`，且本次伤害会将其击杀或压到 `10%` 生命以下。

结论：
- 已落地：入口配置写在 `02．剧情步骤/01．第一章/14．蛇人族卫队长试炼.ts`，由 `01．主线剧情入口/04．主线剧情特殊事件初始化.ts` 通过 `registerAppliedFinalDamageListener` 承接。
- 核心动作：将事件伤害置 `0`、把 Boss 生命钳到 `10%`、切无敌、改所属、暂停、播放对白、移除战斗姿态、清理 `Boss.蛇人族卫队长`、刷新主线目标、创建支线提示。
- 状态：已查实并已落地，后续只剩对白细抛光与周边演出细节可继续补。

## 进度 15 -> 16 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村001`
- 触发条件：`剧情进度 == 15`，触发单位持有 `'I0D6'`（夜光翡翠），且在 `主线NPC.沙漠情报商人` `450` 范围内。
- 事件类型：单位范围交付事件。

TS对应：
- 入口：`主线NPC初始化配置表.ts` 中 `沙漠情报商人回收夜光翡翠 -> 沙漠情报商人 -> 450 -> jlc_desert_merchant_return_jade`。
- 剧情片段：`jlc_desert_merchant_return_jade`。
- 动作ID：`JLC沙漠_情报商人回收夜光翡翠`、`JLC沙漠_源石入手目标刷新`。
- 专用处理器/通用执行器：`JLC沙漠_情报商人回收夜光翡翠` 有专用处理器；`JLC沙漠_源石入手目标刷新` 无专用处理器，但通用执行器可刷新任务。

顺序核对：
1. JASS：设置进度 `16` -> 触发单位停止 -> 删除夜光翡翠 -> 多段对白 -> 给玩家 `魔力源石` -> 播放 `BlueBalllight` 特效 -> 继续对白 -> 更新回村任务 -> 清理村内旧精灵护卫组 -> 创建 `ZXCS.DW` 与 `ZXCS2.DW` 两处动态裂缝，并各注册 `300` 范围回村触发。
2. TS：入口命中后播放 `jlc_desert_merchant_return_jade`；`beforeDialog(1)` 的 `JLC沙漠_情报商人回收夜光翡翠` 专用处理器会清理 `移除临时单位`、创建两处动态裂缝并注册回村入口；通用执行器会处理 `目标进度: 16`、删除 `需要物品: 夜光翡翠`、发放 `物品名: 魔力源石`；`afterDialog(5)` 的 `JLC沙漠_源石入手目标刷新` 由通用执行器刷新任务目标。
3. 差异说明：TS 主干基本完整，但专用处理器里没有做 `BlueBalllight` 特效，也没有复刻 JASS 那种“枚举村内旧护卫组再逐个清理”的同等处理；它只按参数 `移除临时单位` 清理引用。对白数量也从 JASS 多段压成 5 段。

结论：
- 状态：主干可推进。
- 已对齐：`15 -> 16`、回收夜光翡翠、发放魔力源石、回村任务刷新、两处 `ZXCS` 动态裂缝与范围入口。
- 已修：`JLC沙漠_情报商人回收夜光翡翠` 已抽到 `00．主线剧情/15．夜光翡翠回收.ts`，补了 `BlueBalllight` 特效和动态裂缝回村入口注册。
- 剩余缺失：旧精灵护卫/村内单位清理的 JASS 细节。
- 会不会卡流程：按当前实现看，不会卡 `15 -> 16` 与后续回村入口。

## 进度 16 -> 17 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村001`
- 触发条件：`剧情进度 == 16`，触发单位接近 `ZXCS.DW` 或 `ZXCS2.DW` 任一 `300` 范围。
- 事件类型：动态单位范围触发。

TS对应：
- 入口：矩形入口 `精灵村区域回村 -> gg_rct________________QY -> jlc_return_village_after_guard_duel`，以及 `JLC沙漠_情报商人回收夜光翡翠` 专用处理器动态注册的两处裂缝回村范围触发。
- 剧情片段：`jlc_return_village_after_guard_duel`。
- 动作ID：`JLC精灵村_护卫试炼后回村`、`JLC精灵村_教派袭击预置`。
- 专用处理器/通用执行器：`护卫试炼后回村`、`教派袭击预置` 均有专用处理器；其余任务、音乐、进度依赖通用执行器。

顺序核对：
1. JASS：设置进度 `17` -> 删除 `ZXCS` / `ZXCS2` 与 YD 表 -> 处理玩家英雄组 -> 停 `JQBGM03` -> 调整村内单位组、族长位置和朝向 -> 开 `JQBGM04` -> 开启电影模式 -> 提示消息 -> 创建蒙面人、护卫、树木 -> 多段对白和镜头 -> 关闭电影模式 -> 停 `JQBGM04` -> 创建并冻结最终 Boss 姿态 -> 绑定 Boss 战并起定时器收尾。
2. TS：通过动态裂缝触发或矩形回村触发播放 `jlc_return_village_after_guard_duel`；`beforeDialog(1)` 的 `JLC精灵村_护卫试炼后回村` 只清理 `ZXCS.DW / ZXCS2.DW`；随后通用执行器处理 `目标进度: 17`、区域音乐切换、族长位置；`afterDialog(1)` 的 `JLC精灵村_教派袭击预置` 创建蒙面人、护卫和 21 棵树；片段保留 6 段对白。
3. 差异说明：TS 保住“回村 -> 教派现身 -> 进度到 17”的主线，但缺 JASS 的电影模式、玩家英雄组处理、族长朝向、更多镜头控制与中间收尾步骤；而且 TS 的 `JLC精灵村_护卫试炼后回村` 专用处理器只做临时单位清理，明显比 JASS 轻。

结论：
- 状态：主干可推进，但演出简化明显。
- 已对齐：动态裂缝回村、`16 -> 17`、清理 `ZXCS` 引用、教派袭击预置、任务提示与音乐参数主干。
- 已修：`JLC精灵村_护卫试炼后回村`、`JLC精灵村_教派袭击预置`、`JLC精灵村_教派Boss随机姿态` 已抽到 `00．主线剧情/16．第一章最终Boss教派前置.ts`；补了电影模式开启、族长重定位/朝向和教派预置单位独立落点。
- 剩余缺失：玩家英雄组控制、更多镜头和中间过渡。
- 会不会卡流程：大概率不会卡到 `17`，但回村演出还不等价。

## 前置C：第一章最终 Boss 启动（无剧情进度变化）

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：仍属于 `JLC精灵村001` 在 `16 -> 17` 承接段中的后半。
- 触发条件：同回村承接后的演出继续，不额外改剧情进度。
- 事件类型：Boss 战前置。

TS对应：
- 剧情片段：`jlc_cult_final_boss_start`。
- 动作ID：`JLC精灵村_教派Boss随机姿态`、`startBossFight`。
- 专用处理器/通用执行器：`教派Boss随机姿态` 有专用处理器；`startBossFight` 走通用执行器。

顺序核对：
1. JASS：随机 `教派剑士/教派学者` -> 创建并冻结 Boss -> 写 `Boss.蒙面人`、`Boss战.绑定单位`、`Boss战.触发玩家` -> 执行 Boss 启动桥接 -> 若干定时器与对白收尾 -> 之后正式进入战斗。
2. TS：`beforeDialog(1)` 执行 `JLC精灵村_教派Boss随机姿态`，随机创建并冻结 Boss；`afterDialog(2)` 用 `startBossFight` 按 `Boss引用: "Boss.蒙面人"` 启动 Boss 战。
3. 差异说明：TS 基本保住了“随机姿态 -> 两段对白 -> 开战”主线，但少了 JASS 里同段的一大串定时器、过场镜头、收尾对白链。

结论：
- 状态：战斗可起，但演出压缩较大。
- 已对齐：随机姿态、Boss 创建冻结、Boss 战启动。
- 缺失：定时器链、更多镜头和对白收束。

## 进度 17 -> 18 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`剧情进度 == 17`，死亡单位为 `N05N/N05M`（教派剑士/学者），且不是幻象、是英雄、不是召唤物。
- 事件类型：Boss 死亡承接。

TS对应：
- 入口：`Boss死亡剧情索引.ts` 中 `教派剑士` / `教派学者` -> `jlc_cult_final_boss_death`。
- 剧情片段：`jlc_cult_final_boss_death`。
- 动作ID：`SW01死亡事件_蒙面人死亡`、`JLC精灵村_第一章完成任务刷新`。
- 专用处理器/通用执行器：`SW01死亡事件_蒙面人死亡` 有专用处理器；`JLC精灵村_第一章完成任务刷新` 无专用处理器，靠通用执行器刷新任务。

顺序核对：
1. JASS：停多块区域 `JQBGM03` -> 电影模式 -> 提示消息 -> 清理附近机械敌人 -> 多段残影/特效/对白 -> 设置进度 `18` -> 恢复环境音乐 -> 奖励物品/任务更新 -> 族长重定位 -> 指向王城。
2. TS：Boss 死亡索引先写进度 `18` 并播放 `jlc_cult_final_boss_death`；`beforeDialog(1)` 的 `SW01死亡事件_蒙面人死亡` 会清理 Boss 引用、保留尸体、重定位长老，但显式 `delete` 了 `奖励物品名` 与 `恢复环境音乐`，因此不会交给通用执行器；片段播放 4 段对白；`afterDialog(4)` 的 `JLC精灵村_第一章完成任务刷新` 由通用执行器刷新任务描述/提示和小地图。
3. 差异说明：TS 能推进到 `18` 并刷新去王城目标，但没有落 JASS 的停多区域音乐、恢复环境音乐、奖励物品、机械敌人清理、电影模式、提示消息等核心承接细节。

结论：
- 状态：能推进，但丢失内容较多。
- 已对齐：`17 -> 18`、Boss 死亡片段、长老重定位、前往王城任务刷新。
- 已修：`SW01死亡事件_蒙面人死亡` 已抽到 `00．主线剧情/17．第一章最终Boss教派死亡.ts`，不再吞掉 `奖励物品名` 参数，因此 `西里尔魔法书` 会继续走通用执行器发放。
- 剩余缺失：恢复环境音乐表达式仍不等价，机械敌人清场、电影模式与多段提示消息未补。
- 会不会卡流程：大概率不会卡到 `18`，但第一章收束的奖励和氛围承接明显不完整。

## 进度 18 -> 19 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵村.j`
- 触发器：`JLC精灵村001`
- 触发条件：`剧情进度 == 18`，触发单位在 `主线NPC.精灵村长老` `850` 范围内。
- 事件类型：单位范围章节收束。

TS对应：
- 当前状态：已由 `01．第一章/18．精灵城章节承接.ts` 承接，不再是“确认跳过”占位。
- 现有 TS 入口：`精灵城章节承接 -> elven_city_chapter_bridge`，需要剧情进度 `18`。

顺序核对：
1. JASS：设置进度 `19` -> 长老残余对白 -> 特效 -> 隐藏障碍 -> 刷新任务到王城外 -> 传送门归属调整 -> 小地图 ping -> 完成旧任务并创建新任务。
2. TS：现已存在 `elven_city_chapter_bridge` 片段；`beforeDialog(1)` 的 `JLC精灵城_章节承接` 会处理长老特效、隐藏障碍和通行单位归属，并通过通用执行器推进到 `19`；`afterDialog(3)` 的 `JLC精灵城_前往王城` 当前只负责任务文本与小地图，不再直接写 `20`。

结论：
- 状态：已落地，但仍是精简承接。
- 已对齐：`18 -> 19`、长老特效、障碍隐藏、通行单位归属、任务刷新与小地图主干。
- 缺失：JASS 原收束对白链、旧任务完成/新任务创建的更细等价处理。
- 说明：这里不能再按“确认跳过”记录。

## 进度 19 -> 20 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\19-20.j`
- 触发器：`未命名触发器 007`
- 事件类型：环境互动承接，玩家在森林传送阵附近使用 `A08E` 后推进。

TS对应：
- 对应文件：`02．第二章/19．精灵森谷传送抵达.ts`
- 入口实现：`01．主线剧情入口/05．主线剧情事件配置表.ts` + `04．主线剧情特殊事件初始化.ts`
- 已补动作：场景提示、镜头转移、触发单位传送到克林山谷落点、写剧情进度 `20`、解锁 `gg_rct______________053` 视野、播放看守者盘问对白；抵达片段不再额外执行 `QuestSetDescription / QuestMessageBJ`，后续 20→21 再负责任务引导。

结论：
- 状态：已查实并已落地，不再是占位文件。
- 剩余缺口：后续仍可继续补对白逐句等价和局部镜头细节，但主干已闭环。

## 进度 20 -> 21 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `主线NPC.阿尔文` `400` 范围内，且 `剧情进度 == 20`。
- 事件类型：单位范围剧情。

TS对应：
- 入口：`阿尔文范围入口 -> elven_city_alvin_start`，注册范围 `400`。
- 剧情片段：`elven_city_alvin_start`。
- 动作ID：`JLC精灵城_阿尔文接引`、`JLC精灵城_阿尔文路线指引`。
- 专用处理器/通用执行器：`JLC精灵城_阿尔文接引` 已有专用处理器，补了 `stop` 与阿尔文复活特效；`JLC精灵城_阿尔文路线指引` 仍主要靠通用执行器刷新任务与小地图。

顺序核对：
1. JASS：设置进度 `21` -> 触发单位 `stop` -> 多段对白 -> 对阿尔文播放复活特效 -> 继续对白 -> 更新第二章任务 -> 小地图 ping。
2. TS：片段已扩成 7 段对白；`beforeDialog(1)` 的 `JLC精灵城_阿尔文接引` 会先 `stop` 触发单位并对阿尔文播放复活特效；`afterDialog(7)` 的 `JLC精灵城_阿尔文路线指引` 刷新前往王城的任务与小地图。
3. 差异说明：当前主干与关键动作已基本对齐，主要剩余差距是对白逐句措辞与节奏润色。

结论：
- 状态：已基本落地。
- 已对齐：进度推进、触发单位停止、阿尔文特效、任务刷新、小地图。
- 缺失：对白逐句文本与 JASS/WTS 完全等价仍可继续补。

## 进度 21 -> 22 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `gg_unit_n04R_0048` `999` 范围内，且 `剧情进度 == 21`。
- 事件类型：全局单位范围剧情。

TS对应：
- 入口：20→21 完成后动态创建矩形 `(-7552,-13696) - (-6400,-13152)`，进入后播放 `elven_city_gate_open`；21→22 完成后注销监听并删除矩形/区域。
- 剧情片段：`elven_city_gate_open`。
- 动作ID：`JLC精灵城_王城门禁开启`、`主线.进入节点`、`第二章_清理王城门禁矩形监听`。
- 专用处理器/通用执行器：`JLC精灵城_王城门禁开启` 记录动态矩形与触发单位调试信息，并继续由通用执行器处理停单位、延迟开门、隐藏阻挡、视野和进度；门禁矩形清理由 21→22 收尾动作执行。

顺序核对：
1. JASS：设置进度 `22` -> 触发单位 `stop` -> 对话 -> `2.5` 秒后开门并隐藏阻挡 -> 给玩家组加精灵城大区域视野 -> 更新任务与小地图。
2. TS：`beforeDialog(1)` 触发 `JLC精灵城_王城门禁开启`，可处理延迟开门/隐藏阻挡；通用执行器还能处理 `目标进度: 22`、视野、任务、小地图；`afterDialog(3)` 写入 22 并清理门禁矩形监听。
3. 差异说明：主干动作已对齐，剩余主要是对白逐句差异与门前短对白节奏。

结论：
- 状态：已基本落地。
- 已对齐：进度、延迟开门、隐藏阻挡、视野、任务、小地图。
- 缺失：对白细节仍可继续比对润色。

## 进度 22 -> 23 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `主线NPC.jl禁军门卫` `999` 范围内，且 `剧情进度 == 22`。
- 事件类型：单位范围剧情。

TS对应：
- 入口：`jl禁军门卫范围入口 -> elven_city_palace_guard`，注册范围 `999`。
- 剧情片段：`elven_city_palace_guard`。
- 动作ID：`JLC精灵城_王宫禁军盘查`。
- 专用处理器/通用执行器：当前主要靠通用执行器，但片段参数已补进 `stop`、三块视野、任务描述、任务提示、小地图。

顺序核对：
1. JASS：设置进度 `23` -> 触发单位 `stop` -> 给 `121/122/123` 三块视野 -> 多段对白 -> 更新任务 -> 小地图 ping 到王宫内。
2. TS：片段已扩成 5 段对白；`beforeDialog(1)` 的参数已覆盖 `目标进度: 23`、触发单位 `stop`、三块视野、任务描述、任务更新提示与小地图。
3. 差异说明：主干已对齐，剩余主要是对白逐句润色和是否还要单独补专用处理器来承接更多演出。

结论：
- 状态：已基本落地。
- 已对齐：进度推进、触发单位停止、三块视野、任务刷新、小地图。
- 缺失：对白与演出细节仍可继续比。

## 进度 23 -> 24 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `主线NPC.jl禁军门卫2` `600` 范围内，且 `剧情进度 == 23`。
- 事件类型：单位范围支线发现。

TS对应：
- 入口：`jl禁军门卫2支线发现 -> elven_city_side_quest_discover`，注册范围 `400`。
- 剧情片段：`elven_city_side_quest_discover`。
- 动作ID：`JLC精灵城_王宫门卫2支线发现`。
- 专用处理器/通用执行器：已有专用处理器 `执行王宫门卫支线发现()`，会 `stop` 触发单位、调用 `QuestSetDiscovered`，并发送 `DISCOVERED` 类型任务消息。

顺序核对：
1. JASS：设置进度 `24` -> 触发单位 `stop` -> 一条较长对白 -> `QuestSetDiscovered(udg_RW[8], true)` -> `DISCOVERED` 类型任务消息。
2. TS：片段仍是 `absoluteTime(0)` 直接执行 `JLC精灵城_王宫门卫2支线发现`；专用处理器会 `stop` 触发单位、发现支线并发送 `DISCOVERED` 消息，通用执行器负责推进 `目标进度: 24`。
3. 差异说明：这段仍明显压缩，主要缺的是 JASS 原本那条较长对白。

结论：
- 状态：可推进，但压缩严重。
- 已对齐：支线发现开关、进度 `24`、`DISCOVERED` 提示消息、触发单位停止。
- 缺失：对白、入口半径 `600 -> 400` 的等价。

## 进度 23/24 -> 25 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `ZX.克林姆德王` `999` 范围内，且 `剧情进度 == 23 或 24`。
- 事件类型：单位范围章节委托。

TS对应：
- 入口：`克林姆德王接见 -> elven_city_king_audience`，注册范围 `425`，`最低剧情进度: 23`，`最高剧情进度: 24`。
- 剧情片段：`elven_city_king_audience`。
- 动作ID：`JLC精灵城_克林姆德王接见`、`JLC精灵城_发布巨魔线任务`。
- 专用处理器/通用执行器：`JLC精灵城_克林姆德王接见` 在大注册表有专用处理器，但逻辑只生成猎魂；`JLC精灵城_发布巨魔线任务` 没有专用处理器，靠通用执行器刷新任务。

顺序核对：
1. JASS：清理两名禁军门卫 YD 引用 -> 设置进度 `25` -> 触发单位 `stop` -> 处理既有 `jq.npc` 无敌/暂停、设置 `主线NPC.jlw` 归属 -> 长对白链 -> 开 `JQBGM02` -> 全玩家发 `15000` 金币 -> 物品获得提示 -> 关闭 `JQBGM02` -> 更新任务与小地图 -> 创建猎魂 -> 移除阻挡 `gg_dest_Dofw_5490`。
2. TS：`beforeDialog(1)` 的 `JLC精灵城_克林姆德王接见` 专用处理器当前只会创建 `猎魂`；通用执行器能处理 `目标进度: 25`、开始音乐、发放金币、移除阻挡；片段有 4 段对白；`afterDialog(4)` 的 `JLC精灵城_发布巨魔线任务` 仅刷新任务和小地图。
3. 差异说明：TS 丢了禁军门卫 YD 清理、触发单位 `stop`、`jq.npc` 的暂停/无敌控制、`主线NPC.jlw` 归属切换、物品获得提示，以及 JASS 更长的对白链；入口半径 `425` 也比 JASS `999` 小很多。

结论：
- 状态：主干可推进到 `25`，但接见演出和场景控制缺漏较多。
- 已对齐：进度推进、音乐开启、发放金币、创建猎魂、移除阻挡、任务与小地图。
- 缺失：`JLC精灵城_克林姆德王接见` 大量 JASS 逻辑、`JLC精灵城_发布巨魔线任务` 的物品提示类承接、入口半径等价。

## 进度 25 -> 26 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `jq.npc` `400` 范围内，且 `剧情进度 == 25`。
- 事件类型：单位范围剧情。

TS对应：
- 入口：动态创建 `ohun` 巨魔猎头者，以单位为中心注册玩家英雄 `400` 范围监听，剧情片段 `elven_city_troll_guard_start`。
- 剧情片段：`25．巨魔猎头者拦路.ts`
- 动作ID：`JLC精灵城_巨魔猎头者拦路`、`JLC精灵城_巨魔猎头者开战`、`JLC精灵城_预置树魔首领`。
- 专用处理器/通用执行器：对白开始注销一次性监听；对白结束解除猎头者暂停与无敌，进入进度 `26` 后才预置树魔首领与随从。

顺序核对：
1. JASS：设置进度 `26` -> `stop` 并暂停触发单位 -> 多段对白 -> 恢复触发单位 -> 解锁猎魂并清空 `jq.npc` -> 更新任务。
2. TS：对白开始停止触发英雄并注销监听；对白结束或 ESC 跳过都会解除猎头者暂停/无敌、恢复玩家控制、进入进度 `26`，再预置树魔首领与随从。
3. 差异说明：TS 已按当前设计改为“小弟奉命不放行，随后开战”，不再保留猎魂试探语义。

结论：
- 状态：已基本落地。
- 已对齐：剧情进度推进、触发单位控制、猎魂解锁、`jq.npc` 清理、任务刷新。
- 缺失：对白等价与局部演出节奏。

## 进度 26 -> 27 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `Boss.巨魔首领` `400` 范围内，且 `剧情进度 == 26`。
- 事件类型：Boss 战前对白。

TS对应：
- 剧情片段：`elven_city_treant_leader_start`
- 动作ID：`JLC精灵城_树魔首领战前`、`startBossFight`
- 专用处理器/通用执行器：树魔首领 `400` 范围入口只接受玩家英雄，触发后注销；`startBossFight` 由通用播放器支持。

顺序核对：
1. JASS：设置进度 `27` -> 触发单位 `stop` 并暂停 -> 多段对白 -> 恢复触发单位 -> 更新任务。
2. TS：`beforeDialog(1)` 的 `JLC精灵城_树魔首领战前` 会先 `stop` 并暂停触发单位；收尾会恢复控制、进入进度 `27`，再启动树魔首领 Boss 战和战斗中广播对白。
3. 差异说明：战前控制链和开战时点已基本对齐，剩余主要是对白逐句内容和更细的战前压迫感表现。

结论：
- 状态：已基本落地。
- 已对齐：进度推进、触发单位控制、任务刷新、Boss 战启动。
- 缺失：对白等价与演出细节。

## 进度 27 -> 28 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`GetDyingUnit() == 树魔首领`，且 `剧情进度 == 27`。
- 事件类型：Boss 死亡承接。

TS对应：
- Boss死亡索引：`Boss死亡剧情索引.ts` 已有 `树魔首领 -> elven_city_treant_leader_death`
- 剧情片段：`elven_city_treant_leader_death`
- 动作ID：`SW01死亡事件_树魔首领死亡`、`JLC精灵城_树魔首领死亡后返城`
- 专用处理器/通用执行器：`SW01死亡事件_树魔首领死亡` 在大注册表有专用处理器，但当前只做 `清 Boss 引用 + 清 dyingUnit 表 + 写进度 28`；`JLC精灵城_树魔首领死亡后返城` 仅靠通用任务刷新。

顺序核对：
1. JASS：设置进度 `28` -> 随机玩家对白 -> 更新任务 -> 创建选择宝箱 `e070` -> 塞入 `I0C3/I0C5/I0C7` -> 掉落魔法信件 `I0CA` -> 清 `Boss.树魔首领` 和 Boss 表。
2. TS：能通过 Boss 死亡索引播放片段并推进进度；专用处理器只清引用，不创建宝箱，也不掉落魔法信件；片段后半只刷新“返回王城”任务。
3. 差异说明：这段缺的不是纯演出，而是关键掉落。后续 `28 -> 29` 依赖魔法信件物品事件，如果这里不补掉落，流程会断。

结论：
- 状态：表面有片段，实际仍是高风险断点。
- 已对齐：Boss 死亡进度推进、Boss 引用清理、返城任务刷新。
- 缺失：随机玩家对白、选择宝箱、魔法信件掉落。
- 风险：`28 -> 29` 很可能直接卡住。

## 进度 28 -> 29 审查结果

JASS来源：
- 文件：物品事件 `ZX02`（你之前补充的“魔法信件交付”桥接）
- 触发条件：玩家英雄在 `剧情进度 == 28` 时使用魔法信件。
- 事件类型：物品使用桥接。

TS对应：
- 入口：`01．主线剧情入口/03．主线剧情物品事件初始化.ts`
- 当前实现：`on魔法信件物品使用()`
- 专用片段：无；直接通过物品事件写进度和任务 UI。

顺序核对：
1. TS 当前逻辑：校验玩家英雄、校验持有 `texp`、进度 `28` -> 写入进度 `29` -> 播放一条提示对白 -> 更新主线任务 UI。
2. 结构说明：`28．魔法信件交付.ts` 只是重新导出物品事件初始化，不是独立剧情片段。

结论：
- 状态：桥接逻辑本身已落地。
- 已对齐：使用物品推进到 `29`、给出提示文本、刷新任务。
- 缺失：独立片段化表达。
- 风险：真正的阻断点不在 `28 -> 29` 本身，而在 `27 -> 28` 目前没有稳定掉出魔法信件。

## 进度 29 -> 30 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `ZX.克林姆德王` `400` 范围内，且 `剧情进度 == 29`。
- 事件类型：单位范围剧情。

TS对应：
- 入口：`克林姆德王魔法信件汇报 -> elven_city_report_magic_letter`
- 剧情片段：`29．魔法信件汇报.ts`
- 动作ID：`JLC精灵城_魔法信件汇报`、`JLC精灵城_前往赫克提尔`
- 专用处理器/通用执行器：两个动作都没有专用处理器，只能靠通用执行器处理进度和任务。

顺序核对：
1. JASS：设置进度 `30` -> 触发单位 `stop` -> 多段对白 -> 更新任务。
2. TS：`beforeDialog(1)` 的 `JLC精灵城_魔法信件汇报` 只有通用进度推进；`afterDialog(2)` 的 `JLC精灵城_前往赫克提尔` 只刷新任务文本。
3. 差异说明：TS 对白压缩明显，且缺 `stop` 和更完整的王城汇报承接。

结论：
- 状态：可推进到 `30`。
- 已对齐：进度推进、任务切换。
- 缺失：`JLC精灵城_魔法信件汇报` / `JLC精灵城_前往赫克提尔` 专用处理器、对白等价、触发单位停止。

## 进度 30 -> 31 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `ZX.赫克提尔` `400` 范围内，且 `剧情进度 == 30`。
- 事件类型：单位范围剧情。

TS对应：
- 剧情片段：`elven_city_hectel_decode`
- 动作ID：`JLC精灵城_赫克提尔解析信件`、`wait`
- 专用处理器/通用执行器：`JLC精灵城_赫克提尔解析信件` 没有专用处理器；`wait` 由通用播放器支持。

顺序核对：
1. JASS：设置进度 `31` -> 触发单位 `stop` -> 长对白 -> `WARNING` 任务消息 -> 更多对白 -> 更新任务。
2. TS：`beforeDialog(1)` 只能通用推进 `目标进度: 31`；`beforeDialog(2)` 插了一个 `wait(2)`；对白有保留“紧急传讯”，但没有专用动作去补 `WARNING` 消息和更完整承接。
3. 差异说明：TS 有基本结构，但 `JLC精灵城_赫克提尔解析信件` 仍是空挂动作名。

结论：
- 状态：可推进到 `31`。
- 已对齐：进度推进、基础对白结构、等待步骤。
- 缺失：`WARNING` 提示、任务刷新细节、`JLC精灵城_赫克提尔解析信件` 专用处理器。

## 进度 31 -> 32 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `ZX.克林姆德王` `400` 范围内，且 `剧情进度 == 31`。
- 事件类型：单位范围剧情。

TS对应：
- 入口：`克林姆德王紧急会议 -> elven_city_emergency_meeting`
- 剧情片段：`31．王城紧急会议.ts`
- 动作ID：`JLC精灵城_紧急会议`
- 专用处理器/通用执行器：没有专用处理器，只能靠通用执行器推进 `目标进度: 32`。

顺序核对：
1. JASS：设置进度 `32` -> 触发单位 `stop` -> 大段会议对白 -> 更新任务。
2. TS：片段有 3 段对白；`beforeDialog(1)` 的 `JLC精灵城_紧急会议` 只有通用进度推进。
3. 差异说明：第二章末战真正的启动桥接还没落；TS 这里只有会议壳子，没有进入 `32` 阶段后的独立 Boss 战流程。

结论：
- 状态：可推进到 `32`，但只到“会议结束”。
- 已对齐：进度推进、基础会议对白。
- 缺失：`JLC精灵城_紧急会议` 专用处理器、会议长对白细节、第二章末战启动承接。

## 进度 32 -> 33 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`剧情进度 == 32` 时章节末 Boss 死亡。
- 事件类型：Boss 死亡后的长对白承接。

TS对应：
- 第二章运行文件：`00．主线剧情/32．第二章王子Boss战过程.ts`
- 当前实现：`32` 片段本体已存在，但源 JASS 这一步的真正承接仍是 `死亡触发.j` 里的进度死亡事件。
- 后续片段：`33．第二章王子Boss战后承接.ts`，片段 ID `elven_city_chapter_boss_death_bridge`
- 动作ID：`SW01死亡事件_章节末长对白承接`
- 索引/处理器：当前 TS 仅有片段和处理器；具体“哪一个死亡单位”触发 `32 -> 33`，源 JASS 未写明，TS 侧也尚未实装明确单位判定。

顺序核对：
1. JASS：Boss 死亡后直接设置进度 `33` -> 长对白链 -> `WARNING` 消息 -> 更新任务。
2. TS：`33` 号片段与处理器已存在，但当前仍缺“死亡单位判定入口”；也就是还没有把这一步稳定挂到一个已查实的战斗单位上。

结论：
- 状态：片段与处理器已落地，但死亡触发入口仍未查实到具体单位。
- 已对齐：源 JASS 的 `剧情进度 == 32` -> 写 `33` -> 长对白链 -> `WARNING` -> 任务更新，这条承接语义已被识别。
- 已补：`33．第二章王子Boss战后承接.ts` 的正文和收尾任务已改回 `TRIGSTR_960-1006` 语义，结尾目标为“回到王宫”。
- 缺失：`32 -> 33` 的明确死亡单位判定，或等价的“当前阶段唯一合法死亡触发入口”。
- 风险：若直接把任意死亡都接进来会过宽；若继续假定具体 Boss，又可能和后续实装单位不符。

## 进度 33 -> 34 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\精灵城.j`
- 触发器：`JLC精灵城003`
- 触发条件：触发单位在 `ZX.克林姆德王` `400` 范围内，且 `剧情进度 == 33`。
- 事件类型：Boss 战后返王城承接。

TS对应：
- 当前片段层：`32．第二章王子Boss战过程.ts` 的对白内容实际对齐的是这一步 `33 -> 34`。
- 现有相关文件：`32` 片段当前用 `剧情进度 == 33` 进入，并在前置动作里写 `34`、发布“击败里科特”任务。
- NPC入口：`克林姆德王密室承接` 现在就是 `需要剧情进度: 33`，对应这段回王城/密室门前承接。

顺序核对：
1. JASS：靠近克林姆德王时设置进度 `34` -> 长对白 -> 更新任务。
2. TS：这一步并非缺失，而是被当前 `32` 号文件承接了；问题在于“文件序号/命名”和源 JASS 真实步进并不完全直观。

结论：
- 状态：主干已落地，但序号语义存在混淆。
- 已对齐：`剧情进度 == 33` 靠近克林姆德王 -> 写 `34` -> 长对白 -> 更新“击败里科特”任务。
- 缺失：需要在文档层明确说明当前 `32` 文件承担的是源 JASS 的 `33 -> 34` 承接内容。

## 进度 34 -> 35 审查结果

JASS来源：
- 文件：`JASS\世界地图\主线剧情\死亡触发.j`
- 触发器：`死亡事件SW01`
- 触发条件：`剧情进度 == 34` 时章节末最终收束触发。
- 事件类型：章节末最终收束。

TS对应：
- 剧情片段：`34．第二章后续承接.ts`，片段 ID `elven_city_chapter_end`
- 动作ID：`SW01死亡事件_章节末最终收束`、`JLC精灵城_章节末任务刷新`
- 专用处理器/通用执行器：处理器已存在；当前 TS 还额外假定“第二章末战 Boss = 里科特王子”，并在 `Boss死亡剧情索引.ts` 里做了临时绑定，但这不是源 JASS 已查实的单位判定。

顺序核对：
1. JASS：设置进度 `35` -> 多段对白 -> 更新任务。
2. TS：片段配置和处理器都存在；当前问题不在对白壳，而在“死亡单位绑定是 TS 假定，不是源 JASS 明确给出的单位条件”。

结论：
- 状态：片段和处理器已落地，死亡触发入口采用了临时假定。
- 已对齐：源 JASS 的 `剧情进度 == 34` -> 写 `35` -> 收束对白 -> 任务更新，这条语义已落到 TS。
- 已补：`34．第二章后续承接.ts` 的正文和任务刷新已改回 `TRIGSTR_1007-1019` 语义，结尾目标为“查看克林姆德王他们的伤势（暂未制作）”。
- 假定：当前 TS 暂把这一步死亡单位绑定成 `里科特王子`；后续若实装单位名/姿态不同，需要回改索引。
- 缺失：源 JASS 级别可查实的具体死亡单位条件，以及完整对白等价度补齐。
