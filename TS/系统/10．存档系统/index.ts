/**
 * 存档系统
 *
 * 目前只提供玩家本机配置存档能力，不在 init 阶段主动读写文件。
 * 业务系统需要时显式调用 `加载玩家本地存档/保存玩家本地存档`。
 */

export * from "./01．本地存档/index";

import { 初始化本地存档内存默认值 } from "./01．本地存档/index";

export function init(): void {
  初始化本地存档内存默认值();
}
