/** @noSelfInFile */

import { 单位持有影骨战利品, 影骨战利品装备名 } from "./149．影骨战利品公共";

const { 注册击杀回复触发模板 } = require("系统.03．技能系统.00．技能模板+函数.04．机制组件.09．装备通用机制.07．击杀回复触发模板") as {
  注册击杀回复触发模板: (this: void, 配置: any) => any;
};

function 盗贼首领徽记触发条件(this: void, event: any): boolean {
  return 单位持有影骨战利品(event.击杀单位, 影骨战利品装备名.盗贼首领徽记);
}

注册击杀回复触发模板({
  名称: "盗贼首领徽记",
  冷却秒数: 0.5,
  恢复魔法值: 40,
  恢复最大魔法比例: 0.04,
  使用默认魔法特效: true,
  触发条件: 盗贼首领徽记触发条件,
});

export {};
