/** @noSelfInFile */

import { 初始化治疗触发主动技能核心 } from "../01．治疗触发/02．治疗触发核心";
import { 初始化施法主动技能核心 } from "../02．施法触发/03．施法主动技能核心";
import { 初始化物品主动技能核心 } from "../03．物品使用触发/02．物品使用主动技能核心";
import { 初始化主动技能壳参数同步 } from "./05．技能壳参数同步";

let 已初始化 = false;

export function 初始化主动技能系统(this: void): void {
  if (已初始化) return;
  已初始化 = true;
  初始化治疗触发主动技能核心();
  初始化施法主动技能核心();
  初始化物品主动技能核心();
  初始化主动技能壳参数同步();
}

初始化主动技能系统();

export {};
