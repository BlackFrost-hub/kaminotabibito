# 任务 UI 视觉设计稿

状态：设计参考已生成，暂缓接入游戏。2026-09-12 用户要求归档，后续再实施。

## 图片产物

| 文件 | 实际尺寸 | 用途 |
| --- | --- | --- |
| [初始设计稿](../../../output/imagegen/task-ui-1k.png) | 1254 × 1254 | 服务返回原图，文件名保留 1k |
| [放大设计稿](../../../output/imagegen/task-ui-2k.png) | 2048 × 2048 | Lanczos 等比例放大参考图 |

![任务 UI 设计稿](../../../output/imagegen/task-ui-2k.png)

## 生成记录

- 服务：nikoapi.xyz；请求模型 gpt-image-2.5。
- 请求 size=1024x1024、quality=high、reasoning_effort=high。服务实际输出尺寸如上；没有独立证据证明服务执行了推理强度参数。
- 最终成功请求使用文字生成，参考原截图的风格描述；不是对原生边框进行像素级保留。
- 原截图位于临时目录 codex-clipboard-e67e9cde-efab-4a71-8594-ab4f16cc7e09.png，不作为长期资源依赖。
- 前轮临时请求脚本曾写入密钥，随后已删除；本记录不保存密钥。

## 设计方案

银灰石质与金属外框、深蓝石纹底色、金色细边。顶部任务标题及主线/支线/小任务标签；上半部紧凑任务列表，卷轴图标、选中高亮、右侧状态与金色滚动条；下半部独立详情区域，任务说明、目标、NPC、奖励、状态分区。标题金色、NPC 青色、进度绿色，正文浅色。

图中任务名称、人物、奖励与数量均为示例，不是正式任务配置。放大不增加原始细节。整图包含背景场景和文字，不能直接当作可用的 UI 切片素材。

## 后续实施

## 现有素材归属核对（2026-09-12）

- `imports/UI/renwu/page_00.tga ~ page_07.tga`、`jieshourenwuanniu.tga`、`indicator.tga` 属于其他独立系统素材，不属于当前任务 UI。
- 当前任务 UI 的实际 Frame/FDF 素材来源是 Warcraft III 原生 Human UI：`human-options-menu-background.blp`、`human-options-menu-border.blp`、`slider-background.blp`、`slider-border.blp`、`slider-knob.blp`，以及默认任务图标 `BTNScroll.blp`。
- 后续任务 UI 美术重构必须以 `imports/UI/TaskUI.fdf` 的实际引用和九宫格参数为基准，不能把 `imports/UI/renwu` 的素材当作任务 UI 参考或替换目标。

## 背景纹理初稿（2026-09-12）

- 参考原稿背景：黑蓝色、低对比度、烟雾状矿物斑驳和不规则暗纹；背景不包含文字、边框或图标。
- 初稿：[task-ui-background-stone-blue-v1.png](../../../output/imagegen/task-ui-background-stone-blue-v1.png)
- 初稿为程序生成的无文字纹理，仅作视觉验证，尚未接入 `TaskUI.fdf`。
- 生成目标为可重复铺设的背景纹理；边框、金色细线和滚动条仍由独立素材处理。

1. 按实际游戏面板尺寸确认列表与详情比例、文字换行和缩放可读性。
2. 分离无文字背景、边框、标签与滚动条资源；运行时文本继续从任务数据生成。
3. 保留玩家槽位隔离、动态列表创建和已验证的滚轮/拖拽路径，不恢复大规模预创建或加载期全局鼠标注册。
4. 双开检查独立开关、分类、展开、翻页和聊天过滤后再替换正式视觉。

生成提示词要点：Warcraft III task journal, native-style silver-gray stone and metal frame, dark navy stone interior, restrained gold trim, top categories, parchment task icons, selected state, lower detail pane, objectives/NPC/rewards/status, readable game typography; avoid modern web cards, glassmorphism, neon and watermark.
