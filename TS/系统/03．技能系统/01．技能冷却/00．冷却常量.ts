/**
 * 技能冷却常量配置
 *
 * 本文件包含所有冷却缩减相关的上限配置
 * 修改上限值即可影响整个系统，无需改动核心逻辑
 */

//=============================================================================
// 一、冷却缩减上限（玩家上限）
//=============================================================================

/**
 * 通用冷却缩减上限
 * 玩家的「冷却缩减」属性上限
 * 默认：0.33（33%）
 * 修改示例：改为0.40表示40%上限
 *
 * 注意：可通过「冷却缩减加成」属性突破此上限
 */
export const COOLDOWN_REDUCTION_CAP = 0.33;

//=============================================================================
// 二、技能独立冷却上限
//=============================================================================

/**
 * 技能独立冷却上限配置
 *
 * key: 技能ID（字符串形式，如 'A0IM'）
 * value: 冷却上限
 */
export const SKILL_COOLDOWN_CAPS: Record<string, number> = {
  /**
   * A0IM: 25%上限
   */
  'A0IM': 0.25,
  /**
   * A0IP: 20%上限
   */
  'A0IP': 0.20,
  /**
   * A0DG: 33%上限（不可突破）
   */
  'A0DG': 0.33,
};

//=============================================================================
// 三、技能黑名单
//=============================================================================

/**
 * 完全排除的技能ID（不参与冷却缩减）
 */
export const COOLDOWN_BLACKLIST: string[] = [
  'A0FW',
  'A01W',
  'A0IN',
  'A0IO',
  'A0J8',
  'A0K5',
  'A0JP',
  'A0J3',
  'A0K6',
  'A0KR',
  '0005',
];

/**
 * 独立设置冷却的技能（在各自技能触发中处理）
 */
export const INDEPENDENT_COOLDOWN_SKILLS: string[] = [
  'A0EA',
  'A0DB',
  'A0DG',
];
