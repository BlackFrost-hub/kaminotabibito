# 欧尔贝克 技能迁移计划 / 进度记录

> 英雄：欧尔贝克（H012，父类 Harf，模型 Guts.mdl）
> 技能：Q 横一字斩/十字斩（A0IY）、W 积攒（A0IZ）、E 千枝枪剑气（A0J0，被动）、D 刚剑骑士（A0J3）
> 源 JASS：`map/map/war3map.j`（Trigger: OEBR 与全局伤害触发器 KllGJTXXT 的 H012 分支）

## 目录结构

| 文件 | 职责 | 状态 |
| --- | --- | --- |
| 00．配置.ts | 英雄与 Q/W/E/D 全部数值、特效、音效参数 | ✅ |
| 00A．表现工具.ts | 单位音效播放、动作播放（时间缩放+动作序号） | ✅ |
| 00B．积攒状态.ts | W 积攒剩余普攻次数计数（TS 哈希表替代 YDHT） | ✅ |
| 01．被动效果.ts | E 千枝枪剑气：造成伤害消耗积攒 + 概率触发弹幕 | ✅ |
| 02．Q技能.ts | Q 横一字斩/十字斩：冲锋位移 + 落点扇形伤害 + 眩晕 | ✅ |
| 03．W技能.ts | W 积攒：攻击力/暴击率加成 + 周期特效 + 结束还原 | ✅ |
| 05．D技能.ts | D 刚剑骑士：防御 / 掩护 / 挑衅 三分支 | ✅ |
| index.ts | 模块导出并随英雄技能 index 注册 | ✅ |

## 各技能逻辑要点

### Q 横一字斩 / 十字斩（A0IY）
- 施法后 0.35s 前摇，沿施法方向冲锋（步距 20 码/0.03125s 周期）。
- 落点结算 500 码内、前方 ±140° 扇区：攻击力 × (180% + 10% × 等级) 物理伤害。
- 有「积攒」Buff → 十字斩（眩晕 1.5s，消耗积攒）；无 → 横一字斩（眩晕 0.75s）。
- 位移走封装 `开始冲锋`（`02．冲锋·击退/03．对外接口`）；伤害走 `造成单体技能伤害`；眩晕走 `施加眩晕`。

### W 积攒（A0IZ）
- 攻击力提升 = 攻击力 × (25% + 3% × 等级)，暴击率提升 = 5% + 1% × 等级（`临时调整攻击` / `调整玩家属性`）。
- 周期 0.15s 播放特效并检查结束（计数耗尽 / Buff 消失 / 单位死亡 / 5s 到期），任一触发即还原属性并清状态。
- 重复施法先还原旧效果再重开，避免属性叠加。

### E 千枝枪剑气（A0J0，被动）
- 欧尔贝克造成伤害：先消耗 1 次积攒计数（若有）。
- 剑气触发：有积攒 60% / 无积攒 20%，伤害 = 攻击力 × (100% + 5% × 等级)。
- 弹幕走封装 `创建原生弹幕`（`01．弹幕/01．TS原生弹幕`）；伤害监听走 `registerAppliedFinalDamageListener`。

### D 刚剑骑士（A0J3）
- 自己：附加防御技能 S00C + 伤害减少 50%（`调整单位属性 "伤害减少%"`），持续 3s。
- 友军：掩护 2s，单次伤害 ≥ 目标最大生命 10% 时取消伤害、施法者瞬移目标身后 125 码并免伤 0.5s（`registerDamageModifier`）。
- 敌军：每 0.3s 命令目标攻击自己共 5 次（`命令攻击来源`），并播放挑衅特效。

## 优化说明（封装函数使用）

- Buff 判定 → `单位拥有原生Buff`（`00．公共/03．暴击被动公共工具`，`GetUnitAbilityLevel > 0`），不再直接调 `UnitHasBuffBJ`。
- 单位类型判定 → `单位是指定类型`。
- 存活判定 → `单位存活`（`02．通用函数/19．战斗公共工具`）。
- 最大生命读取 → `读取单位最大生命`，替换 japi `GetUnitState(UNIT_STATE_MAX_LIFE)`。
- 特效朝向 → `创建点特效` 使用 `面向角度`（EC_CreateEffect 朝向参数），非 `Z轴角度`（旋转）。
- 未使用的 jass/japi 导入已清理。
- ANCIENT / MECHANICAL / STRUCTURE 过滤沿用项目英雄约定（`IsUnitType` 窄调用），无更高层封装。

## 校验
- `npm run build:lua`（tstl）编译通过，0 错误。
