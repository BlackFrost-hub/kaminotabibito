# 影骨莫特斯技能复盘

## 阴影穿梭施法函数导出异常

- 问题现象：旧测试命令 `影骨莫特斯` 后输入 `1`，运行时在 `03．阴影穿梭.lua` 调用 `开始影骨莫特斯常规施法` 时报告 `attempt to call a nil value`。
- 根因：`03．阴影穿梭.ts` 已从 `11．公共工具.ts` 导入该函数，且 TS 源中的导出存在；但增量构建只刷新了调用方，没有同时刷新被修改的公共工具文件。运行时加载的旧版 `11．公共工具.lua` 没有该导出字段，因此调用方得到 `nil`。这不是技能参数错位或 `self/nil` 调用形态问题。
- 涉及文件：`03．阴影穿梭.ts`、`11．公共工具.ts`、`03．阴影穿梭.lua`、`11．公共工具.lua`；排查入口包含 `09．技能入口.ts`、`16．影骨莫特斯AI.ts` 和 `影骨莫特斯Boss技能测试.ts`。
- 修改内容：本次未改动已有 TS 业务源；使用局部构建同时编译 `03．阴影穿梭.ts` 与 `11．公共工具.ts`，使调用方和模块导出重新同步。未修改 `scripts/fix-lua-for-pack.js`，未手动打包地图。
- 复用封装：`11．公共工具.ts` 的施法包装继续复用 `开始硬直` 和 `显示常规技能吟唱条`；技能入口、AI 调度和测试命令保持现有结构。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/03．阴影穿梭.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/11．公共工具.ts"` 成功；`fix-lua-for-pack` 更新 `0` 个文件，Lua 同步完成。生成 Lua 已确认公共工具导出存在，调用方以普通函数形态调用。
- 测试命令：选择命令已从 `Boss影骨莫特斯` 改为 `Boss莫特斯`；`Boss名称` 仍保留 `影骨莫特斯`，只影响提示文本，不改变单位或技能配置。
- 待实测：尚未进图验证。需要通过 Warcraft VSCode `Run Map`，输入 `Boss莫特斯` 后输入 `1`，确认不再报错，并观察硬直、吟唱条、透明、无敌、位移和背刺准备是否完整生效。
- 可复用经验：新增或修改模块导出函数时，`build:files` 必须同时传入导出方和调用方的实际修改 TS 文件；只刷新调用方会留下旧 Lua 模块表，最终表现为运行时 `nil` 导出。

## 背刺成功后的血月特效

- 问题现象：测试命令 `1` 的下一次纯普通攻击命中后，即使目标正面朝向莫特斯、仅触发正面减伤，仍然会创建背刺血月特效。
- 根因：血月特效原先只判断“背刺准备后的普攻命中”，没有复用 90 度背刺判定区分正面命中与真正背刺。
- 修改内容：保存 `目标正面朝向来源` 的结果；保留正面命中的 `正面减伤比例`，仅在非正面命中、即背刺成功时创建 `背刺命中血月` 特效。爪痕特效和背刺音效保持原有命中反馈。
- 复用封装：继续使用 `目标正面朝向来源` 和 `创建点特效`，未新增公共函数或重复角度计算。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/03．阴影穿梭.ts"` 成功；`fix-lua-for-pack` 更新 `0` 个文件，Lua 同步完成。
- 待实测：尚未进图验证。需分别让固定测试目标正面和背面承受命令 `1` 强化后的下一次普攻，确认正面只有普通背刺命中特效，背面才出现血月特效；同时确认正面减伤仍为配置值 `0.5`。

## 施法表现审查

- 阴影穿梭和暗影禁锢会在施法开始前面向锁定目标；阴影穿梭的消失、无敌和现身仍按同一目标运行，暗影禁锢的圆形预警与结算使用同一坐标快照。
- 阴影穿梭、骸骨召唤、暗影禁锢、幽影爆发和盗贼的遗产均已接入统一常规施法封装，使原有动作窗口同时具备 Boss 硬直和吟唱条。
- 幽影爆发、骸骨召唤和盗贼的遗产属于中心展开或分批召唤，不存在需要朝某个玩家锁定的直线方向；未强行添加无意义转向。
- 测试命令现按阶段覆盖骸骨召唤：`2` 为 P1 基础、`2-2` 为 P2、`2-3` 为 P3 强化。P1/P2 召唤组全灭后会重组骸骨战士，P3 明确不再重组；命令处理会同步设置 Boss 实际生命比例和运行时阶段。
- 影骨的背刺准备、幽灵形态和暗影强化都由 `1`、`4`、`2`/`2-kill` 的正式技能链自然产生，没有独立的“纯被动空命令”。
- 本 Boss 当前没有矩形/方向直线提示调用，未发现“中心点误传为路径起点”的风险点。
- 待实测：需要进图观察各技能读条与第一批召唤/领域表现的时点，并确认阴影穿梭清理时能恢复可见和可受击状态。

## 骸骨召唤快速击杀测试命令

- 问题现象：普通命令 `2` 会按 3 秒时间线分批召唤骷髅盗贼；验证“全部死亡后 3 秒重组骸骨战士”时，必须等待召唤完成后再手动逐个击杀，测试路径过长。
- 根因：正式 `04．骸骨召唤.ts` 已具备召唤物创建、死亡掉落、死亡计数和阶段一/二重组能力，但莫特斯测试入口没有直接创建同一召唤组并批量死亡的命令。
- 涉及文件：`04．影骨莫特斯Boss技能测试.ts`、其生成 Lua；正式复用链路为 `04．骸骨召唤.ts` 的 `创建影骨召唤物`、死亡回调和重组调度。
- 修改内容：新增自定义命令 `2-kill`。它绕过吟唱，立刻在 Boss 周围创建 4 个骷髅盗贼并组成正式召唤组；2 秒后由模块级具名回调逐个调用 `KillUnit`。死亡后的符咒掉落、P1/P2 的 3 秒骸骨战士重组和 P3 停止重组都继续走正式逻辑。重复输入时会先终止上一组测试骷髅的重组资格并击杀其存活成员，避免堆积；延迟回调登记到 Boss 上下文清理篮子。
- 复用封装：复用 `创建影骨召唤物`、`极坐标X/Y`、`addDelayedCallback`、`Boss测试单位存活` 和上下文的 `登记延迟回调`，未复制正式召唤物的死亡、符咒或重组实现。
- 构建验证：`npm run build:files -- "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 `0` 个文件，Lua 同步完成。已检查生成 Lua：`2-kill` 已注册，2 秒回调是具名函数，`创建影骨召唤物`、`addDelayedCallback` 和 `KillUnit` 均未出现 `nil/self` 参数错位。
- 待实测：需进图输入 `Boss莫特斯` 后输入 `2-kill`。确认 0 秒同时出现 4 个骷髅盗贼、约 2 秒全部死亡并掉落符咒，P1/P2 再约 3 秒生成 1 个骸骨战士；将 Boss 调整到 P3 后应只死亡且不重组。还需确认在 2 秒内重复输入命令不会留下额外重组。
- 可复用经验：需要验证“全部召唤物死亡”的 Boss 机制时，测试命令应直接复用正式创建函数和同一召唤组，再通过具名延迟回调触发死亡；不要在测试层重写死亡、掉落或阶段判定。

## 骸骨符咒改为敌对单位拾取

- 问题现象：骸骨符咒使用“敌对英雄列表”命名的筛选入口，实际拾取条件需要覆盖普通敌对单位；测试场景中玩家英雄也不应被误认为可拾取对象。
- 根因：公共目标模块同时维护敌对英雄和普通敌对单位，但莫特斯技能调用的是名称较窄的 `获取Boss技能敌对英雄列表` 别名，无法直观表达符咒对所有敌对目标的意图。
- 涉及文件：`04．骸骨召唤.ts`、生成的 `04．骸骨召唤.lua`；公共目标封装为 `系统.01．单位系统.06．仇恨系统.05．技能目标选择`。
- 修改内容：骸骨符咒的 `可拾取单位列表` 改为调用 `获取Boss技能敌对目标列表`，复用公共敌对目标筛选。公共筛选会排除无效单位、友军、召唤物、幻象、蝗虫单位和远古单位；正式战斗返回敌对英雄与普通单位，测试场景有注册测试目标时仍优先使用注册目标列表。
- 复用封装：继续复用 `创建战斗内拾取物` 的 50ms 距离检测和 140 码拾取半径，没有改成右键物品，也没有复制单位枚举逻辑。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/04．骸骨召唤.ts"` 成功；`fix-lua-for-pack` 更新 `0` 个文件，Lua 同步完成。生成 Lua 已确认导入并调用 `获取Boss技能敌对目标列表`，旧入口引用已消失。
- 待实测：需进图验证正式敌对单位和莫特斯测试注册目标进入 140 码后会自动拾取并获得 12 秒骸骨符咒 Buff；当前测试 Boss 与玩家英雄同属玩家一阵营，玩家英雄仍不会因“敌对单位”筛选自动成为拾取者。
- 可复用经验：技能语义涉及英雄和普通单位时，优先调用“敌对目标列表”而不是名称含“英雄”的兼容别名；同时保留测试目标注册机制，避免测试场景枚举整张地图。

## 骸骨召唤伤害、暗影强化与骸骨符咒复盘

- 问题现象：骷髅贫血金币惩罚在最终伤害完成后直接追加，未经过护甲/魔抗；幽影爆发期间的暗影强化只有 Buff，没有实际把召唤物普攻转换为暗属性魔法伤害；骸骨符咒只有 Buff 记录，没有实际属性施加和到期/驱散回滚。
- 根因：原伤害链只有最终伤害修正回调，伤害类型快照和完整伤害计算直接读取原生事件类型；召唤物伤害映射后使用 `attacker` 查表会丢失召唤物身份；符咒 Buff 未绑定临时属性效果实例。
- 涉及文件：`04．骸骨召唤.ts`、`06．幽影爆发.ts`（已复核，未直接修改）、`02．数值与表现配置.ts`、`TS/系统/05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/04．影骨莫特斯.ts`、`TS/系统/04．伤害系统/00．伤害计算/04．主计算流程.ts`、`06．伤害修正回调.ts`、`07．伤害类型转换.ts`、`00．伤害计算/index.ts`。
- 修改内容：新增带优先级和注销接口的伤害类型转换层；新增免疫判定后、护甲/魔抗前的基础伤害修正层；贫血惩罚改为 `Boss攻击力 × 1.2 + 目标最大生命值 × 4%` 的基础伤害；暗影强化召唤物的原始普通攻击事件先归零，再以原伤害值和 `DAMAGE_TYPE_SHADOW_STRIKE` 重新提交进入正式计算；骸骨符咒施加+15护甲、+20%魔抗、持续12秒，并通过 Buff `onRemove` 和临时属性效果实例回滚。
- 复用封装：复用 `计算组合技能伤害`、`临时属性效果`、Buff `registerManualBuff` 的 `onRemove`、伤害归属 `originalAttacker`、现有 `DAMAGE_TYPE_SHADOW_STRIKE` 和装备属性键 `魔抗`。
- 技能文件复核：`06．幽影爆发.ts` 原有 Boss 幽灵形态承伤修正继续复用统一伤害上下文；接入有效伤害类型后，物理减伤/魔法增伤会自动读取转换后的标记，因此无需复制或改写该技能的承伤逻辑。
- 构建验证：完整 `npm run build` 成功，`tstl`、`fix-lua-for-pack` 和 `sync-lua-from-ts` 完成；随后针对最后的无行为变化清理补丁重跑 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/04．骸骨召唤.ts"` 成功；未修改 `scripts/fix-lua-for-pack.js`，未打包地图。
- 待实测：尚未进图验证。需分别测试骷髅普通阶段对高护甲目标的贫血伤害、低金币目标的扣金与附加伤害、幽影爆发期间召唤物对护甲/魔抗目标的暗属性魔法伤害、符咒重复拾取，以及Buff到期和驱散后的护甲/魔抗回滚；还需确认伤害数字表现是否符合项目对普通攻击显示颜色的既有约定。
- 可复用经验：伤害类型转换必须同时维护原始类型和有效类型；涉及召唤物的伤害修正应优先使用 `originalAttacker`；会影响护甲/魔抗的附加伤害必须在最终伤害前进入基础伤害链路，不能在最终伤害结果上直接相加。

## 暗影强化原生伤害重提交复盘

- 问题现象：幽影爆发期间输入 `Boss莫特斯`、`4`、`2` 后，原生伤害事件仍按物理类型进入事件链，伤害数字也显示为物理色。
- 根因：旧转换层只修改 TS 伤害计算快照，不能修改原生事件的伤害类型；同时伤害数字保留了“普通攻击优先物理色”的判断。
- 涉及文件：`TS/系统/04．伤害系统/00．伤害计算/07．伤害类型转换.ts`、`TS/系统/04．伤害系统/00．伤害计算/04．主计算流程.ts`、`05．影骨莫特斯/04．骸骨召唤.ts`、`TS/系统/09．表现系统/09．伤害数字模型/01．伤害数字核心.ts`；复核 `05．影骨莫特斯/06．幽影爆发.ts`。
- 修改内容：增加转换层的重提交请求；命中暗影强化的原始普通物理事件时，主流程先将当前事件伤害设为0，再用原攻击者、原伤害值、原攻击/远程/武器参数和暗属性伤害类型调用 `UnitDamageTarget`。骷髅转换回调只接受原始 `DAMAGE_TYPE_NORMAL`，二次暗属性事件不会递归转换；伤害数字继续优先显示明确的暗属性色。
- 复用封装：复用伤害类型转换注册表、`YDWESetEventDamage`、现有 `UnitDamageTarget` 原生接口、`DAMAGE_TYPE_SHADOW_STRIKE` 和伤害数字有效类型标记。
- 构建验证：`npm run build:files -- "TS/系统/04．伤害系统/00．伤害计算/07．伤害类型转换.ts" "TS/系统/04．伤害系统/00．伤害计算/04．主计算流程.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/04．骸骨召唤.ts" "TS/系统/09．表现系统/09．伤害数字模型/01．伤害数字核心.ts"` 成功；随后完整 `npm run build` 成功，生成 Lua 已核对事件归零、原伤害值重提交和二次暗伤递归保护；未修改 `scripts/fix-lua-for-pack.js`，未打包地图。
- 待实测：进图输入 `Boss莫特斯`、`4`、`2`，确认原物理事件不扣血、只出现一次暗属性伤害数字；再用高护甲/高魔抗目标确认只按二次暗属性魔法伤害计算，并确认偷金币/攻击效果未重复触发。构建通过不能替代进图验证。
- 可复用经验：需要改变原生伤害类型时，不能只改计算快照；应把原事件归零后重新提交真实类型，并用原始类型或重提交标记阻止转换递归。

## 骸骨符咒拾取验证

- 问题现象：用户反馈骸骨符咒完全无法拾取。
- 根因：日志已确认拾取链路正常；某次候选距离为 145.58，超过配置拾取半径 140，因此当次不会拾取。单位随后进入范围后，连续出现触发拾取、属性施加和 reason=拾取。
- 涉及文件：04．骸骨召唤.ts、战斗内拾取物.ts、05．技能目标选择.ts、04．影骨莫特斯Boss技能测试.ts。
- 修改内容：临时加入的莫特斯符咒 debugLogForce 定位日志已确认问题后删除，正式拾取逻辑未改变。
- 复用封装：继续使用 获取Boss技能敌对目标列表、创建战斗内拾取物 的 50ms 检查和 140 码距离判定；敌对单位为 owner=12 的测试步兵/单位。
- 构建验证：删除日志后重新执行局部构建并检查生成 Lua；未打包地图。
- 待实测：无代码待验证项；后续若再次反馈无法拾取，应优先检查候选单位是否进入 140 码范围。
- 可复用经验：该机制是靠近自动拾取，不是右键物品；重复拾取时先清理旧属性效果再重新施加属于 Buff 刷新行为。

## 骸骨召唤阶段强化测试命令

- 问题现象：测试命令 2-2 和 2-3 只设置 Boss 阶段后执行普通骸骨召唤，实际进图看不到阶段差异。
- 根因：P1/P2 的正式骸骨召唤规则都允许全部骷髅死亡后重组，P3 的差异只在全部骷髅死亡后停止重组；原 2-2/2-3 不会自动击杀召唤物，因此无法直接观察该差异。
- 涉及文件：12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts、04．骸骨召唤.ts。
- 修改内容：2-2 和 2-3 改为复用正式 创建影骨召唤物 链路，分别设置 Boss 生命比例为 60%/30%，在完整分批召唤完成后约 3.5 秒批量击杀骷髅；P2 再经过约 3 秒重组骸骨战士，P3 不重组。2-kill 明确固定为 P1 快速击杀测试。
- 复用封装：复用现有 on影骨技能2Kill延迟击杀、创建影骨召唤物、召唤组死亡计数和正式尝试重组骸骨战士逻辑，未在测试层复制死亡掉落或重组实现。
- 构建验证：`npm run build:files -- "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 曾成功；本轮 P3 强化联动修改后执行 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/01．运行时上下文.ts" "TS/系统/05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/04．影骨莫特斯.ts" "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；未修改 `scripts/fix-lua-for-pack.js`，未打包地图。
- 待实测：需进图输入 Boss莫特斯 后分别输入 2-2、2-3；确认两者在约 3.5 秒击杀骷髅，2-2 再约 3 秒生成骸骨战士，2-3 不生成。当前尚未进图验证。
- 可复用经验：阶段测试命令必须把阶段生命来源和可观察的阶段差异一起准备；只改变上下文阶段、但不触发阶段专属后续事件，不能证明强化生效。

## 2-2 延迟击杀回调报错

- 问题现象：输入 `2-2` 后骷髅已经生成，但没有在约 3.5 秒后被击杀，也没有进入后续骸骨重组；测试入口报 `04．影骨莫特斯Boss技能测试.lua:52: attempt to compare nil with number`。
- 根因：测试入口把正式 `释放影骨骸骨召唤` 的返回值声明为 `any`，TSTL 将 `group.单位列表.length` 原样生成成 `group["单位列表"].length`；Lua 数组没有 `length` 字段，比较时得到 nil，导致延迟击杀回调注册前直接退出。
- 涉及文件：`04．影骨莫特斯Boss技能测试.ts` 及其生成 Lua；正式召唤和重组逻辑 `04．骸骨召唤.ts` 已复核，未发现本次错误来源。
- 修改内容：在测试入口补充召唤组结构类型，并将 `释放影骨骸骨召唤` 的测试侧返回签名改为该类型，使 TSTL 生成 `#group["单位列表"]`；未改变正式召唤、死亡计数或重组规则。
- 复用封装：继续复用正式 `释放影骨骸骨召唤`、`创建影骨召唤物`、`addDelayedCallback` 和 `KillUnit`，延迟回调仍使用模块级具名函数。
- 构建验证：执行 `npm run build:files -- "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；生成 Lua 第 52 行已核对为 `#group["单位列表"] <= 0`，3.5 秒回调已存在；未打包地图。
- 待实测：进图重新输入 `Boss莫特斯`、`2-2`，确认约 3.5 秒全部骷髅死亡并掉落符咒，随后约 3 秒生成 1 个骸骨战士；再输入 `2-3`，确认骷髅死亡后不重组。构建和生成 Lua 核对不能替代进图验证。
- 可复用经验：任何传入 `any` 的数组都不能直接依赖 `.length` 生成 Lua；跨模块返回对象必须补结构类型，或使用项目已有的数组/数量封装后再构建检查生成 Lua。

## P3 阶段强化与文档对齐

- 问题现象：设计文档规定 P3 需要停止骷髅重组、提高莫特斯攻击力 30%、提高幽影爆发使用频率；原运行时进入 P3 时仅更新 `context.阶段`，前两项数值强化没有落地，2-3 也只显示“不重组”而没有说明数值强化。
- 根因：阶段上下文的 `on进入` 回调没有连接 P3 数值配置；测试命令虽然设置了 P3 阶段，但未把攻击力和幽影爆发冷却的可观察信息写入命令提示。P3 Buff 还引用了不存在的 `shadow_overlord.blp` 资源路径。
- 涉及文件：`05．影骨莫特斯/01．运行时上下文.ts`、`05．影骨莫特斯/02．数值与表现配置.ts`、`05．影骨莫特斯/04．骸骨召唤.ts`（复核阶段三不重组逻辑）、`04．影骨莫特斯Boss技能测试.ts`、`TS/系统/05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/04．影骨莫特斯.ts`、设计文档 `影骨莫特斯.md`。
- 修改内容：P3 配置明确为攻击力提高 30%、幽影爆发最大冷却缩短至原始值 65%；进入 P3 时使用现有攻击力读取和 SGSS 属性位 1 施加增量，同时用平台扩展冷却 API 将当前和最大冷却都乘以 0.65；清理上下文或测试切回非 P3 时回滚攻击力、冷却和 Buff。补齐运行时 `P3强化` 的安全类型声明，Buff 图标改为已确认存在的 `shadow_summon_empower.blp`，2-3 提示补充攻击和冷却强化，2-2/2-3 时间提示校正为约 3.5 秒。
- 复用封装：复用 `读取单位攻击力`、`SGSS_SetState`、`技能_获取技能当前冷却时间`、`技能_获取技能最大冷却时间`、`技能_设置技能冷却时间`、`registerManualBuff`，以及骸骨召唤的阶段死亡计数和 `group.阶段 >= 3` 禁止重组判断；未新增 Lua 数学库调用或匿名回调。
- 构建验证：执行 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/01．运行时上下文.ts" "TS/系统/05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/04．影骨莫特斯.ts" "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，生成 Lua 已核对具名 P3 回调、冷却参数和测试命令，未打包地图。
- 待实测：必须进图确认 P3 实际攻击数值相对进入前提高 30%、幽影爆发技能冷却 UI/AI 变为原始值的 65%，并确认 `阴影主宰` Buff 显示已存在图标；还需实测 2-2 约 3.5 秒击杀后约 3 秒重组、2-3 同时具备 P3 数值强化且不重组。构建通过不能替代上述进图验证。
- 可复用经验：阶段文档中的“强化”必须连接到具体运行时状态和可回滚增量；修改技能最大冷却时要同步调整当前冷却，避免剩余冷却超过新上限；新增 Buff 图标必须先在 `imports` 中确认资源，不能凭文件名猜测。

## 暗影禁锢法阵表现与 Buff 图标

- 问题现象：暗影禁锢法阵单位壳可以移动，持续结束后可能残留；设计要求的暗影旋涡未接入；测试命令 `3` 只容易看到原生控制 Buff，未看到 `shadow_prison.blp` 自定义图标。
- 根因：通用可攻击控制法阵没有强制调用 `X_SetUnitMovableSafe`，只依赖召唤物限时生命；法阵附加特效没有保存句柄；莫特斯技能未配置 `Soul Aura.mdx`；测试命令默认仍选中 Boss，而自定义 BuffUI按当前选中单位显示。
- 涉及文件：`00．技能系统/00．技能模板+函数/04．机制组件/05．机制单位/04．可攻击控制法阵.ts`、`05．影骨莫特斯/02．数值与表现配置.ts`、`05．影骨莫特斯/05．暗影禁锢.ts`、`12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts`；复核 `TS/系统/05．Buff系统/02．BuffUI.ts`、Buff表及图标资源，未改动 BuffUI 核心代码。
- 修改内容：法阵创建后显式调用 `X_SetUnitMovableSafe(unit, false)`，并通过具名到期回调主动 `销毁()` 单位；保存并销毁法阵/旋涡句柄，摧毁反馈特效使用安全延迟销毁；接入 `Common\\Effect\\Element\\Dark\\Soul Aura.mdx`，移除莫特斯技能中重复的 `VampireSeal` 点特效；`BSB2` 登记显式覆盖 `BuffIcon\\Boss\\ShadowboneMortes\\shadow_prison.blp`，测试命令 `3` 自动选中被禁锢步兵以显示该目标 Buff。
- 复用封装：复用 `X_SetUnitMovableSafe`、`设置特效缩放`、`YDWETimerDestroyEffectSafe`、机制清理篮子和现有 BuffUI 选中单位事件中心；未新增 Lua 数学库、未修改 TSTL 修复脚本，新增回调均为模块级具名函数。
- 构建验证：执行 `npm run build:files -- "TS/系统/03．技能系统/00．技能模板+函数/04．机制组件/05．机制单位/04．可攻击控制法阵.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/02．数值与表现配置.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/05．暗影禁锢.ts" "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；已核对生成 Lua 的站桩调用、到期具名回调、旋涡路径、图标覆盖和选中目标调用；未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后输入 `3`，确认法阵单位完全不能移动，`VampireSeal.mdx` 法阵壳与 `Soul Aura.mdx` 旋涡同时出现，约 4 秒结束立即删除；确认测试步兵被选中后，自定义 Buff 条显示 `shadow_prison.blp`，原生控制 Buff 与自定义图标同步到期；攻击法阵提前摧毁时确认旋涡和法阵附加特效立即消失，并将剩余控制缩短到 1 秒。
- 可复用经验：需要玩家观察敌对目标的虚拟 Buff 时，测试入口必须把目标选中状态和 Buff 登记时机一起准备；持续机制单位不能只依赖 `UnitApplyTimedLife`，附加特效必须绑定到实例销毁/死亡路径。

## 幽影爆发召唤物持续时间

- 问题现象：命令 `4` 的幽影爆发持续 20 秒，但骷髅盗贼和骷髅射手在技能结束前陆续消失，无法稳定观察结束时的 90% 当前生命移除。
- 根因：`02．数值与表现配置.ts` 中统一的 `骷髅持续秒` 为 18 秒；`04．骸骨召唤.ts` 的 `创建影骨召唤物` 会将该配置用于骷髅盗贼和骷髅射手，导致早期生成单位先于 `06．幽影爆发.ts` 的 20 秒结束回调自然到期。
- 涉及文件：`05．影骨莫特斯/02．数值与表现配置.ts`、`05．影骨莫特斯/04．骸骨召唤.ts`（复核持续时间读取）、`05．影骨莫特斯/06．幽影爆发.ts`（复核结束处理）、设计文档 `05．影骨莫特斯/影骨莫特斯.md`。
- 修改内容：将骷髅盗贼和骷髅射手共用的 `骷髅持续秒` 从 18 秒调整为 25 秒；设计文档明确 25 秒持续时间，以及幽影爆发结束时对存活召唤物移除 90% 当前生命且不致死。
- 复用封装：继续复用集中数值配置、`创建影骨召唤物` 的骷髅持续时间分派，以及 `06．幽影爆发.ts` 中的 `执行非伤害生命移除({ 不致死: true })`；未新增封装，未改动骸骨战士 24 秒持续时间。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/02．数值与表现配置.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，生成配置 Lua 已确认 `骷髅持续秒 = 25`；未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后输入 `4`，确认骷髅在幽影爆发结束前不因持续时间自然消失，并在 20 秒结束时存活骷髅保留约 10% 当前生命而不死亡；还需确认骷髅盗贼和骷髅射手均使用 25 秒配置。构建通过不能替代进图验证。
- 可复用经验：当召唤物需要在技能结束回调中统一处理时，持续时间必须覆盖“技能持续时间 + 最后一批召唤的生成窗口”；应从集中配置修正，而不是在技能或测试入口重复覆盖。

## 幽影爆发承伤测试与状态持续读条

- 问题现象：命令 `4` 只有 1 秒施法硬直对应的常规吟唱条，没有覆盖幽影爆发 20 秒状态的持续读条；同时缺少在状态期间直接对莫特斯分别提交物理和魔法伤害的对比测试。
- 根因：`11．公共工具.ts` 的 `开始影骨莫特斯常规施法` 只显示常规技能通道，`06．幽影爆发.ts` 释放入口没有调用独立的大招吟唱通道；测试入口也没有复用固定测试步兵提交伤害事件。
- 涉及文件：`05．影骨莫特斯/06．幽影爆发.ts`、`12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts`、设计文档 `05．影骨莫特斯/影骨莫特斯.md`；复核 `11．公共工具.ts`、吟唱条对外接口和伤害系统的 `UnitDamageTarget` 调用形态。
- 修改内容：幽影爆发启动时保留原有 1 秒常规施法条，并新增 20 秒“大招”状态持续条；幽影爆发正常结束和上下文清理时关闭大招通道。新增 `4-2`，启动技能后第 1 秒由固定测试步兵以 `DAMAGE_TYPE_NORMAL` 提交 1000 点物理伤害，第 2 秒以 `DAMAGE_TYPE_MAGIC` 提交 1000 点魔法伤害，并记录提交结果、前后生命和实际扣除值。
- 复用封装：复用吟唱条系统的常规 / 大招独立通道、机制清理篮子、固定测试步兵、`addDelayedCallback`、`UnitDamageTarget` 和 `debugLogForce`；所有延迟回调均为模块级具名函数，未新增伤害或 UI 底层封装。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/06．幽影爆发.ts" "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，已核对生成 Lua 的两个具名延迟回调、伤害参数和常规 / 大招吟唱通道；未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后输入 `4-2`，确认先出现 1 秒施法条，再出现持续 20 秒的大招条；日志应记录第 1 秒物理伤害和第 2 秒魔法伤害，实际扣血应体现幽影爆发的物理减伤 / 魔法增伤。构建通过不能替代进图验证。
- 可复用经验：技能有“施法前摇”和“生效持续时间”两个时间轴时，应使用不同吟唱通道分别表达；对承伤修正做验收时，测试入口应直接提交带明确原生伤害类型的事件，并记录生命差值而不是只看伤害数字颜色。

## 盗贼的遗产测试坐标

- 问题现象：输入测试命令 `5` 后看不到“盗贼的遗产”宝箱。
- 根因：测试入口直接复用正式技能，技能默认读取正式地图的四个宝箱坐标；测试场景中心已平移到 `(-540.6, -2495.2)`，但宝箱点没有随测试场景平移。
- 涉及文件：`05．影骨莫特斯/01．运行时上下文.ts`、`05．影骨莫特斯/07．盗贼的遗产.ts`、`12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts`。
- 修改内容：为莫特斯运行时上下文增加可选测试宝箱点；正式技能优先读取上下文覆盖点，未覆盖时继续读取正式配置。测试入口复用测试中心平移映射，以幽影爆发正式召唤中心为原点复制四个宝箱点到测试场景。
- 复用封装：复用 `创建测试中心平移映射`、`按测试映射平移XY坐标` 和现有正式宝箱生成/交互逻辑；未修改正式宝箱配置，未新增匿名异步回调。
- 构建验证：执行 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/01．运行时上下文.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/07．盗贼的遗产.ts" "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，已核对生成 Lua 的坐标映射调用和上下文宝箱点读取；未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后输入 `5`，确认四个宝箱依次出现在测试场景、可接近开启且不会生成在正式地图坐标；重复输入 `Boss重置` 后再输入 `5`，确认映射仍以正式原始坐标重新计算。
- 可复用经验：测试场景中的机制单位、可交互物和提示点必须通过上下文副本读取测试坐标，不能让正式配置对象被永久改写。

## 测试场景使用玩家英雄开启宝箱

- 问题现象：莫特斯测试场景需要使用地图预设大法师 `gg_unit_Hamg_0002` 测试宝箱，但预设大法师没有被放到测试场内并选中。
- 根因：测试入口虽然已有通用基准英雄函数可返回地图预设大法师，但本场景没有把该英雄移动到测试坐标并选中。
- 涉及文件：`12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts`。
- 修改内容：莫特斯测试场景继续使用通用基准英雄函数返回的地图预设大法师，记录原位置和朝向后将其放到测试场景下方并选中；清理测试时恢复原位置和朝向，未固定英雄移动。
- 复用封装：复用 `获取Boss测试玩家基准英雄`、`Boss测试单位存活`、`设置Boss测试单位满血`、宝箱系统已有玩家英雄目标命令注册和现有测试清理流程；未修改全局玩家英雄获取逻辑和宝箱系统。
- 构建验证：执行 `npm run build:files -- "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，已核对生成 Lua 的预设大法师获取、选中、位置恢复调用；未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后确认选中的是地图预设大法师，再输入 `5`，用该大法师右键四个宝箱，确认自动移动到 150 码内并出现 3 秒开启进度；输入 `Boss清理` 后确认大法师回到原位置。
- 可复用经验：需要玩家操作机制的 Boss 测试，不应只准备固定敌对测试靶；应显式使用当前玩家登记英雄、选中并保留移动能力，同时为临时位置修改提供清理恢复。

## 盗贼遗产宝箱物编与模型迁移

- 问题现象：测试模板图输入 `5` 后创建 `B00Z`，但模板图缺少项目 ObjEditing Lua 中的 `B00Z` 定义；模板表还把模型写成 `102.mdl`，实际资源是二进制 `102.mdx`，因此宝箱模型无法可靠显示。
- 根因：模板图的 `B00Z` 和 `102.mdx` 仍停留在 `map/table` 与 `map/resource/war3mapImported`，没有迁移到项目 Lua 物编入口和 `imports`；同一模型还被 `B00D` 复用，不能只迁移 `B00Z` 而不更新共享引用。
- 涉及文件：`objediting/Destructable/BossRewardChest.lua`、`TS/系统/07．地形系统/06．可破坏物数据/00．可破坏物数据.ts`、`imports/Boss/ShadowboneMortes/ShadowboneMortesThievesLegacyChest.mdx`、`imports/war3mapImported/citynatural.blp`、`imports/war3mapImported/CityStructures.blp`、`imports/war3mapImported/Doodads1.blp`，以及模板图 `map/table/destructable.ini`、`map/table/imp.ini` 和对应旧资源。
- 修改内容：在 Lua 物编入口新增 `B00Z`，父物编使用 `LTbr`，模型改为 `Boss\ShadowboneMortes\ShadowboneMortesThievesLegacyChest.mdx`；同步 `B00D/B00Z` 运行时物编记录。模型按原始哈希迁移，三张被多个模板模型共享的贴图保留 `war3mapImported\` 内部引用并迁移到兼容导入目录。
- 复用封装：复用现有 `DestructableDefinition` 的 `new`、`setName`、`setModel` 和 `LTbr` 父物编；未新增运行时宝箱逻辑、未修改 `fix-lua-for-pack`、未引入匿名回调。
- 构建验证：ObjEditing 0.3.5 已成功生成 `.build/objediting/war3map.w3b`，二进制字符串已确认包含 `B00Z` 和 `Boss\\ShadowboneMortes\\ShadowboneMortesThievesLegacyChest.mdx`；`npm run build:files -- "TS/系统/07．地形系统/06．可破坏物数据/00．可破坏物数据.ts"` 成功，`fix-lua-for-pack` 更新 0 个文件；已核对模型及三张贴图哈希一致，未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后输入 `5`，确认四个 `B00Z` 宝箱模型正常显示、右键可移动到 150 码内并进入 3 秒开启；确认 `B00D` 仍能显示同一模型。构建通过不能替代进图验证。
- 可复用经验：迁移可破坏物时必须同时核对 rawcode、ObjEditing Lua、运行时物编表、模型真实扩展名、模型内部贴图路径和 `imp.ini`；共享贴图不能因为单个模型迁移而直接删除。

## 盗贼遗产宝箱交互 ID 对齐

- 问题现象：莫特斯测试命令 `5` 创建宝箱后，玩家英雄右键会直接攻击宝箱，而不会移动接近并进入 3 秒开启流程。
- 根因：`02．数值与表现配置.ts` 正确创建 `B00Z`，但底层宝箱系统只从 `00．常量定义.ts` 的 `CHEST_TYPES` 建立可交互类型集合，其中“盗贼宝箱”误登记为 `LTbr`。目标指令监听因此跳过 `B00Z`，游戏回退为普通攻击命令。
- 涉及文件：`06．经济系统/00．宝箱系统/00．常量定义.ts`、`05．影骨莫特斯/02．数值与表现配置.ts`、设计文档 `05．影骨莫特斯/影骨莫特斯.md`、`99．技能复盘/影骨莫特斯.md`；复核 `02．事件注册.ts` 和 `03．宝箱核心.ts`。
- 修改内容：将“盗贼宝箱”的 `CHEST_TYPES.destructableType` 从 `LTbr` 改为 `B00Z`；莫特斯遗产宝箱继续生成 `B00Z`，两端 rawcode 统一。不改动事件监听、开启核心或 `B00Z` 的迁移物编资源。
- 复用封装：继续复用 `CHEST_TYPES -> isChestType -> onUnitTargetInteractable` 的既有交互链、`交互宝箱桥接` 和通用 3 秒开启/掉落流程；未新增底层分支或回调。
- 构建验证：`npm run build:files -- "TS/系统/06．经济系统/00．宝箱系统/00．常量定义.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/02．数值与表现配置.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后输入 `5`，用测试场景中的预设大法师右键新生成的 `B00Z` 宝箱，确认不再攻击、会自动接近至 150 码内、显示 3 秒开启进度并按“盗贼宝箱”配置掉落；同时确认迁移模型正常显示。构建通过不能替代进图验证。
- 可复用经验：可破坏物继承自同一父物编不代表运行时 rawcode 相同；接入以类型表拦截的交互系统时，应以实际创建 rawcode 登记类型表，不能只凭父物编判断。

## 盗贼遗产宝箱目标指令与主人搜索诊断

- 问题现象：盗贼遗产已统一创建并登记 `B00Z`，但进图右键宝箱时仍未能确认是否正确进入开启流程；同时底层“盗贼宝箱”的主人仍配置为测试步兵，无法把开启过程的主人广播和干扰归属到莫特斯。
- 根因：主人配置已确认错误，原 `hfoo` 会在测试场景中命中固定步兵而非莫特斯；右键链路的最终根因尚未确认，因为目标命令的“英雄未登记”和“目标类型不匹配”分支此前静默返回，且全局监听与单位特定监听均可能接收同一次指令。
- 涉及文件：`06．经济系统/00．宝箱系统/00．常量定义.ts`、`02．事件注册.ts`、`08．宝箱主人.ts`；复核 `05．影骨莫特斯/00．配置.ts`、`07．盗贼的遗产.ts`、`04．影骨莫特斯Boss技能测试.ts` 与 ObjEditing 中的莫特斯单位定义。
- 修改内容：将 `B00Z`“盗贼宝箱”的主人单位类型从 `hfoo` 改为已确认的莫特斯 rawcode `N01Y`。目标指令日志现记录全局/单位特定来源、指令 ID、操作单位 handle/type、英雄登记状态、目标 handle/type 和可交互判定；主人搜索日志记录期望 `N01Y`、搜索半径、候选数、类型命中数、最终主人和距离平方。STES 与全局监听初始化也会各输出一次注册状态。
- 复用封装：统一使用 `lib.扩展函数.自定义扩展函数.03．调试输出.ts` 的 `debugLogForce`；保留 `CHEST_TYPES -> isInteractable -> onUnitTargetInteractable -> 查找宝箱主人` 既有链路，未新增事件注册入口或匿名回调。
- 构建验证：`npm run build:files -- "TS/系统/06．经济系统/00．宝箱系统/00．常量定义.ts" "TS/系统/06．经济系统/00．宝箱系统/02．事件注册.ts" "TS/系统/06．经济系统/00．宝箱系统/08．宝箱主人.ts"` 成功；构建期间 `fix-lua-for-pack` 更新 `02．事件注册.lua` 1 个生成文件，未修改该脚本、未打包地图。已检查相关生成 Lua：目标监听注册和新增日志均为普通函数调用，具名回调参数未错位。
- 待实测：进图输入 `Boss莫特斯`、`5`，右键一个 `B00Z`。日志应先出现“全局目标命令”或“单位特定目标命令”，其中 `targetType` 必须可交互、`registered` 应为 `true`；随后应出现开启核心的“进入交互”和主人搜索的 `expectedOwnerType=N01Y`。若 `registered=false`，继续检查预设大法师登记；若 `interactable=false`，记录实际 `targetType` 检查物编；若两条来源对同一 target 均进入交互，再据日志决定是否去重。构建通过不能替代进图验证。
- 可复用经验：交互失效的首轮日志必须覆盖“事件是否到达、目标类型是否命中、操作者是否登记、业务状态是否解析”四层；主人类型必须使用实际 Boss rawcode，不能用测试靶类型代替。

## 测试预设大法师玩家英雄登记

- 问题现象：莫特斯测试场景已将预设大法师移动并选中，但右键 `B00Z` 宝箱时未进入交互流程；底层诊断预期会显示 `registered=false`。
- 根因：预设单位 `gg_unit_Hamg_0002` 是地图测试单位，不会自然经过英雄选择流程；测试入口只准备单位，未执行玩家英雄登记联动，因此宝箱系统未为该单位注册目标命令。
- 涉及文件：`12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts`；复核 `00．核心系统/00．玩家系统/00．英雄注册联动/00．玩家英雄获取桥接.ts` 与 `06．经济系统/00．宝箱系统/02．事件注册.ts`。
- 修改内容：在 `Boss莫特斯` 会话增加 `注册` 自定义命令；命令通过 `获取Boss测试玩家基准英雄(player)` 获取预设大法师，再调用 `directRegisterPlayerHero(player, hero)`，触发现有完整英雄依赖登记队列，而非直接调用宝箱模块。
- 复用封装：复用 `directRegisterPlayerHero` 的玩家英雄登记和依赖队列；该队列第 2 阶段会调用既有 `registerChestSystemHero`。新增聊天回调保持模块级具名函数，日志统一使用 `debugLogForce`。
- 构建验证：`npm run build:files -- "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件。已核对生成 Lua：桥接导出被解构为局部函数，并以 `directRegisterPlayerHero(player, hero)` 正确调用；命令回调为具名函数，参数没有 `self/nil` 偏移；未打包地图。
- 待实测：进图依次输入 `Boss莫特斯`、`注册`，等待依赖队列完成后输入 `5` 并右键 `B00Z`。日志应显示 `registered=true`，并进入现有的接近、3 秒开启和掉落流程；构建通过不能替代该验证。
- 可复用经验：测试场景使用预设英雄驱动依赖于“已登记玩家英雄”的系统时，测试入口应提供明确的全链路登记命令，不能只移动或选中该单位。

## 盗贼遗产陷阱眩晕同步

- 问题现象：`B00Z` 的陷阱段使用快速 Buff `BuffID: 0`，但没有显示“阴影陷阱眩晕”自定义 Buff 图标，容易被误判为未施加眩晕。
- 根因：快速 Buff 的 `0` 已确认是原生眩晕控制编号；它不会自动登记莫特斯自定义 Buff `BSB8`。宝箱高级掉落动作此前只有快速 Buff 数字 ID，无法携带自定义 Buff 语义。
- 涉及文件：`06．经济系统/00．宝箱系统/00．常量定义.ts`、`06．经济系统/00．宝箱系统/01．宝箱掉落配置.ts`；复核 `05．影骨莫特斯/07．盗贼的遗产.ts`、莫特斯 Buff 表和快速 Buff 控制实现。
- 修改内容：为“对开启者施加效果”动作增加可选自定义 Buff ID / 来源名称；`B00Z` 的 91-100 陷阱段保留 1.5 秒快速原生眩晕，同时引用 `影骨莫特斯BuffID.阴影陷阱眩晕` 登记同持续时间的 `BSB8`。
- 复用封装：复用 `SFB_setBuff(id=0)` 的原生眩晕和 `registerManualBuff` 的 BuffUI/生命周期登记；未新增控制系统或匿名回调。
- 构建验证：因扩展导出动作类型，执行 `npm run build` 成功；构建期间既有 `fix-lua-for-pack` 规则更新 265 个生成 Lua 文件，未修改该脚本，未打包地图。已核对 `00．常量定义.lua` 的 `BSB8` 引用与 `01．宝箱掉落配置.lua` 的 `SFB_setBuff` / `registerManualBuff` 调用，参数没有 `self/nil` 偏移。
- 待实测：进图开启 `B00Z` 至陷阱随机段，确认开启者当前生命保留 30%、实际眩晕 1.5 秒，并显示 `shadow_trap_stun.blp` 至眩晕结束；需同时确认非陷阱掉落不出现该 Buff。
- 可复用经验：原生控制编号和项目自定义 Buff ID 是两套系统；需要同时满足控制、图标和描述时，应通过配置显式同步两个生命周期。

## 盗贼遗产陷阱命中特效

- 问题现象：盗贼宝箱命中 `91-100` 阴影陷阱时能扣血、眩晕并显示自定义 Buff 图标，但没有陷阱命中特效，且要求效果固定播放在宝箱位置。
- 根因：宝箱高级掉落的“对开启者施加效果”动作没有特效配置或执行步骤；`宝箱配置掉落` 虽然接收宝箱坐标，却没有把坐标继续传入高级掉落上下文，导致新增点特效若直接接入会回退到 `(0, 0)`。
- 涉及文件：`06．经济系统/00．宝箱系统/00．常量定义.ts`、`06．经济系统/00．宝箱系统/01．宝箱掉落配置.ts`、`99．技能复盘/影骨莫特斯.md`；复核 `03．宝箱核心.ts`、`lib/扩展函数/封装函数/01．通用工具/03．特效.ts` 和设计文档 `05．影骨莫特斯/影骨莫特斯.md`。
- 修改内容：为高级掉落的“对开启者施加效果”增加可选命中特效模型、持续时间和缩放配置；`B00Z` 的陷阱段配置 `Common\\Effect\\Element\\Dark\\shadowslam(normal size).mdx`、2 秒、缩放 1.1。掉落执行器在扣血和眩晕前以高级掉落上下文坐标创建点特效，并将 `宝箱位置掉落` / `宝箱配置掉落` 的真实坐标贯通到该上下文。
- 复用封装：复用 `创建点特效` 的自动生命周期和缩放能力；保留 `SFB_setBuff(id=0)` 原生眩晕与 `registerManualBuff` 的 BuffUI 同步；未新增匿名回调、未修改 `fix-lua-for-pack.js`。
- 构建验证：因扩展了导出的 `宝箱高级掉落动作` 类型和掉落执行函数签名，已执行 `npm run build` 成功；构建脚本按既有规则更新 265 个 Lua 文件，未修改该脚本、未打包地图。已核对生成 Lua：特效函数为局部函数直接调用，参数表中的 `X/Y` 来自高级掉落上下文；`宝箱配置掉落 -> 按宝箱配置掉落 -> 执行宝箱掉落` 的实参顺序保留 `x/y`，没有 `self/nil` 偏移。
- 待实测：进图输入 `Boss莫特斯`、`5`，触发 `B00Z` 的 `91-100` 陷阱段，确认 `shadowslam(normal size).mdx` 在被开启宝箱原地播放 2 秒、缩放约 1.1，而不是跟随开启者或出现在 `(0,0)`；同时确认扣血、1.5 秒眩晕和“阴影陷阱眩晕”图标仍正常。
- 可复用经验：高级掉落效果的落点必须与掉落入口共享真实坐标；仅在最终创建物品时保留坐标不足以支持陷阱、爆点等先于物品创建的点特效。

## 移除预设大法师注册测试命令

- 问题现象：莫特斯测试命令组中保留了临时 `注册` 命令，额外占用一个不属于 Boss 技能本身的测试入口。
- 根因：该命令曾用于手动调用 `directRegisterPlayerHero`，让地图预设大法师走玩家英雄登记联动；当前测试流程不再需要暴露该手动入口。
- 涉及文件：`12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts`、`99．技能复盘/影骨莫特斯.md`；复核 Boss 测试命令组与玩家英雄登记桥接。
- 修改内容：移除 `directRegisterPlayerHero` 桥接导入、对应模块级回调和技能列表中的 `注册` 命令项；保留预设大法师准备逻辑及其余莫特斯测试命令。
- 复用封装：继续使用现有 `注册Boss测试命令组` 和其他测试命令回调，未新增回调或修改英雄登记底层。
- 构建验证：`npm run build:files -- "TS/系统/12．测试系统/01．Boss测试/02．挑战与隐藏Boss/04．影骨莫特斯Boss技能测试.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件。已核对生成 Lua：命令表从 `1` 开始，未包含 `注册` 项、`directRegisterPlayerHero` 桥接或对应回调；未打包地图。
- 待实测：进图输入 `Boss莫特斯` 后确认命令组不再响应 `注册`；其余 `1` 至 `5`、`2-*`、`4-2` 命令仍可正常使用。
- 可复用经验：用于临时诊断的测试命令在不再需要时，应同时删除导入、具名回调和命令列表项，避免测试入口暴露失效流程。

## Buff 与广播说明精确化

- 问题现象：影骨莫特斯的 Buff 提示和技能广播仍使用“高额伤害”“大幅降低视野”“获得攻击增益”等模糊描述，玩家无法从提示确认持续时间、实际承伤比例、宝箱叠层方式和陷阱结果。
- 根因：运行时数值已经集中在 `02．数值与表现配置.ts`、`01．运行时上下文.ts`、`04．骸骨召唤.ts`、`05．暗影禁锢.ts`、`06．幽影爆发.ts`、`07．盗贼的遗产.ts` 与宝箱常量中，但 `04．影骨莫特斯.ts` 和 `00．配置.ts` 没有同步呈现这些真实数值。
- 涉及文件：`05．影骨莫特斯/00．配置.ts`、`TS/系统/05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/04．影骨莫特斯.ts`；复核 `02．数值与表现配置.ts`、`01．运行时上下文.ts`、`04．骸骨召唤.ts`、`05．暗影禁锢.ts`、`06．幽影爆发.ts`、`07．盗贼的遗产.ts` 和 `06．经济系统/00．宝箱系统/00．常量定义.ts`。
- 修改内容：技能壳提示补充背刺 2.5 倍、300 码、骸骨 3 秒 / 4 个、金币偷取公式、禁锢 0.9 秒 / 320 码 / 4 秒 / 破阵后 1 秒、幽影 20 秒 / 1600 视野 / 60% 与 140% 承伤 / 8 个召唤物、宝箱 0.5 秒 / 4 个 / 3 秒引导 / 3% 固定攻击力叠层 / 10% 陷阱等数值；广播增加括号内的规避指引；Buff tooltip 同步持续时间、固定攻击力公式、30% 保留生命和 P3 的 30% 攻击力追加及 65% 冷却比例。未修改配音台词和技能运行逻辑。
- 复用封装：仅复用现有集中数值配置、Buff 表、广播台词配置和宝箱高级掉落配置；未新增底层 API、回调或匿名闭包。
- 构建验证：执行 `npm run build:files -- "TS/系统/05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/04．影骨莫特斯.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/00．配置.ts"` 成功，`fix-lua-for-pack` 更新 0 个文件，未打包地图。
- 待实测：进图输入 `Boss莫特斯`，依次观察 `1`、`2`、`3`、`4`、`5` 和 P3 阶段；确认技能壳提示与广播完整显示，面向 Boss 可正确规避背刺，法阵破坏后控制缩短，幽影期间承伤 / 视野变化和宝箱陷阱提示与实际一致。
- 可复用经验：Boss 玩家提示必须直接读取已落地的集中数值；凡是百分比属性要说明“追加固定值”还是“倍率变化”，凡是延迟或范围机制要同时给出预警时间、范围、持续时间和玩家规避动作。

## 骸骨召唤物生命公式

- 问题现象：骸骨召唤物生命值没有按莫特斯最大生命值成长，且骸骨战士的固定值与倍率配置容易产生歧义。
- 修改内容：骷髅创建时使用 `4000 + 莫特斯最大生命值 × 3%`；骸骨战士创建时使用 `7500 + 莫特斯最大生命值 × 5%`。两种召唤物都在创建时读取 Boss 的实时最大生命值。
- 涉及文件：`05．影骨莫特斯/02．数值与表现配置.ts`、`05．影骨莫特斯/04．骸骨召唤.ts`。
- 构建验证：执行对应两个 TS 文件的 `npm run build:files` 成功，未打包地图。

## 骷髅偷窃伤害修正保留边界

- `04．骸骨召唤.ts` 的贫血惩罚发生在普通攻击基础伤害修正阶段，返回值必须并入当前攻击的基础伤害；它不是独立施法事件，不能在修正器中再调用 Boss 技能伤害执行器，否则会重复触发伤害事件和后续修正。
- 该处继续使用 `计算组合技能伤害` 读取 Boss 攻击力与目标动态最大生命，保留 `originalAttacker` 身份和原生普通攻击的护甲/魔抗计算顺序。后续若要配置化，应先扩展“基础伤害修正”公式接口，而不是改成一次新的技能提交。

## 幽影爆发条件伤害修正迁移

- `06．幽影爆发.ts` 的物理承伤降低与魔法承伤提高已改用公共 `创建条件伤害修正`，条件仍限制为影骨本体、有效运行时上下文且 `幽影爆发中`。
- 物理/魔法分类和倍率继续由原修正函数处理；幽影形态 Buff、视野压制、召唤潮、染色以及结束时召唤物生命扣除没有并入公共组件。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/06．幽影爆发.ts"` 已通过；生成 Lua 的条件/修正回调为具名函数参数。

## 幽影爆发召唤周期实例隔离

- 问题现象：幽影爆发为每个技能实例注册一个周期回调，但旧回调又遍历全局实例表；多个影骨上下文同时存在时，同一上下文会被每个回调重复推进，召唤数量和召唤间隔都会被放大。
- 根因：中心计时器已经支持周期回调携带 `variable`，旧实现却用全局表加多条回调重复扫描，造成调度器数量与实例数量相乘。
- 修改内容：删除全局幽影周期表和自增键；每次幽影爆发创建一个具名 `幽影爆发召唤变量`，通过 `addPeriodicCallback(间隔, 幽影爆发召唤Tick, variable)` 只推进自己的上下文。幽影状态进行中拒绝重复释放，避免同一 Boss 并行生成多条召唤潮。Boss 无效、状态结束或达到召唤持续时间时，仅移除当前实例回调；清理篮子仍登记该回调。
- 复用封装：直接复用中心计时器已有的周期 `variable` 参数和机制清理篮子，没有新增技能私有调度器，也没有匿名闭包。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/06．幽影爆发.ts"` 已通过；随后完整 `npm run build` 也通过。局部构建未更新文件，完整构建按项目脚本更新既有生成 Lua；未打包地图。
- 待实测：同时保留两个影骨上下文或重复启动测试时，确认每个上下文仍按 `0.3` 秒间隔生成最多 `8` 个召唤物，不会因另一个上下文的回调而翻倍；Boss 死亡、技能结束和测试清理后不再继续召唤。

## 阴影穿梭背刺条件伤害修正迁移

- `03．阴影穿梭.ts` 的背刺修正已改用公共 `创建条件伤害修正`，由公共组件统一登记条件和优先级 `48`。
- 条件仍要求影骨本体的下一次普通攻击、非技能普攻且存在背刺准备；Boss 文件继续消费准备状态，按目标正面角度应用正面减伤，并播放爪痕/血月特效与命中音效。
- 该迁移没有把背刺动态角度或命中特效抽成通用层，避免把影骨专属的穿梭状态机误做成普通强化普攻。
- 构建验证：本批 `03．阴影穿梭.ts` 已通过局部构建；未打包地图。

## 正式 Boss 启动与死亡奖励链路审核

- 问题现象：技能入口和 AI 虽然会懒创建上下文，但正式 Boss 启动后没有显式绑定影骨莫特斯运行时；死亡结算配置使用“死灵盗贼”反查单位名，而实际单位类型 `N01Y` 的名称是“影骨·莫特斯”；设计文档要求的“骸骨圣杯碎片”也没有进入固定掉落配置。
- 根因：被动注册文件只注册运行时死亡监听和技能结构，遗漏了 Boss 自动技能启动监听；死亡结算的“键”与“Boss单位名”被误用成同一名称；任务物品只登记在装备数据表，未接入死亡结算固定掉落字段。
- 修改内容：在 `10．被动效果.ts` 以 `N01Y` 注册具名启动回调，正式战斗启动时创建同一运行时上下文；死亡配置保留结算键“死灵盗贼”，将 `Boss单位名` 改为“影骨·莫特斯”，并加入 `直接掉落物品名列表: ["骸骨圣杯碎片"]`。
- 完整链路复核：启动属性 -> Boss 自动技能监听 -> AI 扫描与阶段上下文 -> P1/P2/P3 召唤及清理 -> Boss 死亡上下文清理 -> 首领奖励池与固定任务掉落，现已闭合。AI 技能表已覆盖阴影穿梭、骸骨召唤、暗影禁锢、幽影爆发和 P2/P3 的盗贼遗产。
- 属性复核：当前固定属性为魔抗 30%、命中 20%、闪避 18%、眩晕抗性 40%、光弱/火弱、天生弱点数 2；不配置暴击率和暴击伤害，避免与固定背刺倍率重复放大。
- 构建验证：`npm run build:files -- "TS/系统/03．技能系统/06．AI自动使用技能/03．Boss战启动桥接/00．战斗启动属性/02．英雄Boss战斗启动属性配置表/12．影骨·莫特斯.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/02．挑战与隐藏Boss/05．影骨莫特斯/10．被动效果.ts" "TS/系统/03．技能系统/06．AI自动使用技能/03．Boss战启动桥接/02．Boss死亡结算/02．Boss死亡非UI掉落与清理配置表.ts"` 已通过；未打包地图，未修改 `fix-lua-for-pack.js`。

## 骸骨召唤组公共化

- 问题现象：骸骨召唤在技能文件内重复维护组 ID、总数、死亡数、单位数组和重组标记；三批召唤尚未结束时，前两批被击杀可能提前触发全灭重组。
- 修改内容：公共 `召唤物组状态` 增加 `开始批次/结束批次`、死亡数量查询和变量透传；影骨召唤组直接使用公共组登记、死亡监听和全灭回调。正式召唤在第三批登记结束后封闭组，P1/P2 全灭时按组回调延迟 3 秒重组一个骸骨战士，P3 不重组。
- 保留边界：骷髅出生特效、骸骨符咒掉落、偷金币与贫血惩罚、幽影爆发伤害类型转换仍属于影骨技能层；骸骨战士重组不加入原骷髅组，避免重复触发重组。
- 测试命令：`2` 仍走正式三批召唤；`2-2`/`2-3` 复用正式组并在约 3.5 秒击杀，分别验证 P2 重组和 P3 不重组；`2-kill` 使用公共组进行快速全灭测试。测试清理先销毁组再移除召唤物，不会留下延迟重组。
- 涉及文件：`04．机制组件/10．复杂战斗通用机制/03．召唤物组状态管理.ts`、`05．影骨莫特斯/01．运行时上下文.ts`、`05．影骨莫特斯/04．骸骨召唤.ts`、`05．影骨莫特斯/06．幽影爆发.ts`、`04．影骨莫特斯Boss技能测试.ts`。
- 构建验证：上述 5 个 TS 文件已通过局部 `npm run build:files`，随后完整 `npm run build` 通过；已抽查生成 Lua 的组方法调用和死亡/全灭回调参数，未发现 `self/nil` 错位。未打包地图，未进行 Warcraft 实机回归。

## 运行时上下文死亡清理公共化

- 影骨死亡台词改由公共上下文工厂的具名 `on单位死亡` 回调播放；Boss 上下文清理、幽影召唤物状态复位和 P3 强化回滚继续由同一工厂 `on清理` 执行。
- 原先影骨上下文私有死亡监听和手动清理已删除，避免死亡事件与测试/战斗清理重复收尾；骷髅召唤组仍按上一批的公共组语义独立清理。
- 构建验证：公共工厂与影骨上下文已通过局部构建，因公共接口变更随后完整 `npm run build` 通过；未打包地图，未进行 Warcraft 实机回归。

## 私有施法包装迁移

- `03．阴影穿梭.ts`、`04．骸骨召唤.ts`、`05．暗影禁锢.ts`、`06．幽影爆发.ts`、`07．盗贼的遗产.ts` 均改用公共 `启动基础施法时间线`，删除 `11．公共工具.ts` 中已无调用的影骨限时动作/常规施法包装。
- 各技能原有召唤组、幽影状态、宝箱时间轴、背刺、清理和 P2/P3 分支保持在 Boss 技能层；公共时间线只负责动作、硬直、吟唱条、生效和取消。
- 本批5个技能文件及公共工具已完成局部构建，生成调用形态已抽查，未发现 `self/nil` 错位；未打包地图。
