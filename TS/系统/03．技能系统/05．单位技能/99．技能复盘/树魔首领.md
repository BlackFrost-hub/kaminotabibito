# 树魔首领技能复盘

## Buff 与广播说明精确化

- 问题现象：树魔首领的 Buff 说明和技能广播只写“提高攻击力”“降低治疗”“放置陷阱”等概念，玩家无法从说明中得到倍率、持续时间、范围和规避方式。
- 涉及文件：`TS/系统/05．Buff系统/03．Buff表/01．Boss/01．主线Boss/04．树魔首领.ts`、`TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/00．配置.ts`。
- 修改内容：补齐 6 个 Buff 的实际数值、持续/刷新方式、触发范围和叠层规则；补齐 4 个主动技能提示的倍率、时间、范围和分支；为开场、随从、4 个技能广播补充括号内的玩家处理建议；未修改配音台词、配音资源和运行时逻辑。
- 兽群号令：每个存活随从按“当前攻击力减去本 Buff 已有增量后的基准攻击力”增加 20% 攻击力，最多 4 层即 80%；状态每 1.5 秒按存活数量重算，随从死亡或入场会同步增减层数。
- 无从暴怒：无任何自身随从时额外获得 100% 攻速和“默认移动速度×50%”的移速；新随从入场后立即移除，Buff 每 1.5 秒刷新。
- 古树衰弱：扩散冲击波命中后，普通攻击最终伤害降低 40%，持续 7 秒；技能伤害不受影响。
- 远古诅咒：点名等待 3 秒；第一段总伤害按结算时点名目标当前生命值×（60%+20%×N）计算，N 为当前有效玩家数且至少为 1，400 码内目标均分；第一段后每名有效玩家恢复等于第一段总伤害的生命值；N≥2 时再于玩家中心 650 码内延迟 1.8 秒结算第二段。
- 治疗枯竭：生命陷阱每秒刷新一次，每次登记 1.4 秒治疗修正；受到的治疗量降低 50%+难度等级×5%，难度 1/2/3 分别为 55%/60%/65%。
- 静止陷阱眩晕：进入静止陷阱 600 码范围后等待 0.75 秒触发，所有有效玩家获得无视韧性的眩晕 8 秒。
- 图腾分支：巫医对应静止陷阱、猎头者对应生命陷阱、投掷者对应爆炸陷阱；多类随从按存活数量加入候选池，无随从时随机选择。
- 构建验证：执行 `npm run build:files -- "TS/系统/05．Buff系统/03．Buff表/01．Boss/01．主线Boss/04．树魔首领.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/00．配置.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件；未打包地图。

## 待实测与实现风险

- 进图输入 `Boss树魔首领` 后，依次验证 `1`/`2`/`3`/`4`：确认提示文本与实际前摇、反击角度、分摊圈、二段预警及图腾分支一致。
- 随从测试需确认 4 层兽群号令为 80% 攻击力增量，清空随从后暴怒为攻速+100%、移速+50%，补员后两种状态均正确回滚/恢复。
- 远古诅咒需分别测试单人、多人进入 400 码圈以及玩家中心 650 码二段范围；注意第一段的 N 使用当前有效玩家总数，不是 400 码内人数。
- 当前 `07．树魔图腾.ts` 的生命陷阱已按图腾当前坐标和配置半径筛选目标；爆炸陷阱被摧毁后的全场伤害仍按设计遍历所有有效玩家。提示圈的 900/650 码半径仍主要承担表现提示，正式进图需继续确认范围读性。

## 生命陷阱区域公共化

- `07．树魔图腾.ts` 删除生命陷阱私有周期轮询，改用公共 `创建持续危险区域` 统一处理半径900、每秒检测、20秒生命周期和 Boss 清理。
- 生命陷阱仍以图腾当前出生坐标为区域锚点；每次周期按目标最大生命值3%造成强化伤害，并刷新治疗降低 Buff 和命中特效，伤害与 Buff 数值未改变。
- 图腾绑定的渐变圆形提示仍是唯一生命陷阱范围表现，区域实例显式关闭公共提示圈；静止陷阱和爆炸陷阱的独立周期/延迟语义未改动。
- 构建验证：目标文件已通过 `npm run build:files`；未打包地图，尚未进行 Warcraft 实机回归。

## 巫医治疗波测试入口

- 新增树魔首领测试命令 `7`：输入 `Boss树魔首领` 后输入 `7`，测试命令会补齐随从，将 Boss 当前生命设为最大生命的 70%，并将存活随从设为 50% 缺血，确保治疗波能连续命中多个友方目标；巫医播放 `spell` 动作并使用标准 `开始充能` 蓄力 1 秒，期间受到硬控制会中断并立即移除进度条且不触发治疗，充能完成后才触发治疗波跳链。
- 治疗波每次跳跃按配置的 `0.8` 系数递减，测试时可从治疗数字观察首跳、第二跳、第三跳依次为初始值的 100%、80%、64%；正式战斗仍只跳向缺血的 Boss 护卫，满血友军不会消耗跳数。
- 正式逻辑仍由巫医创建时注册的周期驱动负责，测试入口复用同一个具名施法函数，不改变正式治疗冷却或目标选择规则。
- 治疗波不是通魔技能壳：`03．随从特性.ts` 没有注册单位技能壳监听，也没有为治疗波配置通魔技能 ID；实际链路是 `开始充能` → `发起治疗波跳链`，使用 `HWPB` 闪电效果代码。
- 构建验证：执行 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/03．随从特性.ts"` 成功；未打包地图。

## 扩散冲击波预警范围修正

- 问题现象：扩散冲击波的弹幕中心最大飞行 600 码，单道弹幕命中半径 120 码，顺弹道方向最远命中外沿约为 720 码；原 620 码预警圈未完整覆盖实际命中外沿。
- 修改内容：将 `02．数值与表现配置.ts` 的 `扩散冲击波.预警半径` 从 620 调整为 720，并同步 `00．配置.ts` 的技能扩展提示与广播引导、Boss 说明和玩家攻略。
- 运行时链路：`04．扩散冲击波.ts` 继续从 `cfg.预警半径` 创建渐变圆形提示圈，无需复制常量或改动伤害、弹幕和 Debuff 逻辑。
- 构建验证：执行 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/00．配置.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/02．数值与表现配置.ts"` 成功；`fix-lua-for-pack` 更新 0 个文件，未打包地图。

## Boss 伤害执行器迁移（第十二批）

- `04．扩散冲击波.ts` 改用 `执行BossAOE技能伤害` 的来源攻击力比例公式；弹幕命中时仍读取 Boss 当前攻击力，保留同一技能实例、AOE 形态、植物伤害和古树衰弱。
- `05．消耗反击.ts` 改用 `提交预计算BossAOE技能伤害`；反击弹道发射时快照的攻击力伤害不改为命中时重算，保留弹道可沿途命中多个目标的 AOE 语义。
- `06．远古诅咒.ts` 的二段爆发改用公式入口，在二段结算时读取 Boss 攻击力和目标最大生命；第一段点名/分摊伤害继续使用结算前已计算的当前生命快照。
- `07．树魔图腾.ts` 的生命陷阱周期伤害改用目标最大生命公式入口，爆炸陷阱继续提交摧毁结算时计算的当前生命比例加难度固定值；治疗枯竭、图腾分支、提示圈和自然结束逻辑未改动。
- 构建验证：执行四个技能文件的 `npm run build:files` 成功；随后执行全量 `npm run build` 成功；未打包地图。

## 古树衰弱条件伤害修正迁移

- `04．扩散冲击波.ts` 已将古树衰弱对普通攻击的承伤降低注册迁移到公共 `创建条件伤害修正`。
- 条件保持为攻击类型是普通攻击且攻击者带有古树衰弱 Buff；倍率、优先级 `35` 以及 Buff effect 回退逻辑保持不变。
- 本次只替换修正器接入方式，扩散冲击波、古树衰弱触发和持续时间等业务状态机没有改动。
- 构建验证：两个迁移文件已通过局部构建；最终全量 `npm run build` 通过；未打包地图。

## 随从特性周期驱动公共化

- 问题：`03．随从特性.ts` 直接注册全局周期回调，在回调内部自行读取 `udg_Boss`、获取全部上下文、读取时间并推进补员和随从状态，重复实现了公共周期调度器的职责。
- 修改：改用 `创建周期机制调度器`，保留当前 `udg_Boss` 尚未建立上下文时的懒创建入口；上下文列表、执行函数和时间读取均使用模块级具名函数，补员、剧情带入随从、状态刷新和死亡清理语义不变。
- 复用：不新增树魔私有计时器或匿名回调，统一复用公共调度器的间隔、上下文遍历和回调注册。
- 构建验证：本批目标文件已通过 `npm run build:files -- "TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/03．随从特性.ts"`；随后执行完整 `npm run build`；未打包地图。

## 图腾周期驱动公共化

- `07．树魔图腾.ts` 的静止陷阱范围提示/触发检测和爆炸陷阱传送，均改用公共 `创建周期机制调度器`；每个图腾仍保持自己的状态、间隔、随机位置和一次性触发语义。
- 调度器统一挂入树魔清理篮子；Boss 死亡、图腾被摧毁或自然结束时会立即停止对应周期，不再由技能文件直接维护 `addPeriodicCallback`/`removePeriodicCallback`。
- 静止陷阱的 600 码判定、0.75 秒延迟和全体眩晕，爆炸陷阱的难度传送间隔与落点提示均未改变。
- 构建验证：本批目标文件已通过 `npm run build:files`；生成 Lua 已抽查调度器实例方法保留正确 `self`，未打包地图。

## 2026-08-02 固定战斗属性与完整链条审计

- 固定战斗属性调整为：魔抗 30%、暴击率 15%、暴击伤害 200%（配置增量 0.50）、命中率 +20%、闪避率 10%、眩晕抗性 35%。树魔首领是带随从和正面反击的近战型 Boss，保留少量普攻暴击属性，不按纯法师或纯暴击 Boss 配置。
- 固定弱点收敛为火弱、弓弱 2 个；移除短剑弱点。`弱点数量基础值` 与 `天生弱点数` 均为 2，满足固定弱点必须为 1～2 个且至少 1 个的约束。公共战斗启动系统的随机追加属性不在本配置中重复定义。
- 正式入口之后的链条已核对：战斗桥接登记并应用启动属性，Boss 运行时创建上下文并接入 AI/技能入口；随从固定编制、30 秒补缺、巫医治疗波和 Boss 增益均由运行时推进；技能弹幕、延迟结算、图腾机制单位和随从治疗周期登记清理篮子。
- 补齐 `05．消耗反击.ts` 的防御姿态状态清理登记。Boss 上下文提前清理时会移除防御特效周期与到期回调，避免状态表残留；自然结束、正面反击和背后破招的原有清理路径不变。
- 死亡链条已核对：死亡配置清理 Boss/当前 Boss 表并保留原剧情执行；死亡剧情索引承接到 `elven_city_treant_leader_death`，剧情动作调用 `主线_树魔首领` 死亡结算；该结算引用 `boss.death.主线_树魔首领` 奖励池并发放直接掉落，未发现奖励池断链或重复结算入口。
- 本轮局部构建：`npm run build:files -- "TS/系统/03．技能系统/06．AI自动使用技能/03．Boss战启动桥接/00．战斗启动属性/02．英雄Boss战斗启动属性配置表/13．树魔首领.ts" "TS/系统/03．技能系统/05．单位技能/03．Boss技能/01．主线Boss/05．树魔首领/05．消耗反击.ts"` 成功，`fix-lua-for-pack` 更新 0 个文件，未打包地图。
- 本轮未打包地图，仍需进图验证实际属性面板、弱点显示、Boss 死亡剧情和奖励 UI。

## 远古诅咒跟随提示周期公共化（2026-08-02）

- `06．远古诅咒.ts` 的点名分摊圈刷新原先直接维护周期 ID；现改用公共 `创建周期行为`，周期实例挂入树魔运行时清理篮子。
- 目标失效或达到配置的点名延迟后自动停止，圆形提示圈仍按原间隔、半径和目标单位锚点刷新；分摊目标、二段伤害和治疗逻辑没有改动。
- 本批目标文件已通过局部 `npm run build:files`；未打包地图，未进行 Warcraft 实机回归。
