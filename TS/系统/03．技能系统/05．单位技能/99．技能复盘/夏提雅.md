# 夏提雅技能复盘

## 伤害类型

- 问题：净化投枪及其英灵复刻版本使用了 `ATTACK_TYPE_MAGIC` 提交魔法技能伤害。
- 原因：把原生攻击类型误当成项目的魔法伤害分类。
- 结论：统一技能链路使用 `ATTACK_TYPE_NORMAL`，魔法分类继续由 `DAMAGE_TYPE_MAGIC` 决定。

## 测试命令与阶段覆盖

- 基础命令：`1/2/3/4` 每次主动恢复 P1 实际生命与运行时阶段，避免先测 P2/P3 后基础命令继承旧阶段。
- 强化命令：补齐 `1-2/1-3/2-3/3-2/3-3`，分别覆盖滴管穿心英灵复刻、P3 两段猎血起手、P3 轮舞加速、投枪英灵复刻与 P3 双投枪；P2/P3 专属技能 `5/6/7/8` 保留纯数字基础命令。
- 阶段模型：进入 P2 英灵战乙女后，夏提雅本体同步切换为 `ShalltearValkyrie.mdx`；P3 与复生仪式继续保持女武神模型，P1/基础测试恢复 `Shalltear.mdx`。测试命令与正式血量转阶段共用同一模型入口，避免只显示英灵投影而本体仍为普通状态。
- 英灵冲锋动作：滴管穿心复刻与镜像夹击的蝗虫单位壳统一播放女武神模型 3 号动画，避免复刻冲锋误播本体 6 号或旧投影 5 号动作。
- 英灵冲锋硬直：两个蝗虫单位壳在冲锋开始回调中同步施加与冲锋时长一致的硬直，防止单位默认待机/攻击状态覆盖 3 号冲锋动画。
- 阶段来源：P2/P3 命令通过 JAPI 读取 Boss 动态最大生命，再把当前生命设置到对应阈值区间，同时同步运行时阶段，保证即时释放与后续周期刷新使用同一阶段来源。
- 被动覆盖：强化普攻在测试场创建后自然监听，不增加空被动命令；`9` 只准备 P3 一次性复生触底场景，测试上下文显式绑定挑战生命下限，随后输入 `55` 验证锁血与复生。
- 真祖血宴：测试命令先在 P1 创建三枚血印，再切入 P3 结算，避免 P3 禁止创建血印导致血宴层数始终为零。

## 施法硬直、动作与朝向

- 问题：调度器的“忙碌时间”只阻止下一次技能调度，不会阻止 Boss 在预警或引导期间移动；部分技能虽然播放了动作，但没有实际硬直。
- 方向技能：强化穿刺、滴管穿心、血月轮舞、净化投枪、镜像夹击和血月终舞均在释放时按锁定目标或锁定路径设置 Boss 朝向。
- 硬直范围：强化穿刺覆盖蓄力，滴管穿心与镜像夹击覆盖冲锋前预警，血月轮舞覆盖横扫与反刺前摇，净化投枪覆盖全部已锁定投枪结算，血月终舞覆盖四段扇区引导；进入冲锋后继续由冲锋系统接管单位暂停与位移。
- 阶段技能：英灵战乙女登场补齐 1.5 秒本体动作、朝向和硬直；真祖血宴补齐转阶段演出硬直；血之复生原本已使用单位暂停和仪式动作，无需重复叠加硬直。
- 非方向机制：鲜血回收补齐回收前摇硬直，但不强制朝向任一血印；真祖血宴、血之复生均为自身中心阶段机制，不设置目标朝向。

## 吟唱条审计

- 项目约定：有明确施法/前摇窗口的技能使用“常规技能”吟唱条；大招使用“大招”通道。
- 已补齐常规吟唱条：强化穿刺、滴管穿心、血月轮舞、净化投枪、鲜血回收、英灵战乙女登场、镜像夹击、真祖血宴。
- 已有无需重复：血月终舞使用“大招”吟唱条，血之复生使用“大招”吟唱条。
- 吟唱条时长与对应硬直/施法窗口一致；P3 净化投枪覆盖两枚已锁定投枪的完整结算等待。

## 测试命令阶段重置运行时报错

- 现象：选择 `Boss夏提雅` 后输入 `1`，提示正在测试但技能没有表现，运行时报 `attempt to call a nil value (method 'slice')`。
- 原因：测试阶段重置通过 `血印句柄列表.slice()` 制作清理快照；该列表在 Lua 运行时是普通 table，不提供 JavaScript 数组的 `slice` 方法，命令在释放滴管穿心前已经中断。同类写法还存在于血印净化、鲜血回收和真祖血宴路径。
- 修复：相关文件统一改为具名本地函数或显式循环复制数组，再遍历快照执行会修改原列表的清理操作；不依赖 Lua 数学库、不修改底层 `lib` 和打包修复脚本。
- 验收：重新 `Run Map` 后输入 `Boss夏提雅`、`1`，应先出现滴管穿心直线预警，再由夏提雅冲锋并对路径目标结算；继续验证 `3` 的血印净化、`4` 的鲜血回收和 `7` 的真祖血宴不再出现 `slice` 报错。

## 滴管穿心特效生命周期

- 调整：本体与 P2 英灵复刻的红色突进拖尾统一复用冲锋残影表现封装，每 `0.02` 秒生成一个实例，每个实例生命周期为 `0.3` 秒；关闭冲锋底层原有的即时销毁路径特效，避免重复叠加。
- 命中：`ShalltearLanceImpact.mdx` 命中爆发保留 `1.0` 秒；到期先通过平台安全封装隐藏，再销毁句柄，避免模型 `Death` 序列在销毁后继续可见。
- 验收：测试 `1` 与 `1-2`，确认红色拖尾不会瞬间消失或长时间残留，命中爆发在创建后约 `1` 秒立即隐藏且不继续显示死亡段。

## 滴管穿心指定模型接入

- 命中爆发：将源 `MapTest/file_001312/file_001312.mdx` 迁移为 `Common\\Effect\\Form\\Explosion\\ShalltearLanceImpact.mdx`，私有贴图改写到项目分类路径；其中 KnifeLight 贴图与项目已有资源 SHA-256 完全一致，直接复用，不重复导入。
- 冲锋残影：将源 `MapTest/file_001380/file_001380.mdx` 迁移为 `Common\\Effect\\Projectile\\ShalltearLanceTrail.mdx`，9 张私有贴图迁入 Projectile 共享 `Texture` 目录。源模型没有 Warcraft 原生贴图，因此未改写或复制任何原生贴图。
- 生成频率与方向：本体和 P2 英灵复刻均每 `0.02` 秒生成一次残影，单实例生命周期 `0.3` 秒；残影通过 `EXEffectMatRotateZ` 使用技能锁定的冲锋朝向。
- 验收：测试 `1` 与 `1-2`，确认细长红白枪迹沿冲锋方向排列，不能固定朝东或横切路径；命中时使用新红黑爆发并在 `1` 秒后隐藏、销毁。

## 血月轮舞横扫弧形叠加特效

- 反刺段改为原生直线弹幕：使用单位载体直接承载 `ShalltearBloodMoonArcSlash.mdx`，沿反向锁定方向飞行；每个敌方单位最多命中一次并结算一次强化反刺伤害。正面与反向弧形叠加特效分别按技能方向旋转。

- 2026-07-31：弧形叠加模型已替换为 `导出特效\\file_001310\\file_001310.mdx`；保留源模型唯一 `Death` 动作，不补 `Birth / Stand`，8 张自定义贴图均迁移到 `Common\\Effect\\Form\\Line\\Texture`。

- 接入：在血月轮舞正面扇形结算点和反向直线反刺结算点，均与当前结算点同坐标叠加 `ShalltearBloodMoonArcSlash.mdx`；两段各自创建独立特效实例。
- 资源：模型和两张私有贴图统一重新导入自 `C:\\Users\\Administrator\\Desktop\\导出特效\\file_001307`；`GradientCrescent.blp` 与 `star2.blp` 全部迁入 `Form\\Line\\Texture`，模型内部不保留外部目录或原生贴图路径。
- 动作：按源文件保留默认 `Birth` 动作，不再为了目录模板补造 `Stand / Death`；生命周期由运行时 `1.1` 秒销毁负责。
- 生命周期：弧形叠加层沿用横扫特效的 `1.1` 秒清理时间，不改变横扫伤害、扇形范围或反刺时间线。
- 验收：测试 `2`，确认正面横扫和转向背后的反刺结算时都出现新增红色弧形斩光，且两段不会共享或提前清理彼此的特效句柄。

## 猎血强化穿刺蓄力与伤害日志

- 蓄力：达到猎血连击阈值后仍按配置随机进入 `0.55～0.7` 秒蓄力窗口，并在蓄力开始记录阶段、目标有效性和实际前摇时长。
- 伤害：强化穿刺改用正式 `DAMAGE_TYPE_ENHANCED`，不再使用普通伤害类型。
- 调试：补充状态失效、硬控制、超出命中距离和伤害结算日志，区分技能未触发、蓄力后被打断及伤害未应用。

## 猎血强化穿刺自有硬直判定

- 问题：蓄力结束后日志显示“Boss仍处于硬控制”，导致强化穿刺伤害被跳过。
- 原因：施法硬直显示 Buff `C037` 被错误标记为 `Debuff:control:cast`，被硬控制合集查询当作外部硬控。
- 修复：将 `C037` 类型改为 `Debuff:cast`。施法硬直仍保留显示和生命周期，但不再把技能自身蓄力判成硬控；眩晕、时停、硬直等外部控制仍可按既有逻辑打断结算。
- 验收：测试猎血强化穿刺，蓄力结束后应继续输出伤害结算日志；在蓄力期间施加真正硬控时仍应输出“Boss仍处于硬控制”并跳过本次结算。

## 冲锋默认动作约定

- 底层 `开始冲锋` 未传动作编号和显式动作名时，开始回调默认播放 `walk` 并按冲锋时长设置行走倍率。
- 调用方传入 `动画序号` 或非空 `动画名` 后，底层不再覆盖为 `walk`；显式动作由调用方的动作表现逻辑负责播放。
- 验收：普通冲锋应进入 `walk`；英灵复刻等传入指定动作的冲锋不应先被底层切回 `walk`，结束时仍恢复单位时间倍率。
- 滴管穿心的 `1-2` 英灵复刻继续使用 3 号动画，并额外以 2 倍单位时间倍率冲锋；本体冲锋与其他英灵技能保持原速度。
- 净化投枪 `ShalltearHolyLance.mdx` 的表现缩放调整为 4（在原缩放 2 的基础上再扩大 100%）。

## 血之狂热显示与血印表现

- Buff 附加参数使用 `effectValue2` 写入技能冷却恢复百分比，避免界面因读取默认值显示 `0%`；三层时应显示 `15%`。
- 血印地面单位创建后播放 `ShalltearBloodPoolRitual.mdx` 的 2 号动画，机制单位缩放为 `1.3`；鲜血回收连线在原距离缩放基础上乘以 `2`，整体扩大 100%。

## 镜像夹击伤害调整

- 镜像夹击本体攻击力倍率调整为 `2.0`；英灵复刻继续按 `0.65` 比例结算，因此英灵段为本体伤害的 `65%`。
- 本体与英灵两段伤害类型统一改为 `DAMAGE_TYPE_SHADOW_STRIKE`，即暗属性伤害，使用目标暗属性抗性计算。

## 镜像夹击重复施放与命中回调

- 问题：投影命中后的减速调用入口错误，命中回调取到 `nil`，导致投影位移和后续阶段无法正常收尾。
- 修复：镜像夹击改用控制与 Buff 正式入口，命中后计时器继续运行并完成投影冲锋。
- 重复施放：运行时保存镜像夹击执行器和执行 ID；新一次释放会先中断旧执行器，旧结束回调不能清理新投影，避免 Boss/英灵停在上一次终点后让下一次距离判定失真。
- 测试重置：每次测试阶段准备会先停止仍在运行的镜像夹击时间轴和单位位移，再把 Boss 与步兵靶恢复到固定测试坐标，避免异常中断后旧冲锋继续驱动 Boss，或因沿用上一次终点导致后续 6 的距离判定失真。

## 净化投枪动作编号

- 净化投枪本体施法动作由编号 `5` 调整为编号 `6`。
- 净化投枪的英灵复刻不再使用通用 `attack` 动作名，改为显式播放同一配置编号 `6`；其他技能的英灵复刻动作保持不变。

## 血月终舞扇区预警

- 复核：用户确认命令 8 原本存在扇形预警，因此移除上一轮临时加入的扇形角度和动画速度覆盖，恢复技能提示圈原始调用；不改公共提示圈底层和其他技能。
- 动作：P3 每个扇区转向时立即播放本体动画编号 `9`，以 `0.32` 秒为重播 tick，动画持续当前扇区时长；最终俯冲仍使用冲锋动画编号 `6`。
- 扇区弹幕：每个扇区转向/预警开始时创建 4 枚原生单位弹幕，四枚锁定当前扇区面向，并在该扇区自身总时长内按时序发射；使用图 2 `ShalltearBloodMoonFinalStrike.mdx`，原扇区组合伤害在命中回调中按 `1/4` 结算，弹幕显式标记 `伤害形态: 'AOE'`。
- 扇区伤害类型：每枚弹幕创建时独立随机确定一种伤害类型，并在该枚弹幕的整个生命周期内固定；可选暗属性 `DAMAGE_TYPE_SHADOW_STRIKE`、强化 `DAMAGE_TYPE_ENHANCED` 或物理 `DAMAGE_TYPE_NORMAL`。最终俯冲固定使用强化伤害 `DAMAGE_TYPE_ENHANCED`。
- 扇区补层：图 3 `ShalltearBloodMoonFinalVolley.mdx` 不再只在扇区结算点播放，而是与该扇区 4 枚弹幕的延迟发射 tick 同步播放，每枚弹幕发射时播放一次，按当前扇区方向旋转并用 0 号动作播放；不承担伤害判定。
- 最终俯冲：保留原 `ShalltearLanceTrail.mdx` 冲锋拖尾与原冲锋伤害，另以单位载体发射一枚图 2 视觉弹幕，缩放为 `2.0`，不重复结算伤害。
- 朝向修复：四个扇区在预警与结算阶段分别调用 `立即设置单位朝向` 锁定当前 `finalFacing + i * 90`；最终俯冲在阶段切换和冲锋开始回调再次锁定 `finalFacing`，并显式传入冲锋参数 `角度`，保证 Boss 本体、弹幕、特效和实际位移同向。
- 资源：图 2 源 `MapTest\\file_001252\\file_001252.mdx` 保留 `Stand AZ / Death AZ`；图 3 源 `MapTest\\file_001376\\file_001376.mdx` 仅保留源 `Birth`。原生贴图路径保持 `Textures\\...`，导出目录中的私有 `AZ_*` 贴图迁入对应共享 `Texture`，未修改原生贴图。
- 回落语义：最终俯冲结束后保留 `4～5` 秒技能收尾锁定窗口，当前实现是阶段执行器的逻辑等待与技能调度忙碌时间，不包含独立的回落动画或额外扇形特效。

## 血之复生特效缩放

- 复生结晶模型 `ShalltearBloodRebirthShell.mdx` 缩放为 `0.25`。
- 复生成功重构丝流 `ShalltearBloodRebirthWeave.mdx` 缩放为 `2.0`。
- 复生成功爆发 `ShalltearRebirthBurst.mdx` 缩放为 `1.5`。
- 复生成功时两种特效按 `0.5` 秒间隔重复创建，表现窗口持续 `2.0` 秒。
- 复生成功总回血量保持原公式不变，拆为四次 `1/4` 恢复，与表现按 `0/0.5/1.0/1.5` 秒 tick 同步。
- 复生仪式固定播放 10 号动作；开始后 `1.7` 秒（冻结前 `0.3` 秒）在当前飞行高度基础上增加 `100`，`2.0` 秒将单位时间倍率设为 `0`；成功、失败或异常结束时恢复为 `1x` 并写回冻结前的原高度。

## 玩家可见 Buff 与广播文案审核

- Buff「鲜血枯竭」明确写出持续 `6` 秒；期间再次承受汲血强化穿刺仍会受本次伤害，但不再为夏提雅恢复 `3%` 最大生命或生成鲜血印记。
- Buff「血之狂热」明确写出持续 `8` 秒、每层攻击速度 `+8%`、技能冷却恢复速度 `+5%`，并列出 `1/2/3` 层对应的 `8%/16%/24%` 攻速和 `5%/10%/15%` 冷却恢复速度。
- Buff「真祖血宴」明确写出 P3 持续到战斗结束、每枚未净化血印转化 `1` 层、最多 `3` 层；每层攻击速度 `+8%`、技能循环速度 `+5%`，满层为 `+24%` 和 `+15%`。
- Buff「猎血连击」按实际触发窗口说明：P1/P2 已有 `2` 层、P3 已有 `1` 层时下一次普攻改为汲血强化穿刺；`3` 秒未继续命中、硬控制或大型技能时清空，改打其他目标则从该目标第 `1` 层重新计数。
- 所有夏提雅技能台词改为“机制事实 + （应对引导）”，补齐了 `0.8` 秒、`1.0～1.3` 秒、`1.2` 秒、`0.65` 秒、`1.5` 秒、`12` 秒、`3` 枚、`10%` 等玩家决策所需数值。
- 强化穿刺达到阈值时新增广播；P3 转阶段、真祖血宴实际转化和复生成功也补齐广播，复生失败改走统一台词入口，避免只显示模糊的技能名或重复广播。
- 复生结晶每次破碎会显示剩余数量和每枚 `10%` 恢复结果；全部击破时单独提示 `12` 秒仪式将失败。复生吟唱条同步显示 `12` 秒、`3` 枚和每枚 `10%` 的处理目标。

## 吸血恢复表现

- 新增 `ShalltearVampiricHeal.mdx`，绑定夏提雅 `origin` 播放约 `1.9` 秒，表现为暗红血能向单位中心收束。
- 汲血强化穿刺实际触发 `3%` 最大生命恢复时播放；目标处于鲜血枯竭、仅承受伤害而不回血时不播放。
- 鲜血回收成功吸收至少一枚血印并结算恢复时播放一次；无血印或回收失败时不播放。
- 血之复生属于复生治疗，继续使用其独立重构丝流和爆发特效，不叠加吸血表现。

## 汲血穿刺广播与配音降噪

- 汲血强化穿刺的长广播和角色配音共用 `10` 秒内置冷却；冷却期间技能仍正常蓄力、伤害、回血和生成血印。
- 命中时的短促汲血 SFX 保留逐次播放，继续承担命中反馈，不受台词冷却影响。
- 强化穿刺恢复量由夏提雅最大生命 `1.2%` 提高至 `3%`，台词和鲜血枯竭 Buff 描述已同步。
