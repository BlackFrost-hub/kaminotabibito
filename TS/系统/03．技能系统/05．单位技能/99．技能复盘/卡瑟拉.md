# 深渊巨鱿·卡瑟拉技能复盘

## 本次玩家侧说明审核

- 审核范围：`03．Boss技能/02．挑战与隐藏Boss/03．深渊巨鱿卡瑟拉/00．配置.ts`、`02．数值与表现配置.ts`、`10．触手再生与残片.ts`，以及 `05．Buff系统/03．Buff表/01．Boss/02．挑战与隐藏Boss/02．卡瑟拉.ts`。
- 目标：Buff 描述必须给出实际持续时间、层数、百分比、固定值或范围；Boss 广播必须说明发生了什么，并提供括号内的简单规避/处理方式；不在 Buff 描述中重复伤害类型名。

## 战斗链路复核（2026-08-02）

- 正式 Boss 战启动后由被动模块预建卡瑟拉运行时上下文，阶段调度、深渊召唤、共生电击、触手再生与残片维护均挂在该上下文上。
- 卡瑟拉死亡事件会清理运行时上下文和其机制清理篮子；统一死亡结算表已接入 `chapter2.hidden.kasera` 奖励池。
- 自定义战斗属性已与设计弱点对齐：风/木双弱点、天生弱点数量 2；魔抗 35%、眩晕抗性 45%、命中率 12%、闪避率 8%，不额外叠加暴击率和暴击伤害。
- 修改边界：未改伤害、判定、冷却、召唤、提示圈和特效数值；只校正玩家说明，并给原本没有广播的“地面残片回收、玩家残片吸收、触手再生”接入已有具名 Boss 台词广播。

## Buff 数值复核

| Buff | 实际玩家可见说明 |
| --- | --- |
| 触手缠绕 | 持续3秒；攻击速度-40%、移动速度-40%；禁止冲锋、跳跃、闪烁等位移技能。 |
| 墨汁遮蔽 | 每次命中刷新0.3秒；视野减少1200、沉默0.1秒；墨汁区域每秒受到 Boss 当前攻击力×55%的伤害。有效水属性抗性≥35%时，伤害为 Boss 当前攻击力×19.25%/秒，沉默每次0.035秒；视野减少1200仍按区域刷新。 |
| 触手残片 | 1~5层；每层水属性抗性+7%。拾取后总层数达到3层及以上，恢复20%已损失生命；共生电击结算时，非安全区目标消耗3层可完全抵消本次命中。 |
| 触手精华 | 持续20秒；每层按吸收瞬间的当前攻击力×3%换算为固定攻击力值加入，不是基础攻击力或总攻击力百分比；每吸收1片恢复Boss最大生命2%；可叠加，最多显示99层。 |
| 麻痹电流 | 持续3秒，无法移动、攻击或施法；只有未处于绝缘珊瑚300码安全半径且未消耗3层触手残片抵消时才会施加。 |
| 绝缘庇护 | 持续3秒；共生电击结算瞬间处于绝缘珊瑚300码安全半径内时获得，免疫本次6500点伤害和3秒眩晕；离开安全区不会提前移除。 |

## Boss 广播复核

- 深海涡流：半径720码，牵引3秒，随后按距离造成4500×30%~100%伤害并眩晕1秒；提示立即离开范围，沿Boss面向反方向移动可减弱牵引。
- 触手鞭笞：点名1人，2秒后出现3条触手，持续8秒、每0.7秒攻击；提示离开红圈并优先击杀触手。
- 墨汁喷吐：前方70°、900码扇形持续6秒；结束后以残留中心为圆心、半径300码的圆形区域残留20秒；喷吐和残留使用各自形状判定，提示离开扇形和残留区域。
- 深渊召唤：0.8秒后出现3只幼鱿；30秒未击杀，Boss恢复10%最大生命；提示优先击杀幼鱿。
- 高压水炮：锁定最远玩家，1秒后发射2400码×200码直线水炮；有效水抗≥35%可免除击退/眩晕；提示离开直线路径。
- 触手解放：4条巨型触手，限时30秒；每击破1条使Boss护甲降低5%，超时恢复25%最大生命；提示分散集火并限时清场。
- 共生电击：3秒后结算6500点伤害并眩晕3秒；提示进入绝缘珊瑚300码安全半径或准备3层残片。
- 触手再生：Boss每下降10%生命生成1条触手，持续18秒；触手每秒攻击并将造成伤害的50%转为Boss治疗；提示优先击杀新生触手。
- 残片机制：地面残片回到Boss附近时广播每片恢复2%最大生命、增加1层触手精华；玩家残片每30秒吸收一次，4层及以上不会被吸收；提示尽快拾取或合理消耗。

## Boss 技能伤害执行器迁移（2026-08-01）

- `03．深海涡流.ts`、`09．共生电击.ts` 的固定伤害改为 `执行BossAOE技能伤害` 的固定值公式；深海涡流仍按距离插值，绝缘珊瑚与触手残片豁免仍在提交前判定。
- `04．触手鞭笞.ts`、`10．触手再生与残片.ts` 的攻击力伤害改为执行器公式；`05．墨汁喷吐.ts`、`07．高压水炮.ts` 使用执行器预计算入口，保留原本每个 tick/整条水炮只读取一次 Boss 攻击力的快照语义。墨汁喷吐仍保留水抗达标倍率、喷吐阶段十等分和残留区域筛选；现已明确喷吐阶段使用70°、900码扇形，残留阶段使用残留中心半径300码圆形，再生触手保留同一伤害值用于吸血。
- 六处均继续使用固定来源类型 `Boss技能`，旧技能伤害入口和调用方 `来源类型` 字段已移除；技能ID、共生电击独立技能实例、AOE/单体形态和攻击元数据保持原语义。
- 复核重点：卡瑟拉 `1`、`2`、`3`、`5`、`7`、`8` 技能命令，确认固定值、动态攻击力、控制、抗性分支、残片豁免和吸血均未因迁移改变。

## 复用与实现检查

- 广播继续复用 `11．台词播放.ts` 和 `Boss台词广播`，新增回调使用具名函数/已有命名函数，没有新增匿名 JASS 回调。
- 卡瑟拉测试阶段生命比例现在使用 `japi.GetUnitState` 读取最大生命；普通 `jass.GetUnitState` 只用于当前生命，普通 `jass.SetUnitState` 负责当前生命写入，并保留空值归零防护。
- 没有新增 Lua 数学库、底层 `lib` 导出或 `@noSelfInFile` 调整，也没有修改 `fix-lua-for-pack.js`。
- 待实测：进图观察墨汁抗性达标时的视野/沉默体验、残片吸收广播频率、再生触手广播时点，以及所有广播括号提示是否在游戏消息区域完整显示。

## 触手解放生命周期迁移（2026-08-02）

- `08．触手解放.ts` 已改用公共 `创建限时摧毁目标组` 创建四条巨型触手；技能文件不再维护触手数组、死亡计数或独立超时回调。
- 触手组由实例级公共 Tick 负责 30 秒超时和即时全灭；每条触手结束时只有击杀、自然到期或单位失效会计入破坏数量，组的机制清理/主动销毁不会误计入破甲。
- 组结束回调统一处理回归：全灭播放成功破甲并按实际击破数量减少护甲，超时播放失败回血；Boss 战清理只取消组和吟唱状态，不显示已清理的 Boss，也不触发回血。
- 触手生成表现仍在公共组创建完成后按实际单位坐标播放，触手模型、数量、生命值、站桩、30 秒时间窗和所有数值配置未改变。
- 回归重点：卡瑟拉命令 `6` 分别验证四条全灭、30 秒超时、Boss 死亡/战斗清理三条路径；确认成功只结算一次，超时只回血一次。

## 本体死亡清理迁移（2026-08-02）

- 卡瑟拉本体上下文启用 `死亡时自动清理`；删除被动文件中按死亡事件调用 `清理卡瑟拉上下文` 的重复监听，避免周期调度与死亡事件竞态。
- 触手、幼鱿、残片和触手解放目标组仍按各自机制单位/组生命周期结束；本体死亡只负责收束当前上下文，不改变掉落和全灭分支。

## 施法时间线审计（2026-08-02）

- `09．共生电击.ts` 已改用公共 `启动基础施法时间线`：绝缘珊瑚、蓄力特效、预警特效和独立技能伤害实例仍在施法开始时创建，3 秒读条结束后才结算全场伤害、麻痹和安全区/残片豁免；吟唱条、动作、可取消生效回调和清理统一由公共控制器管理。
- `08．触手解放.ts` 保留专属机制流程。它不是单次读条后结算，而是“Boss 下潜、隐藏、30 秒限时目标组、全灭/超时分支、回归显形”的组合状态机；直接替换为普通时间线会丢失目标组结束原因和潜入回归语义。
- `04．触手鞭笞.ts` 的每条触手攻击周期已改用公共 `创建限次周期执行器`：持续时间与攻击间隔只在配置中表达最大执行次数，触手、目标或 Boss 失效时由 Tick 返回 `false` 提前停止，Boss 清理统一取消实例。
- `05．墨汁喷吐.ts` 的喷吐阶段和残留阶段分别使用公共 `创建限次周期执行器`；两段都保留首跳即时结算，喷吐末跳立即切换残留并立即结算残留首跳，未改变每跳伤害、扇形/圆形筛选、弹幕和视野压制时序。
- `10．触手再生与残片.ts` 保留血量节点、再生触手攻击周期、地面残片牵引和玩家残片吸收等公共周期调度；其中没有独立主动施法窗口，不额外添加手动施法时间线。
- 再生触手的有限攻击周期已改用公共 `创建限次周期执行器`：持续时间与攻击间隔只在配置中表达最大执行次数，单位失效时由 Tick 返回 `false` 提前停止，Boss 清理统一取消实例，不再由技能层维护 `剩余跳数 + removePeriodicCallback`。
- 本批目标文件 `09．共生电击.ts` 已完成 `npm run build:files`；未打包地图，未进行 Warcraft 实机回归。重点实测命令为卡瑟拉 `9`，检查 3 秒读条、珊瑚安全区、3 层残片抵消、麻痹和清理路径。
- `04．触手鞭笞.ts` 的限次周期迁移已完成局部 `npm run build:files`，随后完整 `npm run build` 通过；未打包地图，未进行 Warcraft 实机回归。重点实测命令为卡瑟拉 `4`，检查每条触手的攻击次数、失效提前停止和 Boss 清理。
- `05．墨汁喷吐.ts` 的两阶段限次周期迁移已完成局部 `npm run build:files`；完整构建与 `04` 批次合并执行，未打包地图，未进行 Warcraft 实机回归。重点实测命令为卡瑟拉 `5`，检查喷吐末跳到残留首跳的连续性和两种区域判定。
