/** @noSelfInFile */

import type { 瑟兰迪尔运行时上下文 } from "./03．运行时上下文";
import { 瑟兰迪尔数值与表现配置 } from "./02．数值与表现配置";

export function 释放瑟兰迪尔罪与罚(this: void, _context: 瑟兰迪尔运行时上下文, _target: any): void {
  const _config = 瑟兰迪尔数值与表现配置.罪与罚;
}

export function 注册瑟兰迪尔罪与罚(this: void): void {
}
