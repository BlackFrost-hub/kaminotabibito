# Boss 动作编号审计与待确认清单

本文档记录旧 Boss 技能动作的代码现状，供后续与安兹·乌尔·恭、夏提雅、沉睡英魂亚伦柯斯、祖地双灵卫四套 Boss 的实际落地代码一起集中处理。

## 判定口径

- 配置表中存在动作编号，只能证明代码会读取该编号，不能证明编号经过用户确认。
- 没有用户截图、口述或模型序列实测记录的编号，一律标记为“来源未记录 / 待复核”，不能继续当作权威动作映射复制到新 Boss。
- `SetUnitAnimationByIndex` 中除恢复默认姿态 `0` 外的数字属于硬编码，后续必须迁移到对应 Boss 的数值与表现配置。
- 被动光环、伤害修正、层数状态、拾取物和纯场地后台 Tick 可以没有施法动作；主动技能、召唤、引导、反击、阶段转换和大招必须检查动作时点。
- 动作确认时同时记录编号、模型路径、动作含义、持续时间、建议倍速、生效帧和恢复时点。
- 原始时长统一从正式 `.mdx` 的 `SEQS` 区块读取，计算方式为 `(IntervalEnd - IntervalStart) / 1000`，保留到毫秒；不得只看模型查看器截图估算。
- 同时记录 `SEQS` 循环标记。循环攻击或施法动作接入运行时时，必须明确恢复动画和恢复时点，不能假设动作会自动回待机。
- `原始时长秒` 只描述模型序列本身；技能硬直、实际播放窗口和 `动画速度` 分开配置，不能互相替代。
- 本轮已重新解析并核对：米亚、瑟兰迪尔、巴尔扎罗斯本体及双护卫、菲利斯常态/异形、里科特、卡瑟拉、古木之蚀莫尔特斯、影骨莫特斯、菲尼克斯尔双形态、树魔首领凯金；更早记录的安兹、雅儿贝德、夏提雅常态/女武神、亚伦柯斯、双灵卫四模型时长也已与正式 MDX 复核一致。

## 名称对应

- 用户口述“巨魔首领”对应代码目录 `01．主线Boss/05．树魔首领`。
- 用户口述“摩尔斯特”对应 `02．挑战与隐藏Boss/04．古木之蚀莫尔特斯`。
- 用户口述“莫斯特”对应 `02．挑战与隐藏Boss/05．影骨莫特斯`。

## 总览

| Boss | 当前动作状态 | 结论 |
| --- | --- | --- |
| 污染之猫米亚 | 已将原硬编码 `5 / 7 / 0`、动作时点及模型 `0-8` 原始时长迁入配置 | 编号语义仍待复核；灵猫分身没有 Boss 召唤动作 |
| 树魔首领凯金 | 模型 `0-13` 时长和语义已复核，主动技能及反击动作已按机制重新接入 | 消耗反击与远古诅咒使用 `3 -> 9 -> 6`；不再四技能共用 `3` |
| 菲利斯 | 常态模型 `0-22` 与异形模型 `0-7` 的原始时长已从 MDX 提取；四个主动技能为 `12 / 15 / 13 / 18` | 常态关键语义由用户确认；异形动作按模型名记录 |
| 里科特 | 模型 `0-13` 时长和语义已复核，全部主动技能动作已接入运行时 | 战斗待机使用 `3`；施法链统一使用 `8 -> 9 -> 3`，攻击按横劈 `4` / 上下劈 `5` 区分 |
| 深渊巨鱿卡瑟拉 | 模型 `0-10` 语义、循环标记和原始时长已配置；七项机制动作已接入运行时 | 使用限时动作令牌避免旧恢复回调覆盖新技能，结束统一恢复待机 `5` |
| 古木之蚀莫尔特斯 | 模型 `0-8` 时长、循环标记和八项技能/阶段动作已配置并接入 | 2 号动作在约 1 秒后成形，根系觉醒保留 1.2 秒显形后再隐藏 |
| 影骨莫特斯 | 模型 `0-11` 时长和语义已复核，五个主动技能动作均已接入 | 使用共享限时动作播放器，结束统一恢复战斗待机 `2` |
| 瑟兰迪尔 | 主动技能和终末审判动作均已配置化；模型 `0-11` 原始时长已提取 | 编号语义仍待复核；审判之环无 Boss 动作 |
| 熔岩恶魔王巴尔扎罗斯 | Boss 本体及格鲁姆、塞拉双护卫动作均已复核 | 塞拉不存在的动画 `4` 已修正，三个技能均恢复正确待机 `1` |
| 双重凤凰菲尼克斯尔 | 两形态完整序列时长与语义已复核，旧错位编号已修正 | 技能动作已读取配置；凤凰蛋使用 `11` 号动画守护 |

## 逐 Boss 明细

### 污染之猫米亚

配置：`02．挑战与隐藏Boss/02．污染之猫米亚/02．数值与表现配置.ts`

- 腐化爪击动作 `7`、污水喷吐动作 `5` 已迁入配置。
- 腐化转移使用 `5 -> 7 -> 0`，分别承担预警、出水和恢复姿态；动作倍速、冻结和恢复时点已迁入配置。
- 终极污染引导动作 `5` 及重播时点已迁入配置。
- `Boss\PollutionCat Corruptor Mia\BAIHU.mdx` 的 `0-8` 原始时长和循环标记已从模型 `SEQS` 区块写入配置。
- 灵猫分身没有 Boss 召唤动作；污染脉冲和污水柱爆发属于场地机制，可在实机确认是否需要 Boss 配合动作。

### 树魔首领

配置：`01．主线Boss/05．树魔首领/02．数值与表现配置.ts`

- 正式模型 `Boss\TreeLord\TreeLord.mdx` 的 `0-13` 原始时长和循环标记已从 `SEQS` 写入配置。
- 用户确认：`0` 为展开双臂和武器的重施法，`1` 聊天，`3` 进入施法姿势，`4` 右手前劈，`5` 右手横砍，`9` 维持 `3` 的姿势，`11` 转圈劈砍，`13` 踮脚维持；其余按序列名记录。
- 扩散冲击波和树魔图腾使用 `0 -> 6`；远古诅咒使用 `3 -> 9 -> 6`。
- 消耗反击复用共享限时动作播放器实现 `3 -> 9 -> 6`，正面反击使用配置化 `4 -> 6`；已移除每 0.5 秒重播 `3` 和硬编码 `4`。
- 基础施法时间线已支持可选后续动画和自定义恢复动画，凯金不再错误恢复到 `0 Spell Slam`。
- 静止陷阱单位的 `3` 属于图腾模型自身动作，与凯金本体动作无关；周期召唤随从仍只有音效和台词，没有强制覆盖 Boss 当前技能动作。

### 菲利斯

配置：`01．主线Boss/06．菲利斯/02．数值与表现配置.ts`

- 常态模型已由用户确认关键动作：`4` 咆哮、`5` 睡眠/眩晕、`12` 双手右前斜劈、`13` 举剑下劈、`14` 横劈、`15` 双手左到右横砍、`16` 跳劈、`18` 战争怒吼。
- 技能映射现为：剑魂杀 `12`、剑气灵斩 `15`、全力封印斩 `13`、异形化 `18`；四项都已接入施法时间线，没有数字散写。
- 常态模型 `0-22` 与异形模型 `0-7` 的原始时长、循环标记均已从模型 `SEQS` 区块写入配置；异形动作语义按模型名记录。

### 里科特

配置：`01．主线Boss/07．里科特/02．数值与表现配置.ts`

- 正式模型 `Boss\\Ricote\\Ricote.mdx` 的 `0-13` 原始时长和循环标记已从 `SEQS` 写入配置；`0 Base` 仅 `0.034s`，不作为战斗待机，统一恢复到 `3 Stand Ready`。
- 用户确认：`3` 为准备，`4` 横劈，`5` 上下劈，`7` 举剑胜利，`8` 举起右手剑施法，`9` 维持举剑姿势，`12` 仰头后低头鉴赏自己的剑；其余按序列名记录。
- 动作映射现为：四重风刃 `5 -> 3`、追击风刃 `4 -> 3`、神风护体 `8 -> 9 -> 3`、粉碎 `5 -> 3`、湮灭之炮 Boss 与投影 `8 -> 9 -> 3`、P3 湮灭之风 `8 -> 9 -> 3`、破魔反击窗口 `8 -> 9 -> 3`、触发 `4 -> 3`。
- `8 -> 9 -> 3` 复用共享限时动作播放器和单位令牌，避免旧恢复回调覆盖新技能；P2 湮灭之风会立即隐藏 Boss，故不浪费不可见动画。
- `7` 和 `12` 已完整记录，但目前没有与之匹配的技能语义，不强行接入。

### 深渊巨鱿卡瑟拉

配置：`02．挑战与隐藏Boss/03．深渊巨鱿卡瑟拉/02．数值与表现配置.ts`

- 模型 `0-10` 的原始时长和循环标记已写入配置；`2/3` 都是 1.041 秒行走，`7` 是 2.083 秒稳定缓行，`8` 是 2.223 秒持续施法，`9` 是 1.388 秒触手内翻蓄力。
- 动作分配为：深海涡流 `8`、触手鞭笞 `0`、墨汁喷吐 `8`、深渊召唤 `9`、高压水炮 `9`、触手解放 `9`、共生电击 `8`。
- 七项机制已接入共享限时动作播放器；该模型所有序列都标记为循环，播放器按技能时长恢复待机 `5`，并用单位动作令牌阻止旧回调覆盖后续技能。
- 触手解放会先完整播放 `9` 号 `1.388s` 内翻蓄力，再隐藏 Boss、生成巨型触手并启动 30 秒限时。
- 触手再生与残片属于后台机制，可不强制增加 Boss 动作。

### 古木之蚀莫尔特斯

配置：`02．挑战与隐藏Boss/04．古木之蚀莫尔特斯/02．数值与表现配置.ts`

- 模型 `0-8` 的原始时长和循环标记已从 `Boss\Moltes\Moltes.mdx` 的 `SEQS` 写入配置。
- 用户确认：`2` 约 1 秒后进入抬臂姿势，`3` 抖动身体，`5` 向右前方横扫，`6` 双手举起后砸向地面。
- 动作分配为：腐朽根须穿刺 `6`、腐败孢子云 `3`、扭曲荆棘鞭笞 `5`、腐败之种 `5`、根系觉醒 `2`、腐朽领域 `6`、共生腐朽虫群 `3`、古木悲鸣 `2`。
- 八项技能/阶段机制均已接入共享限时动作播放器，结束恢复待机 `0`；根系觉醒先显示 1.2 秒再隐藏，旧延迟回调会检查机制是否仍存活。
- 腐败值、根须领域和腐败传输属于状态/被动机制，不强制增加 Boss 动作。

### 影骨莫特斯

配置：`02．挑战与隐藏Boss/05．影骨莫特斯/02．数值与表现配置.ts`

- 正式模型 `Boss\\ShadowboneMortes\\ShadowboneMortes.mdx` 的 `0-11` 原始时长和循环标记已从 `SEQS` 写入配置，技能结束统一恢复 `2 Stand Ready`。
- 用户确认：`4` 为向右后方转约半圈后向前攻击，`6` 为腿踢，`7` 为右爪向前猛击，`8` 为聊天，`11` 为双手高举并仰头；其余按序列名记录。
- 动作映射现为：阴影穿梭 `4 -> 2`、骸骨召唤 `11 -> 2`、暗影禁锢 `7 -> 2`、幽影爆发 `11 -> 2`、盗贼的遗产 `8 -> 2`。
- 五项均已接入共享限时动作播放器，并由单位动作令牌防止旧恢复回调覆盖后续技能；`6` 腿踢与当前五项机制语义不匹配，只记录而不强行使用。

### 瑟兰迪尔

配置：`01．主线Boss/02．瑟兰迪尔/02．数值与表现配置.ts`

- 月光枷锁 `3`，精灵箭阵 `9`，罪与罚 `3`，律法召唤 `9`，月光灌注 `9`。
- 以上动作均已由技能代码读取，但没有用户确认来源记录。
- 终末审判蓄力动作 `8`、结算动作 `5`、恢复动作 `0` 及恢复时点已迁入配置。
- `Boss\Thranduil\Archangel_IncarnationofRedemption.mdx` 的 `0-11` 原始时长和循环标记已从模型写入配置。
- 审判之环为周期场地技能，目前无 Boss 动作；执法印记和秩序领域属于被动机制，不要求动作。

### 熔岩恶魔王巴尔扎罗斯

配置：`01．主线Boss/03．熔岩恶魔王巴尔扎罗斯/02．数值与表现配置.ts`

- Boss 本体已按用户截图和模型动画名复核：`9` 为右爪攻击、`10` 为左爪攻击、`14` 为左爪向前伸；`4` 为张臂展示，`11/12` 为施法准备/释放，`13/14` 为投掷准备/释放，`16` 为重受击。
- Boss 技能现映射为：恶魔咆哮波 `4`、王者天罚 `12`、熔岩喷发 `7`、火焰锁链 `14`、地核召唤 `12`、熔岩护盾 `16`、末日熔爆 `11`。
- 本体 `0-19` 的原始时长已从模型提取并写入配置，例如 `4=3.000s`、`7/8=1.500s`、`15=0.033s`、`16=0.667s`、`17=4.334s`。
- 格鲁姆正式模型 `0-10` 的时长与循环标记已写入配置。用户确认：`4 Spell` 为施法，`5 Stand Victory` 为持续举斧跪姿，`8 Attack Slam` 为踩地。
- 格鲁姆现有映射保持不变：熔岩重锤和熔岩火径使用 `4`，炙热奉献使用可循环的 `5`；`8` 只作为以后踩地结算的备用动作，不强行替换当前正确表现。
- 塞拉正式模型只有四条序列：`0 walk=1.911s` 循环、`1 Stand 2=3.819s` 循环、`2 attack=1.508s` 非循环、`3 1=1.170s` 循环。
- 塞拉动作现为：冰焰双星使用 `2`、`2.0x` 对齐 `0.75s` 硬直；绝对零度领域使用 `2`、`1.5x` 对齐 `1.0s` 硬直；元素转换使用 `3` 限时播放 `1.0s`。三项均恢复 `1 Stand 2`。
- 旧元素转换配置使用了不存在的动画 `4`，现已移除；元素转换复用共享限时动作播放器，避免循环动作永久停留或旧恢复回调覆盖新技能。

### 双重凤凰菲尼克斯尔

配置：`01．主线Boss/04．双重凤凰菲尼克斯尔/02．数值与表现配置.ts`

- `HolyPhoenix.mdx` 的 `0-12` 与 `UndyingPhoenix.mdx` 的 `0-6` 已从模型 `SEQS` 提取原始时长和循环标记，并写入配置。
- 第一形态用户确认：`4` 移动挥翅、`5` 持续展翼飞行、`7` 施法时弯身、`8` 从蛋中复苏、`10` 死亡化蛋、`11` 维持蛋形态；其余按模型序列名记录。
- 第二形态用户确认：`2` 展翼飞行、`4` 快速移动；其余按模型序列名记录。
- 旧错位已修正：炽羽散射 `6`、熔岩吐息/凤凰漩涡 `7`、浴火重生 `10`、骸骨弹幕 `3`、怨火链接/凤凰挽歌/元素爆发/核心暴露 `5`、永恒轮回 `6`。
- 凤凰蛋创建后复用通用单位动画守护，持续重播第一形态 `11 Stand Alternate`，不再只配置而不执行。
- 双形态转换切换模型后播放第二形态待机 `0`，该行为与正式模型序列一致。

## 与四套新 Boss 一起处理

后续处理批次包含：

1. 安兹·乌尔·恭与雅儿贝德。
2. 夏提雅·布拉德弗伦。
3. 沉睡英魂亚伦柯斯。
4. 祖地双灵卫。
5. 本文档列出的十套旧 Boss 动作修正。

处理顺序：

1. 先确认技能实际使用的正式模型，双形态、护卫和双 Boss 必须分模型记录，禁止串用动作表。
2. 对照用户截图/口述与模型序列，记录动作编号、持续时间和真实生效帧；来源不明的现有编号逐项重新验证。
3. 将所有技能动作、倍速、恢复延迟和必要的重复播放间隔集中到各自 `02．数值与表现配置.ts`。
4. 删除技能文件中的非默认姿态数字硬编码；恢复默认姿态也优先读取统一动作配置。
5. 主动技能在前摇开始时播放动作，命中/结算帧与模型动作对齐；长引导需要按动作时长决定冻结、循环或重播。
6. 被动与后台机制不为了“看起来都有动作”强行打断普攻；只给玩家可感知的召唤、阶段、大招和反击流程补动作。
7. 每完成一个 Boss 后构建，并检查对应生成 Lua 的动画参数和回调时点；最终通过实机验证，不以模型预览器单独作为结论。
