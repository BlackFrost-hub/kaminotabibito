# 后续 Boss 迁移工作记录（临时）

> 本文只用于跨上下文保存迁移进度、差异与验收结果。全部 Boss 完成并由用户确认后删除。

## 固定要求

- 迁移目标：地精祭祀、利尔·伯特、教派剑士、教派学者。
- JASS 是时间线、动作、特效和技能壳行为的重要来源；设计文档负责最终机制语义。两者冲突时必须记录取舍，不能静默改写。
- Boss 动作必须在暂停/施法硬直后播放；硬直期间显示对应吟唱条，时长与 JASS 通魔壳、动作和生效时点一致。
- 明显需要玩家识别的状态必须接入 Boss 专属 TS Buff 表。
- 所有技能首次实机定位阶段保留 `debugLogForce` 日志；调试入口统一来自 `03．调试输出.ts`。
- 每个 Boss 建立独立测试文件；测试环境使用中立敌对靶，主动技能可手动触发，被动/自动机制在测试场景自然生效。
- 禁止匿名 JASS 回调、`Math.*`/Lua 数学库、普通版 no-self 不兼容封装、修改 `fix-lua-for-pack.js`。
- TS 修改优先按本次文件运行 `npm run build:files -- "..."`；最终整体验收再运行 `npm run build`，不打包地图。

## 当前资料与目录

| Boss | JASS 来源 | 设计文档 | 目标目录 | 当前状态 |
|---|---|---|---|---|
| 地精祭祀 | `JASS/Boss的jass代码/地精祭祀.j`；受击反应入口在 `JASS/jass复制粘贴/部分AI放技能.j` | `09．地精祭祀/地精祭祀.md` | `09．地精祭祀` | 源码、接入、隔离测试和复审完成，待实机确认 |
| 利尔·伯特 | `JASS/Boss的jass代码/蛇人卫队长.j`、`JASS/几个Boss的被动/被动.j` | `11．利尔·伯特/利尔·伯特.md` | `11．利尔·伯特` | 源码、接入、隔离测试和复审完成，待实机确认 |
| 教派剑士 | `JASS/Boss的jass代码/教派蒙面人/剑士姿态.j`、`JASS/几个Boss的被动/被动2.j` | `12．教派剑士/教派剑士.md` | `12．教派剑士` | 源码、Buff、AI、父级入口、隔离测试和局部构建完成，待实机确认 |
| 教派学者 | `JASS/Boss的jass代码/教派蒙面人/学者姿态.j`、`JASS/几个Boss的被动/被动2.j` | `13．教派学者/教派学者.md` | `13．教派学者` | 源码、Buff、AI、父级入口和隔离测试完成，待实机确认 |

## 被动与伤害事件的精确来源

- `JASS/几个Boss的被动/被动.j` 是全局敌对伤害事件，不是利尔专属文件：
  - `223-306` 属于杀戮食人魔，包含心脏掌握、疼痛复仇两个累计池与解控/刷新逻辑；后续迁移其他 Boss 时不得重复搬入。
  - `308-339` 属于利尔·伯特的“检查”受伤累计与超阈值全体惩罚，是主动机制的伤害事件支撑。
  - `344-355` 属于利尔·伯特“正义审判”的受伤附加精神伤害。旧 JASS 为已损失生命值 `15%`，设计文档为 `20%`，最终按设计文档实现。
  - 设计文档要求的“周期检查未面向玩家并造成攻击力 100% 精神伤害”未在该文件找到现成实现，不能因 JASS 缺失而漏做。
- `JASS/几个Boss的被动/被动2.j` 是全局普攻伤害事件：
  - `96-139` 属于普通沙漠食人魔蓄力重击，后续迁移教派 Boss 时不得重复搬入。
  - `141-160` 属于教派剑士“黑魔法侵蚀”的普攻附加暗魔法伤害，并承接黑洞跨越的 `CN` 强化普攻消费。力量英雄为目标最大生命值 `2%`，非力量英雄为 `3%`。
  - “Boss 所有魔法伤害可以法术暴击，且黑魔法侵蚀自身不享受”未在该分支找到完整现成实现，必须按设计文档接入 TS 伤害链并防止被动自身递归暴击。
  - `163-207` 与 `8-70` 共同组成教派学者“暗影索命”：普攻替换为可攻击、可反弹的暗影弹幕；命中/自然销毁与玩家摧毁走不同退出分支。
  - 旧 JASS 弹幕生命值为 Boss 最大生命值 `25%`，命中还附带目标最大生命值 `3%`；设计文档为 `15%` 且只写攻击力 `150%`，最终按设计文档实现并记录差异。
  - `212-237` 是冥神魔门召唤物的普攻效果，不是学者 Boss 本体被动，但属于召唤机制完整链路，迁移冥神魔门时必须一并处理。
- 地精祭祀 `N00C` 在 `JASS/几个Boss的被动/被动.j` 和 `被动2.j` 中均无分支。其受玩家英雄伤害后立即命令施放 `A0HZ` 的入口位于 `JASS/jass复制粘贴/部分AI放技能.j:4-33`，TS 已有 `受击反应配置表.ts` 配置；测试必须通过真实受击事件验证，不能只手动调用主动技能。

## 先行修复

| 项目 | 文件 | 核对点 | 状态 |
|---|---|---|---|
| 血海绞杀四方向预警 | `10．杀戮食人魔/05．血海绞杀.ts` | 四个预警方向和四条真实伤害方向使用同一角度表；确认特效 API 朝向单位 | 已修复，`build:files` 通过 |
| 雷霆敲打四方向预警 | `01．沙漠食人魔/05．雷霆敲打.ts` | 每波四方向预警、弹幕和伤害共用同一基础朝向与 90 度偏移 | 已修复，`build:files` 通过 |

## 地精祭祀迁移结果

- 新增 `09．地精祭祀` 的配置、运行时、受击观察、三个主动技能、入口、设计文档；主动技能均在硬直后播放配置动作并显示吟唱条。A018 与 A019 的预警、特效和伤害复用同一份坐标快照，圆形预警统一使用技能提示圈工厂；A019 左侧保留攻击力 `240%` 暗伤，右侧保留 `300+150×N` 酸性伤害，未错误合并为同一伤害类型。
- A00J 源 JASS 的 `e00D` 是 `file=".mdl"`、`modelScale=2.5` 的不可见辅助壳，`A00J` 继承 `Afod`，其真实目标表现来自 `TargetArt=DemonBoltImpact.mdl`。返修后不创建马甲、不从 Boss 额外拉线，而是在每个命中目标 `origin` 直接播放 TargetArt 并安全定时销毁。正式入口在 `SPELL_CHANNEL` 只显示 1.33 秒读条，`SPELL_EFFECT` 关闭读条并立即读取目标当前位置结算；测试导出入口通过基础施法时间线执行“面向、硬直、动作、读条”，1.33 秒后调用同一个结算函数，避免正式入口重复等待成 2.66 秒。A0HZ 不重写召唤，继续使用既有受击反应表和物编 `n008`，并增加实际 `SPELL_EFFECT` 观察日志。
- 新增专属空 Buff 表并接入主线 Boss Buff 聚合；新增英雄 Boss AI；父级 Boss index 和测试入口均已接入。测试 Boss 归属中立敌对，测试初始化执行 `directRegisterPlayerHero`，`地精4` 通过真实 `UnitDamageTarget` 触发 A0HZ，而非直接调用技能。
- 构建：首次 `build:files` 因受击观察遗漏导入数值配置失败，修正后对 16 个本轮 TS 文件的 `build:files` 通过；因局部构建曾失败且新增聚合导出，随后 `npm run build` 通过。未手动查看 Lua，未打包地图。
- A00J 返修验证：对 `02．数值与表现配置.ts`、`04．破坏死光.ts` 执行 `build:files` 通过，`fix-lua-for-pack` 更新 0 个文件。只抽查对应 `04．破坏死光.lua`：CHANNEL/EFFECT 均以具名回调直接注册，测试导出保持 `(上下文, 目标单位)` 两参，基础施法时间线接收单个参数表，具名闭包未出现 `self/nil` 错位。

## 逐 Boss 落地清单

每个 Boss 完成时逐项填写：

- [ ] JASS、设计文档、被动文件和相关剧情入口已逐段对照。
- [ ] 技能差异与采用口径已记录。
- [ ] 数值、范围、动作、特效、颜色、Buff、召唤物集中在配置文件。
- [ ] 每个主动技能已明确 AOE/单体、独立技能实例、位移语义。
- [ ] 时间线已核对：朝向 -> 暂停/硬直 -> 动作/吟唱 -> 预警 -> 生效表现 -> 伤害/控制/Buff -> 清理。
- [ ] 退出矩阵覆盖 Boss 死亡、目标死亡、测试清理、技能取消、召唤物死亡和计时器结束。
- [ ] Boss 专属 Buff 表已接入且描述与真实逻辑一致。
- [ ] 技能目录父级 `index.ts`、AI、启动桥接、单位配置、死亡/奖励入口已核对。
- [ ] 独立 Boss 测试文件已接入；被动在测试环境自然生效。
- [ ] 所有技能已加入可定位的 `debugLogForce` 日志。
- [ ] 本次 TS 文件局部构建通过。
- [ ] 必要的生成 Lua `self/nil` 与回调参数检查通过。

## 已确认复用的公共封装

- 利尔·伯特面向判断：使用 `04．机制组件/10．复杂战斗通用机制/08．方位判定工具.ts` 的 `目标是否面向来源(来源, 目标, 90)`，不要私自重写角度算法。
- 利尔·伯特装备检查：背包枚举后使用 `TS/lib/扩展函数/物品相关函数/装备数据查询.ts` 的 `getItemDataEntry` 确认物品属于装备数据；返还优先使用 `物品判断函数.ts` 的 `ReturnItemToHeroOrDropBJ`。检查结束按设计将物品丢在 Boss 脚下时，必须保留原物品句柄并显式设置 Boss 坐标，不能销毁后重建。
- 教派学者暗影弹幕：使用 `01．技能函数/01．弹幕/01．TS原生弹幕`；底层已支持 `弹幕生命值`、`可被攻击摧毁`、`弹射`、`随机弹射`、`弹射次数上限`、`on被击落` 和统一 `on结束`，不得另造单位移动/墙壁反弹系统。
- 教派学者终结锁链：每条锁链需要两次纯普攻摧毁，优先使用 `04．机制组件/05．机制单位/03．固定受击次数机制单位.ts`，并明确过滤纯普攻、同步生命条和战斗清理。
- 教派剑士分身、教派学者冥神魔门召唤物：使用 `03．召唤物组状态管理.ts` 或其召唤组模板；分批召唤时先 `开始批次`，全部登记后再 `结束批次`，防止生成途中提前判定全灭。
- 可攻击黑洞、分身、次元门等独立目标：优先使用 `04．机制组件/05．机制单位/01．可攻击机制单位.ts`，按 `被击杀/自然到期/主动销毁/机制清理/单位失效` 分别处理退出行为。

## 利尔·伯特已定实现口径

- 正义审判：玩家没有在 90°范围面向 Boss 时，Boss 对其造成伤害后附加玩家已损失生命值 `20%` 的精神伤害；精神伤害事件必须排除递归。设计要求的周期审判在 JASS 中缺失具体周期，配置暂定每 `5` 秒检查一次，对未面向玩家造成 Boss 攻击力 `100%` 精神伤害，后续实机只需调整配置值。
- 裂地斩：施法开始快照 Boss `1750` 范围内敌对英雄及各自位置；预警 `1.1` 秒后，以每个快照点 `275` 半径结算 Boss 攻击力 `200%` 技能普攻伤害并眩晕 `1` 秒。旧 JASS 的 `220%` 不采用。
- 审判拷问：固定记录目标起始位置并等待 `5` 秒；只有目标“没有面向 Boss”且“离开起始点 `233` 范围”同时成立时才处罚，造成目标最大生命值 `30%+10%×N` 精神伤害、最低保留 `1` 点生命并眩晕 `1` 秒。旧 JASS 的 `3.5+0.5N` 不采用。
- 检查：只从 `getItemDataEntry` 可识别的装备中随机选择一件，保留原物品句柄并转移给 Boss；检查持续 `3` 秒，期间累计实际结算伤害严格超过 `560-60×N` 时只触发一次失败惩罚。失败按旧 JASS 保留 `2` 秒延迟后对全体造成目标最大生命值 `20% + Boss攻击力250%` 强化伤害；无论成功或失败，装备最终都从 Boss 背包移出并放在 Boss 脚下。
- 检查状态必须防重复施法、Boss 死亡、目标死亡、物品失效和测试清理；任何提前退出都要先归还/丢下原物品，再清空伤害累计和监听状态。

## 利尔·伯特完成记录

- 当前状态：正义审判、裂地斩、审判拷问、检查、专属 Buff、技能入口、父级入口和独立隔离测试均已接入；本轮局部构建及相关 Lua `self/nil` 定点检查通过，待实机确认表现和数值链。
- JASS 差异：正义审判附加已损生命由旧 JASS `15%` 改为设计 `20%`；裂地斩由旧 JASS 攻击力 `220%` 改为设计 `200%`；审判拷问由旧 JASS `3.5+0.5N` 秒改为固定 `5` 秒，处罚条件按设计采用“未面向且离开233码”的 AND 关系；检查累计改按最终实际承伤并严格 `>` 阈值触发。
- 隔离测试命令：`利尔1` 验证 Boss 对背对靶造成真实伤害并自然触发正义审判；`利尔2` 验证裂地斩；`利尔3` 验证审判拷问背对且离开快照点；`利尔4` 验证检查正常完成；`利尔5` 由专用中立敌对步兵对 Boss 造成原始 `5000` 伤害，验证检查失败。
- 配置同步：设计移速为 `400`，已将 ObjEditing 来源、Boss 配置表和总单位配置表中的 `N05L` 从 `382` 同步为 `400`。
- 审查修复：检查失败时原装备只在超过阈值瞬间落地一次，失败等待、处罚完成和上下文清理不再二次传送；检查失败测试伤害提高为 `5000`；正义审判与审判拷问均不再直接混用 `jass.japi`，生命公式统一交给 Boss 技能伤害执行器。
- 复审结论：90°面向工具按总扇区角度计算；正义审判附加伤害通过零延迟具名回调避免递归；审判拷问临时不死状态会恢复原值；检查只读取 `getItemDataEntry` 可识别装备并保留原句柄。未发现新的源码缺陷。

## 教派剑士完成记录

- 黑魔法侵蚀：普通攻击最终生效后附加力量英雄 `2%`、其他英雄 `3%` 最大生命值暗伤；被动自身标签明确排除法术暴击。非攻击型魔法伤害走专属暴击补偿，`isSkillAttack` 已由全局暴击系统处理，因此专属修正器排除该分支以防双重暴击。
- 深渊旋风：按旧 JASS 每 `0.3` 秒产生一轮，共 `12` 轮，每轮先预警 `0.15` 秒再发射真实 TS 原生弹幕；弹幕终点 `150` 范围结算攻击力 `100%` 暗伤。末轮在发射回调之后才收束，施法期间魔法免疫且硬控可打断。
- 黑洞跨越：使用可攻击机制单位和战斗自身位移；入口黑洞生命为 Boss 最大生命值 `18%`，持续 `3` 秒，火/光承伤 `125%`。成功进入后隐藏并等待 `2` 秒，在随机玩家身后创建出口，`0.5` 秒后出现、激活 `0.8` 秒强化普攻并立即下达追击命令。黑洞摧毁伤害、摧毁者减伤和吸引均已接入。
- 魔祭吸魂：硬直并施法 `1.2` 秒，成功后自损 `3%`、持续 `2` 秒增伤 `30%`，`1.9` 秒结算全体暗伤；吸收量按最终 `applied` 汇总后乘 `200%-20%×T`。生效阶段首次受到火/光最终伤害时触发一次反噬和无视韧性眩晕。
- 深渊分身：按 `T=4` 或 `N>=3` 创建 `2` 个，否则 `1` 个；分身造成伤害归零、承伤 `500%`、持续 `5` 秒，死亡 `0.5` 秒后爆炸。召唤组严格使用一次 `开始批次/结束批次`，玩家英雄和玩家召唤物都计为玩家摧毁者；全由玩家摧毁时 Boss 恢复原位并硬直 `2.5` 秒。
- 接入与验证：主线 Boss 父入口、教派剑士 AI、专属 Buff 和独立测试已接入。测试 Boss 属于中立敌对，真实普攻/暗魔法均可验证被动，四个主动可分别触发。剑士相关 `19` 个 TS 文件局部构建通过，`fix-lua-for-pack: 0 file(s) updated`。

## 教派学者已定实现口径

- 技能 ID 映射固定为：`A0L9` 深渊之牢、`A0LA` 冥神魔门、`A0LB` 冥之念欲、`A0LC` 邪狱追魂冥法。四个原生壳均为 Channel；A0LA/A0LB/A0LC 的原生 `Cast=0.2`，动作与硬直仍必须由 TS 施法时间线统一控制。
- 暗影索命：复用 TS 原生弹幕的可攻击、随机反弹和击落生命周期；普通攻击原伤害归零后生成弹幕。生命值按设计取 Boss 最大生命值 `15%`，命中只造成 Boss 攻击力 `150%` 暗魔法伤害并眩晕 `0.4` 秒，不采用旧 JASS 的 `25%` 弹幕生命和目标最大生命值 `3%` 附加伤害。雷/光承伤按设计 `130%`。击落者按自身攻击力恢复 `50%` 生命与 `25%` 魔法。
- 深渊之牢：JASS 补足判定半径 `285`，检查间隔 `0.04` 秒，无伤完成后目标暗属性伤害抗性提高 `80%`、持续 `1` 秒；牢笼持续按设计取 `1.5` 秒，不采用 JASS 马甲 `1.75` 秒生命周期。目标离开时结算 Boss 攻击力 `300%` 暗伤与 `0.75` 秒眩晕；无伤结束时 Boss 受到目标攻击力 `300%` 暗伤。
- 冥神魔门：生命周期、单人生命、雷/光承伤按设计取 `20` 秒、Boss 最大生命值 `30%`、`130%`；不采用 JASS 的 `25` 秒、单人 `25%`、`125%`。JASS 补足门随机偏移 `250-650`、每 `8` 秒召唤一次、召唤类型 `u00G/n05P/n05O`；门被玩家摧毁时清理召唤组并让 Boss 失去全部魔抗、硬直 `3` 秒，所有退出路径恢复魔抗且自然到期不触发反噬。
- 冥之念欲：JASS 补足统一等待 `2.4` 秒；念引安全半径 `385`，念退要求处于 `635` 以外，念赶生成两个半径 `285` 的安全区。JASS 的硬编码剧情矩形中心不得迁入，正式与隔离测试均以本次施法时 Boss 的动态位置为锚点。三个变体伤害严格采用设计公式。
- 邪狱追魂冥法：设计文档所称“原文截断”只适用于用户摘要，`学者姿态.j` 实际完整给出锁链后逻辑。数量仍按设计 `5+2×N`，不采用 JASS 的 `7+3×N`；锁链持续 `10` 秒、减速 `20%`，每条只接受纯普攻并在第 `2` 次受击时摧毁。暗影弹幕复用暗影索命同一创建函数和设计版命中公式，JASS 仅补足发射延迟 `0.95` 秒、碰撞半径 `120`、随机反弹及击落恢复语义。
- 学者所有可观察状态均已接入专属 Buff：暗影弹幕、深渊牢笼暗抗、冥神魔门反噬、三种冥之念提示和终结锁链等状态均使用正式白银边框 BLP 图标，不再使用占位资源。

## 图标资源完成记录

- 地精祭祀不新增可见状态图标；利尔·伯特 `2` 张、教派剑士 `4` 张、教派学者 `9` 张，共 `15` 张正式 Buff BLP 已生成、回读并接入，目录分别为 `imports/BuffIcon/Boss/LirBert`、`MaskedSwordsman`、`MaskedScholar`。
- 食人魔共 `6` 个 Buff 已全部替换为专用图标：蓄力Hit、食人魔咒、深渊魔咒、疼痛复仇、痛之束缚、心脏掌握。正式资源位于 `imports/BuffIcon/Boss/Ogre`，均为 `64x64`、完全不透明、带 Warcraft 白银 BTN 边框的 BLP；`08．食人魔.ts` 不再引用原生图标。

## 迁移顺序与状态

1. [x] 修复食人魔两处四方向预警。
2. [x] 地精祭祀：源码、接入、隔离测试、局部构建和复审完成，等待实机确认。
3. [x] 利尔·伯特：源码、Buff、接入、隔离测试、局部构建与 Lua 定点检查已完成，待实机确认。
4. [x] 教派剑士：差异审查、实现、Buff、接入、隔离测试和局部构建完成。
5. [x] 教派学者：补全 JASS 语义、实现、Buff、接入、隔离测试和局部构建完成。
6. [x] Buff 图标、最终完整构建与 `Boss技能总复盘.md` 静态复审完成；Warcraft 实机确认仍待用户执行。

## 最终构建与复盘

- 对本轮返修涉及的 `13` 个 TS 文件执行精确 `build:files`，退出码 `0`，`fix-lua-for-pack: 0 file(s) updated`。覆盖公共最大生命读取、地精延迟回调、利尔三个技能、剑士四个技能、学者两个技能和食人魔 Buff 表。
- 最终执行 `npm run build`，退出码 `0`；完整构建按项目既有链路执行 TSTL、`fix-lua-for-pack` 和 Lua 同步，未打包地图。
- 最终静态检查：四 Boss 目录无 `Math.*`、无旧式 `GetUnitState(...UNIT_STATE_MAX_LIFE)`、无匿名中心计时器回调、无误写 `Common` 特效路径；源 JASS 使用的 `war3mapImported` 特效路径按用户要求保留。
- 最终返修：裂地斩落点只取敌对英雄快照；正义审判零延迟附加伤害登记到清理篮子；审判拷问和魔门治疗压制均用 Buff runtime 身份保护；血爆/毒蕴延迟回调登记清理；深渊旋风正常结束后已发射弹幕继续结算、上下文清理时禁止终点 AOE；黑洞/魔门仅在真实被击杀且 killer 有效时触发摧毁语义；剑士自损、学者魔门自损统一使用动态最大生命、可致死且无额外生命移除表现。
- 四个独立测试文件、测试系统总开关和四个 Boss 测试加载开关均已启用；测试环境使用中立敌对目标并登记玩家英雄，使被动在隔离环境自然生效。全部技能 `debugLogForce` 按用户要求继续保留。

## 审核重点

- 预警形状、角度、坐标和持续时间必须与实际判定一致，尤其多方向、扇形、路径与延迟 AOE。
- JASS 写有投掷/飞行/追踪时必须有真实弹幕，不用静态特效加延迟冒充。
- 伤害与控制只能在视觉命中/爆炸时点结算。
- 原生施法事件可能覆盖动作；必要时在硬直内具名延迟回调重播动作。
- 测试坐标以 `(-540.6, -2495.2)` 为中心整体平移正式场地数据，不污染正式配置。
- 测试 Boss 与中立敌对靶的阵营、仇恨和目标选择要允许正式被动触发。
- 最终复盘检查重复提示圈、无效 Buff、未接入 AI/事件入口、未清理状态、普通版 no-self 调用和错误特效路径。
