/**
 * AI自动使用技能系统 - 常量定义
 *
 * 后续接手者：修改开关、检测周期请在此文件
 */

// ==========================================================================================
// 系统开关
// ==========================================================================================

/** 系统启用开关，true启用，false禁用（默认禁用） */
export const AI_SKILL_SYSTEM_ENABLED = false;

// ==========================================================================================
// 定时器相关常量
// ==========================================================================================

/** AI检测周期（秒） */
export const AI_CHECK_INTERVAL = 0.5;

// ==========================================================================================
// JASS事件ID常量
// ==========================================================================================

/** 单位死亡事件ID */
export const AI_EVENT_ID_UNIT_DEATH = 52;

/** 玩家数量 */
export const AI_PLAYER_COUNT = 16;

/** 中立敌对玩家ID */
export const AI_PLAYER_NEUTRAL_AGGRESSIVE = 12;

/** 中立被动玩家ID */
export const AI_PLAYER_NEUTRAL_PASSIVE = 15;

// ==========================================================================================
// 技能目标类型
// ==========================================================================================

/** 无目标技能（立即施放） */
export const TARGET_TYPE_NONE = 0;

/** 点目标技能 */
export const TARGET_TYPE_POINT = 1;

/** 单位目标技能 */
export const TARGET_TYPE_UNIT = 2;

// ==========================================================================================
// 技能优先级常量
// ==========================================================================================

/** 最低优先级 */
export const PRIORITY_LOWEST = 0;

/** 低优先级 */
export const PRIORITY_LOW = 25;

/** 普通优先级 */
export const PRIORITY_NORMAL = 50;

/** 高优先级 */
export const PRIORITY_HIGH = 75;

/** 最高优先级 */
export const PRIORITY_HIGHEST = 100;

/** 控制技能优先级（最高） */
export const PRIORITY_CONTROL = 100;

/** 伤害技能优先级（高） */
export const PRIORITY_DAMAGE = 75;

/** 辅助技能优先级（普通） */
export const PRIORITY_SUPPORT = 50;

/** 治疗技能优先级（高） */
export const PRIORITY_HEAL = 80;

// ==========================================================================================
// 默认配置常量
// ==========================================================================================

/** 默认施法距离（当无法从技能获取时） */
export const DEFAULT_CAST_RANGE = 600;

/** 默认魔法消耗检查阈值 */
export const DEFAULT_MANA_THRESHOLD = 0;

/** 默认冷却时间（秒） */
export const DEFAULT_COOLDOWN = 0;

// ==========================================================================================
// 通魔技能命令ID（待确认）
// ==========================================================================================

/**
 * 通魔技能命令ID
 * 注意：这些值需要根据实际地图中的技能确认
 * 当前为占位值，后续需要根据实际情况修改
 */
export const ORDER_ID_CHANNEL_MAGIC = 0;  // 待确认

/**
 * 通魔技能ID
 * 注意：需要根据实际地图中的技能确认
 */
export const ABILITY_ID_CHANNEL_MAGIC = 0;  // 待确认
