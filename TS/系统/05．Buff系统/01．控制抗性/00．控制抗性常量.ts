/**
 * 控制抗性常量配置
 *
 * 本文件包含所有控制抗性相关的上限配置
 * 修改上限值即可影响整个系统，无需改动核心逻辑
 */

//=============================================================================
// 一、控制时间削减上限（玩家上限）
//=============================================================================

/**
 * 通用控制时间削减上限
 * 单位/玩家的「减少控制时间」属性上限
 * 默认：0.90（90%削减，即控制时间缩短为原来的10%）
 * 修改示例：改为0.80表示80%上限
 */
export const CONTROL_REDUCTION_CAP = 0.90;

//=============================================================================
// 二、Boss控制时间上限
//=============================================================================

/**
 * Boss单位控制时间上限配置
 *
 * key: 单位类型ID（字符串形式，如 'N05U'）
 * value: 最大控制时间（秒）
 */
export const BOSS_CONTROL_LIMITS: Record<string, number> = {
  /**
   * N05U: 控制时间上限1秒
   */
  'N05U': 1.0,
  // 可扩展更多Boss...
};

//=============================================================================
// 三、排除单位
//=============================================================================

/**
 * 不参与控制抗性的单位类型ID（字符串形式）
 */
export const EXCLUDED_UNIT_TYPES: string[] = [
  'e02A', // 辅助马甲
  'bHun', // 快速Buff系统马甲（SFB_Unit）
];
