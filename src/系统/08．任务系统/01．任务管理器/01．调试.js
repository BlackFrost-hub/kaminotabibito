/**
 * 任务管理器内部调试输出
 *
 * 被 `02．任务提示与奖励`、`03．原生任务同步`、`04．QuestManager` 引用。
 * 默认空实现，避免正式地图刷屏；开发时可在函数体内接 `print` / 日志系统。
 *
 * @param _msg 建议传简短中文或 key，便于检索
 */
export function questDebugPrint(_msg) { }
